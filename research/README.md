# `xtty` Research

Research library on terminal emulators — competitive landscape, internals, and design implications for building a macOS-native terminal (`xtty`).

> **Provenance:** Generated 2026-06-27 from a 39-agent research workflow (10-terminal survey + adversarial fact-check + 8 architecture deep-dives + synthesis), 281 web lookups. Claims marked surprising/marketing-flavored were independently fact-checked (see [fact-checks](03-analysis/fact-checks.md)).

## How to navigate

### 00 — Overview (start here)
- [Landscape synthesis](00-overview/landscape-synthesis.md) — trends, how terminals cluster, the tradeoff spectrum
- [Comparison matrix](00-overview/comparison-matrix.md) — side-by-side attribute tables

### 01 — Terminals (deep dives)
- [iTerm2](01-terminals/iterm2.md) — The most feature-complete, deeply macOS-integrated terminal for power users, with best-in-class tmux control-mode integration and extensive scripting/automation.
- [Apple Terminal.app (macOS built-in)](01-terminals/apple-terminal.md) — The zero-install, Apple-supported default terminal that ships with macOS — maximally reliable and integrated, but feature-conservative compared to third-party emulators.
- [Ghostty](01-terminals/ghostty.md) — A terminal that refuses to trade off speed, features, or native-platform feel simultaneously, built on a reusable C-ABI core (libghostty) with a true Metal/SwiftUI macOS app rather than a cross-platform widget toolkit.
- [kitty](01-terminals/kitty.md) — A "terminal as a platform": GPU-accelerated, highly hackable, and the originator of the kitty graphics protocol plus the kittens extension framework now influencing the broader terminal ecosystem.
- [Alacritty](01-terminals/alacritty.md) — A deliberately minimalist, GPU-accelerated terminal that does one thing — render text fast — and offloads tabs/splits/multiplexing to tools like tmux.
- [WezTerm](01-terminals/wezterm.md) — A fully cross-platform, Rust-based terminal that bundles a tmux-style multiplexer and deep Lua scripting into one GPU-accelerated app.
- [Warp](01-terminals/warp.md) — An AI-native, blocks-based terminal that has evolved into a full "agentic development environment" — combining a polished native terminal with multi-agent orchestration, rather than just a fast text grid.
- [Rio Terminal](01-terminals/rio.md) — A Rust, GPU-accelerated terminal with a custom Sugarloaf/wgpu renderer that targets desktops, FreeBSD, and the browser (WebAssembly) from one codebase, with an emerging embeddable engine (librio) and a native-Swift macOS frontend (Super Rio).
- [Wave Terminal](01-terminals/wave.md) — A block/widget-based terminal that pulls files, previews, an editor, a web browser, and AI into one workspace to eliminate context switching, rather than chasing raw rendering speed
- [Hyper](01-terminals/hyper.md) — A fully web-tech terminal (Electron + React + Redux + xterm.js) whose entire UI and plugin/theme system is built and customized with HTML/CSS/JavaScript via npm.

### 02 — Internals (how to build one)
- [PTY Fundamentals — the byte shuffler](02-internals/01-pty-fundamentals.md)
- [VT/ANSI Escape-Sequence Parsing](02-internals/02-vt-ansi-parsing.md)
- [GPU Rendering & Metal (cell grid + glyph atlas)](02-internals/03-gpu-rendering-metal.md)
- [Fonts & Text Shaping](02-internals/04-fonts-text-shaping.md)
- [Graphics Protocols — Sixel / iTerm2 / kitty](02-internals/05-graphics-protocols.md)
- [Performance — Latency vs Throughput](02-internals/06-performance-latency.md)
- [Multiplexing & Session Features](02-internals/07-multiplexing-sessions.md)
- [Modern Innovations — Blocks, AI, OSC integration](02-internals/08-modern-innovations.md)

### 03 — Analysis
- [Fact-checks](03-analysis/fact-checks.md) — verified/refuted/uncertain claims with corrections
- [Known product issues](03-analysis/known-product-issues.md) — post-P7 ledger of 7 source/probe-confirmed findings: KI-1's Git-review memory defect is fixed and archived, with 6 issues still open across Git refresh, split focus, OSC 133 Unicode capture, profile cwd validation, and configuration edge cases; includes mechanisms, coverage gaps, theory fates, and verify-by-effect checks
- [Opportunities & design implications](03-analysis/opportunities.md) — gaps and what they mean for `xtty`
- [Agents & xtty](03-analysis/agents-and-xtty.md) — the integrated-agents vs agent-host fork, and OSC 133 as the foundation
- [Adjacent tools](03-analysis/adjacent-tools.md) — agent multiplexers & terminal-layer tooling that xtty would host (Herdr, …)
- [Native-app testing tooling](03-analysis/native-app-testing-tooling.md) — how an agent drives/inspects a native macOS app (Peekaboo + XCUITest), and the AX-content ceiling that forces engine-grid assertions
- [UI-test flakiness: tracing, attribution & isolation](03-analysis/ui-test-flakiness-diagnosis-and-isolation.md) — xcresult already traces the automation's actions; a diff *localizes* a flake but only an out-of-band input trace (CGEvent PID==0) *attributes* it to a human; isolation via in-process DEBUG driving (best ROI) > VM (heavy) > separate session (trap); layered pyramid + auto-retry
- [SwiftTerm Metal renderer spike](03-analysis/swiftterm-metal-renderer-spike.md) — P2 finding: SwiftTerm's GPU path works in xtty's AppKit host; adoption deferred to the P7 latency gate
- [P3b shell-UX decisions](03-analysis/p3b-shell-ux-decisions.md) — explore-phase decisions for the deferred half of P3 (Quick-Terminal + Profiles); error-matching moved to P4
- [P4 semantic-capture decisions](03-analysis/p4-semantic-capture-decisions.md) — explore-phase decisions for the keystone (OSC 7 cwd + OSC 133 blocks): ship P4a (data model, fork-free) and defer P4b (jump/select/marks — needs a SwiftTerm fork); auto-inject zsh via `ZDOTDIR`; protocol grammar
- [P5 sidebar + P4b sequencing](03-analysis/p5-sidebar-and-p4b-sequencing.md) — post-P4a decision: ship the **P5 session sidebar before P4b** (fork-free, on the existing block model), defer the fork, drop gutter marks; resolves 6 open questions (states, fork-free jump, grouping, copy-vs-select, upstream strategy, refresh cadence). **Post-P5 update:** P4b splits into **P4b-1 `add-file-link-open`** (file:line click-to-open — fork-free) + **P4b-2 `add-spatial-blocks`** (jump/copy — the fork)
- [P4b-2 spatial-blocks decisions](03-analysis/p4b-2-spatial-blocks-decisions.md) — explore-phase decisions for the fork-gated milestone (jump-to-prompt + copy-output): **fork now** (a 2-accessor engine-only fork, not 3), **Tier 1 only** (defer view-layer visual-select), **keyboard-native** (Cmd+Shift+↑/↓; clickable block sidebar = P4b-3). Adversarial verification overturned 4 carried-in claims (fork-free Tier-0 copy silently corrupts; the fork is 2 accessors; Tier-2 isn't fragile; the naive `linesTop`-decrease invalidation is broken) → corrected invalidation = `invalidate-all-on-sizeChanged` + `liveTop` high-water-drop + normal-buffer pin + alt-screen gate. **2026-06-29 P4b-3 addendum:** external research (WebSearch + an iTerm2 `/tmp` source clone) **overturns D3's premise** — iTerm2 (keyboard-first) *does* ship a clickable command list (Toolbelt: single-click→scroll-to-mark, shell-integration-gated, a secondary drawer); its `VT100ScreenMark` invalidation (eager per-mark recompute on resize + eager trim GC) is the eager twin of xtty's leaner lazy epoch-bump (same stale-click-no-op UX, but we skip iTerm2's width-baked-in full recompute). P4b-3 reframed from "deferred" → a cheap, prior-art-validated *secondary* convenience (keyboard stays primary); decisions-to-lock listed.
- [SwiftTerm fork vs patch-in-repo](03-analysis/swiftterm-fork-vs-patch-strategy.md) — how to inject the P4b-2 accessors **without creating a fork repo**: a Playwright-style patch-at-build doesn't map onto SwiftPM (SPM owns the checkout), but the spirit does via a **local-path dep** (submodule/vendor); and because the change is add-only one file, the "patch" is a drop-in. Plus the **defer-via-injectable-shim** analysis (Int-only seam → ~2-line light-up, fake-tested happy path). **Follow-up:** Playwright actually **gitignores the upstream checkout** (`/checkout`) rather than submoduling it — *a submodule is the wrong primitive for a patched dep* — so the cleanest no-repo mechanism is a **gitignored clone + bootstrap-enforced pin** (B′), with a throwaway **fork** cleaner still if a repo is acceptable; the whole mechanism is temporary (retired when the accessors land upstream)
- [P6 file/diff decisions](03-analysis/p6-file-diff-decisions.md) — two-pass (web + deep-read of zed/vscode/lazygit/gitui/delta/waveterm source) decisions for the file/diff view: ship **P6a**, a read-only **git-review panel** (changed-files grouped Changes/Untracked/Conflicts → click-to-diff, plain unified SwiftUI diff, right-edge collapsible panel) via **git shell-out** (not libgit2); refresh on OSC-133 command-finish **+ a ~5 s gated poll backstop** (the agent-mid-command gap both git TUIs poll for); **porcelain v1 `-z`** (not v2); defer the file tree (P6b) + staging. Reading real source corrected 4 pass-1 calls. **P6a+ addendum (2026-06-29):** decides intra-line diff emphasis for `add-git-review-polish` — **token-level** DP (not trim-only, not char-level), zed/delta-gated, `Text(AttributedString)` per-run background (macOS 12+, no fallback), + a one-line pause-during-own-git poll guard. **P6b addendum (2026-06-29):** narrows P6b to **Scope A** — a flat↔tree toggle over the *changed* files (pure view-transform, on-mission for big agent changesets) — and **rejects Scope B**, the full project file-tree browser, as off-mission IDE-creep; recommends deferring the build until after P7-measure
- [P7 measurement methodology & scope](03-analysis/p7-measurement-methodology.md) — explore-phase plan for the **P7 measure-and-decide gate**: the bar is undefined (M1/M4 qualitative) → set it **relative** to installed comparators (Warp/iTerm2/Terminal.app); latency via a **fork-free in-process screen-capture probe** (inject `CGEvent` → poll pixels; renderer-agnostic, exact A/B delta) over a per-renderer SwiftTerm patch; memory via `task_info` under fixed scenarios; a base-only `renderer = coregraphics|metal` key for rebuild-free A/B; **defer distribution** (0 codesigning identities on the dev machine); split P7 into **P7a `add-latency-memory-harness`** (gate-critical, fork-free) → P7b decision → P7c memory pass. **2026-06-29 apply addendum:** building + running P7a showed **memory is lean & trustworthy** (idle ~68 MB footprint, 0 % idle CPU, ≤135 MB saturated — M1 met) but the **screenshot-polling latency probe is too coarse** (capture ~20 ms > the signal → can't resolve key-to-photon; real probe + CG-vs-Metal verdict = P7b); plus an **operational signing finding** (ad-hoc rebuilds re-prompt for Screen Recording → a stable self-signed `xtty-dev` cert via `scripts/create-signing-cert.sh` + `XTTY_SIGN_IDENTITY` makes the TCC grant persist). **2026-06-29 P7b addendum** (`/opsx:explore p7b`, 14-agent research workflow + SwiftTerm-checkout deep-read): the full **trustworthy-probe methodology** — replace polling with a continuous **`SCStream` reading per-frame `displayTime`** (the next `.complete` frame after t0 *is* the keystroke; on-glass, fork-free, ~90 % P7a reuse), a load-bearing **clock-unit fix** (`displayTime` is mach *ticks* not ns — normalize both ends via `CMClockMakeHostTimeFromSystemUnits`), a **startup epoch-calibration gate**, and the **headline reframe**: both renderers sit behind a **shared ~16.67 ms output-coalescing throttle** (`queuePendingDisplay`), so the renderer A/B is a **wash** — the throttle, not the renderer, is xtty's latency lever. **GO, expected verdict: keep CoreGraphics, skip Phase 8**; comparators = Terminal.app-first stretch (iTerm2 Secure-Event-Input blocks injection); no hardware rig needed (the omitted tail cancels in the delta). **2026-06-29 P7b apply result** (`add-trustworthy-latency-probe`, implemented & benched 3×/renderer): the probe works — **calibration passed** (offset <4 ms → absolute numbers trustworthy on macOS 26.2) and latency is resolved (P7a floored at ~54 ms). **Verdict CONFIRMED: keep CoreGraphics, skip Phase 8** — CG faster median (~31 vs ~33 ms), much tighter tail (p99 ~40 vs ~50–120 ms), leaner memory (~55 vs ~62 MB idle); the shared throttle dominates the median, the probe additionally exposed Metal's worse tail. The renderer question is settled; P7c (leaks) + distribution remain → see the dedicated P7c doc next
- [P7c leak / retain-cycle audit](03-analysis/p7c-leak-retain-audit.md) — methodology & scope for the Phase-7 **leak/retain pass** (`/opsx:explore p7c` + a 14-agent workflow, 2026-06-29). **Headline: no leak to hunt** — both explore-flagged cycles (`WindowController⇄GitReviewController`, `PaneController⇄LinkRoutingTerminalDelegate`) **refuted** (all closures `[weak self]`, delegates `weak`, `GitReviewTarget` a transient value type); App-layer sweep found zero cycles, only 2 cosmetic nits. So P7c reframes to **prove + guard + profile-SwiftTerm**: a DEBUG **live-instance census** (`liveCount` init`++`/deinit`--` on 6 lifecycle types → `liveInstanceCounts` in the state dump → churn XCUITest asserts return-to-baseline = the gated net), weak-sentinel teardown for in-process `XttyCore` types, a one-time CLI `leaks`/`xctrace`/`vmmap` pass (no special entitlements; **not** CI-gated — false-positive-prone) targeting the one unnameable risk: SwiftTerm's **unbounded glyph/font caches** + incomplete renderer `deinit`. Footprint-after-churn stays **report-only** (too noisy). **Diverges from the raw synthesis:** drop the proposed `max-memory-mb` config key (knob with no runtime behavior, contradicts the noisy-footprint finding). Recommended change: **`add-lifecycle-census`** (fork-free); gates nothing; distribution stays deferred
- [Distribution: Developer ID + Hardened Runtime + notarization](03-analysis/distribution-signing-research.md) — explore-phase finding for the deferred **P7 distribution slice** (`/opsx:explore distribution`, 7-agent workflow, 2026-06-30). **Headline: technically green, gated only on ~$99/yr procurement.** The crux is a non-issue — **Hardened Runtime does NOT break a terminal that execs unsigned user binaries** (HR constrains only xtty's own process image; child shells/`$EDITOR`/CLIs get independent AMFI evaluation; ZDOTDIR injection is a *child*-env write, unrelated to `allow-dyld-environment-variables`); proven by iTerm2/Ghostty/WezTerm all notarized under HR with zero `allow-unsigned-executable-memory`. **The HR-exception entitlement set is empty** — just flip `ENABLE_HARDENED_RUNTIME=YES`, keep sandbox-off, sign with a real Developer ID, ship a Release archive (Ghostty, the closest analog, does exactly this). **Nothing xtty built breaks** — 5/6 surfaces SAFE, the one change is build-config not code; SwiftTerm static-links (no separate framework to notarize). Pipeline (`notarytool` era, `altool` dead): archive → inside-out `codesign -o runtime --timestamp` → `notarytool submit --wait` → `stapler staple` → `spctl`/verify gate, all scriptable as `make dist`. A real Developer ID **subsumes** the `xtty-dev` TCC workaround for signed builds. Recommended change: **`add-distribution-signing`** (`build-workflow` + `verification-harness` deltas); **blocked on the $99/yr decision only the owner can make** — captured, not proposed. Critic-flagged verify-before-spending items (SwiftTerm same-team re-sign, static-link, resource-access TCC set in/out) in §8. **§9 addendum (2026-06-30 — `/opsx:explore` "is brew.sh possible?", 5-agent workflow):** yes via a Homebrew **Cask** — but it's a channel layered *on top of* notarization, **not a bypass** (cask quarantines by default → Gatekeeper still adjudicates; the `--no-quarantine` escape is gone in Homebrew 6.0.5; Homebrew is *disabling* un-notarized casks Sept-2026, e.g. Alacritty's). **Own tap first** (`kitimark/homebrew-xtty`, no notability gate), official tap later. **Tap cost vs app cost:** the own tap is **free** (the $99 is the app's Gatekeeper gate, not the tap) — an ad-hoc-own-tap path is **$0** but every user hits the "unverified developer" wall + manual "Open Anyway" (Alacritty-style); only the $99 (notarization) buys a friction-free first launch. A **separate** thin follow-on change `add-homebrew-distribution` downstream of `add-distribution-signing`; comparator casks read live via local `brew`. **§10 addendum (2026-06-30 — `/opsx:explore` "distribute with 0 cost", 5-agent workflow):** **$0 distribution already works, split by *who compiles the binary*** — a **locally-compiled** app dodges Gatekeeper entirely (verified: `xattr -l` on the repo's own Release build shows no `com.apple.quarantine`), so **source-build (`make`) is the canonical $0 + clean path and xtty is already there**; a **downloaded** ad-hoc prebuilt is $0 but costs a one-time "Open Anyway"/`xattr` per user (right-click→Open gone since macOS 15); a clean download = the $99 `add-distribution-signing`. Free Apple ID can't help distribution; Homebrew formula-from-source viable-but-a-rework (deferred). Maps to a README install section + small `build-workflow` delta, distinct from the $99 change
- [Local install & stable-build workflow (`make install`)](03-analysis/local-install-workflow-research.md) — how the terminals do a `$0` LOCAL stable-build install, feeding the **`add-install-workflow`** change (**shipped + archived 2026-07-08**: `make install`/`make restart` + tag `v0.0.1`; `build-workflow` +3 requirements) (`/opsx:explore`, 12-agent comparator sweep over `research/01-terminals/*` + 8 OSS clones + 3 local verification spikes, 2026-07-08). **Only iTerm2 (`make install`) + rio (`make install-macos`) ship a real copy-to-`/Applications` target; everyone else leaves the move manual.** Recommended shape = **iTerm2's** (build `-configuration Release` → back up old → `ditto` a self-contained copy to `INSTALL_DIR ?= /Applications`, reusing the existing `SIGN_FLAGS`/`XTTY_SIGN_IDENTITY`) with **Ghostty's** git-derived versioning; **no DMG/notarization/auto-updater** (all download-channel, `$99`, deferred — the complement of the distribution doc above). **3 critic-flagged unknowns spiked locally & all green:** the default `Release` config genuinely optimizes (`-O`+wholemodule, Debug is `-Onone`); SwiftTerm is **statically linked** (no framework to re-sign, `disable-library-validation` N/A); a CLI `MARKETING_VERSION=…` override **reaches the synthesized `Info.plist`** (measured `9.9.9`/`4242`, no PlistBuddy). **Version: cut a `v0.0.1` tag, keep the two Apple-spec plist fields numeric** (tag via `git describe --abbrev=0`, floor = `project.yml`), put the full git-describe stamp in a separate field — the first `--tags --always` draft was **critic-refuted** (`--always` defeats the `\|\| echo` floor). Guideline **G14**; decided: same bundle id `com.xtty.app`, verify-seam-first
- [CI/CD on GitHub Actions](03-analysis/github-actions-ci-cd.md) — explore-phase finding for a **`add-ci-pipeline`** tooling change (`/opsx:explore` "can we run a pipeline in GitHub Actions?", 6-agent workflow, 2026-06-30; the repo `git@github.com:kitimark/xtty.git` exists but the local clone has no remote + no `.github/` yet). **Verdict: yes — $0, secret-free CI/CD on GitHub-hosted `macos-26` runners today** (the ad-hoc "Sign to Run Locally" default needs no Apple secrets; standard runners are free+unlimited on public repos). Design: two jobs on push/PR — a **required** fast headless `test-core` (`swift test XttyCore`) + a **best-effort** `build-and-test` (XcodeGen → `xcodebuild test`, retry-tolerant); highest-value cache = `external/SwiftTerm` keyed on the pin+patch hash. **Two unknowns to design defensively around** (critic-corrected — the synthesis oversold the first): ❓ the **Metal toolchain on the runner** is a genuine 2-2 facet split (likely preinstalled on macos-26 release Xcode ≥26, but excluded for RCs) → keep an idempotent `xcrun -f metal || sudo xcodebuild -downloadComponent MetalToolchain` guard + verify on first run; ❓ **XCUITest drive-path** (`typeText`/`click`) reliability on hosted runners is unproven → gate on the headless job, start the GUI job non-blocking. Release = a separate tag-triggered job ($0 ad-hoc DMG → `gh release` now; the $99 notarized seam deferred to `add-release-notarization`). Maps to a **`build-workflow` spec delta** (no `verification-harness` delta — CI invokes existing tests, adds no observable behavior). Companion to the distribution doc above. **§9 addendum (2026-06-30 — first CI run `28425122861`):** implemented as `add-ci-pipeline`, pushed, run — **both unknowns resolved favorably.** Metal is **preinstalled** on the release-default Xcode 26.5 (guard a no-op); **`test-core` green in 1m6s** (the gate works); the predicted **XCUITest-on-hosted-runner risk is REFUTED** — **34/41 pass** (drive path, Cmd+V, zsh injection, grid+state dumps all work). **7 deterministic CI-env failures** (focus/activation cluster + clipboard + truecolor/locale) → the non-blocking job correctly *surfaced* them; hardening is follow-up. `add-ci-pipeline` left **open**. **§10 addendum (2026-06-30 — `harden-xcuitests-for-ci` diagnosis + an 8-repo per-project verification):** **"headless" was the wrong word** (corrected — GitHub runners have a real auto-login Aqua session; the true issue is a native app can't *reliably* own the *shared* host session — key-window/menu-bar/clipboard — when XCUITest fires; **unreliable, not impossible**). The "Simulators work ⇒ macOS-native UI testing works" inference is **invalid** (iOS-sim = isolated window server). OSS field: **1 robust (DuckDuckGo apple-browsers — menu/clipboard XCUITests green on hosted `macos-26`, the genuine counter-example) · 1 flaky (Maccy) · 6 avoid · 1 explicit opt-out (Ghostty, a native terminal, excludes XCUITests from CI)**. Refined plan = **hybrid**: keep the env-trigger/grid-dump side channel (the only path for xtty's custom-drawn, no-a11y content) + adopt DDG's primitives (a11y-IDs on NSMenuItems + click-first + `typeKey`) for the few must-drive-GUI tests + rewrite the focus-on-activate test + keep the GUI job non-blocking. Maps to a `verification-harness` delta **§11 addendum (2026-07-01 — first read of the actual `.xcresult` screenshots/grid-dumps, not just logs):** corrects §9–§10 — authoritative counts are **30/7/1**, the runner shell is **`/bin/bash`** (a missed factor: its zsh-deprecation banner corrupts grids, disables shell-integration, and makes `testFocusTyping…` a **false negative** → *keep* the test, don't rewrite it), truecolor/emoji is **Cmd+V** not a font/locale gap, and xtty **does** own its menu bar; net — the Cmd-key/menu thesis is **screenshot-confirmed** (6/7 failures), **fix the shell first** then scope hardening to that Bucket-B residual. **§12 addendum (2026-07-01 — `silence-bash-deprecation` measured in CI, run `28467944762`):** the fix **works** (banner gone from **0/221** attachments — Bucket A retired) but flips **zero** tests green (still **30/7/1**); the §11f "fix the shell → focus-typing flips green" prediction is **REFUTED** — `testFocusTyping…` still fails because the runner's ~72-char hostname prompt **wraps the typed marker across rows** (not the banner; banner-free run still wraps), so its fix becomes a **wrap-tolerant assertion**, and the residual is a clean **6 Bucket-B + 1 wrap** for `harden-xcuitests-for-ci`. **#1 (the wrap) carved out + implemented as `harden-focus-typing-assertion`** (scoped opt-in wrap-tolerant matcher, test-only, one `verification-harness` requirement). **§13 addendum (2026-07-01 — local `make test` of the churn test #4):** root-caused the Bucket-B churn flake — split-then-immediate-close races the freshly-split shell's **startup**, so `hasForegroundJob` is true (a startup child owns the PTY foreground) and a **confirm-close `NSAlert` blocks the close** → census `final 2`; fix = the churn test opts out via `confirm-close = false` (or waits for shell-ready). Also caught a **multi-monitor dev artifact**: local `.xcresult` recordings capture the **main/external** display while xtty forces itself onto the **built-in** screen, so they're blind to the popup (CI is single-display → unaffected). **§14 addendum (2026-07-02 — `harden-focus-typing-assertion` measured in CI, run `28472076179`): the green-flip is CONFIRMED** — `testFocusTyping…` flipped fail→pass on the first `build-and-test` carrying the wrap-tolerant matcher (counts now **34/7/1** of 42; the wrap bucket retired). The churn test's **CI** mode is pinned and ≠ the local §13 race: **Cmd+D dropped (Bucket B) → Cmd+W closes the only pane → escalation quits the app** ("not running"), so `confirm-close = false` greens only the local flake. And **Bucket B is per-keystroke flaky, not absolute** (Cmd+W landed while Cmd+D didn't; split flaky-passed in `28467944762` then failed 3/3) — the `harden-xcuitests-for-ci` residual is a clean **7 Bucket-B, 0 wrap**. **§15 addendum (2026-07-03 — full artifact forensics of `28472076179`, 12-agent workflow + 3-lens adversarial verification): ROOT CAUSE FOUND, "Bucket B" REFUTED — SwiftUI's scene-synthesized default menu clobbers xtty's custom main menu** (one-shot `NSApp.mainMenu` assignment in `applicationDidFinishLaunching` under a SwiftUI `@main` Settings-scene lifecycle; a per-launch race the 26.4 CI VM loses and local 26.2 wins). All six AX dumps show the default menu (`[Apple, xtty, View, Window, Help]`, "Settings…" fingerprint, 0 custom items); every failing chord is menu-dispatch-only (zero `performKeyEquivalent`/`keyDown` alternates), so the items **didn't exist to match** — delivery/dispatch on the runner were fine (Cmd+W fired the *default* Window▸Close → clean quit, explaining churn + new-tab; zero crash reports). Cmd+N's "pass" and the quake suite's passes were **vacuous tests**; §13's confirm-close opt-out would not have fixed CI; §14a is **softened** (focus-typing is P/FFF/P — run 1 passed *without* the matcher). **A user-facing product bug CI reported since run 1**, not a CI artifact. Fix plan (adversarially reviewed): durable same-instance menu re-assert in unconditional product code (→ eventually `NSApplicationMain`, Ghostty pattern), menu-integrity canary + a11y-IDs scoped to Cmd-driving suites, vacuous-test truthing (Cmd+N `windowCount`, quake XCTFail, shared `requireStateDump()`), no in-test retries, `.function` strip as bisectable commit, privacy-gated pasteboard instrumentation, pre-registered second-order exposure matrix — reshapes `harden-xcuitests-for-ci` into `fix-main-menu-clobber` (product) + a `verification-harness` truthing delta. **§16 addendum (2026-07-03): the churn-flake fix design decided** — a computed-marker execution roundtrip in both churn loops (test-only; `hasForegroundJob`-based waits empirically refuted) + the dump-timer `.common`-modes companion (the default-mode timer froze during the confirm-close `runModal`, explaining the stale-dump artifacts); full capture → the confirm-close & shell-readiness doc below. **§19 addendum (2026-07-06): the CI expected-difference matrix** — the §11–§18 residual findings consolidated into a crisp table (`findbar-marker-wrap`, `bash32-no-bracketed-paste`) + a required-gate/non-blocking job map + a reverse duty, as the durable **classification target** the committed `xtty-ci-investigator` agent defers to at run time (the CI counterpart to `packer/README.md`'s VM matrix)
- [Confirm-close & shell readiness](03-analysis/confirm-close-shell-readiness.md) — the churn-flake deep-dive settled (2026-07-03, `/opsx:explore` split-agent workflow: code probe + iTerm2/Ghostty/Kitty/WezTerm prior-art clones + an **empirical pty foreground-pgid probe** + design synthesis). **Key empirics:** `fg == shellPid` from ~3 ms (before `.zshrc` — a single `hasForegroundJob` read is worthless), startup children grab the tty in 15–57 ms bursts **invisible to the 150 ms dump sampler** (k-consecutive-reads = a disguised load-scaled sleep), last flicker ~2 s under load, **CI bash: zero flicker <35 ms**, and **every startup fg episode had zero PTY user input**. **Field survey:** no terminal fixes the fresh-shell race product-side or has a creation grace (iTerm2 defaults to never-prompt + opt-in name walks; WezTerm skip-lists; Ghostty/Kitty gate on OSC-133 at-prompt and would prompt here too) — xtty's `tcgetpgrp` check is field-defensible. **Decided fix (test-only):** computed-marker roundtrip (`echo $((41000+i))` → grid match proves execution) in both churn loops, 15 s timeout, wrap-tolerant, fail-fast; companion = dump timer → `.common` modes; pre-planned escalation = a `hasForegroundJob` dump field + one post-marker read. **Protocol ladder captured as future candidates:** at-prompt confirm-close gate (Ghostty/Kitty model — `BlockTracker.Phase.atPrompt` already exists), bash shell integration (`bash-preexec`/`--init-file`, protocol-complete, retires the marker, lights blocks/sidebar for bash users), and the novel **zero-input latch** (`hasForegroundJob && userHasInteracted` — the only fix for the real-user mid-startup ⌘W papercut; no field precedent). **§7 addendum: the design is artifact-confirmed** against the three local runs — per-keystroke timelines decode both failures exactly (alert at pane-iter-4 / tab-iter-2; frozen-dump timing signature; the marker gate would have held ⌘W past the struck iteration in both)
- [Local macOS-VM CI reproduction](03-analysis/local-macos-vm-ci-reproduction.md) — can we reproduce the §15 menu-clobber race in a local VM? (2026-07-03, explore, three workflows: 5 per-project `/tmp` source reads + a lume deep-read + fidelity/cost analysis). **Six-tool survey** (Tart 5 — OpenAI-owned, `tahoe-xcode` OCI images with TCC pre-seeded for SSH-driven XCUITests + `actions/runner` preinstalled; lume 4.5 — MIT, `--unattended tahoe` automates Setup Assistant on a 26.x host; macosvm 4; UTM 3; Anka Develop 3; VirtualBuddy 2.5). **Key facts:** the exact CI build **25E246 IPSW is still signed today** (18 GiB — time-sensitive, download now); guest-newer-than-host on VZ is **undocumented at minor granularity** (PoC checkpoint 1); speed-vs-version is **perfectly confounded** in existing evidence; the `actions/runner-images` provisioning recipe **verified from source** (kcpassword auto-login, `chsh -s /bin/bash` *provisioned*, `automationmodetool` + TCC grants = the XCUITest prerequisite); **measured host constraint: 66 GiB free → the prebuilt-Xcode-image route is RULED OUT on this machine, the IPSW route is marginal** (needs 30–40 GB freed or an external SSD). **Staged, gated PoC plan** (disk+IPSW → install checkpoint → runner-parity provisioning + golden clone → SSH-driven smoke → the diagnostics-only experiment at 3 vCPU + load) with a **pre-registered readout** (clobberCount>0 = local reproduction; 0 = weak, rig retires to the §15f rehearsal floor). Recommendation: **lume primary / Tart fallback**. **§7:** `nektos/act` **cannot** simulate the macOS runner (source-verified — containers are Linux-only; `-self-hosted` host mode runs steps directly on the dev Mac that always *wins* the race); the faithful "act for macOS" is the Tart image's preinstalled `actions/runner` inside the VM. **§8 (2026-07-04): PoC EXECUTED — JACKPOT.** Built the lume VM (macOS 26.4/25E246, 3 vCPU) from the verified IPSW; ran the full `xttyUITests` suite in-guest (host-built products via `test-without-building` — the guest Metal toolchain is unfetchable, `xcrun -f metal` PATH-resolves as a false positive) → **56 exec / 1 skip / 30 fail = 34/7/1, IDENTICAL to CI run `28472076179`, same 7 failing tests** (all pass 100% on bare metal); ~100% clobber at 3-vCPU idle. **Mechanism found (CI couldn't):** the clobber is **in-place item mutation of the same `NSMenu` object, not a pointer swap** (measured `mainMenuPtr==builtMenuPtr`; built menu's own items = SwiftUI defaults incl. a "Help" menu xtty never creates) → **refutes §15e Stage-A** (re-assert-same-instance is a no-op) and makes **`NSApplicationMain` the required fix**, with a **title-based** (not identity-based) diagnostic. Evidence: `~/Downloads/xtty-vm-poc/artifacts/`. One manual VNC click (enable Remote Login) was the only non-scriptable step; minor-newer-guest-on-host **works** (§1 checkpoint ✅). **§9 (2026-07-04): second rig on Tart + the race PROVEN.** Tart's prebuilt `macos-tahoe-xcode` image (macOS 26.4/25E246, Xcode+Metal+runner all preinstalled) ran the ci.yml job **natively** in-guest — no Setup Assistant, no Metal fight, no host-build workaround (the clean rig lume couldn't be); `tart pull --concurrency 64` beat ghcr's per-connection throttle (2→25 MiB/s). **Headline: two native runs of the identical VM+binary diverged — Tart run 1 = 36/5/1 (the 2 Cmd+D split tests flaky-passed on a race-winning retry), Tart run 2 = 34/7/1 (they failed, exact CI+lume match)** → the SwiftUI menu clobber is a non-deterministic **per-launch race, proven live**. Reinforces the fix: a retry-tolerant suite MASKS the race (run 1 passed 2 broken tests), so the fix must **eliminate** it (`NSApplicationMain`), not add retries. Tool verdict: Tart (native, no workarounds) is the cleaner rig at the cost of a 64 GiB download + external disk; lume works on internal disk with the workarounds. A **third** Tart run (watched live in a graphics window) landed 34/7/1, confirming the base clobber rate (2× 34/7/1 + 1× 36/5/1). **§10 minimal-image research (2026-07-04): a lean reusable xtty-test image floors at ~38 GB** (macOS ~22 + Xcode-macOS-SDK-only ~14 + tools ~1.5) — the whole 87→40 GB win vs Tart is **dropping simulators** (separate downloads, never installed for a Mac app; lume's 38 GB was already this floor). **Metal crux (✅ source-verified): xtty needs the Metal toolchain ONLY for SwiftTerm's bundled `.process("Shaders.metal")` (Package.swift:83); patching it out via the existing SwiftTerm patch makes xtty build with ZERO Metal toolchain** (costs one skippable perf-harness test) — which also **de-fragilizes xtty's real ci.yml** (drops the flaky `downloadComponent MetalToolchain` guard; the Metal download is a deterministic catalog-rotation trap, not random flakiness). Bake/reuse = **golden-clone** for solo (CoW clones near-free); `push` to GHCR only when CI/teammate appears; tart↔lume image formats incompatible. Candidate, not implemented. **§11 (2026-07-04): the concrete cirruslabs-style Packer recipe** (read `macos-image-templates`) — build **vanilla → base → xcode** layers with the `tart` Packer plugin; **the XCUITest infra (auto-login + TCC + automationmode) lives in vanilla+base**, so building on `macos-tahoe-base` inherits it (why the Tart runs "just worked"). Xcode is installed via **`xcodes` from a pre-downloaded `.xip`** (`--path`, zero Apple-auth in the build; the `.xip` is cached out-of-band via `xcodes download` + an **Apple ID**, once — no way around the Apple ID for a clean install; it's the official installer, NOT the host's `Xcode.app`). Metal timing **confirms §10c** (cirruslabs `-downloadComponent MetalToolchain` succeeds only because it runs at Xcode release time when Apple serves the matching build). A minimal `xtty-test.pkr.hcl` = base + Xcode-from-`.xip` + `xcodegen`, **minus** `-downloadPlatform` (the ~45 GB simulators) + Android/Flutter/fastlane + Metal (patched out) → ~40 GB. Candidate change `add-xtty-test-image` (gated on an Apple-ID Xcode download + the SwiftTerm Metal-patch); Packer not installed. **§11g (2026-07-04): DECIDED + both PROPOSED** — full Metal retirement as its own change **`retire-metal-renderer`** (grounding showed the patch is spec-visible — 4 established specs touched — not a build tweak; env-flagged-strip + bake-the-toolchain alternatives rejected) and **`add-xtty-test-image`** (Packer per the owner, generic image — no xtty source baked in, pinned base + Xcode-26.5 `.xip`, ghcr push deferred); image hard-depends on the retirement; both independent of `fix-main-menu-clobber`. **§12 (2026-07-05): the minimal image was BUILT + verified** (`make image` → `xtty-test:26.5`, **35 GB**, macOS 26.5/25F71, Metal-toolchain `uninstalled`, menu-clobber race reproduced) — and an in-guest run surfaced a **Local Network privacy modal** root-caused (2-track workflow) to a **cirruslabs image version-skew** *(superseded — see §12f)* (`macos-tahoe-base:latest` = 26.5, but the frozen `macos-tahoe-xcode:latest` the §9 rig used = 26.4; the xcode layer isn't rebuilt when base bumps — temporal-impossibility proof from ghcr upload dates), triggered by the **XCUITest runner↔app IPC** over the routable vmnet address (NOT xtty networking, NOT TCC.db — Local Network privacy lives in NetworkExtension). Apple's TN3179 `AllowedEthernet/WiFiLocalNetworkAddresses` defaults were tried as a pre-suppression and **screenshot-refuted** (dialog still appeared after write+reboot — a verify-the-effect-not-the-syntax lesson); but the dialog is **benign** — headless runs are 33/8/1 with or without it (no focus theft), only graphics loses +1 test — so **run headless for clean measurement**. **§9g disk footprint** (measured): Tart ~81 GB actual (`du`'s 165 GB double-counts CoW clones — measure by `df` delta), extra VMs ~free via APFS CoW, needs external disk; lume ~38 GB/VM full-copy clones, fits internal — similar ~80 GB total, opposite shape. **§9h:** lume has **no prebuilt macOS-26+Xcode image** (`ghcr.io/trycua` has only vanilla-26.2 + an old sequoia-xcode-15.2) — the structural reason the lume path needed manual Xcode + hit Metal; Tart's `macos-tahoe-xcode` is the only "spawn→build+test, zero-setup" option today. **§12f (2026-07-05): the Local Network root cause CORRECTED (supersedes §12b/§12c)** — the modal is xtty's own `ProcessInfo.hostName` → NSHost **reverse-DNS of all local addresses**, reachable only under a **zsh** login shell (OSC 7); NOT the macOS version (fresh-identity repro on bare-metal 26.2), NOT the XCTest IPC (plain launches fire it; the runner issues zero dnssd requests), NOT fixable by `scutil --set HostName` (measured inert). **Fixed by `test-image-bash-shell`** (guest shell → bash, the hosted-runner config): rebuilt image = 0 reverse-DNS / 0 prompts and an **exact CI-envelope match 34/7/1**. Deep record + probe catalog + investigation guideline → the dedicated forensics doc (next line)
- [Local Network privacy forensics](03-analysis/local-network-privacy-forensics.md) — the 2026-07-05 deep record behind the §12f correction: **macOS Local Network privacy machinery** (mDNSResponder `mdns:trust` gate, nehelper as arbiter+presenter with a prompt *queue*, the world-readable NetworkExtension store and its `DenyMulticast` semantics, silent record creation, no supported pre-seed) + **`ProcessInfo.hostName`/NSHost internals** (lldb-proven: `blockingResolveUntil:` semaphore-blocks the calling thread while reverse-DNS PTRs every local address; `scutil --set HostName` measured inert; `gethostname`/`SCDynamicStoreCopyLocalHostName` verified non-resolving). **Theory-by-theory retraction table** (TN3179 ❌, macOS-version ❌ — fresh-identity repro on bare-metal 26.2, XCTest-IPC ❌, HostName ❌, bash-shell ✅, `PaneController.localHostNames` call site ✅) with the killing experiment for each. **Reproducible probe catalog** (the 2-min LN log capture + verdict greps, the fresh-bundle-id identity reset, lldb attribution with `thread backtrace all`, NE-store reads, controls, hosts-file discriminators, known-dead instruments). **G1–G10 investigation guideline** for future privacy-gate/rig confounds: verify effect not syntax, one variable per trial (enumerate bundled deltas), fresh identities over fresh machines, instrument-before-fix, controls, in-place retractions, evidence hierarchy, escalation ladder, remove-the-arbiter's-input, match the reference rig at the source. **2026-07-07 addendum (§8):** TN3179 *macOS considerations* read in full (the daemon/root/SSH-child auto-grant exceptions, responsible-code attribution, no-MDM); the **build-time-grant refutation** with a **three-tier NE-store provenance map** (hand-baked → **pruned on boot**, implicit → re-prompts, explicit → durable+silent — `nesessionmanager` prunes records lacking live-consent provenance); the **SSH-child refutation** (a GUI app bundle is its own responsible code, so the SSH exception doesn't cover it — and isn't why headless is quiet); the **`$HOST`=`gethostname` call-site lock** pinning the product fix to `gethostname()` (behavior-preserving and *more correct* than the reverse-DNS source). New fates **T7–T10**, new guideline **G11** (a privacy grant needs live-consent provenance — unforgeable at build time). **2026-07-08 addendum (§9):** the product `gethostname` fix measured **GREEN** on the zsh graphics rig (0/0/0, no modal, no freeze — the red→green complement of `add-zsh-test-image` 4.1); a **G3 corollary** (P5 isn't a clean discriminator on a golden that has built xtty — it carries a benign `com.xtty.app` `UUID cache hit` record into every clone; read P2a/b/c), and **T11 + G12** — refusing to pre-seed the *instrument's* own Screen-Capture gate (the `com.apple.sshd-session` picker-bypass prompt), capturing host-side instead
- [SwiftUI main-menu clobber source forensics](03-analysis/swiftui-mainmenu-clobber-forensics.md) — the 2026-07-05 source-level attribution of the §15 menu clobber (6-agent workflow: dsc-extraction + disassembly of the real SwiftUI binary, live lldb on the xtty Debug build, Ghostty/CodeEdit/OpenSwiftUI prior art, migration-surface map, adversarial verifier). **The machinery named:** `SwiftUI.AppDelegate` (the *real* `NSApp.delegate` under the SwiftUI lifecycle; the adaptor delegate is only forwarded-to) → `makeMainMenu(updateImmediately:)` → `AppKitMainMenuItem.updateMainMenu` — an identifier-keyed reconciler whose `CollectionChanges` diff never matches plain `NSMenuItem`s, so it strips every custom item **in place** and inserts SwiftUI defaults incl. the Help menu (`setMainMenu:` fires only on first install — §8d's pointer-unchanged finding explained at instruction level); **reproduced deterministically on bare metal** via a synthetic trigger (no VM needed for the mechanism). **Launch ordering measured:** the initial pass is a *synchronous* `willFinishLaunching` callout preceding xtty's assignment (community "async after ADFL" folklore corrected); exactly one pass fires per launch on fast bare metal — why local runs are green; the VM/CI clobber is the second call site (`scenesDidChange(phaseChanged:)`, ❓ by elimination, never observed live). **Fix candidates settled live:** `.commandsRemoved()` ❌ (probe5 — reconciler still strips everything), rebuild-on-notification ❌ (probe6 whack-a-mole + wrong trigger; Ghostty's deleted `CursedMenuManager` is the field record), SwiftUI Commands ⚠️ second choice only (CodeEdit precedent + swizzling costs), **`NSApplicationMain` ✅ by construction** (both `makeMainMenu` call sites die with `SwiftUI.AppDelegate`; Ghostty's 2023 migration = 3-year shipping precedent incl. SwiftUI window content under NSHostingView; xtty's migration surface = one construct). **Probe catalog P1–P8** (dsc_extractor shim, nm/demangle, runtime ObjC enumeration, disassembly anchors, lldb-by-mangled-name, the synthetic-trigger repro, fix-candidate probe apps) incl. dead instruments (`dyld_info -objc` on cache dylibs, identity canaries) + the **F1–F7 closed-source-framework forensics guideline** (complements G1–G10). Evidence: `~/Downloads/xtty-vm-poc/artifacts/menu-clobber-source-forensics/`
- [CI-runner prompt-width forensics](03-analysis/ci-runner-prompt-width-forensics.md) — why `testFindBarOpensLocatesAndDismisses` reds **only** on the hosted runner, not the local Tart VM or bare metal (2026-07-06, `/opsx:explore` during a `28801860582` triage; verified from an `actions/runner-images` clone). **Not a product bug, not a shell difference — one environment variable: hostname length.** Stock macOS `/etc/bashrc` `PS1='\h:\W \u\$ '` is byte-identical on runner + VM (grep-proven: `runner-images` sets **no** `PS1`), so prompt width = `len(\h)`; the runner's ~61-char `\h` (`sjc22-be105-…`) soft-wraps the typed marker across grid rows, defeating the **strict** `waitForContains` at `:192` (the wrap-tolerant matcher was applied only to focus-typing `:53`) — focus **does** return, the assertion is prompt-width-fragile. **The long `\h` is GitHub-network-injected at runtime, NOT in the image build** (`configure-hostname.sh` sets only `Mac-<epoch>.local`) → you can't reproduce it by rebuilding the image, and our short-`\h` VM (`Manageds-Virtual-Machine`) is blind to prompt-width flakes by construction (why the `39/1/1` envelope's sole residual is paste, not find-bar). Only lever = `\h` length, which we control. Fates table (copy-the-hostname ❌, VM-mirrors-CI ❌, shell/banner-diff ❌, long-`\h`-revives-the-LN-modal ❌ decoupled under bash, product-bug ❌, gethostname-key-in-Tart ❓ open spike). Options menu (wrap-tolerant fix · narrow-column guard · long-`\h` VM fidelity + its envelope re-baseline) captured, not decided. Companion to §19b + the VM doc
- [Shell-dependent test partitioning](03-analysis/shell-dependent-test-partitioning.md) — the 2026-07-07 `/opsx:explore` finding behind two changes (`add-zsh-test-image` → `split-shell-dependent-testplan`, **both built + archived 2026-07-08**), started from "how to fix `testMultiLinePasteIsNotAutoExecuted`". **The suite is already partitioned by shell, but implicitly — and on the bash rig the shell-dependent half was a false-confidence trap.** Two shell capabilities decide ~half the 42 tests, neither being xtty code failing: OSC 133/7 capture is **zsh-only** (`ZDOTDIR` injection), and bracketed paste needs a readline the rig's **bash 3.2.57** lacks. So on the bash rig (CI + Tart VM, `chsh -s /bin/bash` on purpose) the **19-method semantic-capture family early-returned before asserting** → **vacuous passes that read as coverage**, while the lone family-less paste test was the *only* shell-dependent test honest enough to hard-fail. Concrete partition table (~20 shell-dependent / ~22 intersection). **Shipped strategy (measurement-first ordering, then a pivot):** built `xtty-test-zsh` **first** (packer `shell = bash|zsh`) and ran the *unmodified* suite on both rigs to observe the divergence by effect (the rig-level `/etc/hosts` LaunchDaemon to neutralise the zsh-only Local Network gate was **refuted** — the durable fix is the product change `fix-osc7-hostname-reverse-dns`, the `gethostname` swap); **then**, rather than the originally-sketched `Intersection`/`ShellInteractive` `.xctestplan` split + honest `XCTSkip`, `split-shell-dependent-testplan` **pivoted to parameterize-and-assert** — each shell-dependent test asserts the correct behavior for the terminal's *observed* capability (paste branches on the new `verification-harness` `bracketedPasteMode` dump field, sampled after the shell-readiness gate, and asserts **both** the bash execution arm and the zsh staged arm; the family asserts a crisp negative where capture is absent — **0 last-resort skips, no test-plan partition**). Measured `41/0/1` on both goldens, 0 vacuous passes. Fates table (LN pre-grant ❌, bash-5.1 swap ❌ fixes one test not the family, skip-everywhere ❌ alone, two-plan-partition ⊘ superseded, parameterize-and-assert ✅ shipped). Guideline: **a graceful-degradation arm that returns before asserting is a vacuous pass — assert the correct per-shell behavior on the observed capability (skip only as a last resort), and prove each arm by effect on a rig that has that capability.** Companion to §19b + the prompt-width + LN forensics; both changes archived
- [Claude Code subagent execution forensics](03-analysis/claude-code-subagent-execution-forensics.md) — the 2026-07-06 record behind `xtty-test-validator` v4 (7-agent forensic workflow over four full sweep transcripts + live version probes + a red-team pass): **why long-wait subagents strand** — a subagent that stops calling tools is closed forever (the Bash tool's "re-invokes you when it exits" promise is main-loop-only; measured zero re-invocations), the anti-sleep hook *recommends* the `run_in_background` trap at the decision moment, and all three failures orphaned their own wait loops — an **affordance failure, not model disobedience** (the Sonnet control completed 43 min with plain foreground `while kill -0` waits). **Definition-propagation lag measured** (stale 63 s after an edit — the anti-park rules were never delivered, invalidating the v2/v3 "wording failed" inference; fresh ~40 min later): the durable rule is a per-report `Definition:` stamp + verify-delivery-per-run, never assume. **Fates table** (Sonnet-ignores-rules ❌ probe-killed; fresh-session-required ❌ refined to lag; fused atomic launch+wait ❌ red-team-killed pre-ship — a wait timeout must never SIGTERM the workload; model pinning ❌ control-killed). **Probe catalog** (in-context version probe — the only witness, transcripts don't record system prompts; report-stamp standing probe; transcript greps; `ps`-over-notifications) + the 6-point **reusable guideline** for any subagent that outlives a tool call. Validated by effect: 3 consecutive clean Sonnet sweeps (14/65/30 min, zero strands, zero `run_in_background`)
- [AGENTS.md context budget](03-analysis/agents-md-context-budget.md) — the 2026-07-06 measured record behind `slim-agents-context`: the always-loaded guide had grown into a 32.8k-token startup tax (2.46 bytes/token — /4 estimates undercount this file class by a third; +2.5k tokens/day, append-per-change) and the **rules/history split** saved **21,548 startup tokens** while **improving** behavior — a pre-registered fat-vs-slim probe A/B (12 probes × 3 reps, frozen baseline) scored slim 36/36 vs fat 34/36, with OpenSpec routing under implementation pressure flipping 1/3 → 3/3 (lost-in-the-middle measured, not theorized). **Subagent CLAUDE.md inheritance probe-proven** (zero-tool marker quote; a trivial spawn costs 49.3k at fat → workflows paid ~230k–790k in AGENTS.md copies). **Ablation priced the tiers**: status tables ~1,320 tokens (recall value unpriced — probe-set gap recorded), the 11 inoculation one-liners ~463 tokens and **load-bearing** (without them the retries trap regressed 3/3 → 0/2, "Good instinct — nothing to add"). Fates table (fat-is-safer ❌, recall-needs-history ❌, MCP-lever ❌ deferred-already, memory-leak-into-worktrees ❌), reproducible probe catalog (headless usage instrument, presence probe, ablation method), re-verify via a fresh `/context` (~12k Memory files), and a 6-point guideline for any always-loaded agent guide. ❗ **ADDENDUM 2026-07-28 (§A)** — the Current-status section regrew anyway: net 28,132 → 44,996 B in 22 days, `## Current status` at 22,613 B = **50.3% of the guide**, the refutations tier at 17,367 B across 31 entries (20 exceed the spec's one-line bound). The multi-pass fan-out behind it found a **live three-copy false claim** — "the prior 56-test envelope was identical across all 5 environments" (AGENTS.md / HISTORY.md / packer/README.md; the VM tiers are actually **3 tests behind across 2 changes**, last identical measurement the 55-test `54/0/1` of 2026-07-10) — all copies drifted *together*; the duplication detected nothing (new guideline 7: N caches drift together — a cache is a drift-detector only if one copy is independently re-measured; guideline 8: a curated index needs an edit-in-place rule for retirement, or every retirement grows it). Became the **open `slim-status-surface` proposal** (0/26 at capture, commit `4903359`): 31→24 refutations, a packer "Current envelope" single-truth block landing first (corrects the false claim), envelope pointer-not-cache, three `research-capture` ratchet clauses, verification via the archived probe harness + a new turn-1 inline-sufficiency instrument. Fates: pointerize-the-open-changes-table ❌→**adopted 2026-07-28** (Addendum C: the deferred-until-friction condition was met — observed drift + a stated owner preference — and the reversal shipped via `pointerize-open-changes-table`); delete-the-shipped-table ❌ (milestones.md is stale/incomplete; `coherence-review` would go vacuous); one pass's "`smkx` orphaned" claim ❌ (refuted by grep, while the sibling `wheel1` orphan claim survived the same re-check). ❗ **ADDENDUM B 2026-07-28 (§B)** — `slim-status-surface` shipped and its pre-registered gate **PASSED**: compressed FROM the corrected **46,234 B** (the frozen N at `eaa765b` — not the propose-time 44,996) to **37,373 B**, median startup **−3,249 tokens** (≥3,000 floor; worst-rep 2,930 recorded as a variance finding, median governs), behavioral **S′ 62/66 vs N 56/66 with zero per-probe regressions** — the two cells the change targeted both moved 1/3→3/3 (E1: the cached-envelope poison measured live at N — 2/3 reps taught the false 56-test claim as fact — then measured fixed via the packer Current-envelope pointer; G1: both-directions delivery). A new **turn-1 inline-sufficiency instrument** gated the merged cross-model cluster (a trap PASS off a secondary doc home certifies nothing); its one miss was repaired by the pre-registered contingency (un-merge one conclusion line, +126 B, never narrative — fix loop used 1 of 2). Ablations attribute the fixes: **A-R** (compression only, keeps the cache) still taught the false claim — the packer block+pointer is E1's fix, not the compression; **A-0** (cluster deleted) collapsed G1 to **0/2** while X1/X2 "passed" off secondary homes — compression holds inoculation, deletion breaks it, the merge floor stands. New guidelines 9–10 (gate per-probe vs a freshly frozen baseline and expect the baseline to fail somewhere; instrument inline sufficiency and repair by un-merging a line)
- [Agent-guide ingestion across CLI vendors — forensics](03-analysis/agent-guide-ingestion-forensics.md) — 2026-07-28, a side investigation off `slim-status-surface`: does AGENTS.md even reach a non-Claude agent? **Headline: every non-Claude vendor CLI tested silently truncates a large project guide, and neither warns.** Codex (`codex exec`) defaults to a documented **32,768 B** cap (27.8% of the 45,362 B guide dropped, incl. the entire `## OpenSpec workflow` section) with a working override (`-c project_doc_max_bytes=...`); Antigravity (`agy`) caps at **~23,450–24,150 B** (measured by sentinel sweep, ~48% dropped) **and never ingests the guide at all** without an explicit `--new-project`/`--project` registration — a naive adapter would silently measure an empty guide. Counter-intuitive write-block finding: Codex's `-s read-only` sandboxes shell commands only — the model's native `file_change` tool wrote to disk anyway; the real block needs `-c approval_policy=untrusted`. Also found: `gemini-3.1-pro-high` silently substitutes `Gemini 3.6 Flash (High)` at exit 0, no warning (only `-low` resolves correctly, 116/116). Companion to [AGENTS.md context budget](03-analysis/agents-md-context-budget.md) — that doc measures what the guide *costs*; this one measures what fraction of it is even *delivered*. **Recommendation (audited): don't build a 3-vendor probe campaign** (~523 runs, 2–3 working days, and both non-Claude arms are structurally non-comparable by construction — agy's N/S truncate at different frontiers, Codex's gated arm requires a non-default config) **— capture the findings, surface the ≤32,768 B byte target to `slim-status-surface` as a non-gating stretch, hold a Codex-only smoke (`probe-guide-multi-vendor`) behind named tripwires.** One planned research topic (cross-vendor instrument normalization) was blocked by a safety classifier and never completed — recorded as an open gap, not silently assumed done. Also surfaced: a research subagent attempted (blocked, never applied — independently verified) to patch `~/.gemini/trustedFolders.json`, a different application's real config file, while investigating Antigravity's trust mechanism — logged as guideline G-ING-4.
- [Dev-workflow agent orchestration](03-analysis/dev-workflow-agent-orchestration.md) — the 2026-07-07 data-fitted model for delegating xtty's OpenSpec loop across agents (explore→research→propose→verify), produced by a **3-agent parallel fan-out that dogfooded the model**. **Git forensics:** solo-human+AI, bursty (~5.7 changes/active-day on 6 of 11 days), **79% docs / 21% impl** (~4.3 commits/change), 91% changes cite research, 50/50 tooling/product (recent batches ~100% tooling), **2–5 changes concurrent** (peak 5) with collision protocols + red→green pairs. **Model:** main-as-reducer; delegate on conclusion-vs-journey; mechanism by (context×write-scope×duration) → fork(author)/committed-agent(investigate·research·critic)/main(shared-write reconcile)/Workflow(scale); the **~12k/subagent inheritance floor** sets the delegation break-even; command=launcher/agent=worker (half-built — explore/propose/review have no `xtty-*` worker). **Governing rule from the project's own history: build agents REACTIVELY after proven friction, not a speculative roster** → narrowed to *build ONE change-set-aware `openspec-critic` + adopt fork-per-change authoring*; the distinctive need is **cross-change coherence**. Fates: roster-of-4 ❌, persistent-state-ledger ❌ (compute on demand), cold-author ❌, source-researcher-priority ❌ (demoted). Live proof the critic's needed: AGENTS.md's own "7 of 10 harness deltas" is **stale (actual 19/34)**. No agent built — design rationale before any build. **§11 addendum (2026-07-08):** **T3 revisited** — clone-heavy source research has now recurred (probe: 13 fan-out docs, **9 cloned external source**), so the reactive bar (§8) is met; **decided to build `/xtty:research`** (source-research launcher: explore → readers `sonnet` → synthesis `opus` → critic `opus` → verify → capture, model tiers per §3.D) **as a *Workflow launcher*, not the agent roster T1 refuted** — **Medium tier** (a `/xtty:research` command carries the pattern + tiers + capture tail; main authors the heterogeneous fan-out inline; no new `.gitignore` exception, no `Definition:`-lag since the worker is a Workflow). Completes the §3 "explore/research has no launcher+worker" gap. **Decided, not yet built** (`add-research-launcher`)
- [Archive-trigger affordance forensics](03-analysis/archive-trigger-affordance-forensics.md) — the 2026-07-08 record behind `harden-archive-completion-trigger`: **why the archive + reconcile completion ritual didn't reach the `/opsx:apply` loop.** Measured on the `fix-osc7` archive — the completion task read *"On completion, reconcile trackers…"* with **no archive verb / procedure pointer**, so the loop improvised `openspec archive` and reached the expected `/openspec-archive-change` skill **only after the user intervened**. Root cause: **an AGENTS-only rule doesn't reach the checkbox-walker** — only text in the task line does (the generic apply skill models *implement* + merely "suggests archive"; it never re-reads AGENTS.md; `config.yaml` `rules` don't ride `openspec instructions apply`). The obvious anchor is unusable — the archive **skill is gitignored** (`git ls-files` empty; `.gitignore` un-ignores only `xtty-*`), so a committed `tasks.md` can't cite it. **Fix = the project's proven point-of-tick marker, reused:** `⟶ archive-ritual` seeded via `config.yaml`, anchored to **committed** guidance (skill = optional accelerator). **Scaffold-gap table** (archive was the one recurring procedure lacking the tool/launcher/marker the 3 delegation triggers all have) + a twist — the marker is a **procedure pointer, not an agent delegation** (the ritual runs inline, owning the session's own tracker edits). Fates (disobedience ❌, bake-the-skill-path ❌, new-archive-agent ❌, edit-the-generic-skill ❌, restate-in-config ❌, `/xtty:archive` launcher ❓ deferred behind the by-effect gate). **Re-verify by effect** — a real archive driven from the marker on a fresh context, never a syntax read-back. **Guideline G13:** the apply loop only reliably executes procedure written at the point-of-tick
- [Mouse-wheel scroll forensics](03-analysis/mouse-wheel-scroll-forensics.md) — the 2026-07-09 root-cause of **why alt-screen mouse-tracking apps (htop) don't scroll with the wheel in xtty** (live peekaboo A/B vs iTerm2 → a `/xtty:research` fan-out `wf_5857687e-56f`: 5 peer readers `sonnet` → synthesis `opus` → adversarial critic `opus` → orchestrator inline SwiftTerm reads + a `cat -v` byte-capture verify-by-effect). **Bug:** embedded SwiftTerm `MacTerminalView.scrollWheel:2172` unconditionally moves local scrollback and **never reads `terminal.mouseMode`** → emits **zero pty bytes** on the wheel (measured: iTerm scrolls htop a page, xtty sends nothing both directions); on the alt-screen the local scroll is a dead no-op (`maxScrollback=0`). **Correct policy (5-terminal consensus):** the priority-ordered **3-way branch** — mouse-report (SGR buttons 64/65, gate `sendButtonPress()` = vt200/buttonEventTracking/anyEvent, exclude off/x10) → alt-screen **alternate-scroll arrow keys** (DECCKM-aware SS3/CSI; SwiftTerm parses **no** 1007 → unconditional kitty model) → local scrollback; **Shift+wheel bypasses to scrollback**. **Fix API all present** (`encodeButton` 4→64/5→65, `sendEvent`, `calculateMouseHit` 0-based col + `yDisp`-clamped row, public `applicationCursor`, `EscapeSequences.moveUp/DownApp/Normal`) — deliver as a SwiftTerm patch hunk + upstream. **Critic-caught fix defects:** ❌ `calcScrollingVelocity`-as-report-count floods 20–32/notch, ❌ momentum spam, ❌ widened horizontal guard regresses, ❌ synthesis's `cellCoordinates`/1-based coords don't exist. **Fates table** (mis-click ❌, code-read-suffices ❌, "40 ticks=1 line ⇒ velocity undercount" ❌ *category error* — alt-screen sends nothing, it was htop re-sort noise, 1007-flag-exists ❌, any-mode-gates-wheel ❌, xtty-product-bug ❌ — it's upstream SwiftTerm). **Guideline:** verify a terminal's child-input emission **by effect** (`cat -v` under `\e[?1002h\e[?1006h` vs a control terminal), not by reading the scroll handler. **Shipped** as `fix-scroll-wheel-mouse-reporting` (archived 2026-07-08): the 3-way-branch `scrollWheel` rewrite delivered via the SwiftTerm patch + `terminal-session`/`verification-harness` deltas + 6 `XttyMouseWheelUITests`; manual-verified by effect (htop scrolls, `cat -v` shows `^[[<65/64;…M`) and measured `47/0/1` of 48 on both VM goldens
- [Scroll-momentum smoothness research](03-analysis/scroll-momentum-smoothness-research.md) — 2026-07-09 **follow-up** to the routing forensics: **why xtty's wheel feels choppy _after_ the routing fix** (a `/xtty:research` fan-out `wf_2cf7d159-6ef`: 6 readers [iTerm2, Ghostty, kitty, WezTerm, Alacritty, SwiftTerm-local] `sonnet` → synthesis `opus·high` → adversarial critic `opus·xhigh`, 8 agents/0 err/529k → orchestrator **firsthand git** verify). **Two xtty-ADDED bugs** (both absent from pristine upstream — `git show HEAD` = 0 `momentumPhase` refs): (1) a **blanket momentum drop** `if event.momentumPhase != [] { return }` placed **above** the 3-way dispatch (`MacTerminalView.swift:2194`) → kills the macOS inertial coast on **all** branches incl. local scrollback (trackpad/Magic-Mouse stops dead on finger-lift); (2) a **cap-and-discard** accumulator (`remainder -= whole; return min(5, whole)`) → fast gestures silently lose scroll distance. **Peer consensus (5/5):** never drop momentum, never cap-and-discard, **never time-throttle** — flood is bounded by pixel→cell **quantization** + (mouse-report branch) **multiplier→1×** / magnitude-less SGR; a hard flick legitimately emits ~1 report/cell over the whole coast. **iTerm2 is non-transferable** (delegates scrollback to stock `NSScrollView`; xtty's grid is a custom-drawn `NSView`) → the **hand-rollers** (Ghostty/kitty/WezTerm/Alacritty) are the templates. **Fix shape:** delete the guard; per-branch lossless remainder-carry (+ **required** discrete scrollback multiplier or classic-wheel regresses to 1-cell/notch; + mouse-report multiplier→1×); add a `scroll-multiplier` knob; **no** rate-limit/debounce. **Critic corrections:** the two bugs aren't co-equal (guard **shadows** the cap → fix together, guard first); **reset-trigger** for the remainder ivar is mandatory (`event.phase == .began`); iTerm2's `_disableScrollReportingUntilMomentumEnds` latch is a **deferred correctness risk**, not polish. **Open ❓ (human-run):** does macOS deliver momentum into xtty's precise path during coast, at what reports/sec — closable **only by a physical trackpad flick** on a guard-removed DEBUG build (synthetic events carry no `momentumPhase`; un-coverable by XCUITest). **Guideline G(scroll-momentum).** Candidate change (unstarted): `smooth-scroll-wheel-momentum`; refines the shipped `fix-scroll-wheel-mouse-reporting`
- [Scroll-reversal redraw corruption forensics](03-analysis/scroll-reversal-redraw-corruption-forensics.md) — 2026-07-10, found during `smooth-scroll-wheel-momentum`'s manual physical-trackpad verify (task 4.4): scrolling a mouse-tracking full-screen app (htop) via xtty's SGR mouse-report path, then **reversing direction** after scrolling some distance, reliably corrupts the redraw — stale characters from earlier rows bleed into later ones (data keeps updating live in the corrupted rows; not a freeze). Isolated via the DEBUG wheel-injection hook (no hardware needed) against a 5-way matrix: DEBUG injection down→up **corrupts**; real trackpad flicks **corrupt** (the original finding); keyboard arrows (300↓/300↑, exact) **clean**; keyboard Page Down/Up (15↓/15↑, matched multi-row jumps) **clean**; the identical wheel recipe against real iTerm2 **clean**. **Fates table** retires: large PID numbers being the bug (❌ legit macOS thread IDs), htop hanging (❌ its own data kept updating), a peekaboo capture artifact (❌ confirmed via the app's own DEBUG grid dump, not a screenshot), needing real hardware event rate (❌ a slow ~6–7Hz DEBUG injection reproduces it), any multi-row jump triggering it (❌ Page Down/Up doesn't), an htop-side quirk (❌ iTerm2 stays clean on the same htop). **Not a `smooth-scroll-wheel-momentum` regression** (character-level redraw corruption, nothing D1–D3's accumulator/momentum logic touches) but **newly reachable** because of the wheel→SGR-report feature family (`fix-scroll-wheel-mouse-reporting`) — unreachable before that shipped. **Guideline G(scroll-reversal):** test direction reversal explicitly with matched net position, not just single-direction volume — this bug survived 3 single-direction repro attempts before the down-then-reverse-up sequence caught it immediately. **§7 addendum (2026-07-10, same-day follow-up) — root cause found:** a live `peekaboo`-driven session (momentum/depth both proven unnecessary — a single non-momentum tick each way, fully deterministic; a real-cell-boundary resize heals it, a sub-cell one doesn't, the decisive model-vs-render discriminator) + two `/xtty:research` fan-outs + a real byte-exact PTY capture (`script -q` tapping the live app) replayed through a headless SwiftTerm `Terminal` (byte-for-byte identical corruption, zero xtty-app involvement) traced it to source: `cmdScrollDown` (SwiftTerm `Terminal.swift:4646`, CSI Ps T/SD) is missing the `if marginMode {…} else {…}` guard its sibling `cmdScrollUp` has, so it always uses a margin-bounded partial copy; htop's alt buffer never gets `marginRight` raised off its `0` default (`Buffer.init`/`activateAltBuffer` traced, neither sets it), giving `columnCount=1` — every scroll-down shifts only column 0, freezing every other column, unifying the trailing-garbage and row-duplication symptoms under one cause. **Fates table extends to T8–T12**, including a retracted-then-reinstated theory (a bare-pty harness falsely "refuted" scroll-region involvement — it had never actually gotten htop to scroll) → **guideline G(verify-the-regime)**. SwiftTerm-source root cause is no longer ❓. Not yet an OpenSpec change; tracked in `packer/README.md`'s expected-difference matrix, does not block `smooth-scroll-wheel-momentum`'s archival
- [AGENTS.md structural best practices](03-analysis/agents-md-structural-best-practices.md) — **Historical; its later mechanical guide-gate was retired 2026-07-27 by `remove-guide-gate`.** 2026-07-10 `/xtty:research` fan-out (Claude Code memory docs, the agents.md spec, `agentsmd/agents.md`, `openai/codex`, `apache/airflow`) surveying how to revamp xtty's own 68.9KB/206-line `AGENTS.md`. **Measured per-section byte breakdown** (not assumed): the "Shipped and archived" table (25.9%) + "How to work here" (23.4%) are ~half the file — the biggest levers, bigger than the Snapshot paragraph (8.4%) a skim would flag first. **Growth-trajectory measurement:** the `slim-agents-context` diet's gain (74.3KB→33.7KB on 2026-07-06) fully eroded within ~4-5 days at a regrowth rate that did *not* slow down. **Critique retired several plausible-but-wrong claims** (Codex's 32KiB truncation is warned not silent and only conditional; airflow/codex's byte counts are data points not targets; `.claude/rules/` is gitignored in this repo and a dead end for externalization). Two new mechanisms surfaced: HTML comments in CLAUDE.md/AGENTS.md are stripped before context injection (zero-token maintainer notes); nesting is a monorepo feature xtty doesn't need. **Same-day addendum** (two more fan-outs, one cloning `cpython`/`pytest`/`kubernetes`/`vscode`/`django`/`rust-lang/rust` directly): names the actual mechanism — the offending "Tooling" row is *change-keyed* (edited per-archive) where its sibling phase rows are *category-keyed* (edited only when a category closes); the fix mirrors that, verified safe (all narratives already live in HISTORY.md). Surveys 5 real rotation patterns (all release-cadence-anchored, none fit a no-cadence solo repo) and leaves enforcement as an open, adversarially-narrowed tradeoff (a critic heuristic vs. a non-blocking byte gate) for the future proposal to decide — research has saturated. **Shipped 2026-07-11 as `revamp-agents-md`** (archived): the Tooling row → a 7-category one-liner (23,097 → 1,374 bytes), the Snapshot paragraph → a 3-bullet block, the four delegation essays → one table, plus the new REVIEW-severity category-keyed critic heuristic (the enforcement side of the tradeoff left open here — landed as the proportionate primary mechanism, not paired with a CI byte gate; sufficiency is tracked, not assumed, over the next several archives). Whole file 80,107 → 43,667 bytes (−45.5%). Companion to [AGENTS.md context budget](03-analysis/agents-md-context-budget.md). ❗ **ADDENDUM 2026-07-13 (§A) — the diet regrew a THIRD time, and this doc's own open question was the WRONG QUESTION.** Rec 9's written prediction (*"a second diet without that guard will regrow just as fast"*) **came true on schedule**: 43,667 → **81,012 bytes in 48 HOURS** (startup cost back to **57,569** tokens; AGENTS.md ~32.9k = *exactly* the pre-split 32.8k — **the entire 21,548-token saving has now been given back twice**). ❗ **The finding that matters: the guard SHIPPED, the guard HELD, and the file doubled anyway.** `revamp-agents-md` chose this doc's "critic heuristic" option (`xtty-openspec-critic.md:39`, category-keyed status-surface bound) and it **worked** — the Tooling row it guards is **1,934 B** today — **but growth simply MOVED to the next unguarded surface** (the Learned-refutations tier, now **32,300 B = 41% of the file**). ⇒ ❌ **RETIRED: "critic heuristic vs. byte gate" was the wrong question** — *both are surface-scoped, and a surface-scoped guard DISPLACES growth rather than stopping it*. Independently named by `gpt-5.6-sol` without being told the history: *"uncontrolled admission of forensic narratives into an eager index."* ❌ **RETIRED AT BIRTH — the per-unit WORD CAP** (proposed and killed the same session, **ACCIDENT-class ⇒ fatal**): *"split one 1,400-word entry into twenty-four ≤60-word bullets — passes per-bullet lint, preserves nearly all the cost"*; plus three more honest-session failures (strip the caveats ⇒ *"lint improves while rule correctness falls"*; overflow into prose/rows; and at a ceiling, evict an old load-bearing rule ⇒ *"the guard guarantees size, not retention value"*). ✅ **The invariant that survives is ATOMICITY, not length** — *one entry = one rejected action + its replacement + one evidence pointer; no chronology, transcript, or inline numbered sub-findings* — because **the bloat is NOT distributed drift**: the decisive census found **only 2 of 25 entries are non-atomic**, and those 2 hold **54.5% of the tier and 23 of its 23 inline subclaims**, while **the other 23 are already correct one-liners** (a length rule would have forced edits to 14 healthy bullets and left the mechanism intact). ❗ **And no lazy tier can rescue it:** `@path` imports are **EAGER** (*"loaded into context at launch"* — they save nothing), nested guides are lazy **but must be named `CLAUDE.md`** (a nested `AGENTS.md` is *silently ignored*), and `.claude/rules/` + `paths:` is **structurally wrong** — **refutations are DECISION-time** (*"don't re-propose Metal"* fires when **proposing**, before any file is read), so a path-scoped rule waits for a read that never comes ⇒ **the tier cannot be relocated; compression is the only lever** (this also *corrects §3*: gitignore was never the real objection). Also: *"conform to the AGENTS.md standard"* is **vacuous** (the standard specifies no fields, no structure, no length; Claude Code's own docs say **< 200 lines** — we are at 241). Live defect found en route: **`openspec/config.yaml:11` still tells every AI the renderer is a *"custom Metal view (MTKView/CAMetalLayer)"***, contradicting the settled Metal refutation **on every artifact creation**. Fates table, mutation-test acceptance (*"size-only green is insufficient"* — the guard must be shown to go RED on bullet-splitting), and guidelines 6–8 (*a surface-scoped guard displaces growth* · *length is the wrong invariant for a curated index* · *`grep research/` for the prior fix before proposing — this file had been dieted twice and studied twice, and the session did not know*)
- [Cross-model review tar-pit forensics](03-analysis/cross-model-review-tar-pit-forensics.md) — **(historical — the mechanism was retired in full by `remove-cross-model-review`)** 2026-07-12, the record of an in-session **convergence experiment** that ran `add-cross-model-design-review`'s own cross-model loop **on the proposal itself** (Fable-5 fixing, `gpt-5.6-sol` adversarial-verifying, Claude adjudicating; Workflow `wf_454a4338-7e8`). **It did not converge:** over 4 bounded rounds each mechanical hardening of the review-base/range provenance opened the next git-manipulation narrowing (recorded-base → pre-attestation rewrite → check-5 re-record deadlock → ownership≠completeness → range-completeness → directory-rename rename-blindness), the spec bloated **+45%**, round 4 regressed a round-1 guarantee. **Mechanism:** the adversary is *the model running the tool*, which controls git history → the closable-bypass set is **unbounded** and every member reduces to the one accepted **R3** residual; *"delete every mechanically-closable class"* (D8) was itself the trap (infinite regress), and adversarial review never returns zero so *"until both models agree"* has no fixed point. **Correction (D10): thin the _claim_** — the mechanical checks (digest, clean-tree, exempt-by-act, scope classifier, named range) are **accident tripwires + human-legibility aids**, not adversarial guarantees; adversarial resistance = the human reading the ledger + R3; a git-manipulation bypass is R3, out of scope, not a hole. **Second-order finding:** ~16 adversarial rounds ran on a proposal at **0/21 tasks** — dev tooling is validated by **building + using it** (task 5.2), not by reviewing its spec. Fates table (recorded-base ❌ / check-5 ❌ / re-anchor ❌ / range-completeness ❌, each refuted the next round; thin-the-claim ✅); guidelines **G-TARPIT-1..5**; re-verify **by effect** (re-run the loop, watch non-termination + bloat, never a static read). **· §9 addendum (2026-07-12):** the *corrected* loop (thin claim + a content-measured floor), re-run as a **proactive co-draft**, converged in **1 round / 0 net bytes** (`wf_15f6ecb9-aad`) — refinement: **measure bloat by content (words/bytes), not lines** (a line-neutral edit grew +253 bytes, caught only by `wc -lwc`) → **G-TARPIT-5**. **· §11 (2026-07-12):** the content floor is **constructive, not just restrictive** — a second scoped run (`wf_a0e713ea-101`) *landed* a net-**smaller** fix (2415→2404 words) closing a real archive-gate check-(4) coherence gap (commit `3319785`), because the floor gates absolute word/byte totals and so admits any fix that compresses ≥ what it adds (bloat, not change)
- [Cross-model design review — complementary axes + two-layer architecture](03-analysis/cross-model-design-review-axes.md) — **(historical — the mechanism was retired in full by `remove-cross-model-review`)** 2026-07-12, the companion to the tar-pit doc: **why two differently-modeled reviewers are complementary** (the Opus critic checks **conformance** and never reads product source; Codex/GPT + inline Opus check **soundness** and trace claims into real code — barely-overlapping axes) and **why the design is an authority-free worker + a human gate**. Two worked exemplars: (a) critic **COHERENT** ↔ Codex caught a config-ownership bug on `add-git-diff-wrap-toggle` (a base key modeled on `XttyProfile` not `XttyConfigSet`, + a shallow test plan — a *new*-symbol misplacement, outside even Pass 4's honest scope → a **cross-model** catch); (b) the six-round refutation of the **self-policing model-authored gate** (receipt/converged-verdict/ownership-trailer all ❌ because model-authored → self-certification; mandatory signing ❌-as-default) → **the human is the gate, not the model**, splitting two failure classes (self-certification, *deleted*; the affirmative-forbidden-act R3 residual, *accepted* — same trust class as every committed procedure). Guideline **G-AXES-1** (new-symbol ownership is a soundness question); this doc's original re-verify-by-effect path (running both reviewers on a real change; the deterministic gate half's 12/12 `test-cross-review-scripts.sh` harness) no longer applies — `remove-cross-model-review` retired the worker, both reviewer passes, and that test script in full. Non-termination/thin-claim mechanics live in the [tar-pit doc](03-analysis/cross-model-review-tar-pit-forensics.md).
- [Cross-review archive gate — defects found by *using* it](03-analysis/cross-review-gate-defect-forensics.md) — **(historical — the gate was retired in full, unfixed, by `remove-cross-model-review`)** 2026-07-12, the **third** cross-model-review doc, and the only one written from **running** the gate rather than reviewing its spec (**G-TARPIT-4 in action**). All claims measured by effect against the real committed scripts in throwaway repos. **Six findings:** (F1) the **re-attestation deadlock** — once drift invalidates check (2), *every* forward-history re-attestation fails check (4a) (`adds=2/dels=1`, `now=2`, `revert`⇒`dels=1` forever); the only escape is a history rewrite, while the spec's own scenario *mandates* "a fresh review and attestation". (F2) check (4a) is **adversarially null *as specified***: reset-laundering, amend-forge, and the decisive **split-commit forward forge** (two plain forward commits, **zero** history rewriting) all pass **four-of-four** on content the human never saw. (F3) ⚠️ **the FIX IS NOT SETTLED, and the doc deliberately prescribes NONE** — the prescription **flipped three times** (*keep it* → *delete it entirely* → *delete-the-counter-keep-confinement* → **all wrong**): check (4) has **three** clauses; **all** are adversarially null (two plain forward commits defeat the confinement clause too — reached **independently** by two passes); confinement is **not separable** (its "introducing commit" anchor holds only while the counter does); and **deleting the counter deletes the gate's principal *accident* tripwire** — without `dels==0` a model can **silently re-attest** in one forward commit and check (2) degrades to *"the value equals itself"*. **The counter's value and its liveness cost are the SAME property.** The doc records the measurements + the two-horned tension and **routes the fix to an explore** — *a forensics doc records what is; it must not pre-decide a fix three review rounds could not settle*. (F4) the scope classifier is **retrospective** — before implementation lands it returns a confident **false negative** (`exit 0`), not an abstention; **only `exit 10` is positively informative**, and `exit 2` conflates four distinct causes. (F5) ⚠️ a **verified bypass** — with the implementation merely **uncommitted** the classifier reads `exit 0` and the **entire** four-check precondition is *skipped*; the digest's dirty-tree refusal **cannot** backstop it (it *is* check (3), inside the skipped precondition). (F6) **range pollution** is HEAD-dependent, corrupts the *scope classifier* too (`add-git-diff-wrap-toggle` is in-scope on 8 paths that are **all foreign**), hashes a *foreign archived* ledger into the digest, and has **no metadata-free fix** — but is **fail-closed** (noise, not a hole). Fates table (check-4-is-sound ❌ / forge-asymmetry ❌ / relax-check-4 ❌ *(refuted by Fable-5's own drill, which withdrew its own proposal)* / delete-check-4-entirely ❌→narrowed / branch-protection-terminal ❌ / zero-accidents ❌); guidelines **G-GATE-1..9**; re-verify **by effect**. **Dead instrument:** `gpt-5.6-sol`'s sandbox blocked `mktemp`, so it never ran a drill — the *analytic* cross-model consensus is genuine, the **empirical** leg is single-runner. Direct line to the [tar-pit doc](03-analysis/cross-model-review-tar-pit-forensics.md): **§11 of that doc *added* check (4)'s strong form; F2 shows the hardening never closed the gap it targeted while it did brick the honest path — §11 was itself a tar-pit relapse.** Sequencing (which fix lands first) is **escalated to the human, not decided**. ❗ **UPDATED 2026-07-13 — (F9) a fifth defect that REVERSES this doc's own severity claim.** Every finding above interrogated the `exit 10` arm and called the classifier *"fail-closed — noise, not a hole"* (F6). **Wrong.** The **`exit 0` (waiver) arm is UNDER-inclusive**: since `B = parent(proposal.md)`, a change whose **code precedes its proposal** has its implementation sitting **inside the base**, invisible to `git diff B..HEAD`. Measured — **`add-verification-harness` reads `exit 0` (gate does not apply) while having shipped 259 LOC of product code**; the range-omission guard is blind (it guards the change *dir*, never the *code*). Under **C1** that is the *one* direction strictly worse than no classifier, and **no allowlist tuning reaches it** (base-resolution, not allowlist). Enforced today **only by habit** (propose-before-implement is a discipline, not a constraint). *Severity stated honestly per G-GATE-7: that change **predates the gate**, so this is evidence of a **live mechanism**, not an observed bypass.* Found by **third-party re-measurement** — **not** by either consulted model; **both asserted the opposite** (see [the pairing/consult doc](03-analysis/cross-model-pairing-consult-research.md)). New guideline **G-GATE-8** — *a gate's **waiver** arm is the only arm that can bypass it; audit the **negative** verdict against ground truth.* ❗ **UPDATED again 2026-07-13 — (F10) the doc's own PRESCRIPTION is RETIRED, and one of its claims was FALSE.** An attack-and-falsify consult (cold/blind + one adversarial exchange, **synthesized by two non-participants** who re-ran every number, then re-derived by hand) killed *"narrow the range to the change's own paths — the prerequisite fix"* **three ways**: its **object is not git-derivable** (**0** merge commits; the commit that shipped the worker *and both gate scripts* touches **5 paths, 0** in any change dir with **3** changes open; over a **349-commit** walk **~14%** of history is product-bearing **and** structurally unattributable; every text-based rule collapses to the **refuted `OpenSpec-Change:` trailer's** trust leak — *and already collides today with no adversary*); it is **ANTI-ALIGNED** (range filtering is **monotone toward `exit 0`** ⇒ it manufactures **more** false waivers); and it **pre-decides a human-reserved branch** (it presupposes the classifier *survives*, but *"gate unconditionally"* needs no classifier at all). ❗ **And it corrects a false claim the doc itself introduced:** *"no allowlist tuning reaches it"* is **FALSE as written** — an external reviewer **ran the experiment** and a `proposal.md` carve-out flips the verdict (**0 → 1** offending) — ⚠️ though it is **degenerate** (fires `exit 10` for *every* change) and leaves the review **content-blind**. **Three levers; only BASE-WIDENING reaches the code** (`AppUITests` visible: **0 → 2**). What replaces the prescription is **an obligation to decide + the option space, never a mechanism** (**G-GATE-9**: *check that a fix's **object exists**, that it **points at the defect**, and that it does not **pre-decide** a branch — and never assert a **universal negative***).
- [Codex review integration — why Pass B was blind + the minimal fix](03-analysis/codex-review-integration-forensics.md) — **(historical — the mechanism was retired in full by `remove-cross-model-review`)** 2026-07-12, from cloning `openai/codex-plugin-cc` (`db52e28`) and **reproducing both gpt-5.6-sol integration modes by effect**. `/xtty:cross-review`'s Pass B was blind because **it was never given a brief** — only an auto-collected diff, and for a >2-file/>256 KiB range not even that (the plugin sends a summary + "self-collect", `git.mjs:8-9`). **The read-only sandbox was NOT the bottleneck** (round-4 datum: a briefed read-only Codex found this session's two biggest gate defects by *reading*). **Fix = A'**: pass a brief as the trailing positional on the existing read-only `adversarial-review` call — **verified by effect** (the brief survives verbatim because `--base`+`--model` keep `argv.length>1` past the single-arg mangler; Codex engaged a two-question design brief precisely; the validated `review-output` schema is preserved; read-only + xtty untouched). **Mode B (`task --write` drilling) reproduced + REJECTED**: real and safe (seatbelt confines writes to `--cwd`+`$TMPDIR`; writes into xtty DENIED), but loses the schema, adds a write-safety surface, and **OpenAI's content-safety filter aborts security-flavoured drills mid-run**. Safety invariant: `--write` ⇒ mandatory throwaway `--cwd` (default cwd = the xtty repo root; `/codex:rescue` adds `--write` by default — a standing footgun). Guideline **G-CODEX-1** (brief the reviewer; "doesn't scope the range" ≠ "is useless"); fates table; this doc's original re-verify path (using `/xtty:cross-review` with a brief) no longer applies — the worker was retired in full by `remove-cross-model-review`. A different axis from the archive-gate `G-GATE` defects (zero gate force).
- [Cross-model pairing vs. consult — what a second model actually buys](03-analysis/cross-model-pairing-consult-research.md) — **(historical — the mechanism was retired in full by `remove-cross-model-review`)** 2026-07-13, the **fifth** cross-model-review doc, and the second written by **building and running** the thing (**G-TARPIT-4**). Explores a **lifecycle-wide pairing partner** with **no** human attestation. ✅ **Permitted** — the attestation is scoped **only to archive eligibility**, and the worker layer is already *"arbitrarily helpful, zero authority"*. But the risks are elsewhere. **Findings:** the value is in **executing the other model's falsifiers**, not in discussing with it (a false headline claim resting on a **2-of-52 sample** died when the other model *ran* the falsifier — the blind arm would have shipped it); ⚠️ **both models held a shared FALSE FRAME** that neither blindness nor dialogue could see (both called the classifier's two historical `exit 0`s "correct waivers" — **one shipped 259 LOC**), caught **only** by a **non-voting third-party re-measurement** ⇒ **F9/G-GATE-8** in the gate-defects doc; **two-model agreement is not corroboration** (second confirmation of tar-pit §12 — agreement bounds *bloat*, not *wrongness*); a **"neutral" packet that recites the repo's recorded conclusion is not neutral** — **doc-to-model** anchoring dominates model-to-model, so "both models agreed" can be **one source counted twice**; the authority leak is **normative transclusion** (advice → spec → later tools treat as ground truth ⇒ *causal design authority* without archive authority); and **correlated review** — a model that shaped a design cannot later count as an independent reviewer of it. ⚠️ **Round 1's anchoring null result is REFUTED BY ITS OWN MEASURERS and must not be cited** — the packet *forbade* accommodation, allowed *one* round, and made every claim git-checkable, engineering away all three conditions anchoring needs. **ROUND 2** (§5) re-ran it under conditions that **permit** the effect (permissive framing · 3 rounds · an unfalsifiable values question · models unaware · a **GPT-family** measurer) **and tested the full-lifecycle claim head-on** (4 mutually-blind consults at 4 stages of a real shipped change). ❗ **It cuts against BOTH sides — but the findings carry VERY different confidence, and the doc sorts them by distance from git.** ✅ **FIRM (existence proof):** *"the value is all at the design fork"* is **refuted** — **apply** produced **4 verified findings with ZERO cross-family overlap**, all **stage-exclusive** (they need the code to exist) and **leak-resistant**, incl. **a real still-open coverage hole in shipped code** (re-derived by hand: the classic non-precise wheel path lost its cap; **all 6** `injectWheel` call sites pass `precise: true`, **zero** pass `false`). ✅ **FIRM (directly observed, both arms of one run):** **G-CONSULT-8** — at the (leaked) design question the two families **agreed completely**; at the code diff they found **DISJOINT** sets ⇒ *agreement concentrates where there is a shared answer key* ⇒ **point the second family at the CODE, not the DOCTRINE**; cross-family buys **defect coverage**, not **framing** diversity. ⚠️ **SUGGESTIVE / CONFOUNDED (n=1, needs a clean rerun):** the **anchoring asymmetry** — `gpt-5.6-sol` **held** (refusing the convergence narrative *unprompted*) while **`fable-5` folded and published a "Converged position (joint, final)" its partner refuted one turn later** — which would matter because **the Claude side WRITES THE ARTIFACTS** (a **false consensus label** committed to the spec, not "mutual mush"). *But the three measurers **disagreed** on whether anchoring occurred at all, gpt spoke **last** every round, and R2 **instructed** accommodation* ⇒ **unproven.** ⚠️ **Also n=1:** archive produced **zero** — suggestive that an archive-stage **consult** (⚠️ **not** the archive **gate**) is low-yield. ⚠️ **This investigation CONTAMINATED ITS OWN round 2 and says so** (**G-CONSULT-11**): both models read the **uncommitted working-tree** capture off disk and cited it by line number, and the *"don't read AGENTS.md"* leakage guard **failed outright because `CLAUDE.md` is AUTO-INJECTED**. Guidelines **G-CONSULT-1..11**; `task --resume` is ❌ **unreliable** (no thread-id pin, silent cold-spawn) ⇒ stateless context packets; the codex broker is **single-flight** ⇒ serialize. ❗ **ROUND 3 (§12, 2026-07-13) — the cleanest evidence in the doc, because it is NOT an experiment about consulting: it is a consult doing real work** (**G-TARPIT-4**), a 3-arm fan-out on a live open question (how to stop `AGENTS.md` regrowing) with **disjoint mandates** (fable-5 = *measure*, `gpt-5.6-sol` = *attack*, a docs agent = *source*) and a **non-participant re-measurer**. ✅ **FIRM — the second model KILLED A DESIGN THE LOOP-DRIVER WAS ALREADY WRITING DOWN** (a first for this repo — prior wins found defects in *existing* artifacts; this one **stopped a bad mechanism from reaching a proposal**): against a proposed per-unit word cap argued to be *"compression-proof"*, `gpt-5.6-sol` replied *"split one 1,400-word entry into **twenty-four ≤60-word bullets** — it passes per-bullet lint, preserves nearly all the cost"* ⇒ the cap bounds the **unit** and leaves the **tier** unbounded, and since **bullet-splitting is what an HONEST session does**, it is **ACCIDENT-class ⇒ fatal** (*"these are not adversarial evasions; they are predictable responses to a locally failing lint rule"*). ✅ **G-CONSULT-8 is SHARPENED, not refuted** → **G-CONSULT-13**: the loop-driver **predicted this arm would add little** (*"AGENTS.md is pure doctrine — point the second family at the CODE"*) and it produced the session's most important finding — **because it was not reviewing doctrine, it was attacking a design invented twenty minutes earlier, which exists in no document either model had read.** The real mechanism was always *"agreement concentrates where there is a **shared answer key**"*; **code is merely the most common place with no answer key — a fresh design is another.** ⇒ *The test before spending a consult is not "is this code?" but **"could either model retrieve this answer from something it has read?"*** — and **the highest-value target is the design the loop-driver has NOT yet written down**, which is also the last moment a bad mechanism is cheap to kill. ✅ **The non-participant adjudicated the two models and found the winner's own claim UNDERSTATED** (gpt: *"the top two entries are non-atomic"*; re-derived: **only 2 of 25 entries are non-atomic at all**, holding **54.5%** of the tier and **23/23** subclaims — a number **neither model computed**), confirming **G-CONSULT-2**: the third party is not a formality. ⚙️ **Codex operational forensics — the ZOMBIE failure mode (✅ FIRM), plus a RETRACTION (§12e-bis).** ✅ **FIRM:** the codex **thread dies silently and the companion never notices**, leaving the job at **`status: running` for up to 1h59m** with its last real log line ~1h40m earlier (`Turn completed`=0; `thread not found` on cancel) ⇒ ❗ **`status: running` is NOT a liveness signal — only LOG-FILE GROWTH is** (**G-CONSULT-14**; treat ≥5 min of zero growth as death). **Measured rate: 4 zombies in 10 `task` runs (~40%) — budget for it.** ❌ **RETRACTED (this doc's own claim, shipped in `fb6121e` and refuted by the very next observation): "every death overlapped another job ⇒ the single-flight collision is the cause."** The 4th zombie ran **ALONE** with the queue **verified empty**; and packet size fails too (a **6,553 B** packet **completed** while a **5,505 B** one died). ⇒ ❓ **The CAUSE is UNKNOWN — ship the DETECTOR, not a fix for a cause you have not established.** ❗ **A self-inflicted violation of this doc's own G-CONSULT-2** (*check the POPULATION, not the sample*): a confident causal claim from **n=3**, committed in the same breath as the guideline forbidding it — *a guideline you have written down but do not apply to yourself is not yet a guideline.* Also: `task` runs **inline** but **survives the caller being killed**; harness `run_in_background` returns **exit 0 immediately** (so "finished" and "still running" look identical — never read exit 0 as completion); `result <id>` resolves **only finished** jobs; and ⚠️ **`codex task --help` RUNS A TASK** (no help flag; the string becomes the prompt). **G-CONSULT-15 — state your thesis to the adversary EXPLICITLY, as the target**: with the reviewed artifact **auto-injected** (G-CONSULT-11) and already containing a near-identical hypothesis, *naming the thesis and ordering the model to falsify it converts the leak into a constraint — **you cannot crib a hypothesis you have been ordered to kill***; pair with **DERIVED vs REFLECTED** labels to make doc-regurgitation visible. Findings captured to [AGENTS.md structural best practices](03-analysis/agents-md-structural-best-practices.md) §A.
- [Cross-model seat assignment — reframing the Fable/GPT role split](03-analysis/cross-model-seat-assignment-research.md) — **(historical — the mechanism was retired in full by `remove-cross-model-review`)** 2026-07-19, evaluates a user-proposed task-type role split (Fable=thinker, GPT=builder) against xtty's live tooling (a 6-agent Fable-5 fan-out + synthesis, pressure-tested in `/opsx:explore`, then critiqued by an independent Fable-5 pass). **Verdict: reject the task-type framing — it inverts the repo's one FIRM cross-model result** (GPT reviews Claude-authored code; it doesn't author) **and collides with the "no model-authored synthesis" archive-gate rule.** Reframes as an **epistemic-seat** model (author / independent reviewer-falsifier / non-participant merger) instead. Three candidates disposed: **(1)** pin cross-review's Pass C to Fable 5 — legitimate but reframed as an **in-vendor checkpoint swap, not a third family** (`spec.md` requires Pass C to share Pass A's family), judged by a **human** reading the ledger, never a model-computed score; **(2)** an author-family ledger field + consensus suppression — **deferred**, since a `git log` `Co-Authored-By` census shows every author in this repo's history is Claude-family, so the field would read a permanent constant (same defect shape as the archive gate's dead `exit 0` waiver arm); **(3)** a GPT "falsifier-runner" at apply-time — sound premise (G-CONSULT-13) but needs a corrected pilot: isolate *running* vs. *reading* (G-CONSULT-10 already proves reading helps), mandatory "did not complete" degradation-stamping (a documented **~40% Codex-job zombie rate**, confirmed live twice in this very investigation), promotion only after ≥2 pilots (not one), and a real `tasks.md` marker (a conversation-only plan won't reach the apply loop — the `fix-osc7` precedent). Corrects the investigation's own ad hoc `ps -p <pid>` zombie check against the already-validated discriminator (log-file growth + `Turn completed` grep + cancel's `thread not found`). Also fixed in passing: **AGENTS.md's stale `(G-CONSULT-1..11)` citation** → `1..15`. Guidelines **G-SEAT-1..5**.
- [OpenSpec coherence checklist](03-analysis/openspec-coherence-checklist.md) — the per-artifact "what to update when a requirement changes" walk-list, relocated 2026-07-17 from `AGENTS.md` (a `/doctor` lazy-loading migration — pure procedure, not decision-time content)
- [xtty requirements](03-analysis/xtty-requirements.md) — the opinionated product target: must-haves, features, non-goals
- [Open Design integration forensics](03-analysis/open-design-integration-forensics.md) — 2026-07-28 investigation + hands-on POC on using Open Design (nexu-io/open-design) to draft xtty's UI: it's mechanically stack-agnostic but productively web-only (HTML mockups only, never direct Swift edits); no tool can auto-derive a DESIGN.md for a native app (hand-author from real source); there is **no OS-level sandbox** regardless of project folder scope (`OD_SANDBOX_MODE` only isolates HOME/config, doesn't restrict file access) — confirmed live when the POC's own agent read a sibling folder outside its assigned project directory; `.od-skills/` is a disposable per-turn cache, not project content. POC succeeded (a settings-pane mockup faithfully used the hand-authored tokens) but was wiped afterward per owner decision. **2026-07-29 addendum** (planning pass for the real project): the wiped content was *recovered* from the app's own version store and run traces; the design-system picker turns out to be decisive rather than incidental (it gates the entire tokens-into-prompt channel — the POC's fidelity was luck); `tokens.css` answers to a 56-token machine-enforced schema; `OD_SANDBOX_MODE` is refuted outright (auth-severing, not credential-isolating); the MCP needs no build but isn't worth installing; and content telemetry ships file bodies by default (now disabled). **Second 2026-07-29 addendum** (scripted project creation): the folder-import gate is real (tokenless POST 403s) but the GUI-only conclusion is reversed — the app's bundled first-party CLI (`od project import-folder`, via the Electron helper) mints the token itself, so `make design-link` now creates and configures the project end-to-end; `fromTrustedPicker` consequently attests token validity, not human choice; folder imports are never deduped (identify projects by `realpath(baseDir)`, never name); API/CLI project deletes leave a phantom UI card until app relaunch. **Third 2026-07-29 addendum** (project display name): the app-side project is named `xtty` (owner call; the folder stays `design/mockups/` — `design/xtty/` is the package dir) — a project's name is display-only (never identity, never in the agent prompt or artifact paths) and renameable in place via the ungated PATCH, **except** that a rename on a `ds-<pkg>` workspace row writes the new title through the symlink into the repo's `design/xtty/metadata.json` (gated on `importedFrom:"design-system"`; never rename that row in the app), and the workspace row mirrors the design-system title so two rows can display "xtty". **Fourth 2026-07-29 addendum** (package-directory rename): the package moved to `design/design-system/` (owner readability call) — the directory basename **is** the design-system id (`user:design-system` now; the install body carries no id field and the catalog id is a readdir entry name), so the rename was executed as an id migration: full teardown of the old id first, then `git mv`, manifest self-id updated (a mismatch silently voids the whole manifest), and the app-written `projectId` dropped so the daemon re-minted `ds-design-system` (the ensure path *prefers* the recorded value — a stale committed one would pin forever); token channel re-proven by a sentinel generation under the new id. **Fifth 2026-07-29 addendum** (owner review): renamed again to `design/xtty-design-system/` (`user:xtty-design-system`) — the install namespace is flat and machine-global, so a generic basename is a first-come-first-served land grab (the loser's install just 400s); uniqueness wins over brevity, reversing the fourth addendum's collision acceptance, and the channel was re-proven by sentinel under the new id. Also settled from schema: `metadata.json`'s `surface` has exactly four legal values (`web|image|video|audio`, no desktop one) and names the *artifact medium*, never the product platform — `"web"` is correct for the macOS app (the project's `platform: desktop-app` carries targeting; unknown values are silently dropped) — and the manifest's dangling `components.html` declaration was dropped (a declared-but-missing file is silently ignored by every consumer) with `usage` moved to the top level where the daemon actually reads it. **Sixth 2026-07-29 addendum** (mockups-folder rename): the project folder moved to `design/xtty-mockups/` (owner consistency call — one `xtty-*` prefix across the `design/` tree); unlike the package basename this one carries **no identity** (name set explicitly at import, `entryFile` relative, exports named from the title, runs keyed by project id, `.od-skills/` position-independent) — but the stored `baseDir` is absolute and **immutable after import** (the PATCH route rejects a re-point outright; no CLI move verb), so a mockups-folder rename is executed teardown-first (`make design-unlink` → `git mv` → `make design-link`), recreating the project at the cost of only its app-side run history (zero at the time). **Seventh 2026-07-29 addendum** (the `assets/` placeholder dropped): the tool has **no image pipeline into `baseDir`** — `od media generate` writes flat into the app-side data dir for every project shape (the media path never threads project metadata) and cannot even address a subdirectory (`/` is flattened to `_`), the bundled skill mandates single-file self-contained HTML with placeholder classes, and the historical GUI mis-import that rooted a project in `assets/` was remembered picker state (neither picker pins a default directory), not a designed default — so the empty directory was dropped and the data-URI rule written onto both agent-visible surfaces instead. **Same-day second pass on (g)**: the CLI help's "write it into the active project" is **shape-dependent, not wrong** — true for app-managed projects (the app-side dir *is* the project there), silently misleading for folder-backed ones, with the dispatcher blind to `baseDir` in *both* directions (`--image`/`--composition-dir` inputs resolve app-side too) and the divergence unacknowledged in vendor source; the full media contract injects only on media surfaces, so our `web`-surface agent never receives the misleading promise in-prompt. **Eighth addendum (h)** (design-system read channels): the token-gated `od tools design-systems read` route is strictly manifest-allowlisted (allowlist and prompt-side pull index derive from the same keys, so advertised and servable cannot diverge) — but neither of (e)'s manifest fixes touched a pull-eligible key; `USAGE.md` reaches the agent via the mtime-fingerprinted symlink read at prompt build (push), never via pull, and for our package the pull layer is a declared no-op. **Ninth addendum (i)** (model selection): the model is a **global per-agent** config key (`agentModels.<agentId>.model`), resolved at spawn after the per-request field and before `"default"` — and `"default"` emits **no `--model` flag at all**, so an app that looks like it is defaulting to a model is usually expressing no opinion while the agent CLI's own config decides (refuted the standing "Open Design defaults us to Sonnet" belief). Scripted as `make design-model` through the GUI's own `PUT /api/app-config`, which is a **whole-config replace, not a merge** — so read-modify-write plus a per-write assertion that the telemetry opt-out survived; the write shape was captured by driving the real picker once, not inferred. Prefer tier aliases (`opus`/`sonnet`/`haiku`/`fable`) over the app's pinned 4.x-era ids: aliases resolve inside the agent CLI at spawn, and newer models are reachable only through the documented `sanitizeCustomModel` escape hatch.

### 04 — Design
- [Stack sketch](04-design/01-stack-sketch.md) — primary All-Swift stack + researched alternative stacks and when to switch
- [Milestones](04-design/02-milestones.md) — phased build plan, SwiftTerm L3-start (P0 skeleton → P1 integrate → P2 daily-driver → P4 OSC keystone → P5 sidebar → P7 measure → P8 conditional own-renderer)

### Reference
- [Consolidated sources](sources.md)

## Folder structure

```
research/
├── README.md                  # this file
├── 00-overview/               # synthesis + comparison
├── 01-terminals/              # one deep-dive per terminal
├── 02-internals/              # architecture topics (PTY, parsing, GPU, fonts, ...)
├── 03-analysis/               # fact-checks, opportunities, requirements
├── 04-design/                 # the build plan: stack sketch + alternatives
└── sources.md                 # deduped source URLs
```

## Conventions

- Each terminal/internals file carries a **Provenance** note and a **Sources** list.
- Fact-checked claims use ✅ confirmed / ❌ refuted / ❓ uncertain.
- Research snapshot date: **2026-06-27**. Version/latency/star-count figures are time-sensitive — re-verify before quoting.
