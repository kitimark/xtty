# Open Design integration for xtty — forensics + POC outcome

> **Provenance:** 2026-07-28, produced across three staged investigations plus one hands-on POC run: (1) a Fable-plan → Opus-fan-out (8 angles) → Fable-audit Workflow assessing whether/how to use Open Design (github.com/nexu-io/open-design, brew-installed as the desktop app) for xtty; (2) two Fable investigate→scrutinize Workflows drilling into (a) whether/how a "design base" could be derived from xtty's *already-implemented* UI and (b) the concrete app setup steps, both grounded in a `git clone --depth 1` of the real source rather than marketing copy; (3) a live, hands-on POC — a hand-authored design-system package written to `design/xtty/` (never committed to git), a folder-scoped Open Design project pointed at `design/mockups/`, one real generation run, and direct forensic inspection of the resulting files, the daemon's `app.sqlite`, and its source for the mechanisms observed. The POC's artifacts — the repo's `design/` tree and the app's installed design-system package — were wiped after this capture (owner decision, clean-slate restart); nothing described below was ever committed to the repo. **The wiped content was later recovered** — see the *Salvage* section — so this document is a summary, not the only record.

> **Addendum 2026-07-29 (planning pass for the real project).** A second Fable-plan → Opus-fan-out (6 angles) → dual-lens-critique Workflow, run to plan the non-POC setup, re-derived this ground from source and **corrected a number of claims below**. Corrections are folded in place and marked `⟲ 2026-07-29`. The load-bearing ones: the wiped content is recoverable (Salvage); the POC project is named **`mockups`**, not "Prototype"; the design-system picker is **decisive**, not incidental; `tokens.css` answers to a machine-enforced token schema this doc never surfaced; and `OD_SANDBOX_MODE` is refuted outright rather than an open option.

**Sources:**
- `/tmp/open-design` clone @ SHA `f52fda2` (`ssh://git@github.com/nexu-io/open-design`) — all Open Design source citations below (`apps/daemon/src/*`, `skills/*`, `design-systems/*`, `packages/contracts/src/*`, `README.md`) are against this checkout.
- xtty repo @ commit `b0e3998` — `App/*.swift`, `XttyCore/Sources/XttyCore/XttyConfig.swift`, `XttyProfile.swift`.
- Live local machine state: brew cask `open-design` v0.16.1; `~/Library/Application Support/Open Design/namespaces/release-stable/data/` (`app-config.json`, `app.sqlite`, `design-systems/`, `projects/`, `runs/`) plus the running daemon's loopback API (`/api/app-config`, `/api/mcp/install-info`) — read-only inspection throughout, with one deliberate exception on 2026-07-29 (a `PUT /api/app-config` disabling content telemetry, backed up and verified on disk). No app UI ever driven (no native-macOS-app automation tool was available in this environment; every mechanism claim below is source-code or on-disk-state evidence, never a UI walkthrough).

## Mechanism / internals (✅ verified from source + live execution)

### Two integration modes exist — and they are not alternatives  ⟲ 2026-07-29
(A) **Desktop Studio app** — PATH-scans for coding-agent CLIs at onboarding and spawns the user's own authenticated `claude` binary for generation. Zero extra setup; this is the mode the whole rest of this document concerns.

(B) **MCP-into-Claude-Code**, via `od mcp`. The `od` *name* does collide with the system octal-dump utility (`which -a od` → only `/usr/bin/od`), but the original claim that mode B therefore "needs a real setup project (Docker or build-from-source)" is **❌ refuted**: the MCP server binary already ships inside the installed app, and the running daemon hands out a ready absolute-path launch spec — `curl -s -H 'Origin: http://127.0.0.1:<port>' http://127.0.0.1:<port>/api/mcp/install-info` returns `command` = the packaged Helper binary, `args` = `[.../prebundled/daemon/daemon-cli.mjs, mcp]`, `cliExists:true`, `buildHint:null`. Nothing is built, fetched, or containerized.

Three properties the original framing missed:
- **B strictly requires A.** `od mcp` is a *stateless proxy* — every one of its 18 tools is a `fetch()` against the daemon (`apps/daemon/src/mcp.ts:1-11`, `:1478-1485`), and on this machine the daemon **is** the desktop app's packaged sidecar (`ps aux` → `daemon-sidecar.mjs --od-stamp-source=packaged`). Quit the app and every tool fails. B is a remote control for A, never a substitute.
- **It is not read-only**, despite the README's framing: 7 of the 18 tools are write-annotated, including `write_file`, `delete_file`, `delete_project` (`mcp.ts:305-483`) and `start_run` — which commissions the daemon to spawn its own `bypassPermissions` agent (`mcp.ts:396-403`). The proxied project API carries no bearer token on loopback.
- **The installer hardcodes `--scope user`** (`mcp-agent-install.ts:158`) — 18 tool schemas plus a ~90-line instruction block injected into *every* Claude Code session in *every* repo.

**Decision (2026-07-29): do not install.** For a folder-linked project whose mockups land in the repo working tree, the MCP adds nothing a plain `Read`/`Grep` doesn't already give, while costing per-session context in unrelated repos and widening the write surface. Revisit only if the loop switches to *managed* projects (artifacts inside the app data dir, where `get_artifact` becomes the only access path) or if GUI round-trips get frequent enough that driving runs from a session pays for itself. Verify any future install by effect: quit the app and confirm the tools fail.

### Output is HTML by design, even for "native" briefs — mechanically agnostic, productively web-only
The Claude runtime adapter (`apps/daemon/src/runtimes/defs/claude.ts:52-89`) is a thin wrapper spawning the literal `claude` CLI in print-mode (`-p --input-format stream-json --output-format stream-json --verbose`), with `--permission-mode bypassPermissions` appended unconditionally (`claude.ts:87`). Nothing about that spawn is web-specific — such a process could edit any file in its working directory, including `.swift` files. But the *composed prompt* is unconditionally HTML-bound: the core charter states the deliverable outright — *"You are an expert designer working with the user as your manager, delivering in HTML. HTML is your tool, not your medium"* (`apps/daemon/src/prompts/core-slim.ts`, `SLIM_CORE_CHARTER`, ~line 71) — and even explicitly native briefs are shipped as HTML simulations (`PLATFORM_CONTRACTS_BLOCK`, ~line 180: an iOS brief produces `mobile-ios.html` with "iPhone frame + Dynamic Island + 44px targets"). The entire preview/export/HyperFrames pipeline renders HTML; a `.swift` file written by the spawned agent would be invisible to it. **Conclusion:** Open Design is mechanically stack-agnostic but productively web-only — its coherent use for a native app is HTML mockups that get hand-translated afterward, never direct source edits.

### DESIGN.md/token-package *derivation* has exactly three paths, and none apply to a native macOS app
*(Heading narrowed ⟲ 2026-07-29: this section is about **deriving** a package from an existing artifact. Getting an already-authored package **into** the app is a separate, well-supported problem — see "Registration" below. The original wording invited the wrong inference that a hand-authored package can't be installed.)*

1. **Hand/agent-authored curation** — how the entire 151-package bundled catalog (including "apple" and "warp") was made; every manifest records `source.type: "bundled"` / `"Open Design curated bundled fixture"` (`design-systems/apple/manifest.json`, `design-systems/warp/manifest.json`) — prose, not a tool extraction.
2. **`brand-extract` skill** — strictly URL-based: drives a live website open in an in-app browser, measuring DOM/CSS (`skills/brand-extract/SKILL.md:31-38,60-70`). No screenshot/image-input variant exists. Inapplicable — xtty has no website.
3. **`od design-systems import-local`/`import-github`** — a deterministic scan of `.css/.scss/.sass/.less` + `.tsx/.jsx/.vue/.svelte` only (`apps/daemon/src/design-systems/import.ts:114` `STYLE_EXTENSIONS`, `:115` `COMPONENT_EXTENSIONS` — the original `:110-117` citation brackets the truth but points partly at the `IGNORED_DIRS` set above it). Run against xtty's Swift tree it would find zero tokens and zero components.

> **Pre-emptive note (so the refutation isn't mistakenly reopened):** the daemon *does* ship a SwiftUI `Color(...)` parser — `apps/daemon/src/design-systems/swift-colors.ts`, handling `red:green:blue:`, `hue:saturation:brightness:`, and `white:`. It is **not** an importer. Its only caller is `extractSwatches`, which reads the **markdown body of DESIGN.md** to build the picker's 4-colour thumbnail row (`design-systems/index.ts:3822`, docstring at `:3762-3774`) — never a Swift source tree. Useful upshot: if DESIGN.md *quotes* xtty's `Color(...)` declarations verbatim, the picker derives swatches from them for free.

**Conclusion:** the design base must be hand-authored by reading the real source; this is exactly what the POC did (`design/xtty/DESIGN.md`, 9 H2 sections, `manifest.json`, `tokens.css`, cited `path:line` throughout against `App/*.swift`/`XttyConfig.swift`/`XttyProfile.swift` @ `b0e3998`).

### Registration: four supported routes ingest an already-authored package  ⟲ 2026-07-29 (new)
Derivation ≠ registration. Any of these installs a hand-authored package without deriving anything:
1. **Local install by symlink** — `library-install.ts:133-181`, `fs.symlinkSync(realPath, linkPath, 'junction')` at `:175`, naming the folder `path.basename(source)`. Discovery explicitly honours symlinks (`design-systems/index.ts:277`). **This is the route to use**: repo and app become the same bytes, so drift is impossible at this layer.
2. **GitHub install by clone** — `library-install.ts:74-96`.
3. **`POST /api/design-systems`** → `createUserDesignSystem` (`design-systems/index.ts:1154-1197`) writes `DESIGN.md` verbatim from a supplied body and registers `user:<slug>`; surfaced in the UI via Settings → Design Systems.
4. **Plain manual placement** into `data/design-systems/<slug>/` — discovery is a bare `readdir` (`index.ts:269-285`).

⚠️ **Never** use "Import from folder"/`import-local` on a hand-authored package — that is route 3 of *derivation*, and it regenerates DESIGN.md from a CSS/JS scan, destroying the authored prose.

### `tokens.css` answers to a machine-enforced token schema  ⟲ 2026-07-29 (new — a gap in the original capture)
`packages/contracts/src/design-systems/token-schema.ts` declares **56 tokens across 4 layers** (measured: 8 `A1-identity`, 18 `A1-structure`, 26 `A2`, 4 `B-slot`), with A2 documented as "required-with-fallback rather than optional" (`:39-48`). A package that only defines the tokens it happens to care about leaves pasted artifacts with `var()` targets that silently resolve to nothing.

Two concrete traps the POC's own `tokens.css` fell into, both invisible at authoring time:
- **`--font-chrome` is not a token.** The schema defines exactly three font slots — `--font-display` (`:136`), `--font-body` (`:137`), `--font-mono` (`:138`). The POC's package bound `--font-chrome` and the generated mockup dutifully referenced `var(--font-chrome)`; it was off-schema on both sides.
- **`#242628` has no source in xtty.** `grep -rn '242628' App/ XttyCore/` returns nothing — xtty defines no chrome surface colour at all (its only literal colours are `XttyConfig.swift:38-72` and `SpatialToast.swift:24`). It was a *derived* value presented in the probes table alongside genuinely cited ones. A re-authored package must annotate derived values as derived.

### The design-system picker is the whole tokens-into-prompt channel  ⟲ 2026-07-29 (new — answers an open question below)
The original capture left "whether explicitly setting the formal design-system picker changes anything" as an open question, having observed correct colours with `design_system_id = null`. It changes *everything*: selecting a system is what causes the daemon to paste **the entire DESIGN.md and the entire tokens.css verbatim into the system prompt**, with explicit anti-invention imperatives (`apps/daemon/src/prompts/system.ts:1129-1131`, `:1151-1155`), plus the USAGE.md, import-mode, component-manifest/fixture and pull-index blocks (`:1121-1127`, `:1133-1138`, `:1157-1168`, `:1170-1174`). The channel is gated on a non-null `effectiveDesignSystemId` **and** a non-draft status (`server.ts:3889-3915`; `server-services.ts:171`).

So the POC's fidelity was **luck, not mechanism** — a null `design_system_id` means tokens.css never entered the prompt at all, and the palette match came entirely from the agent's opportunistic sibling-file read. Two independent reasons the picker *mechanically could not* have been responsible:
- `null` explicitly means "No design system", with the app default deliberately **not** reapplied (`server.ts:3619-3629`).
- A package carrying no `metadata.json` is status `draft`, and drafts are rejected at selection with `DESIGN_SYSTEM_NOT_PUBLISHED` (`server-services.ts:122-127`, `:171-173`, `:193-199`). The POC's three-file package **was not even selectable.**

Practical consequences for the real setup: `metadata.json` must carry `"status":"published"` (else the picker rejects it) and `"artifactMode":"agent-managed"` (else the app generates ~23 scaffolding files into the folder). Note also that `metadata.json` is **app-owned on write-back** — on the first run the daemon rewrites the whole file through the symlink as `JSON.stringify(…, null, 2)` of a 9-key allowlist (`title, category, surface, status, artifactMode, createdAt, updatedAt, provenance, projectId`); any other key is silently deleted, so author those keys only, 2-space indent, and expect an added `projectId` line as the first-run diff.

### There is no OS-level sandbox, and folder scoping bounds blast radius, not the theoretical risk
`--permission-mode bypassPermissions` (`claude.ts:87`) strips Claude Code's own confirmation prompts entirely. `OD_SANDBOX_MODE` (`apps/daemon/src/sandbox-mode.ts`, read in full) sounds like a jail but isn't one: `isSandboxModeEnabled()` gates only (a) redirecting `HOME`/`XDG_CONFIG_HOME`/`XDG_CACHE_HOME`/`TMPDIR`/`CLAUDE_CONFIG_DIR` to an isolated area (so a run can't read/write the user's *real* global Claude Code config or credentials), and (b) which folders are even *allowed* to be selected as an import/project-location root (`isSandboxImportedProjectRootAllowed`). Neither restricts what the spawned process can actually read/write on disk once running — that's ordinary OS file permissions, same as running `claude --permission-mode bypassPermissions` yourself in a terminal. Folder-imported/project-location projects resolve `metadata.baseDir` directly, no copy or shadow tree (`apps/daemon/src/projects.ts`, `resolveProjectDir` → `path.normalize(baseDir)`); the only always-on guard blocks hidden (dotfile) path segments (`projects.ts:80-94`).

⟲ **2026-07-29 — that last guard is weaker than "does not protect tracked source" suggests: it protects nothing at all from the spawned agent.** `grep -rn "assertVisibleForImportedProject" apps/daemon/src` returns 9 hits, *all* inside `projects.ts` and **zero** in the runtime/spawn path. It guards the daemon's own file API (and therefore also what the telemetry object-uploader can read) — the agent's own Read/Write/Bash tools never pass through it. Don't read it as a dotfile shield.

⟲ **2026-07-29 — `OD_SANDBOX_MODE` is worse than "not a jail": it is auth-severing.** Measured under its exact env (`sandbox-mode.ts:175-192`), `claude auth status` goes `{loggedIn:true, subscriptionType:'max'}` → `{loggedIn:false, authMethod:'none'}`, with the `HOME` redirect (`:178`) and the `CLAUDE_CONFIG_DIR` redirect (`:188`) each sufficient alone. Generation would simply not run. A negative grep across the packaged/desktop/web surfaces also found no desktop wiring that reaches it. It is retired, not an option — see the fates table.

### `.od-skills/` is a disposable per-turn write-barrier, not project content
`apps/daemon/src/cwd-aliases.ts` (read in full): the active skill is staged into the project cwd once per turn — "the previous-turn copy is replaced wholesale on every call." It exists specifically because an earlier symlink-based draft (PR #435 round 1) was flagged as a write-amplification vulnerability: an agent with cwd write access could mutate the *shipped* skill resource through the symlink; per-project copies contain that blast radius ("corrupting it has no effect on other projects or on the source"). Folder names are content-hashed (`skillCwdAliasSegment` — a sha256 digest of the absolute source path), fully reproducible from the app's own installation. **Conclusion:** nothing here is xtty-specific or worth version-controlling — equivalent to a build cache.

⟲ **2026-07-29 — two corrections.** (1) The original sentence "`design/mockups/.gitignore` excluded `.od-skills/` for exactly this reason" is **unsupported and probably wrong**: an in-run `ls -la design/mockups/` captured in the run trace lists exactly two entries — `.od-skills` and `README.md`, no `.gitignore` — and `git log --all -- design/` is empty, so no such file ever reached git. Treat the ignore rule as *unwritten work that must be created before the folder is recreated*, not as an existing artifact. (2) `.od-skills/` is **not** the only untracked write the charter produces: it also emits `brand-spec.md` (`core-slim.ts:127`), `-v2` duplicate files (`:163`), curl'd remote images (`:158`), and a `<name>.html.artifact.json` sidecar per artifact (`projects.ts:862-866`, always reached via `artifacts/create.ts:74`). Scope the ignore rule to the whole project folder's *machine-local* residue, not one subdirectory.

## Reproducible probes (what was actually run, and what each does / doesn't prove)

| Probe | Command | Proves | Does NOT prove |
|---|---|---|---|
| Install state | `brew info --cask open-design` | v0.16.1 installed 2026-07-26 | App UI behavior (never visually driven — no native-app automation tool available) |
| Onboarding/telemetry state | `cat ~/Library/Application\ Support/Open\ Design/namespaces/release-stable/data/app-config.json` | `onboardingCompleted:true`, `agentId:"claude"`, `telemetry:{metrics:true,content:true,artifactManifest:false}` at read time — **set to `content:false` on 2026-07-29**, see below | Current live value if changed afterward in-app; what past runs already transmitted (no local send-log exists) |
| Project registry | `sqlite3 -json app.sqlite "SELECT id,name,skill_id,design_system_id,metadata_json,created_at FROM projects;"` | The folder-linked "mockups" project's `metadata_json.baseDir` correctly resolved to `design/mockups`; **`design_system_id` is `null`** | That the hand-authored design system went unused — it did shape the output, just via the agent's own file read rather than this field (see next probe) |
| Conversation/message trace | `sqlite3 -json app.sqlite "SELECT role,content,produced_files_json,run_status FROM messages WHERE conversation_id=...;"` | The assistant's own reasoning trace explicitly reads `design/xtty/tokens.css` before building ("The xtty design system explicitly forbids inventing colors — let me check the companion tokens.css") | That this generalizes to every future run — one observed trace, not a guaranteed pipeline step |
| Generated-artifact fidelity | `grep -oE "#[0-9a-fA-F]{3,6}" design/mockups/xtty-settings-pane.html \| sort \| uniq -c` | The exact hex values from `tokens.css` (`#1d1f21`,`#c5c8c6`,`#242628`,`#0a84ff`,`#34c759`,`#ff9500`,`#ff3b30`) appear verbatim in the output, plus `var(--font-chrome)`/`var(--font-mono)` token references and direct DESIGN.md fact citations ("900 × 560 pt is xtty's shipped default") | ⟲ **that any of it was schema-valid** — `--font-chrome` is not an Open Design token and `#242628` has no xtty source (both derived, see the token-schema section). Nor the mechanism: the picker was `null`, so this was an exploratory file read (⟲ now explained above) |
| `.od-skills/` mechanism | `grep -rn "\.od-skills" apps/daemon/src/*.ts` then reading `cwd-aliases.ts` in full | Per-turn wholesale regeneration and the write-barrier rationale, from the maintainers' own code comments | — (direct code read, not inferred) |
| ⟲ MCP is installable without a build | `curl -s -H 'Origin: http://127.0.0.1:$PORT' http://127.0.0.1:$PORT/api/mcp/install-info` | `cliExists:true`, `buildHint:null`, and an absolute-path launch spec into the app's own `prebundled/daemon/daemon-cli.mjs` | That installing is *advisable* — the scope, context cost and write surface are separate arguments (see mode-B section) |
| ⟲ Content telemetry is live and ungated by `artifactManifest` | `curl -s http://127.0.0.1:$PORT/api/app-config` + read `app-config.json` | Live state was `{metrics:true, content:true, artifactManifest:false}`; the gate at `trace-object-manifest.ts:645-649` reads **only** `metrics` and `content` | That `artifactManifest:false` contains anything — it is declared in the prefs type (`:60`) but read by no gate. A reader seeing it could wrongly conclude file bodies are already withheld |
| ⟲ Telemetry can be disabled without the GUI | `curl -X PUT .../api/app-config -H 'Origin: …' -d '{"telemetry":{"metrics":true,"content":false,"artifactManifest":false}}'` then re-read `app-config.json` **on disk** | The write merges (only supplied keys change — `app-config.ts:783-788`) and lands: exactly one line differs from the pre-write backup | That the Settings → Privacy toggle is where the doc guesses — the GUI path remains visually unconfirmed (same dead instrument) |
| ⟲ Git panel width | `grep -rn "gitPanelWidth\|minWidth: 240" App/*.swift` | Shipped width is **280pt** (`TerminalWindowController.swift:79`), applied as the host constraint at `:299` | That `GitReviewView.swift:30`'s `240` is a width — it is the SwiftUI *content minimum* |

**Dead instrument:** no attempt to observe daemon/UI behavior via screenshots or a driven GUI session — this environment has no native-macOS-app automation tool (only browser-tab automation, inapplicable to an Electron/native app). Every mechanism claim above is source-code or on-disk-state evidence, not a UI walkthrough; exact Settings-UI labels were never visually confirmed.

## Retired theories (fates table)

| Theory | Status | Killed by |
|---|---|---|
| Open Design can derive an xtty DESIGN.md automatically (`brand-extract` or `import-local` against the Swift source) | ❌ Refuted | Source read of `skills/brand-extract/SKILL.md` (URL-only) and `apps/daemon/src/design-systems/import.ts` (CSS/JS-family extensions only) |
| Open Design's spawned agent could safely restyle xtty's Swift files directly, since the runtime is just the general-purpose `claude` CLI | ❌ Rejected on design grounds | `core-slim.ts`'s charter ties the entire preview/export pipeline to HTML; a Swift edit would be invisible to it |
| Selecting "xtty" as the project's design system (the formal picker) is what caused the mockup to use xtty's real colors | ❌ Refuted | `app.sqlite`'s `projects.design_system_id` for the actual project is `null`; palette accuracy came from the agent independently reading `design/xtty/tokens.css`, evidenced in its own message trace. ⟲ **2026-07-29 — strengthened from "didn't" to "mechanically could not":** `null` means "No design system" with the default deliberately not reapplied (`server.ts:3619-3629`), *and* a package with no `metadata.json` is `draft`, rejected at selection with `DESIGN_SYSTEM_NOT_PUBLISHED` (`server-services.ts:122-127,193-199`) — the POC's package was never selectable |
| ⟲ The picker is therefore incidental — a folder-linked project gets fidelity from file proximity anyway | ❌ Refuted 2026-07-29 | `server.ts:3889-3915` + `prompts/system.ts:1129-1155`: the picker gates the *entire* verbatim tokens.css/DESIGN.md injection channel. The POC got a correct palette **by luck**, not by mechanism |
| `OD_SANDBOX_MODE` provides OS-level sandboxing of the spawned agent's file access | ❌ Refuted | Full read of `sandbox-mode.ts`: only redirects HOME/config paths and gates *which folders can be chosen*; no runtime file-access enforcement |
| ⟲ `OD_SANDBOX_MODE` is still worth enabling as credential defense-in-depth (it "isolates" HOME/config) | ❌ Refuted 2026-07-29 — *promoted from the open-questions list, where the verb "isolates" presumed it worked* | Measured: under its exact env (`sandbox-mode.ts:175-192`) `claude auth status` flips `{loggedIn:true}` → `{loggedIn:false, authMethod:'none'}`; `HOME` (`:178`) and `CLAUDE_CONFIG_DIR` (`:188`) are each sufficient alone. It doesn't isolate credentials, it destroys access to them — generation would not run. A negative grep also found no desktop wiring that reaches it |
| ⟲ Mode B (MCP) needs a real setup project — Docker or build-from-source | ❌ Refuted 2026-07-29 | `/api/mcp/install-info` returns a ready launch spec with `cliExists:true`, `buildHint:null`, pointing at the binary already inside the installed app bundle |
| ⟲ The two integration modes are alternatives; you pick one | ❌ Refuted 2026-07-29 | `mcp.ts:1-11`, `:1478-1485` — every MCP tool is a `fetch()` at the daemon, which is the desktop app's own sidecar. B requires A running |
| Scoping a project to a small subfolder (e.g. `design/mockups/`) fully contains what the spawned agent can touch | ❌ Refuted (the refutation confirmed live, not just from source) | The POC's own agent read a sibling folder (`design/xtty/`) outside its assigned project directory, unprompted — the folder boundary is not enforced, though this specific read was benign |
| `.od-skills/` folders dropped into a project directory are meaningful, version-control-worthy project content | ❌ Refuted | `cwd-aliases.ts` — per-turn disposable copy, "replaced wholesale on every call," content-hashed and fully reproducible from the app's own install |

## Re-verify by effect
- **"No OS sandbox"** → point Open Design at a small scratch folder and ask it to read a file one directory up (a sibling); if it succeeds, the boundary still isn't enforced.
- **"`design_system_id` doesn't drive a folder-linked project's token accuracy"** → `sqlite3 app.sqlite "SELECT design_system_id FROM projects WHERE id='<id>';"` against any folder-linked project, compared against whether its output nonetheless matches a design system's tokens.
- **"`.od-skills/` is disposable"** → delete it, run another turn in the same project, confirm it reappears (folder name changes only if the source skill's absolute path changes).
- ⟲ **"The picker is the tokens channel"** → the DB read is only a *precondition* (it still reads `user:xtty` if the symlink dangles or the package regresses to draft, while the daemon composes zero design-system blocks). The real gate is the artifact: generated HTML must contain the pasted `:root` block **and a token value unique to our tokens.css**. Change one token value in the repo, regenerate, and grep the output for the new value.
- ⟲ **"Content telemetry is off"** → never trust the API echo; re-read `app-config.json` on disk and assert `telemetry.content === false`. Note this is a *prevention-only* control — there is no local send-log, so it cannot be verified retroactively for past runs.
- ⟲ **"MCP is not installed / is not needed"** → if ever installed, quit Open Design.app and confirm every `od mcp` tool fails; that failure *is* the proof it was only ever a remote control for the GUI.

## Reusable guidelines
1. For a native (non-web) app, hand-author the DESIGN.md/tokens.css design base by reading the real source — no Open Design tool can derive one automatically for it.
2. Treat every Open Design project as running an unsandboxed, confirmation-free coding agent: scope it to a small, dedicated, otherwise-empty folder (never the repo root), understand that this bounds blast radius rather than eliminating the theoretical risk, and write the ignore rules **before** the folder is created. ⟲ *Amplified 2026-07-29:* `.od-skills/` alone is not enough (the charter also writes `brand-spec.md`, `-v2` duplicates, fetched images and `*.artifact.json` sidecars), and since folder scope is not enforced, pair it with a **detect-and-revert** posture — clean, pushed tree before any run; repo-wide `git status`/`git diff` plus `git reflog`/`git stash list` after; stage by explicit path only.
3. Don't assume the formal design-system picker is what applied a palette to a folder-linked project's output — verify from the project's own DB record and the agent's message trace if precision matters; the agent may find and use project-adjacent files on its own. ⟲ *Corollary 2026-07-29:* conversely, **do** set the picker — it is the only supported channel that puts tokens in the prompt, and its absence is why the POC's success was luck.
4. Open Design's coherent role for xtty is HTML mockup exploration that gets hand-translated into SwiftUI afterward — never a path to direct Swift edits.
5. ⟲ **Turn content telemetry off before the first run of anything that reads a private repo.** Default is `content:true`, which per run ships prompt, agent output, a listing of the project folder, bash stdout, and **base64 bodies of produced files**. It is one of only two failure modes here with no rollback, and the only one with no detection either (no local send-log). The sibling is `git clean` on untracked files — which is why "clean, pushed tree before any run" is the other half of the discipline.
6. ⟲ **When a doc says a tool "isolates" or "sandboxes" something, measure the claim before relying on it.** Both safety-shaped mechanisms here inverted under measurement: `OD_SANDBOX_MODE` severs auth rather than isolating credentials, and `artifactManifest:false` gates nothing at all despite reading like a containment switch.

## POC outcome (this specific run, now wiped)
- Design base authored at `design/xtty/{manifest.json,DESIGN.md,tokens.css}` — 9 H2 sections (Visual Theme, Color Palette, Typography, Component Stylings, Layout, Depth & Elevation, Do's/Don'ts, Responsive Behavior, Agent Prompt Guide), every claim cited `path:line` against `App/*.swift`/`XttyConfig.swift`/`XttyProfile.swift` @ `b0e3998` — e.g. dark theme `#1d1f21`/`#c5c8c6`, SF Mono 13pt, zero materials/vibrancy anywhere in chrome (grep-verified), 220pt sidebar / **280pt** git panel (⟲ corrected — see Postscript), 40% quake height, the one bespoke component (SpatialToast, radius 8). Also present but never inventoried here: a hand-authored `design/mockups/README.md` (1,744 B) that served as the project brief the agent actually read.
- ⟲ **The folder-linked project was named `mockups`** (id `6b448e0b-…`), *not* "Prototype" as originally written. `Prototype · 7/28/2026` (id `0d3413fc-…`) is a **separate, non-folder project** created 58 minutes earlier with no `baseDir` and no run at all. This correction is load-bearing for cleanup: anyone deleting by the original name would remove the wrong row and leave the real residue in place. Verified via `app.sqlite`: `baseDir` correctly resolved, `importedFrom:"folder"`.
- ⟲ **The project was minted by `POST /api/import/folder`, not by the project location.** Its `metadata_json` key order exactly matches `import-export-routes.ts:398-407`; a projectLocation-created project would instead carry `importedFrom:'project-location'`. The surviving `projectLocations` entry is functionally **inert** — `project-locations.ts:115-129` scans only immediate *child* directories containing `.open-design/project.json`, and `design/mockups` had none. Two independent mechanisms; the original capture conflated them.
- One generation succeeded end-to-end: `xtty-settings-pane.html` (863 lines) — used the real dark theme, the reference accent explicitly annotated as inheriting the system accent, the status-color vocabulary, and cited specific DESIGN.md facts directly in the markup. ⟲ **But the daemon's run row never left `running`**: the second assistant message still reads `run_status='running'` with empty `produced_files_json`, though the file was demonstrably written. A run can therefore finish with the app still believing it is in flight — one more reason to quit fully and confirm with `ps`.
- Everything in the repo (`design/`), plus the app's installed design-system package, was deleted after this capture — owner decision, clean-slate restart. ⟲ The original text called that package "the app's installed **copy**"; if it was produced by the local-install route (the most plausible producer — that route names the folder `path.basename(source)` = `xtty`) it was a **symlink**, not a copy (`library-install.ts:175`). Confidence `likely`, not confirmed: no DB row is ever written for a design system and the folder is gone, so a manual `cp -R` can't be excluded. The distinction matters — the correct registration route has no copy/drift problem at all.
- **App-side residue that was NOT wiped** — ⟲ *substantially larger than originally listed.* Beyond the `mockups` project row (`design_system_id` null) and the "forbids inventing colors" message trace: the **complete generated HTML** in the version store (`projects/6b448e0b-…/.file-versions/…/0001-….html`, 41,288 B); **two full run directories** (`runs/bd7ca797-…/events.jsonl` 319,505 B and `runs/ec3e9be6-…/events.jsonl` 11,661 B, plus ~50 KB `state.json` each — no pruning code path was found, implying unbounded growth at ~320 KB per substantial turn); the spawned CLI's own **640 KB session transcript** at `~/.claude/projects/-Users-markmark-source-contribute-xtty-design-mockups/`, entirely outside the app and unreachable by any in-app action; a `tabs` row; 6 orphan `run_devloop_iterations` rows and 2 `applied_plugin_snapshots` rows (no FK, so no cascade); the stale `projectLocations` entry; and **three** other project rows — `Prototype · 7/28/2026`, plus two `Untitled` (one from 2026-07-26, three minutes after onboarding and two days pre-POC; one from POC day). A clean-slate restart must delete all four projects in-app, and the project location separately.

## Salvage — the wiped content was recovered (2026-07-29)  ⟲ new
Before any cleanup, the surviving app-side residue was mined and copied to `/tmp/od-poc-salvage/` (5.6 MB, scratch — not committed):

| Recovered | From | Result |
|---|---|---|
| `recovered/DESIGN.md` | `runs/bd7ca797-…/events.jsonl`, as a `Read` tool-result | 9,460 B / 84 lines — the wiped design base, verbatim |
| `recovered/tokens.css` | same run trace | 8,951 B / 135 lines — verbatim |
| `mockup/xtty-settings-pane.html` | `projects/6b448e0b-…/.file-versions/…` | 863 lines, byte-complete |
| `runs/`, `claude-transcript/`, `app.sqlite.snapshot` | app data dir | full traces + a point-in-time DB snapshot |

**This refutes the original Provenance claim that this document was "the only surviving record of the wiped artifacts' *content*."** It was not: the app's own version store and run traces retained everything. Two durable lessons — (1) `.file-versions/` is app-side **even for folder-linked projects**, which is the strongest argument for committing accepted mockups to git (git strictly supersedes it); (2) a run's `events.jsonl` embeds the full body of every file the agent read, so it is both a recovery source *and* a disclosure surface worth knowing about.

## Open questions carried forward
*(Two of the original three were answered on 2026-07-29 and moved to the fates table: the design-system picker — it is decisive, not incidental; and `OD_SANDBOX_MODE` — refuted, auth-severing.)*

- Exact Settings-UI navigation for **every** GUI step — adding a project location, the folder-import picker, Settings → Design Systems, the per-project design-system picker, the Privacy content-telemetry toggle, and the MCP-server snippet. Still never visually confirmed; **the dead instrument persists** (no native-macOS-app automation in this environment — only browser-tab automation, inapplicable to an Electron app). Every label below the source layer is inferred, so the owner's clicks are themselves the experiment.
- ⟲ **Whether the UI exposes the local-install (symlink) route at all.** The HTTP endpoint exists and is origin-guarded, but if the UI only surfaces "Import from folder" (which maps to the *destructive* CSS-scanning generator), then `curl` to the daemon is the only safe path. **Highest-value thing the next setup settles.**
- ⟲ Whether the picker UI even *lists* draft systems. Drafts appear in the catalog (`server-services.ts:113-138`) but are rejected at selection (`:193-199`) — if listed without explanation, a package missing `"status":"published"` looks selectable and fails confusingly. Possibly exactly what happened in the POC.
- ⟲ Whether `deleteUserDesignSystem`'s `rm(path, {recursive:true, force:false})` (`index.ts:1455`) unlinks a top-level **symlink** or recurses into the repo. POSIX semantics say unlink, and the sibling `uninstallById` lstats-and-unlinks explicitly (`library-install.ts:209-214`), but this was **deliberately never executed** — the downside if wrong is the git working tree. Remove symlinks by hand; if ever tested, test on a throwaway.
- ⟲ Whether the `ds-<dirId>` workspace copy has any in-app re-sync affordance. `tokens.css` propagates through the symlink automatically (mtime-fingerprinted); **DESIGN.md does not** — the picker's workspace copy freezes after first sync, so a `rm -rf .../data/projects/ds-xtty/` is needed to force a re-copy. Skipping it is a silent new-tokens-with-old-prose bug.
- ⟲ Whether `OD_DESIGN_TOKEN_CHANNEL` is set in the installed app's environment. If ever `0`, the entire tokens/components/pull-index injection silently disappears and only DESIGN.md is pushed (`index.ts:646-657`) — halving the picker's value with no visible signal.
- ⟲ Whether the installed app (0.16.1, built 2026-07-23, cask marked `auto_updates`) still matches the `/tmp/open-design` clone @ `f52fda2`. `resolveDaemonResourceDir` (`server.ts:785`) prefers the *packaged* resource dir over the repo path, and the bundle's own resources were never inspected — so re-check the version at creation time.
- ⟲ Whether `data/runs/` is ever garbage-collected. Only readers were found, no pruning path — a slow disk leak at ~320 KB per substantial turn.

## Postscript — independent re-verification (2026-07-29 review pass)

Every citation above was independently re-checked against ground truth the day after capture, before the doc's sources could rot:
- **Open Design source** (the still-present `/tmp/open-design` clone, SHA re-confirmed `f52fda2`): `claude.ts:87` (`--permission-mode bypassPermissions`, exact line), the `core-slim.ts:71` charter quote (verbatim) + `PLATFORM_CONTRACTS_BLOCK` at 177 with the `mobile-ios.html` contract at 180, `import.ts:110-117` extension sets, `sandbox-mode.ts` (HOME/XDG/TMPDIR/`CLAUDE_CONFIG_DIR` redirection at 177-188; `isSandboxImportedProjectRootAllowed` at 87), `projects.ts` (`resolveProjectDir` → `path.normalize(baseDir)` at 117-122; the hidden-segment guard at 80-94), `cwd-aliases.ts` (PR #435 symlink rationale, "replaced wholesale on every call", sha256 `skillCwdAliasSegment`), `skills/brand-extract/SKILL.md` (live in-app-browser methodology), `README.md` ~302-306/~323-326/~398 (MCP snippet + `/usr/bin/od` shadow + `claude mcp add-json`), and the bundled catalog: **151** `manifest.json` packages under `design-systems/`, incl. `apple`/`warp` with `source.type: "bundled"` / "Open Design curated bundled fixture". All matched.
- **xtty facts relayed from the wiped DESIGN.md** (source @ `b0e3998`, unchanged at review HEAD): dark theme `#1d1f21`/`#c5c8c6` (`XttyCore/Sources/XttyCore/XttyConfig.swift:58-64`), fontSize 13 default + monospaced system font (= SF Mono) fallback (`XttyConfig.swift:124`, `App/TerminalConfigurator.swift:14-19`), 220pt sidebar + 900×560 default window (`App/TerminalWindowController.swift:68,92`), 0.4 quake height fraction (`App/QuickTerminalController.swift:30`), SpatialToast radius 8 (`App/SpatialToast.swift:25`). All matched.

  > ⚠️ **One miss, caught in the 2026-07-29 pass:** this postscript relayed "240pt git panel (`App/GitReviewView.swift:30`)". The citation resolves, but **240 is the SwiftUI content minimum, not the panel width** — the shipped width is **280pt** (`App/TerminalWindowController.swift:79`, applied as the host constraint at `:299`). The sidebar's parallel minimum (`App/SessionSidebar.swift:114`, `minWidth: 180`) was missing entirely. Correct pairing: **220pt sidebar / 280pt git panel, content minimums 180 / 240.** Re-verified on disk 2026-07-29. `AGENTS.md` caches no panel widths, so nothing propagated to the guide — but this is a good example of a citation that *resolves* while still not supporting the claim, which a read-back check would never catch.
- **Wipe state**: `design/` absent, zero `design/` paths in all of git history (`git log --all -- design/` empty), `data/design-systems/` empty — and the app-side residue listed in the POC outcome section confirmed present.

The review's corrections were precision fixes (the wipe's exact extent and surviving residue, the Sources citation roots, two confusingly-worded table cells), not substance changes.

**The 2026-07-29 planning pass that followed was not.** Re-deriving the same ground with a different question ("how do we do this for real?") surfaced substantive errors the citation-by-citation review had passed over — a wrong project name, a false "only surviving record" claim, an inverted safety mechanism, a missing schema contract, and a mechanism (the picker) whose effect the doc had recorded as an open question while the answer sat in `server.ts`. Every one of them survived a pass that checked whether each citation *resolved*. The lesson is general enough to state plainly: **verifying that a doc's citations resolve is not the same as verifying its claims are true** — only re-deriving the subject with a fresh purpose reliably finds the second class of error.
