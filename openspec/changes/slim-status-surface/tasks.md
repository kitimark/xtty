# Tasks — slim-status-surface

## 1. Pre-registration (before any edit or probe run)

- [x] 1.1 Commit `probes/` into the change dir: `probes.json` (rule, recall, orientation, and trap classes per design D7), the written per-probe pass criteria, and the harness copied from `openspec/changes/archive/2026-07-06-slim-agents-context/probes/` (`measure-context.sh`, `run-probe.sh`, `run-batch.sh`, `extract-results.py`) with the **two declared edits** applied identically to every arm: turn cap 8 → 10, and a per-probe `tools` override used only by the Bash-enabled orientation probe (O1b). **Correct probe C1's pass criterion before freeze**: the current VM-envelope ground truth is `54/0/1` of 55 on both goldens (2026-07-10, the last full VM measurement) with the VM tiers **3 tests behind across 2 changes** — not the superseded `41/0/1` of 42 — and add `54/0/1` to the pre-flight grep list. E1's pass truth: `XttyCore` `260/0/0`; local Tier-1 `57/0/1` of 58; home `packer/README.md` → Acceptance → Current envelope. Variants: N, S, A-R, A-0 only (the A-T pointerized-tables arm is dropped — the open-changes table is kept, per design D4). Record `claude --version` in `ledger.log`.
- [x] 1.2 Commit the **acceptance envelope** (the gate; never adjusted after seeing results): (a) **Context** — `startup(S) ≤ startup(N) − 0.85 × (bytes_removed_from_AGENTS.md / 2.49)`, **absolute floor ≥3,000 tokens saved** (predicted ≈4.2k); below the floor the guide half is reconsidered, not shipped. (b) **Rule + trap classes** — `pass(S, of 3) ≥ pass(N, of 3)` for every probe; zero regressions. (c) **Recall + orientation** — facts correct in ≥2/3 reps AND `median num_turns(S) ≤ median num_turns(N) + 3` (medians from the freshly frozen N, never the 2026-07-06 figures). (d) **Inline sufficiency, gating for the merged cross-model cluster (X1/X2/G1)** — ≥1 of 3 S-reps refutes at turn 1 with zero file reads. (e) **Wobble** — a 2/3-vs-3/3 flip on a gated cell triggers +2 reps; a 0/3-vs-3/3 flip is signal as-is. (f) **Fix loop** — a trap failure is repaired by restoring that one line's conclusion clause and re-running that probe at 3 reps, **never** by restoring narrative; **cap 2 iterations**, after which that entry ships at full length.
- [x] 1.3 Record the **N baseline**: all probes × 3 reps in a worktree at current `main`; grade against the rubric; commit `probes/grading-N.md` + the N results table + `context/n-rep{1,2,3}.json`. **Frozen before any S content is authored** (precedent: `slim-agents-context` design D6).
- [ ] 1.4 Pre-flight mechanical checks (not probes, no LLM): every pointer target named by a drafted compressed refutation resolves on disk; the project memory index carries no home for any probed trap (verified 2026-07-28 — it covers agent-definition caching, automation mode, SwiftUI-black, push-to-main, peekaboo only); annotate the grading sheet's rule for any rep that reaches a memory file by disk search.

## 2. Rehome + `packer/README.md` Current-envelope block (commit 1 — must land before §3: AGENTS.md's counts may not become a pointer to a section whose leading figure is six supersessions stale)

- [ ] 2.1 Move the orphaned `CGEvent` `wheel1` sign-inverse sentence from `AGENTS.md:56` into the existing sign-convention comment at `AppUITests/XttyMouseWheelUITests.swift:26–32` (verified: zero occurrences elsewhere in the repo; the compressed E19 entry also retains the one-line sign clause per design D3). Comment-only — confirm the test target still builds; this is the cheap/inline case within the documented delegation boundary, **no suite run, no validator delegation**.
- [ ] 2.2 Insert the **Current envelope** block at the top of `packer/README.md` → `### Acceptance` (re-headed from "Acceptance (measured — post `fix-main-menu-clobber`)" to bare `### Acceptance`): one row per (tier, golden) with Envelope / Suite / Last-measured / `CURRENT | STALE (n behind) | SUPERSEDED` status token; the lone-skip note (the opt-in benchmark e2e, `XTTY_RUN_BENCH_E2E=1`); the "not yet covered on any VM tier" table for the 3 post-2026-07-10 tests; corrected evidence paths (Tier-1's accepted `57/0/1` is task **4.8**'s post-cross-review clean run, not task 4.4's bundle); the open-anomaly pointers into the matrix.
- [ ] 2.3 Land the **F4 correction**: the "prior 56-test envelope was identical across all 5 environments" claim is false (`packer/README.md:644–648` — the 56th test's change never ran the VM tiers); the last identical 5-environment measurement is the **55**-test `54/0/1` (2026-07-10), so VM tiers are 3 tests behind across 2 changes. Correct it in `packer/README.md` here, and carry the same correction to `AGENTS.md` (§3.1) and `HISTORY.md:64` (§5.2).
- [ ] 2.4 Convert everything currently at `packer/README.md:297–668` into `#### Envelope history` (append-only, newest-first, capped entries), stripping every "current"/"authoritative" claim — including `:603`, the only such string in the file, which points at a superseded figure — and removing the two retired `/xtty:cross-review` references (`:618–619`, `:631`).
- [ ] 2.5 Strip the two illegal counts from the **Expected-difference matrix** (`:683`, `:688` — it declares its own no-numbers rule at `:675–676`) and collapse the reverse-duty paragraph (`:690–712`, three envelope generations stale) to the rule alone: overwrite the Current-envelope table + append one capped history entry in the same session; never enumerate past changes there.
- [ ] 2.6 Append the **anti-reaccretion maintenance rules** (overwrite-in-place · history append-only · entry cap · matrix carries zero numbers · status is a token · reverse duty names the table not the changes · no current count anywhere else in the repo · projections are labelled and never enter the table · subset runs are never envelope figures) + the maintainer comment above the table.
- [ ] 2.7 Verify the validator contract is intact: `### Acceptance` and the Expected-difference-matrix headings still resolve; envelope and matrix remain adjacent (`openspec/specs/test-validation/spec.md:35–38`); **no edit to `.claude/agents/xtty-test-validator.md` and no `test-validation` delta**. Land commit 1.

## 3. The AGENTS.md restructure (worktree; main stays fat until §4 passes)

- [ ] 3.1 Author the replacement `## Current status` (L9–69) from the drafted redesign: snapshot with the cached test counts replaced by the `packer/README.md` → Acceptance → Current envelope pointer (and the F4-corrected posture — no 5-environment claim); the **open-changes table kept as a table** with its "must equal `openspec list`" duty stated; the shipped table keeping its 5 closed phase rows with the `Tooling` row (1,773 B) collapsed to a category-keyed summary + `HISTORY.md`/`research/README.md` pointers (all 14 dropped doc names verified indexed in `research/README.md`); the established-specs derive-on-demand line unchanged.
- [ ] 3.2 Apply the refutation compression (31 → 24 entries): **11 entries byte-for-byte untouched** (E1 `retry-ban` explicitly frozen — the only entry with an ablation result); **10 compressed** to conclusion + applicability + verified pointer (carve-outs per design D2: E9 `env-seed-wall` kept verbatim + given a pointer; E14's four refuted Local-Network route names kept complete — the enumeration *is* the inoculation; E19 keep-medium with the sign clause inline; E20's `smkx` token kept inline); **10 merged into 3** (self-certification · tar-pit/consult · gate-shapes-never-fixed, with the "instructed writing" claim's pointer corrected to `HISTORY.md`). For each dropped clause, confirm its surviving home before removal; route anything homeless to `HISTORY.md` (§5.2), never dropped.
- [ ] 3.3 Amend `AGENTS.md:127` *Keep progress current* — tracker clause only; the change tail, `⟶ archive-ritual` anchoring, and pre-tick self-check stay byte-identical: the open-changes **table row** duty stays; the snapshot is refreshed only on milestone/posture movement and **never caches the test envelope** (`packer/README.md` → Acceptance → Current envelope is its single home — update that instead); a settled refutation is **one line** (conclusion + applicability condition + evidence pointer); a change that retires/supersedes a refutation's mechanism **edits that entry in place**, never appends; verify-against-disk gains `packer/README.md` → Acceptance alongside `openspec list` / `ls openspec/changes/archive/` / `ls openspec/specs/`.
- [ ] 3.4 Update the committed tooling in the same commit: `.claude/skills/xtty-capture-research/SKILL.md` (step-3 snapshot bullet — envelope not cached here; step-3 refutation bullet — one-line bound + edit-in-place-on-retirement; step-4 verify block gains the packer Acceptance check; fix the stale `.claude/`-exception guardrail; `version 1.2 → 1.3`); `.claude/commands/xtty/capture-research.md:10` (same reconcile shape); `.claude/agents/xtty-openspec-critic.md` — Pass-1 category-keyed bullet extended to the **Learned-refutations list** (an entry exceeding one sentence + condition + pointer is flagged; a retirement that appends instead of editing in place is flagged), Pass-2 gains a **cached-envelope check** (a re-introduced test-count/envelope figure in the guide is a BLOCKER; the existing open-changes table-match check stays — the table is kept), and the **definition stamp v7 → v8 at all three coupled sites (L9, L59, L62)**.
- [ ] 3.5 Confirm the staged `specs/research-capture/spec.md` delta still matches what ships (the three ratchet clauses; the "tabular per-change entry" wording untouched) and `openspec validate "slim-status-surface"` is green.
- [ ] 3.6 Sanity-measure S: run `measure-context.sh` on the S worktree, 3 reps; check the measured Δ against the 0.85× formula and the ≥3,000-token floor **before any behavioral run**; investigate any >15% disagreement with the byte prediction before trusting behavioral comparisons. Mechanical grep of the new packer block for `260/0/0`, `57/0/1`, `58`, `54/0/1` (the envelope probe cannot otherwise distinguish "pointer broken" from "guide broken").
- [ ] 3.7 Build the two ablation worktrees as throwaway states, each exactly one deletion so any delta is attributable: **A-R** (N + refutation compression only) and **A-0** (S with the cross-model cluster **deleted** rather than merged).

## 4. Verify (the gate)

- [ ] 4.1 Run the S arm: all probes × 3 reps in the S worktree; same rubric, same grader, graded against the **frozen** N sheet; commit `probes/grading-S.md`, the results table, `context/`, `max-turns-tool-trails.md`, and `inline-sufficiency.md`.
- [ ] 4.2 Run the ablation arms (diagnostic, **non-gating**): A-R and A-0 × 2 reps each; assemble the dose-response table (`arm | bytes | startup tokens | rule | recall | orientation | trap`) and apply design D2's content decisions (A-0 decides the merge floor for the cross-model cluster).
- [ ] 4.3 Judge N-vs-S against the §1.2 envelope. Apply the bounded fix loop where needed (cap 2 iterations per probe; each fix + re-run recorded). Named contingency: an inline-sufficiency miss on a merged cross-model entry → un-merge that entry back toward its pre-merge conclusion, never restore narrative.
- [ ] 4.4 Land commit 2 on main — the AGENTS.md section + the *Keep progress current* amendment + the three tooling files together (the critic edit must ride along so its new checks and stamp match the shape they police) — with the probe results referenced in the commit message.

## 5. Capture + trackers

- [ ] 5.1 Write the dated addendum to `research/03-analysis/agents-md-context-budget.md` per the capture-depth bar: the regrowth curve (28.1 KB 2026-07-06 → 45.0 KB 2026-07-28, ≈0.77 KB/day) with the section decomposition; the mechanism of regrowth (append-on-retirement; the perpetual-row/mini-index shape; a cached measurement all three copies of which drifted together into the same false claim); the probe method + full results including anything **N** itself failed; the inline-sufficiency instrument and what it can and cannot prove; the A-0 verdict on compress-vs-delete; each retired option next to the evidence that killed it (bare pointers ← spec + V3's T1 0/2; delete-the-shipped-table ← vacuous `coherence-review`; pointerize-the-open-changes-table ← kept instead, deferred until friction is observed); re-verify-by-effect (`measure-context.sh` on a fresh clone); the distilled guideline. Index it in `research/README.md`.
- [ ] 5.2 Reconcile the trackers **in the new format** (this change eats its own cooking): snapshot refreshed (envelope now a pointer); no new Tooling-row clause; a single new refutation one-liner if one settled; the full narrative appended to `HISTORY.md` under a dated heading, **including the 5-environment-claim correction at `HISTORY.md:64`**; `research/04-design/02-milestones.md` touched. Land commit 3.
- [ ] 5.3 Pre-archive coherence review — ⟶ xtty-openspec-critic (slim-status-surface)
- [ ] 5.4 Archive + reconcile — ⟶ archive-ritual. Before ticking: confirm the `research-capture` delta merged into `openspec/specs/` and reads as **what shipped** (not what the proposal guessed — if 4.3's contingency fired, the merged text must match the shipped shape); `openspec validate --all --type spec` green; trackers reconciled (snapshot + HISTORY + milestone + refutations); verify-against-disk run (`openspec list` · `ls openspec/changes/archive/` · `ls openspec/specs/` · `packer/README.md` → Acceptance); `readlink CLAUDE.md` → `AGENTS.md` and `git ls-files -s CLAUDE.md` → mode `120000` (parity by effect).
