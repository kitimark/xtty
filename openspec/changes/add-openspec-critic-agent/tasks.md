## 1. The `xtty-openspec-critic` agent

- [x] 1.1 Write `.claude/agents/xtty-openspec-critic.md` frontmatter (`name`, `description`, `model: opus`) + a `Definition version: v1 (<date>)` stamp as the first report line (with a maintainer note to bump it on every edit) + the **deference chain** (AGENTS.md's "Keeping a change coherent" + spec-delta-format sections for the rules — read fresh each run, never hardcode; the live repo state for drift ground-truth).
- [x] 1.2 Encode the **three review passes**: (a) **single-change coherence** — operationalize the AGENTS.md rules as concrete checks (the `## ADDED/MODIFIED` headers; `### Requirement:` + SHALL/MUST; ≥1 four-hashtag scenario each; proposal↔specs capability contract both directions; MODIFIED pastes the *entire* established block from `openspec/specs/<cap>/spec.md`; verify tasks running Tier-1/VM carry `⟶ xtty-test-validator`, CI-investigation tasks carry `⟶ xtty-ci-investigator`; new observable behavior carries a `verification-harness` delta; requirements mechanism-neutral); (b) **disk-drift** — established specs match `ls openspec/specs/`, active changes match the AGENTS.md open-changes table, any prose count/fraction re-derived against disk, promised reverse-duty tracker edits present; (c) **cross-change** — shared established-requirement collisions, declared archive/apply ordering (incl. red→green pairs), inter-change dependency references, **derived at run time** from `openspec list` + the change dirs, never cached. A rule it cannot mechanically check is **flagged for human review**, not dropped.
- [x] 1.3 Encode the **fixed report skeleton**: stamp · verdict (coherent/issues-found/blocked-prerequisite) · review scope (change(s) reviewed) · per-finding list (rule · location file+requirement/scenario/task · severity **BLOCKER**|**REVIEW**) · a distinct **cross-change** findings section when >1 change is open · recommendation (proceed/fix-then-proceed/blocked-prerequisite); any BLOCKER forbids a coherent verdict; heuristic REVIEW findings never block.
- [x] 1.4 Encode **observe-never-repair + read-only guardrails** (no edits to any artifact/spec/tracker/code, no "fixing" a finding, exact locations + rule names reported, blocked-prerequisite reported instead of guessing) and the **deliberately-lighter posture** (short read-only `openspec`/`git`/`grep`/`find` runs — no VM, no turn-alive invariant, no ledger/babysitter; it clones the `xtty-ci-investigator` shape, not the validator's).

## 2. The `/xtty:review` launcher

- [x] 2.1 Write `.claude/commands/xtty/review.md`: spawn `xtty-openspec-critic` **by name** (subagent invocation, not Skill), forward the change reference (name or "all active") verbatim, relay the report; document the **delivery check** (report's first-line stamp must match `.claude/agents/xtty-openspec-critic.md`; mismatch = stale-served, treat as blocker) and point at AGENTS.md as the source of truth for the validate-vs-review boundary.

## 3. Wire the trigger boundary + tooling docs

- [x] 3.1 Update `AGENTS.md`: add the tooling-table/committed-tooling row for `xtty-openspec-critic` + `/xtty:review`; add the **validate-vs-review delegation boundary** (`openspec validate` = inline; a full coherence review = delegate) with the per-task marker `⟶ xtty-openspec-critic (<change|all>)` to "How to work here"; state its deference chain (rules in AGENTS.md's coherence sections; drift ground-truth in the live repo). Note the recommended trigger points (post-propose, pre-archive) without hardcoding them into the `/opsx:*` skills.
- [x] 3.2 Update `openspec/config.yaml` `rules.tasks`: add a pointer instructing that a task performing a full coherence review carries the `⟶ xtty-openspec-critic (<change|all>)` marker, deferring to AGENTS.md for the boundary (single source of truth).

## 4. Verify + reconcile

- [x] 4.1 `openspec validate --strict add-openspec-critic-agent` passes (proposal ↔ `coherence-review` capability match; every requirement has ≥1 four-hashtag scenario). **Done:** `Change 'add-openspec-critic-agent' is valid`.
- [x] 4.2 Confirm the two new `.claude` files are git-tracked, not ignored (`git check-ignore -v` returns nothing for them; they fall under the `xtty-*` / `commands/xtty/` exceptions). **Done:** `git check-ignore` (no `-v`) returned exit 1 + no output for both (neither ignored — the `!.claude/agents/xtty-*` / `commands/xtty/` exceptions apply); both stage cleanly via `git add -n`.
- [x] 4.3 **By-effect dogfood proof** (spawn the agent on this session's real state): review the two open prompt-width changes and confirm the critic (a) returns a **coherent** verdict for each (or names any real finding), (b) surfaces the **cross-change red→green pair** (`add-vm-prompt-width-parity` → `harden-findbar-wrap-assertion`) as a cross-change finding, and (c) catches the known **disk-drift** — AGENTS.md's "7 of the 10 archived changes carry a harness delta" vs the disk truth (19/34) — as a drift finding; and the report stamp `v1` matches the file. ⟶ xtty-openspec-critic (all active) — **PASSED** (after a skills reload made the new agent spawnable — the same-session propagation lag first blocked it, exactly as documented): stamp `Definition: v1 (2026-07-07)` matched (fresh); **VERDICT: COHERENT**, 0 BLOCKERs across all 4 active changes; (a) each change coherent + `openspec validate --strict` green; (b) the red→green pair surfaced by name (add-vm-prompt-width-parity before harden-findbar-wrap-assertion); (c) the `7/10`→**19/34** harness drift caught **plus** a bonus open-changes-table drift; and it confirmed the MODIFIED-superset paste + delegation markers + mechanism-neutrality while correctly clearing legitimate RFC-2119 `MAY` (no false-flag). Both REVIEW drift items actioned in 4.4.
- [x] 4.4 Reconcile trackers (Keep progress current): add the change's Current-status row + refresh the snapshot in `AGENTS.md`; append a dated narrative to `HISTORY.md`; add the Tooling-group milestone note in `research/04-design/02-milestones.md`; then verify against disk (`openspec list`, `ls openspec/changes/archive/`, `ls openspec/specs/`). **Done:** AGENTS.md open-changes table brought level with `openspec list` (all 4 active rows) + the critic added to the Tooling snapshot row; **actioned the dogfood's two drift findings** — the `7 of the 10`→`19 of the 34` harness-count fix (grep now 0) and the table drift; HISTORY.md 2026-07-07 narrative appended (newest-first); milestones Coherence-review tooling note added. Disk-verified: `openspec list` = 4 (matches table), archive 34 (not archived — correct), specs 19 (matches Established-specs line; `coherence-review` merges at archive).
