# Cross-review ledger — add-git-diff-wrap-toggle

Advisory only. Nothing in this file is read mechanically by any downstream step; archive eligibility is the human-attestation gate in `AGENTS.md` (`⟶ archive-ritual` step-0), computed from git, never from this ledger.

## Round 1

**Reviewed range:** base `0ba5056f26a0277d8689942203e8c53e5023bbcc` (parent of the commit that added `openspec/changes/add-git-diff-wrap-toggle/proposal.md`) → HEAD `ecd7431ae4a03e26fff339d8d2da1d182c170dae` at review launch.

**Reviewed file list (this change's own files — the mechanical `B..HEAD` range also swept in ~80 unrelated paths from prior already-archived changes, a known non-attributive artifact of this repo's scope classifier; both soundness passes were briefed to scope to the list below):**
- `App/GitReviewView.swift`, `App/TerminalWindowController.swift`, `App/XttyApp.swift`
- `AppUITests/XttyGitReviewUITests.swift`
- `XttyCore/Sources/XttyCore/GitReviewStore.swift`, `XttyConfigLoader.swift`, `XttyProfile.swift`
- `XttyCore/Tests/XttyCoreTests/GitFileTreeTests.swift`, `XttyConfigTests.swift`
- `openspec/changes/add-git-diff-wrap-toggle/{proposal,design,tasks}.md`, `specs/*`
- `packer/README.md` (the envelope note)

**Passes run:** all three (companion resolved at `1.0.6`, `codex.available: true`, `auth.loggedIn: true` — Pass B ran, not skipped).
- **Pass A** — `xtty-openspec-critic`, Definition v6 (2026-07-18). Verdict: **COHERENT**. No blockers; 2 REVIEW items (AGENTS.md tracker drift), both owed to the not-yet-run archive-ritual.
- **Pass B** — Codex, model `gpt-5.6-sol` (effort: project-config default, not independently confirmed this run). Verdict: **needs-attention**. 4 findings (1 high, 3 medium).
- **Pass C** — inline Fable 5, self-attested served model `claude-fable-5`. Effort inherited from the invoking session (not independently pinned). Verdict: **needs-attention**. 6 findings (0 critical/high, 4 medium, 2 low).

**Brief given to Pass B and Pass C (identical, staged out-of-repo via `mktemp`):** design intent (fixing the ~40%-width-collapse bug + adding wrap/no-wrap modes), 5 specific claims to soundness-check (the PreferenceKey→onGeometryChange diagnosis; the 1pt geometry tolerance; the eager no-wrap VStack's memory cost; the emphasis-across-wrap-point assumption; whether any other test-synchronization race remained after the already-fixed one), a digest of drills already run (Tier-0/Tier-1 results, manual by-effect checks), and an explicit scoping note plus the standing additive instruction to report anything outside the brief.

### Findings and disposition

| # | Pass | Severity | Finding | Disposition |
| --- | --- | --- | --- | --- |
| 1 | B | high | No-wrap's `.frame(minWidth:)` floor applied before padding made every row (even short ones) report ≥ `viewportWidth+16`, so `diffContentOverflows` was tautologically `true` — the automated regression guard could not have caught the bug it exists to catch. | **Fixed** — floor reordered to apply after padding; added a short-line negative-control assertion to the XCUITest. Manually re-verified via the DEBUG state dump (short line → `overflows=false`, long line → `overflows=true`, same session). |
| 2 | B | medium | DEBUG's `viewportWidth` came from an async `onGeometryChange`-fed `@State`, Release's from a synchronous `GeometryReader` — a materially different view tree, and DEBUG's value stayed at its `0` default on a pre-macOS-15 host, silently breaking the no-wrap floor. | **Fixed** — unified on one `GeometryReader`-driven path for both configs; the DEBUG-only geometry signal is now a pure additional observation that never feeds row sizing. |
| 3 | B | medium | No-wrap's eager `VStack` (5000 lines × up to 3000 chars/line, ~15M chars worst case) was unmeasured against the product's lean-memory hard requirement. | **Fixed in round 1, SUPERSEDED in round 2** — round 1 capped no-wrap's eager rendering to 500 rows; round 2 found this created a worse defect (see round-2 finding #1) and replaced it with a laziness-preserving mechanism. |
| 4 | B | medium | The new XCUITest's repo/selection setup depends on OSC 7 shell-integration (zsh-only), so it degrades to the capability-absent arm on bash — contradicting a "shell-independent" claim this session had written into `packer/README.md`. | **Fixed** (documentation only) — corrected the `packer/README.md` note: the test is in the same shell-arm class as its `XttyGitReviewUITests` siblings, not shell-independent. The test itself is unchanged — this is the same correct, already-established pattern every sibling test in the file follows. |
| 5 | C | medium | The single-value `onGeometryChange` overload used is back-deployed to macOS 13 (verified against the installed SDK's `SwiftUICore.swiftinterface`), not macOS 15 — the shipped `#available(macOS 15, *)` gate was factually wrong and its no-op arm unnecessary given the 14.0 deployment target. | **Fixed** — gate removed after independently re-verifying the claim against the SDK interface file myself. Independently re-confirmed by Fable in round 2. |
| 6 | C | medium | (Same underlying issue as B's #2, phrased independently: DEBUG/Release render structurally different trees; only DEBUG was ever verified.) | **Fixed** — covered by the same fix as B's #2. |
| 7 | C | medium | No-wrap's eager-VStack ceiling is unmeasured; suggested a laziness-preserving alternative (pre-measure the widest line, pin a `LazyVStack` to an explicit width). | **Deferred to round 1's simpler cap, then SUPERSEDED in round 2** — round 2 ultimately implemented exactly this alternative (see round-2 finding #1), once the cap approach was shown to be a real defect, not just an unbenchmarked one. |
| 8 | C | medium | Geometry measurement assumes the `ScrollView`'s outer frame equals the content viewport — would be narrowed by a legacy (space-reserving) vertical scrollbar once a diff is tall enough to show one; not exercised by this change's short-diff tests. | **Accepted residual, re-raised independently by Codex in round 2 (see below) — still not fixed, impact narrowed.** Documented in design.md R6. |
| 9 | C | low | The store never reset `diffFillsWidth`/`diffContentOverflows` on selection change — a dump could transiently pair a newly selected file with the previous file's stale geometry. | **Fixed** — `GitReviewStore.select`/`apply`'s stale-selection branch now reset both booleans; added 2 unit tests. Manually re-verified via the state dump (selecting file B after file A does not carry A's geometry). Round 2 extended coverage further (see below). |
| 10 | C | low | An emphasis run consisting only of whitespace at a wrap boundary can render invisibly (wrapped trailing whitespace isn't drawn) — cosmetic. | **Accepted, documented** — one line added to design.md R4 naming the edge case; no code change (matches Fable's own recommendation). |

**Round bound:** 1 of 2 (N=2). Round 2 ran given the substantive fixes above warranted re-verification.

## Round 2

**Reviewed range:** base `0ba5056f26a0277d8689942203e8c53e5023bbcc` → HEAD `8afeb4edf5a04a034e107a9e624e7eca4f12d1c9` at review launch (round 1's fixes + ledger, committed).

**Passes run:** all three again.
- **Pass A** — `xtty-openspec-critic`, Definition v6 (2026-07-18). Verdict: **COHERENT**. No blockers; same 2 tracker-drift REVIEW items, owed to the not-yet-run archive-ritual. All round-1 fixes independently re-verified present and correct in source (Pass-4 code-accuracy check).
- **Pass B** — Codex, model `gpt-5.6-sol`. Verdict: **needs-attention**. 5 findings (2 high, 2 medium, 1 low). Explicitly opened with "Do not ship."
- **Pass C** — inline Fable 5, self-attested served model `claude-fable-5`. Verdict: **needs-attention**. 4 findings (0 critical/high, 2 medium, 2 low).

**Brief:** round-1's 6 fixes summarized with file/line pointers, an explicit ask to verify each is *actually* correct (not just plausible), the 4 accepted residuals from round 1, and the standing additive instruction.

### Findings and disposition

| # | Pass | Severity | Finding | Disposition |
| --- | --- | --- | --- | --- |
| 1 | B (high) + C (medium, corroborating independently) | high | Round 1's 500-row no-wrap cap made toggling wrap→no-wrap on a diff over 500 rows **drop rows wrap mode still showed** — violating the "switching modes never changes which lines are shown" invariant (Codex), which Fable independently traced to the same defect surfacing as an inaccurate git-review spec-delta SHALL. | **Fixed properly** — replaced the row cap entirely. No-wrap now uses a `LazyVStack` (matching wrap), floored — container and every row — to `max(realViewportWidth, precomputedWidestRowEstimate)`, the estimate computed once from character counts (no per-line text layout). Every row shares the identical floor, so the `LazyVStack` never needs to inspect an off-screen row — genuine laziness restored without capping which rows exist. Manually re-verified: a 600-line diff (over the old cap) renders uncapped in both modes; short-only/long-line diffs each measure the correct geometry signal. No spec-delta wording change was needed since actual behavior now matches the original invariant. |
| 2 | B | high | Every substantive `StateDumpReader.waitForState` timeout in the new XCUITest (repo setup, no-wrap overflow, wrap geometry) attached a screenshot and `return`ed **without `XCTFail`**, so a genuine regression in the feature under test could still produce a green test — "fail-open." Additionally: the short-line predicate could be satisfied during the loading state / by the just-added reset default, not a real measurement. | **Fixed** — added `XCTFail(...)` to the 4 subject-behavior-timeout branches (repo-never-surfaced, short-line-control-never-reported, nowrap-default-never-reported, toggle-never-flipped); left the legitimate capability-absent early-return (`assertSemanticCaptureInactive`) and the universal Release-build "no state dump" check untouched, matching the established sibling-test convention. The vacuous-short-line-predicate half of this finding is the same root cause as Fable's finding #4 below — fixed together. **Noted, not fixed:** this fail-open `attachScreenshot(...); return` pattern (no `XCTFail`) is pre-existing across ALL sibling tests in `XttyGitReviewUITests.swift`, not unique to this change's new test — fixing the whole file is out of scope here; flagged as a worthwhile dedicated follow-up. |
| 3 | B | medium | The marker/content `HStack`'s default `.center` vertical alignment centers the single-character marker beside a multi-row **wrapped** content block instead of aligning it with the first row, undermining the hanging-indent presentation for genuinely wrapped lines. | **Fixed** — `HStack(alignment: .top, spacing: 0)`. Manually re-verified: a long line's `+`/`-` marker now sits flush with the wrapped block's first row, not centered across all rows. |
| 4 | B | medium | (Independently re-raised, same underlying issue as round 1's C-#8/R6) Legacy/space-reserving scrollers are a real, user-facing supported macOS configuration (not hypothetical) that would narrow the true content viewport versus the `ScrollView`'s outer frame for a tall diff, causing false-overflow / wrong-floor behavior. | **Accepted residual, not fixed** — same reasoning as round 1: narrow, environment-dependent, not exercised by current tests. Round 1's fix for finding #1 above (the `max(viewportWidth, …)` floor formula) narrows this residual's real-UI impact to short-only no-wrap diffs specifically (a long diff's floor is dominated by the content estimate, unaffected); the DEBUG signal's precision is still affected either way. Documented in design.md R6. |
| 5 | B | low | `clear()` and `apply()`'s "selection drops out" path (the previously-selected file is no longer listed) did not reset the geometry booleans — only the stale-preserved-selection branch did. | **Fixed** — `apply` now resets whenever the selection's path changes OR its diff goes stale for the same path; `clear()` and `setDiffWrap()` also reset. Added 3 more unit tests (dropped-selection, `clear()`, `setDiffWrap()` — 8 total store tests now). |
| 6 | C | medium | Round 1's short-line negative-control predicate (`diffContentOverflows == false`) is satisfiable by the store's own reset **default** (also `false`), landing in the window between selection and the real `onGeometryChange` measurement — a stale dump could pass the guard vacuously, exactly the class the test's own long.txt comment warns against. | **Fixed** — added `diffFillsWidth == true` to the wait predicate and assertion (a real floored short row measures `fillsWidth == true`; the reset default never does), making the negative control deterministic. |
| 7 | C | medium | Round 1's no-wrap row cap contradicts the git-review spec delta's "never which lines are shown" SHALL — would merge untrue spec text at archive if left as-is. | **Moot after the round-2 fix for finding #1** — the cap no longer exists, so the spec delta's original wording is accurate again; no spec-text edit needed. |
| 8 | C | low | Geometry reset (round 1) covered `select`/`apply`'s stale-preserve branch only, not `clear()`/`setDiffWrap()`/the dropped-selection path — same class as B's finding #5. | **Fixed** — covered by the same fix as B's #5. |
| 9 | C | low | The proposal's wording ("tint spanning the full scroll width") slightly overclaims relative to the round-1 implementation (each row's own content width, floored to the viewport). | **Resolved by the round-2 architecture change** — every row (short and long) now floors to the SAME diff-wide estimate, so tints DO span the full scroll-content width uniformly; re-verified this is now accurate, no wording change needed. |

**Escalated residuals (not fixed, final):** design.md R6 (legacy-scroller viewport assumption, narrowed impact); the pre-existing fail-open test pattern across `XttyGitReviewUITests.swift`'s OTHER tests (out of scope for this change); the no-wrap floor estimate's safety margin (4 characters) is reasoned, not benchmarked, against real font-metric edge cases; VM tiers (headless/graphics) have not been run for this change at all.

**Round bound: 2 of 2 (N=2) — reached.** Per protocol, no further review rounds; residuals above are escalated to the human, not looped on further.

---

**Reminder (per protocol): "converged" has no gate force here. Every dismissal above is a proposal, not an accepted state, until the human reads this ledger. There is no receipt. The archive gate is the human attestation in `tasks.md`, computed independently from git by `scripts/cross-review-digest.sh`.**
