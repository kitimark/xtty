## 1. Fix the multi-line-paste content assertions

- [x] 1.1 In `AppUITests/XttyUITests.swift`, change the `:82` and `:84` assertions in `testMultiLinePasteIsNotAutoExecuted` from strict `GridDumpReader.waitForContains(lineA/lineB, timeout: 5)` to the wrap-tolerant `GridDumpReader.waitForContains(lineA/lineB, timeout: 5, ignoringLineWraps: true)`, mirroring `:53` and `:198`; keep the surrounding flow (pasteboard set → Cmd+V → assert lines reached grid → `:87` not-executed check → `Ctrl+U` clear) unchanged.
- [x] 1.2 Leave the `:87` negative "command not found" check strict (design D3) — do not touch it.
- [x] 1.3 Add/extend the inline comment at the `:82`/`:84` call sites noting a pasted line can soft-wrap behind a long prompt (the wide-prompt zsh-rig case), that paste insertion is still what's asserted, and that the bash execution arm (`:87`) is a separate shell-capability concern owned by `split-shell-dependent-testplan`.

## 2. Confirm the existing regression guard covers this (no new guard — design D2)

- [x] 2.1 Confirm `testSoftWrapGuardIsWrapTolerant` (shipped by `harden-findbar-wrap-assertion`) is present in `AppUITests/XttyUITests.swift` and still deterministically proves the wrap-tolerant matcher is required and works (strict whole-token match false, wrap-tolerant true) independent of prompt width. No new guard is added; record this in the change (do not duplicate the guard). *(Confirmed present at `:268`; self-validating — wrap-tolerant match MUST succeed while a strict whole-token physical-row match MUST fail; prompt-width-independent (types a 150-char marker guaranteed wider than the pane). No new guard added.)*

## 3. Verify

- [x] 3.1 Iterate inline while coding: `make test-core` (matcher/unit sanity) and `xcodebuild build-for-testing` to confirm the edited `:82`/`:84` assertions compile under the real toolchain. (Inline — cheap iterate tier.) *(`make test-core` 237/0; `xcodebuild build-for-testing` → **TEST BUILD SUCCEEDED** — edited assertions compile.)*
- [x] 3.2 Acceptance (regression): run the Tier-1 local XCUITest suite (`make test`) and confirm `testMultiLinePasteIsNotAutoExecuted` still passes locally (local prompt does not wrap), the existing guard passes, and no strict-default caller regressed. ⟶ xtty-test-validator (Tier-1 local XCUITests) *(validator v4 (2026-07-06), IN-ENVELOPE: Tier-1 local **41/0/1**, `testMultiLinePasteIsNotAutoExecuted` passed 3.6s, `testSoftWrapGuardIsWrapTolerant` passed 9.6s, no strict-default regression; the 1 skip is the opt-in bench e2e.)*
- [x] 3.3 Acceptance (red→green proof): on the wide-prompt **zsh** VM rig (`xtty-test-zsh:26.5`), confirm `testMultiLinePasteIsNotAutoExecuted` **flips from the reproduced `:82` red to green** under the wide prompt — the in-guest fix proof — and the zsh-rig envelope moves `40/1/1` → `41/0/1`; confirm 0 `capture inactive` (semantic family still asserts for real) and no Local-Network/modal regression. ⟶ xtty-test-validator (zsh VM graphics+headless — paste :82 red→green) *(validator v4 (2026-07-06), IN-ENVELOPE: `:82` red→green on **both** arms (headless 4.77s / graphics 5.11s); zsh envelope **40/1/1 → 41/0/1**; **0** "capture inactive" attachments (all 6 semantic tests green 3.9–8.8s); **0** `mDNSResponder policy 'pending'` events / no LN modal on either arm. Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-08-harden-paste-wrap-assertion/`.)*

## 4. Reverse-duty tracker updates (same session)

- [x] 4.1 `packer/README.md` Acceptance/matrix: correct the "Bracketed-paste — zsh grid-capture arm" row cause from "staged/highlighted region scrape miss" to **prompt-width soft-wrap (find-bar wrap class)** and move it residual → **fixed** (wrap-tolerant matcher at `:82`/`:84`; a recurrence is now a REGRESSION); flip the zsh-rig envelope `40/1/1` → **`41/0/1`**; leave the bash-rig `:87` execution-arm row and the bash envelope `40/1/1` unchanged. *(Updated the zsh-rig envelope headline + paste bullet + graphics line + matrix row (now "zsh soft-wrap arm (FIXED)") + the reverse-duty change list; bash rows/envelope untouched.)*
- [x] 4.2 `research/03-analysis/github-actions-ci-cd.md` §19b: update the `add-zsh-test-image` zsh cross-check note so the paste guarantee on zsh reads as **exercised via a now wrap-tolerant `:82`** (fixed), not a strict-matcher residual; the hosted-CI bash `:87` residual (`bash32-no-bracketed-paste`) is unchanged (CI runs bash). *(Rewrote the cross-check note: zsh `:82` was prompt-width soft-wrap, fixed by wrap-tolerant matcher `40/1/1`→`41/0/1`, not an XCTSkip; only bash `:87` remains shell-dependent. Also corrected a stale `:84`→`:87` in the bash-arm row so the section is internally coherent.)*
- [x] 4.3 `research/03-analysis/shell-dependent-test-partitioning.md`: correct the characterization so the zsh `:82` paste red is a **matcher fix (this change)**, not a `split-shell-dependent-testplan` skip candidate; only the bash `:87` execution arm is shell-dependent (skip). Update the strategy/divergence bullets accordingly. *(Corrected the status header + the two divergence/re-verify bullets: zsh `:82` = prompt-width soft-wrap, fixed by wrap-tolerant matcher `40/1/1`→`41/0/1`, paste does stage both lines on zsh (the "doesn't stage" reading was a strict-matcher artifact); change 2 skips only bash `:87`. The doc's older skip-and-partition framing is `split-shell-dependent-testplan`'s own reverse duty, left for its reconcile.)*
- [x] 4.4 `openspec/changes/split-shell-dependent-testplan/design.md`: add a one-line interaction pointer — the zsh `:82` soft-wrap red is fixed by `harden-paste-wrap-assertion`; the split change's `XCTSkipUnless(bracketedPasteMode)` skips only the **bash** execution arm and must preserve the `:82`/`:84` wrap-tolerance (design D4). Minimal edit, no restructuring. *(Added to D2; adapted to the split-plan's post-pivot framing — it now **asserts** the bash `:87` arm rather than `XCTSkipUnless`, and preserves the `:82`/`:84` wrap-tolerance since the zsh arm is already green from this change.)*

## 5. Close-out

- [x] 5.1 `openspec validate harden-paste-wrap-assertion` (fix any delta/scenario errors) and confirm the `verification-harness` MODIFIED "Soft-wrap-robust content assertion" requirement pasted the **full** existing block (all four prior scenarios verbatim) then extended it (prose adds multi-line-paste coverage; 1 new scenario for a soft-wrapped pasted line). (Inline — cheap validate tier.) *(`openspec validate` → valid; diffed vs `openspec/specs/verification-harness/spec.md`: all 4 prior scenarios verbatim, prose extended to add multi-line-paste/"pasted input", +1 new "soft-wrapped pasted line" scenario inserted before the guard scenario.)*
- [x] 5.2 Pre-archive coherence review of the change (and the active change-set, incl. the `split-shell-dependent-testplan` interaction / red→green framing). ⟶ xtty-openspec-critic (harden-paste-wrap-assertion) *(critic v1 (2026-07-07), **VERDICT: COHERENT, no BLOCKER**: spec delta a clean superset (4 prior scenarios verbatim +1), contract/markers/coverage/reverse-duty all sound, cross-change ordering + orthogonality hold. 2 non-blocking REVIEW items folded in: (1) skip→assert wording corrected in harden's proposal.md/design.md D4 to match split's pivot; (2) AGENTS.md "20 of the 39"→"21 of 41" handled in the 5.3 reconcile.)*
- [x] 5.3 Archive + reconcile: `openspec archive harden-paste-wrap-assertion`, finish the merge by hand (verify the `verification-harness` block merged correctly), then update trackers per Keep-progress-current — AGENTS.md Current-status snapshot + table row, append the narrative to HISTORY.md, milestone state if any (none expected — test-hardening tooling), Learned-refutations unaffected — and verify-against-disk (`openspec list`, `ls openspec/changes/archive/`, `ls openspec/specs/`). ⟶ archive-ritual
