## 1. Fix the find-bar focus-restore assertion

- [x] 1.1 In `AppUITests/XttyUITests.swift`, change the `:192` assertion in `testFindBarOpensLocatesAndDismisses` from the strict `GridDumpReader.waitForContains(marker, timeout: 5)` to the wrap-tolerant `GridDumpReader.waitForContains(marker, timeout: 5, ignoringLineWraps: true)`, mirroring `testFocusTypingOnActivateWithoutClicking` at `:53`; keep the surrounding flow (type marker → assert reached grid → `Ctrl+U` clear) unchanged.
- [x] 1.2 Add/extend the inline comment at the call site noting the marker can soft-wrap behind a long prompt (the hosted-runner `findbar-marker-wrap` case) and that focus restoration is still what's asserted — pointing to `ci-runner-prompt-width-forensics.md`.

## 2. Deterministic soft-wrap regression guard

- [x] 2.1 Add a new test to `AppUITests/XttyUITests.swift` (DEBUG/`-UITestGridDump`-gated, matching the file's other dump-gated assertions) that types a **single contiguous marker wider than the focused pane** (e.g. ≥ ~120 chars of a unique random token) at the prompt.
      - Done: `testSoftWrapGuardIsWrapTolerant` — `XCTSkipUnless(GridDumpReader.isAvailable)` gates it (matches the dump-gated pattern); marker = `WRAPGUARD<6-digit rand>` + 150×`Z` (single contiguous alphanumeric token, no metachars, ~165 chars ≫ any default pane).
- [x] 2.2 Make the guard **self-validating**: assert (a) a **strict** `waitForContains(marker)` returns *false* (the marker genuinely spanned ≥2 physical rows — a real wrap occurred), and (b) the wrap-tolerant `waitForContains(marker, ignoringLineWraps: true)` returns *true*. The test can never pass without a real wrap; if the marker unexpectedly does not wrap, (a) fails loudly (widen the marker).
      - Done: order inverted for robustness — (b) tolerant `waitForContains(…, ignoringLineWraps: true)` **first** (gates presence: a false strict result then means "wrapped", not "not yet echoed"), then (a) strict `gridContains(grid, marker, ignoringLineWraps: false)` on the read dump asserted **false** (proves a `\n` split the token). Fails loudly if the pane is wider than the marker.
- [x] 2.3 Clear staged input (`Ctrl+U`) at the end, consistent with the sibling type-at-prompt tests.

## 3. Verify

- [x] 3.1 Iterate inline while coding: `make test-core` (matcher/unit sanity) and a one-off `-UITestGridDump` launch or a focused run of the two tests to confirm the guard wraps and the find-bar assertion passes locally. (Inline — cheap iterate tier.)
      - Done: `make test-core` **232/0/0** green; `xcodebuild build-for-testing` = **TEST BUILD SUCCEEDED** (both the edited find-bar assertion and the new `testSoftWrapGuardIsWrapTolerant` compile under the real toolchain — the `XCTest` SourceKit warning is a known false positive; `gridContains` accessible). The interactive focused *execution* (guard-wraps / find-bar-green) could not initialize the UI runner in this GUI-less background job ("Timed out while enabling automation mode") → behavioral confirmation folds into the delegated 3.2 Tier-1 acceptance on a GUI-capable rig.
- [x] 3.2 Acceptance: run the Tier-1 local XCUITest suite (`make test`) and confirm the new guard passes, `testFindBarOpensLocatesAndDismisses` still passes locally, and no strict-default caller regressed. ⟶ xtty-test-validator (Tier-1 local XCUITests)
      - **Validator (Definition v4, delivery-check passed; 2026-07-07 — verbatim):** Tier 1 local **NOT RUN — BLOCKED** by a host prerequisite: this Mac's Automation Mode requires interactive user auth (`automationmodetool`: "Automation Mode is disabled… requires user authentication"), so 3 `make test` launches each failed at XCUITest session init ("Timed out while enabling automation mode", 0 tests executed). Not a test result, not a regression, not a product issue — a one-time human action (out of validator scope). **Substance confirmed elsewhere:** the new guard passes and `testFindBarOpensLocatesAndDismisses` passes on all 3 VM runs (same test bundle; see 3.3), and `xcodebuild build-for-testing` compiled cleanly (no strict-default caller regressed).
      - **Update (2026-07-07, blocker cleared → Tier 1 local now RAN):** `sudo automationmodetool enable-automationmode-without-authentication` (Touch ID) resolved the host block; re-run **Tier 1 local `41/0/1`** (`** TEST SUCCEEDED **`, 42 exec −1 skip=bench e2e, 0 failures) + Tier 0 `232/0/0`, verdict **IN-ENVELOPE**. Both change tests green — `testFindBarOpensLocatesAndDismisses` + the new `testSoftWrapGuardIsWrapTolerant`. Count reconciles: README local `40/0/1` baseline +1 guard = `41/0/1`. The change is now validated on **both** environments (VM `40/1/1` red→green + local `41/0/1`). Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-07-findbar-wrap-local-tier1/`.
- [x] 3.3 If `add-vm-prompt-width-parity` was applied first (the red→green order): on the wide-prompt VM rig, confirm `testFindBarOpensLocatesAndDismisses` **flips from the reproduced red to green** under the same wide prompt — the in-guest fix proof (and update the wide-prompt envelope to find-bar green). ⟶ xtty-test-validator (graphics VM — find-bar red→green)
      - **Validator (verbatim):** VM **`40/1/1` of 42**, byte-identical across headless run1 + run2 + graphics. `testFindBarOpensLocatesAndDismisses` **flipped from the `38/2/1` interim red to GREEN**; new `testSoftWrapGuardIsWrapTolerant` GREEN (+1 test). Sole red = `testMultiLinePasteIsNotAutoExecuted` (bash-3.2 paste `:87`, known-benign). **D2 verified by effect** on all 3 runs: find-bar's `AFTERFIND####` genuinely soft-wraps behind the 58-char `\h` prompt (strict would fail; wrap-tolerant passes), and the guard's 165-char marker spans **3 physical rows** (strict whole-token match False) — neither vacuous. Verdict OUT-OF-ENVELOPE vs the interim README = the design-predicted red→green resolution, zero regressions → PROCEED. Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-07-findbar-wrap-fix-green-proof/`.
- [x] 3.4 Post-merge: watch the next `build-and-test` run (`gh run watch` — inline) and confirm the find-bar red flips green under the runner's real long-prompt wrap; `test-core` stays green. If it is unexpectedly still red, investigate the run ⟶ xtty-ci-investigator (<run>).
      - Done (watched inline, run `28841237111`, 2026-07-07): overall `failure` (expected — `build-and-test` non-blocking). **`test-core` (required gate) => success.** `build-and-test` => failure with its **sole** failing test method `testMultiLinePasteIsNotAutoExecuted` (the `bash32-no-bracketed-paste` residual). **`testFindBarOpensLocatesAndDismisses` absent from failures → flipped GREEN under the runner's real long prompt**; the new `testSoftWrapGuardIsWrapTolerant` also green. IN-ENVELOPE per §19b — no `xtty-ci-investigator` needed (find-bar not still red).

## 4. Reverse-duty tracker updates (same session)

- [x] 4.1 `research/03-analysis/github-actions-ci-cd.md` §19b: move the `findbar-marker-wrap` bucket from known-benign residual → **fixed** (note the shipped fix: wrap-tolerant matcher at `:192`); update the §19b "Fixes" line and the intro provenance so a future CI investigation classifies a recurrence as a **regression**, not a benign residual.
      - Done: removed the `findbar-marker-wrap` row from the residual table (paste row remains); added a **"Fixed — `findbar-marker-wrap` (shipped 2026-07-07…)"** block (wrap-tolerant matcher at `:198` + the guard; red→green `40/1/1` proof; "a recurrence is now a REGRESSION"); intro now says "one residual remains"; the mechanism note is past-tensed to "historical — how it arose, now fixed"; the §19 provenance line notes the residual was since fixed.
- [x] 4.2 `packer/README.md` Acceptance: refresh the note to reflect the assertion is now wrap-tolerant (fixed). If applied after `add-vm-prompt-width-parity`, record that find-bar was the *reproduced red* on the wide-prompt VM and this change greens it (the final envelope carries find-bar green); otherwise note the count is unchanged (find-bar passed on the short-`\h` VM).
      - Done: interim block → **"pair CLOSED"**; headline **Envelope `40/1/1` of 42, measured** (2026-07-07, wide-prompt image; lineage `39/1/1`→`38/2/1` interim→`40/1/1`; `+1` guard test); the "gap CLOSED" note → **"reproduced, then fixed, in-guest"** (D2-confirmed); the regression rule retires the interim carve-out (find-bar red is again a regression); the Prompt-width-wrap matrix row → **"now fixed"**.
- [x] 4.3 `research/03-analysis/ci-runner-prompt-width-forensics.md`: add a dated line in §6 recording that Option A (wrap-tolerant fix) + the deterministic guard landed, with the change name.
      - Done: §6 **"Addendum (2026-07-07) — Options A + B landed: `harden-findbar-wrap-assertion`"** (red→green in-guest, D2 verify-by-effect for both find-bar + guard, final `40/1/1` of 42, evidence path); also past-tensed the §3 stale "asymmetry is the whole bug" claim to "was … fixed 2026-07-07 (`:198`)".

## 5. Close-out

- [x] 5.1 `openspec validate harden-findbar-wrap-assertion` (fix any delta/scenario errors) and confirm the `verification-harness` MODIFIED requirement pasted the full existing block.
      - Done: `openspec validate` → "is valid". The MODIFIED "Soft-wrap-robust content assertion" pasted the full established block (both original scenarios — "soft-wrapped present" + "absent fails" — verbatim) then extended it: prose adds "and the find-bar focus-restore coverage" + the deterministic-guard sentence; 2 new scenarios (find-bar focus-restore tolerates a wrapped marker; the guard proves the matcher is required and works).
- [x] 5.2 On archive, update the AGENTS.md Current-status snapshot/table and append the narrative to HISTORY.md (per Keep-progress-current); the Learned-refutations bullet is unaffected (the *reproducibility* refutation still holds — the fix doesn't make the runner hostname derivable).
      - Done (2026-07-07 archive): open-changes row removed (table now = `openspec list`: add-ci-pipeline + add-openspec-critic-agent); Shipped/Tooling row gained the pair-#2 entry; HISTORY.md narrative appended (pair #2, above #1). Snapshot already carried the find-bar-fixed / `40/1/1 of 42` / `42 XCUITests` state (set during #1's archive) — verified accurate, no further edit. Learned-refutations reproducibility bullet left as-is.
