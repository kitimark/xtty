## 1. Emission — the task-authoring configuration (propose-time)

- [ ] 1.1 Add a fifth `rules.tasks` entry to `openspec/config.yaml`: a change **in scope for cross-model review** MUST carry, in its tail, exactly **one blocking human-attestation cross-review task** — placed **after** the `⟶ xtty-openspec-critic` coherence-review task and **before** the `⟶ archive-ritual` task — with its **HUMAN-ONLY / model-MUST-STOP** wording (the model may neither tick it nor derive/compute the attested value). Mirror the four existing rules' shape and **defer to AGENTS.md** for the scope boundary rather than restating it.
- [ ] 1.2 State in that rule that it is an **authoring instruction, NOT a delegation marker** — it directs what task is *written*, and emitting it MUST NOT fire the paid external review on task arrival (design D2).
- [ ] 1.3 State in that rule that the propose-time scope call is **semantic and fail-closed**: at propose the change's implementation paths do not exist yet, so **when in doubt, emit the task** (design D1/D5).

## 2. Enforcement — the coherence-review agent (pre-archive)

- [ ] 2.1 Rewrite the gate-task check in `.claude/agents/xtty-openspec-critic.md` to be **classifier-driven**, with the corrected arms (design D4 — the first draft's exit-0 arm was **fail-open** and this is the fix):
  - **exit 10** (in scope) ⇒ **BLOCKER** unless the tail carries **exactly one** attestation task, **positioned after the critic task and before the archive task**, with the human-only wording. Absent, **duplicated**, **misordered**, or mis-worded all block (design D6).
    ⚠️ **Cardinality must count gate *tasks*, not gate *mentions*.** A naive grep for the HUMAN-ONLY wording **false-positives on this very change**: its own `tasks.md` carries **three** such lines — 1.1 (the task that authors the emission rule), 4.1/4.2 (the task that migrates the gate task into *another* change's tail), and 6.2 (the one real gate task). A duplicate-BLOCKER on 6.1's own self-review would be a self-inflicted false red. The check counts only lines that **are** the gate task, never lines that **describe, specify, or migrate** one — and where it cannot tell the two apart it reports a **REVIEW** naming the ambiguity, not a false-duplicate BLOCKER.
  - **exit 0** (*not yet knowable to be in scope*) ⇒ **semantic** judgment, at most **REVIEW**, **never suppressed**; state what the classifier returned. *(⚠️ Round 4: `exit 0` is **not** reliably "the normal post-propose state" — after any intervening archive a change latches to `exit 10` (design D1). The arm still stands: the classifier's answer is never about **this** change, so it can never license silence about it.)*
  - **exit 2** with `proposal.md` **absent from HEAD** (genuinely uncommitted) ⇒ **semantic** judgment, at most **REVIEW**.
  - **exit 2** with `proposal.md` **committed** (a malformed range) ⇒ **BLOCKED-PREREQUISITE**. *Measured:* **both** committed tools refuse on such a range (`cross-review-scope.sh` **and** `cross-review-digest.sh`, exit 2, `range-omission`), so step-0's check (2) cannot be satisfied. *Honest limit (design D4):* no committed rule yet **requires** step-0 to run on a refusal — blocking at review is right regardless, but do **not** claim archive is mechanically forced to refuse.
- [ ] 2.2 Bump the agent's definition stamp to **`Definition: v5 (<date>)`** — the delivery-check probe lies if the stamp does not move on every edit (design R6).
- [ ] 2.3 Verify (by effect) the **exit-10 BLOCKER** arm across all four defect shapes — task **absent**, **duplicated**, **misordered** (placed after the archive task), and **missing the human-only wording** — using scratch copies of a change the classifier resolves in scope. Each must yield a **BLOCKER** and a non-coherent verdict. Confirm the report's first line reads **v5** (a v4 stamp = stale-served definition ⇒ the run is invalid, not a pass).
- [ ] 2.4 Verify (by effect) the **exit-0** arm: a committed-but-unimplemented change with no gate task must yield a **REVIEW** that **states the classifier returned "out"** — never silence. *(This is the regression the first draft would have shipped; it is the single most important arm to test.)*
  ⚠️ **NO NATURAL SPECIMEN EXISTS ON DISK — you must synthesize one.** All **three** open changes read **exit 10** today (design D1's latch: `openspec archive` writes the non-allowlisted `openspec/specs/**`, so any change open across any archive latches in-scope permanently — even `add-git-diff-wrap-toggle`, whose own 7 paths are 100% allowlisted). So testing this arm against any real open change would **silently exercise the exit-10 arm instead** and report a false pass. Build a **scratch change on a clean range** (propose it at HEAD so its range holds only its own allowlisted artifacts), confirm the classifier returns **exit 0** for it, then assert the REVIEW. Delete the scratch change afterwards.
- [ ] 2.5 Verify (by effect) both **exit-2** arms: an uncommitted change ⇒ **REVIEW**; a committed change with a malformed range (change dir touched before its `proposal.md`) ⇒ **BLOCKED-PREREQUISITE**.

## 3. Guide

- [ ] 3.1 Update `AGENTS.md`'s cross-review gate section + the standard-change-tail rule: the gate task is **emitted by the task-authoring configuration** at propose (fail-closed semantic scope call) and **enforced mechanically by the critic** — with blocker force only from an in-scope verdict. State the **retrospective limit**: a not-in-scope verdict means *not yet knowable to be in scope*, never *out of scope*. Keep the existing statement that there is **no auto-firing `⟶ xtty-cross-review` marker**.
- [x] 3.2 Add the change's row to `AGENTS.md`'s **Current status → Open changes** table (the table *"must match `openspec list`"*). — *DONE during the cross-review's round-1 fixes (it was live disk-drift the critic flagged, not deferrable work).*
- [ ] 3.3 Add a **Learned-refutations** line — **rewritten in round 4; the earlier draft's version rested on a claim since refuted** (*"the classifier returns a confident false negative before implementation lands"* — true in principle, but **not** the observed steady state):
  *An obligation that lives only in AGENTS.md prose does not reach `/opsx:propose`, which reads `openspec/config.yaml` (**G13** again). **And the path-based scope classifier can never answer "is THIS change in scope?" — it is NON-ATTRIBUTIVE and it LATCHES.** It scopes over `parent(proposal.md)..HEAD`, a **repo-wide** range, so an in-scope verdict says only that *some* path in that range is out-of-allowlist — never **whose**. Worse, `openspec archive` **always** writes `openspec/specs/**`, which is **not** allowlisted — so **any change left open across any other change's archive latches to `in scope` permanently and can never return.** Measured 2026-07-13: all **three** open changes read `exit 10`, and for **two** of them (`fix-cross-review-gate-task-emission` at 1/20 tasks, `add-git-diff-wrap-toggle` at 0/20) **every** driving path is **foreign** — their own paths are 100% allowlisted. So an in-scope verdict is **not** evidence that a change's own implementation landed, and no check may use it as that proxy. The direction is still safe (over-inclusion never opens a bypass), but the precision is a function of **repo activity, not of the change** — which is why range-narrowing is routed to the base-resolution change before any BLOCKER leans on this verdict.*

## 4. Migration — ⛔ BLOCKED ON A HUMAN DECISION (design Q3)

> ⚠️ **Do not implement this group until the human answers Q3.** Round 4 **refuted the premise** these tasks were written on. The earlier plan (migrate `add-git-diff-wrap-toggle`, exclude `add-ci-pipeline` as *"unsatisfiable"*) is **withdrawn**: the unsatisfiability claim is false (`add-ci-pipeline`'s remaining work — a test PR and a **GitHub repo setting**, not even a commit — sits *before* its tail, so owner-steps → attest → archive is an ordinary available ordering), and **both** open changes **predate the obligation** (by 12 days and 1 day), making this a retroactively-applied rule. The three live options are in design **Q3**: **(a)** migrate both, **(b)** grandfather all pre-gate changes and migrate neither, **(c)** keep the split and accept a standing unclearable BLOCKER. **The model must not choose.**

- [ ] 4.1 **[Q3 = a or c]** Append the blocking human-attestation cross-review task to `openspec/changes/add-git-diff-wrap-toggle/tasks.md` — after its `⟶ xtty-openspec-critic` task (5.1) and before its `⟶ archive-ritual` task (5.2), with the HUMAN-ONLY wording. Safe per design: it has not attested, and the digest normalizes checkbox state and excludes the attestation line. **This must be a separate commit from any later attestation** (design D3 — check (4) confines the attestation-introducing commit's `tasks.md` hunk to the attestation line + ticks, so the task *text* must already be committed).
- [ ] 4.2 **[Q3 = a]** Give `add-ci-pipeline` a **conforming, clearable tail**: add the missing `⟶ xtty-openspec-critic` coherence task (it has none — verified — so *"place after the critic task"* currently has no anchor), then the attestation task before its existing `⟶ archive-ritual` task (6.2). Record the real cost honestly rather than calling it impossible: its digest range is **324 paths, 319 foreign (98.5%)** and **HEAD-dependent**, so the human attests a range dominated by work they did not write, and the attestation must be the **last** pre-archive act (no intervening commits). **[Q3 = b]** Instead record the grandfathering rule (pre-gate changes are exempt; compare the change's proposal commit against the gate's ship commit `8950884`) and migrate neither. **[Q3 = c]** Record the exclusion *and* its accepted residual: a **standing, unclearable critic BLOCKER** on `add-ci-pipeline` that no permitted action can clear until the base-resolution change lands.
- [ ] 4.3 Verify (whichever branch of Q3 was chosen): each migrated change carries **exactly one** attestation task, correctly positioned between its critic task and its archive task; `scripts/cross-review-scope.sh` still returns exit 10 for it; and no change was left with an obligation it cannot satisfy.

## 5. Verify the fix end to end

- [ ] 5.1 Verify **emission by effect** (design D8 — a grep for the rule string in `config.yaml` is a read-back check, NOT evidence): run `/opsx:propose` on a **scratch** in-scope change and confirm the generated `tasks.md` tail carries the blocking human-attestation task with its human-only wording, correctly positioned. Delete the scratch change afterwards.
- [ ] 5.2 Verify **no regression in the model-STOP boundary**: the emitted task carries no delegation marker, and reaching it does not fire the paid external reviewer.
- [ ] 5.3 Run `openspec validate fix-cross-review-gate-task-emission --strict` (cheap, mechanical — stays inline).

## 6. Land the change

- [ ] 6.1 Pre-archive coherence review of this change. ⟶ xtty-openspec-critic (fix-cross-review-gate-task-emission)
  **Validity precondition — ATTRIBUTIVE, not classifier-based.** ⚠️ An earlier version gated this on *"the classifier resolves this change as **exit 10**"*, and called a run at **exit 0** *"not a pass"*. **That test is dead:** the classifier reads **exit 10 today**, at 1/20 tasks with **zero implementation landed**, driven entirely by foreign paths (design D1) — so the exit-0 disqualifier can never fire again and the guard would green-light an invalid run. **Replace it with a test that actually measures "this change's implementation landed":** assert that `git diff --name-only "$B"..HEAD` contains **this change's own implementation paths** — `openspec/config.yaml` **and** `.claude/agents/xtty-openspec-critic.md` — **and** that tasks 1.1–2.2 are ticked **and committed**. Only then is a 6.1 run meaningful. (The v5-stamp rule still applies independently: a report reading **v4** is a stale-served definition ⇒ invalid, not a pass.)
- [ ] 6.2 **BLOCKING human-attestation cross-review — HUMAN-ONLY, the model MUST STOP.** This change **will be** mechanically in scope once its implementation lands (it touches `openspec/config.yaml`, `.claude/`, and `openspec/specs/` — all outside the docs/tracker allowlist); at propose time the classifier reads **exit 0**, because only its own artifacts have landed — *which is precisely the defect this change fixes.* **This task was added by hand**, because the rule that emits it is what this change introduces (design R1). The **human** (deliberately; never auto-fired) runs `/xtty:cross-review fix-cross-review-gate-task-emission`, reads the complete advisory ledger, runs `scripts/cross-review-digest.sh` **themselves**, and ticks this task **recording the reviewed-state digest on the delimited attestation line**. The model SHALL NOT tick this task and SHALL NOT derive, compute, or fill in the attested value.
- [ ] 6.3 Archive + reconcile. ⚠️ **COMMIT NOTHING BETWEEN 6.2 AND THIS TASK.** Any post-attestation commit touching **any** reviewed path — including `AGENTS.md` / `HISTORY.md`, which are in this change's reviewed range — invalidates the digest, and **every** forward-history way to re-record the attestation then fails check (4) (`adds=2 dels=1` / `now=2`). **Attest as the last pre-archive act; archive in the same sitting; zero intervening commits; hold the attestation commit local until archive completes.**
  Then: **step-0 precondition first** — run the committed scope classifier (⇒ in scope), then the fail-closed four-check precondition (attestation present + well-formed; digest recomputes to the attested value; clean tree; exempt-by-act) **before** merging. Run the digest/scope tooling **as of the reviewed base**. ⚠️ **Narrow reading of the self-validation clearance:** this change modifies neither gate **script** (`cross-review-digest.sh`, `cross-review-scope.sh`), so the reviewed-base tooling rule is satisfied *for those two* — **but this is not a blanket clearance.** The change **authors the v5 critic**, which *is* the enforcement layer it introduces, and task 6.1 then runs that v5 critic **on this change**. The reviewed-base rule does not cover the critic, so the only thing standing between v5 and self-clearance is 6.1's validity precondition — which is why that precondition must be the **attributive** one (see 6.1), not the dead classifier proxy. A critic BLOCKER is advisory, so this is bounded, not fatal — but do not read the parenthetical as clearing more than the two scripts. Then merge the spec deltas (`openspec archive`), finish the merge by hand, tick trackers (Current-status row + snapshot + HISTORY narrative + milestone), add the Learned-refutations line (3.3), and verify-against-disk. ⟶ archive-ritual
