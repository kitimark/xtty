## 1. SwiftTerm patch (the product fix)

- [x] 1.1 In `patches/swiftterm/xtty-accessors.diff`, add a new hunk to `cmdScrollDown` (`Terminal.swift:4646`) mirroring `cmdScrollUp`'s existing `if marginMode { … } else { … }` shape (D1): keep the current narrow, margin-bounded `copyFrom` path when `marginMode` is true, unchanged; add an `else` branch doing a full-width whole-line `splice` — direction-mirrored from `cmdScrollUp`'s non-margin branch — deleting the line scrolling off `scrollBottom` and inserting a fresh blank line at `scrollTop`, per iteration of `p`.
- [x] 1.2 Update the patch file's header comment to note it now also carries a `Terminal.swift` correctness fix, not only additive `MacTerminalView.swift` accessors (D2) — a departure from every prior hunk in this file, worth flagging explicitly for the next reader.
- [x] 1.3 Re-run `scripts/bootstrap-swiftterm.sh` to confirm the revised patch applies cleanly from the pristine pin.

## 2. Tests

- [x] 2.1 Add an `AppUITests` end-to-end scenario (`testScrollRegionReversalDoesNotCorruptOtherColumns` in `XttyMouseWheelUITests.swift`) driving the exact escape sequences directly via `printf` (D3): enter the alternate screen, set an 8-row scroll region, write three rows of distinct 10-character content, issue `SU` once then `SD` once, and assert row 1 is fully restored — not just column 0. Investigated and rejected driving a real full-screen program first (`vim`/`less`, bundled in the test VM, don't exercise the buggy `cmdScrollDown` path at all; `htop`, which does, is brew-only and absent from the VM image) — see D3's full rationale. Confirmed red against the current unfixed patch: row 1 reads `"BCCCCCCCCC"` instead of the expected `"BBBBBBBBBB"`, matching the hand-traced defect exactly. Ticket for re-verification once 1.1–1.3 land: this test must flip green.

## 3. Verify

- [x] 3.1 `make test-core` — `XttyCore` regression (inline, cheap; no new headless test in this change — see D3).
- [x] 3.2 Run the Tier-1 XCUITest suite + full acceptance matrix (both goldens) and confirm the new end-to-end scenario is green with no regressions elsewhere. ⟶ xtty-test-validator (Tier-1 + full matrix, both goldens)

  **Validator report (`Definition: v5`, 2026-07-10) — IN-ENVELOPE.** Tier 0: `237/0/0`. Tier 1 (local): `54/0/1 of 55`. VM matrix — headless bash ×2: `54/0/1 of 55` each (byte-identical); headless zsh: `54/0/1 of 55`; graphics bash: `54/0/1 of 55`; graphics zsh: `54/0/1 of 55`. All 5 environments byte-identical (empty failing set everywhere); the lone skip in every tier is the opt-in `XTTY_RUN_BENCH_E2E` benchmark, not a shell-dependent vacuous pass (0 `capture inactive` hits checked explicitly on both zsh rigs). `testScrollRegionReversalDoesNotCorruptOtherColumns` passed on all 6 runs (6.7–8.9s), confirming the `cmdScrollDown` fix reached every VM host-build and holds identically on bash and zsh (expected — VT-engine fix, `printf`-driven test, same arm both shells). The previously-documented graphics-zsh runner-stall anomaly did not recur. `packer/README.md`'s living envelope predates this change's 55th test; acceptance is anchored on the delta (prior clean baseline + 1 new green, 0 new reds), per the validator's own note — `packer/README.md` is updated in task 5.2 below.

## 4. Upstream (deferred)

- [x] 4.1 Note the upstream SwiftTerm PR opportunity (the `cmdScrollDown` margin-guard fix) as a deferred maintainer action, per the precedent set by `fix-scroll-wheel-mouse-reporting`/`smooth-scroll-wheel-momentum` — not filed as part of this change.

## 5. Coherence + archive (the standard change tail)

- [x] 5.1 Pre-archive coherence review of this change. ⟶ xtty-openspec-critic (fix-scroll-reversal-redraw-corruption)

  **Critic verdict (`Definition: v1`): COHERENT.** No BLOCKER findings. Two non-blocking REVIEW items: (1) a mechanism-neutrality style note on the `verification-harness` requirement body (design rationale embedded in the requirement text — not a coherence defect); (2) disk-drift reminders that 5.2 must update `packer/README.md`'s matrix row + the AGENTS.md snapshot count (already planned) and that `harden-test-precision-vs-claim` is separately missing from AGENTS.md's open-changes table (pre-existing drift, unrelated to this change, addressed opportunistically in 5.2 below). Cross-change pass: zero requirement collisions with `smooth-scroll-wheel-momentum` (verified against disk, confirming D4), no declared ordering, no cross-change red→green pair.
- [x] 5.2 Archive + reconcile: `openspec archive`; correct the merged requirement text to reflect what actually shipped; reconcile the trackers (AGENTS.md Current-status row + snapshot, HISTORY.md narrative, `packer/README.md`'s expected-difference matrix row — flip from "root-caused, not yet fixed" to fixed, citing the new tests); verify against disk (`openspec list` / `ls openspec/changes/archive/` / `ls openspec/specs/`). ⟶ archive-ritual

  **Archived** as `2026-07-10-fix-scroll-reversal-redraw-corruption` (`openspec archive -y`; `terminal-session` +1 modified requirement, `verification-harness` +1 added requirement; merged diff verified — full established block pasted, no collapsed blank lines; `openspec validate --all --type spec` 24/0). Reconciled: `AGENTS.md` snapshot (dated 2026-07-10, test count 48→55 with attribution noted), the Shipped-and-archived Tooling row, the `smooth-scroll-wheel-momentum` open-changes row ("not yet fixed" → "fixed"), the `harden-test-precision-vs-claim` open-changes row added (a separate pre-existing drift the critic flagged, addressed opportunistically), `packer/README.md`'s matrix row (still-open→FIXED) + a new dated Acceptance blockquote, and `HISTORY.md`'s narrative appended under 2026-07-10. Verified against disk: `openspec list` shows 3 open changes matching the table (`harden-test-precision-vs-claim`, `smooth-scroll-wheel-momentum`, `add-ci-pipeline`); `ls openspec/changes/archive/` includes this change; `ls openspec/specs/` (21) matches AGENTS.md's Established-specs line. Transient validator VM instances deleted (`tart delete` ×5); golden images untouched.
