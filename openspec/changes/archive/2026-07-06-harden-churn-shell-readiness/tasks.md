# Tasks: harden-churn-shell-readiness

## 1. Dump liveness during modals (App, DEBUG-only)

- [x] 1.1 In `App/XttyApp.swift` (~line 194), construct the dump `Timer` explicitly and register it via `RunLoop.main.add(timer, forMode: .common)` (replacing `Timer.scheduledTimer`'s `.default`-mode-only registration), leaving the `#if DEBUG` + `-UITestGridDump` gating untouched
- [x] 1.2 Sanity-check no behavior change outside modals: build + run one existing dump-consuming XCUITest (e.g. `testBasicTypedEcho`) green *(done 2026-07-06: `testBasicTypedEcho` passed 5.664 s; the `.common`-mode timer change compiles and non-modal dump consumption is unregressed)*

## 2. Churn test — shell-readiness gate + asserted steps (test-only)

- [x] 2.1 In `AppUITests/XttyLifecycleCensusUITests.swift`, set `continueAfterFailure = false` and convert every discarded `_ = StateDumpReader.waitForState(...)` in both churn loops into hard assertions that name the loop and iteration on failure
- [x] 2.2 Pane loop: after the `paneCount == 2` wait, type `echo $((41000+i))` (unique per iteration, i = 1…4) and require `GridDumpReader.waitForContains` of the computed output token with `timeout: 15`, `ignoringLineWraps: true`; on timeout attach the grid dump, fail, and return without sending ⌘W
- [x] 2.3 Tab loop: same gate with `echo $((42000+i))` (i = 1…3) between the `tabCount == 2` wait and ⌘W
- [x] 2.4 Keep the final settle-and-assert census logic unchanged (the leak-net assertion itself is not modified)

## 3. Verification

- [x] 3.1 Run the churn test 5× consecutively locally (`xcodebuild test … -only-testing:xttyUITests/XttyLifecycleCensusUITests`) — expect 5/5 green against the recorded F/F/P baseline; if any run fails, treat it as the design's D6 escalation signal (do not add retries) *(done 2026-07-06: **5/5 green** (14.6–15.4 s each) vs the recorded F/F/P baseline; no retries added, D6 escalation did not fire — the readiness gate makes the leak-net deterministic locally)*
- [x] 3.2 ⟶ xtty-test-validator (full sweep) — full-suite acceptance (`make test`, bench e2e opt-in/skipped): **delegate to the validator** per AGENTS.md → test-validation (a product-code change gets the agent's full-sweep default — Tier 0+1 + headless ×2 + graphics — so the churn fix is exercised across local *and* the VM rigs where the per-launch race lives), and tick from the report's verbatim counts rather than running the suite inline (retrofit by `harden-validator-delegation-trigger`) *(done 2026-07-06: **autonomously delegated** to `xtty-test-validator` (Definition v4, fresh — stamp matched) — **VERDICT: IN-ENVELOPE**. Verbatim counts: Tier 0 core **232/0/0**; Tier 1 local **40/0/1** (skip = `testBenchmarkRunWritesReport`, bench-e2e gated); Tier 2 headless ×2 **39/1/1** + **39/1/1**; Tier 3 graphics **39/1/1** — every red is the sole benign residual `testMultiLinePasteIsNotAutoExecuted` (bash-3.2 no bracketed paste), zero unexplained reds, no menu-class red. **`testLifecycleChurnReturnsCensusToBaseline` GREEN in every tier** (Tier 1 14.9 s + 3 VM runs 14.56–14.95 s) — the readiness gate holds on the CI-parity rigs where the race lives. Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-06-harden-churn-shell-readiness/` (REVIEW.md + 4 `.xcresult` + logs).)*
- [x] 3.3 `openspec validate "harden-churn-shell-readiness"` passes *(done 2026-07-06: "Change 'harden-churn-shell-readiness' is valid")*
- [x] 3.5 **Archive-time reconciliation (three-way, inserted by `fix-main-menu-clobber`):** this change's `verification-harness` delta MODIFIES the shared "Deterministic content assertion channel" requirement, which `retire-metal-renderer` and `fix-main-menu-clobber` also MODIFY. Before `openspec archive`, re-paste the then-current established block from `openspec/specs/verification-harness/spec.md` into this change's delta and re-apply only this change's own edit (the modal-liveness dump sentence/scenario), so an earlier-archived sibling's merged text (e.g. `fix-main-menu-clobber`'s `mainMenuTitles`/`windowCount` sentence + two scenarios) is not silently reverted *(done 2026-07-06 during apply — safe to do now: both colliding siblings are already archived (`retire-metal-renderer` + `fix-main-menu-clobber` 2026-07-05), so the established block is final, and the only other open change (`harden-validator-delegation-trigger`) touches `test-validation`, not `verification-harness`. Re-pasted the current established "Deterministic content assertion channel" block — preserving retire-metal's single-CoreGraphics backend wording AND fix-main-menu-clobber's `main-menu top-level titles`/`terminal-window count` sentence + its two scenarios — and re-applied only this change's own edit (the modal-liveness sentence + "Dumps stay live while a modal panel is presented" scenario). `openspec validate` passes. Trivially re-verify at archive; nothing else will touch verification-harness before then.)*
- [x] 3.4 Document the expected CI readout in the change (commit message or PR note): churn stays red on CI until `fix-main-menu-clobber` lands (different root cause — pre-registered), so a red CI churn is not a regression of this change *(done 2026-07-06 — premise now resolved, documented corrected: `fix-main-menu-clobber` **already landed** (archived 2026-07-05), so the churn test's CI root cause (SwiftUI menu clobber) is fixed; this change fixes the other root cause (fresh-shell confirm-close race). The validator's CI-parity headless VM ×2 confirms `testLifecycleChurnReturnsCensusToBaseline` is **green** (39/1/1, sole red the unrelated bash-3.2 bracketed-paste residual). **Corrected expected CI readout:** churn is now expected **green** on CI; the only remaining `build-and-test` reds are the 2 pre-existing non-churn rig fragilities (find-bar marker-wrap on the runner's ~72-char prompt; multi-line paste on bash-3.2), per the AGENTS.md CI snapshot. Carried into the commit message.)*

## 4. Trackers

- [x] 4.1 Update `AGENTS.md` Current status / open-changes narrative and `research/04-design/02-milestones.md` (change implemented; test counts refreshed), and note the implementation result in `research/03-analysis/confirm-close-shell-readiness.md` as a dated addendum if anything diverged from the §5 design *(done 2026-07-06: AGENTS.md snapshot (churn race now "fixed", full-sweep-validated) + open-changes table row (implemented — pending archive, 12/12); milestones §16 phrase (implemented + validated, not "not yet applied"); research doc gained an "Implementation addendum — landed clean, no divergence" section (design shipped as D1–D5, §7 predictions held). **Test counts unchanged** — no test added/removed, still 232 core + 41 XCUITests, envelope 39/1/1. Also discharged the reverse-duty flagged by the validator: `packer/README.md` Acceptance + matrix row (b) updated from "not-yet-applied fix" to applied+validated.)*
