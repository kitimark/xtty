## Context

xtty embeds SwiftTerm (`external/SwiftTerm @ v1.13.0` + `patches/swiftterm/xtty-accessors.diff`). The shipped `fix-scroll-wheel-mouse-reporting` change rewrote `MacTerminalView.scrollWheel(with:)` into the priority-ordered 3-way branch (mouse-report → alt-screen cursor-keys → local scrollback), which was correct. But it added two anti-flood guards that regressed **trackpad/Magic-Mouse feel** — both **absent from pristine upstream SwiftTerm** (verified firsthand: `git -C external/SwiftTerm show HEAD:…MacTerminalView.swift` has **0** references to `momentumPhase`/`hasPreciseScrollingDeltas`) and implemented by **none** of five mature peers (iTerm2, Ghostty, kitty, WezTerm, Alacritty):

1. **Blanket momentum drop above the dispatch** — `if event.momentumPhase != [] { return }` at `MacTerminalView.swift:2194`, above BRANCH 1/2/3 (`:2201/:2226/:2243`). It discards **all** inertial-coast events for **all three branches**, including plain local scrollback (which has no child to flood), so scrolling stops dead on finger-lift — the native macOS coast is gone.
2. **Cap-and-discard accumulator** — `xttyWheelRowCount` does `whole = Int(remainder); remainder -= CGFloat(whole); return min(cap, whole)` (`cap = 5`): it subtracts the *full* `whole` but emits at most 5, so a fast gesture with `whole = 12` emits 5 and **7 rows vanish**.

Full root-cause, 5-peer consensus, the critic corrections, the fates table, and G(scroll-momentum): `research/03-analysis/scroll-momentum-smoothness-research.md` (fan-out `wf_2cf7d159-6ef`). This change is the **Targeted, knob-free** cut chosen for it (the fuller per-branch accumulator rework + a `scroll-multiplier` config knob are a deliberate deferral — see Non-Goals).

## Goals / Non-Goals

**Goals:**
- Restore the native macOS inertial coast to trackpad/precise-pointer scrolling on **every** branch (mouse-report, alt-screen cursor-keys, local scrollback).
- Stop discarding scroll distance on fast precise gestures (lossless remainder carry, no cap-discard).
- Bound flood by whole-cell quantization (peer-standard), not a fixed per-gesture cap.
- Make the momentum/precise nature of a routed event observable in the DEBUG dump for both the injection-based momentum coverage and the manual physical-trackpad verify.

**Non-Goals:**
- **No config knob** — no user-facing `scroll-multiplier`; keep the hardcoded constants (parallels the routing fix's "knob-free first cut"). Deferred follow-up.
- **No local-scrollback accumulator rework** — BRANCH 3 keeps its existing `calcScrollingVelocity` line-based logic; classic-wheel scrollback feel is unchanged (so the "classic-wheel regresses to 1-cell/notch" risk the research flagged for the *full* rework does not arise here — the precise accumulator change is confined to the program-directed branches). Deleting the guard alone restores BRANCH 3's coast because momentum's per-event `deltaY ≈ 0–1` → `calcScrollingVelocity` returns 1 (no flood).
- **No** iTerm2-style `_disableScrollReportingUntilMomentumEnds` late-report latch — recorded as a known deferred correctness risk (Risks).
- **No** horizontal wheel reporting (the `deltaY == 0` guard stays); **no** momentum-friction synthesis (macOS supplies the coast frames).
- **No** upstream SwiftTerm PR filed here (deferred to a maintainer action, as with the routing fix).

## Decisions

- **D1 — Delete the momentum guard entirely (do not narrow it per-branch).** Remove `if event.momentumPhase != [] { return }` at `:2194` so momentum frames flow into the normal branch dispatch. *Alternative — keep the drop but scope it to BRANCH 1 only (the flood-sensitive one)* — rejected: it still starves scrollback and alt-screen of the coast, and the flood on BRANCH 1 is already bounded by whole-cell quantization (D2), not by dropping. Peer-unanimous: 0/5 drop momentum on any branch.
- **D2 — Lossless remainder carry, no cap, for the precise program-directed path.** In `xttyWheelRowCount`'s `hasPreciseScrollingDeltas` branch, emit the full accumulated `whole` and subtract only what is emitted (carry the sub-cell fraction) — drop the `min(cap, whole)`. Flood is bounded structurally: at most ~1 report/key per whole cell traversed. *Alternative — keep a cap but carry the overflow instead of discarding it* — a valid safety valve, but during momentum `whole` per event is ~0–1 so a cap never triggers; the cap only ever bit a single pathological finger-down event, and no peer keeps one. Omit it for simplicity; revisit only if the physical-flick verify (Risks) shows a pathological single-event burst.
- **D3 — Reset the remainder on the user-driven `.began` phase.** `xttyWheelRowRemainder` is a persistent stored property; reset it to 0 when `event.phase.contains(.began)` so a stale sub-cell fraction cannot leak across two separate flicks as a phantom first-line jump. *Alternative — also reset on direction reversal (WezTerm) or a staleness timer* — deferred polish; `.began` is the minimal-correct trigger. (Alacritty's momentum-Began→Started reset does not transfer: winit synthesizes it; xtty reads raw `NSEvent`, so we key on `.phase` directly.)
- **D4 — The non-precise (classic wheel) program-directed path is unchanged in spirit.** Keep `max(1, Int(abs(event.deltaY).rounded()))` for discrete notches (a classic wheel emits no momentum frames and no sub-cell deltas). The `cap` is dropped here too for consistency, but a discrete notch's `deltaY` is small so behavior is effectively identical. Classic-wheel *scrollback* (BRANCH 3) is untouched (Non-Goals).
- **D5 — "Multiplier→1×" is a no-op under knob-free.** The peers' mouse-report flood bound is "neutralize the user scroll multiplier to 1× under mouse-tracking." xtty has no scroll multiplier (D-knob-free), so the accumulator already emits ~1 report per cell — the 1× behavior is the default. No multiplier code is added; if the deferred config knob lands, it MUST neutralize to 1× on BRANCH 1.
- **D6 — Observability: add `momentum` + `precise` to `XttyWheelRouting`.** Extend `public struct XttyWheelRouting` (`XttyAccessors.swift`) with `let momentum: Bool` (the event was an inertial-coast frame) and `let precise: Bool` (`hasPreciseScrollingDeltas`); set them at each of the three `xttyLastWheelRouting = …` assignments; surface them through the App-layer DEBUG state dump's wheel-routing field. The automation-channel gesture always sees `momentum == false` (synthesized gestures carry no coast) → a crisp-negative automated assertion on the **real-gesture** path; `momentum == true` is asserted by the D8 injection hook, and the manual physical-trackpad verify closes only the OS-delivery/rate/back-pressure. *Alternative — no harness field, acceptance purely by `cat -v` byte capture (the fix-osc7 precedent)* — rejected: a wired dump field makes both the injection coverage and the manual verify rigorous (classifies each frame), matching the repo's "assert per observed capability" posture.
- **D8 — Automate the momentum routing *logic* via a DEBUG synthetic-injection hook (not the automation channel, not posting).** Add a `#if DEBUG` + `-UITestGridDump`-gated trigger following the existing XttyApp XCUITest-hook family (`App/XttyApp.swift` already routes synthetic link-open / spatial-op / git-select / git-open / block-select specs through the real path). The test writes a wheel-event spec (momentum phase, precise flag, vertical delta, modifiers); the app constructs a faithful wheel `NSEvent` via `CGEvent(scrollWheelEvent2Source:…)` → `NSEvent(cgEvent:)` and drives it through the **real** `scrollWheel(with:)` on the focused pane's terminal view; the routing dump records the outcome. **Feasibility verified by effect (spike):** `NSEvent(cgEvent:)` faithfully bridges the three fields the router reads — `momentumPhase` (CG none/begin/continue/end → NSEvent raw `0`/`.began`/`.changed`/`.ended`), `hasPreciseScrollingDeltas` (from `kCGScrollWheelEventIsContinuous`), and `scrollingDeltaY` — so a constructed momentum-CONTINUE event has `momentumPhase == .changed` (non-empty), exactly what the deleted guard tested. *Alternatives rejected:* the **automation channel** (`scroll(byDeltaX:)`) carries no momentum (it can only prove the crisp negative); **posting a raw CGEvent** to the HID tap is dropped in the runner (`CGPreflightPostEventAccess == false` — the prior learned refutation, which is about *posting*, not *constructing*); a **hook that bypasses `NSEvent` and calls a reimplemented branch** would not exercise the real `event.momentumPhase` read (low fidelity — the injection MUST go through a constructed `NSEvent`). **Scope of what it proves:** the routing/accumulator *logic given a momentum event* — momentum-not-dropped, lossless-carry count, `.began` reset, **and** (added after the human manual verify below succeeded and asked for the automatable slice of its own technique to be captured as a regression guard) that the routed decision produces **real SGR mouse-report bytes a child process actually receives** — `testInjectedMomentumFrameProducesRealSGRMouseReportBytes` arms `printf '\033[?1002h\033[?1006h'; cat -v` (the exact manual-verify technique: `cat -v` renders the child-received `ESC` byte as literal `^[` text instead of xtty's own VT parser silently consuming an unrecognized incoming CSI sequence) and asserts the grid dump shows the real `^[[<64;…M` wheel-up SGR press. This is a different assertion channel than the other three (the visible grid/byte stream, not the `lastWheelRouting` DEBUG-dump field), closing the gap between "xtty's bookkeeping says reported" and "the byte stream a real unmodified program receives is actually correct." It does **not** prove the OS *delivers* such events during a real coast, the coast reports/sec, or htop back-pressure (those stay the manual verify — Risks). *Fidelity caveat:* a constructed `NSEvent` is not byte-identical to an OS-delivered one, but it populates exactly the fields under test; the two coverages are complementary (injection = "given a momentum event, xtty does the right thing, bytes and all"; manual flick = "macOS hands xtty those events").
- **D7 — Same patch-hunk delivery + deferred upstream.** Revise the existing `scrollWheel`/`xttyWheelRowCount` hunk in `patches/swiftterm/xtty-accessors.diff`; `scripts/bootstrap-swiftterm.sh` reapplies. The upstream PR (the pure branch logic + this momentum/accumulation correction) stays deferred to a maintainer action, extending the routing fix's retire-on-merge plan.
- **D9 — A real, unmodified, off-the-shelf program as an additional end-to-end coverage channel (added after the human manual verify's step 2 succeeded and the user asked for a native-command alternative to htop to be captured as a test).** Every other test in this suite arms mouse mode via a `printf`-simulated escape sequence (`\033[?1002h…`) — a faithful simulation of what a real program requests, but still a simulation. `testWheelScrollsRealMouseTrackingPager` instead drives `seq 1 500 | less --mouse` (native `/usr/bin/less` 668, no Homebrew dependency) — a real off-the-shelf program that negotiates its **own** mouse tracking — through XCUITest's real automation-channel gestures (not the D8 injection hook; this test needs no momentum, only a genuine routing + redraw round-trip), asserting **both** the DEBUG dump's `branch == "report"` and that the program's own visible content genuinely advances (a grid-content diff). This is a materially different coverage channel than D8 (constructed-event injection) or the byte-echo proxy (`cat -v`, D8's SGR-byte test): it is the only scenario in the suite where neither the mouse-tracking request nor the on-screen reaction is xtty-side simulated. **Why `less --mouse` over `htop`:** native (`/usr/bin/less`, ships with macOS) vs. a Homebrew dependency the VM goldens and CI runners are not guaranteed to carry; `htop` remains available as a heavier, closer-to-the-original-bug-report manual-verify option (task 4.4) but is not suited to an automated dependency. **Feasibility verified by effect (two spikes):** (1) a real scroll over `less --mouse` produced `lastWheelRouting.branch == "report"`, confirming it requests an included mouse mode (`.vt200`/`.buttonEventTracking`/`.anyEvent`), not the excluded X10-only mode BRANCH 1's gate (`sendButtonPress()`) rejects — independently re-confirmed via `strings /usr/bin/less | grep '1000h\|1006h'`, which found both `?1000h` (VT200 tracking) and `?1006h` (SGR extended coordinates) requested by the binary; (2) a single real wheel tick moved `less`'s viewport by exactly one line locally, but since a single XCUITest-synthesized gesture is flagged **precise** (`hasPreciseScrollingDeltas == true`) and its accumulated delta doesn't reliably cross one whole accumulator cell on every launch, the test loops up to 15 ticks, giving each a real settle window (not a single synchronous read) rather than assuming one tick suffices. **VM-environment verified, not assumed:** `less 668` with `--mouse` support was confirmed present and identical on both VM goldens (`xtty-test:26.5` bash, `xtty-test-zsh:26.5` zsh) via a direct SSH check into resumed stopped clones, and `TERM=xterm-256color` is hardcoded at the product level (`ShellResolver`), not shell- or environment-derived, so `less`'s mouse-mode negotiation is not exposed to a TERM/terminfo divergence risk between local and VM. **Read-freshness hardening:** an early version used a single synchronous `GridDumpReader.read()` per loop tick with no settle wait — the adversarial review (independently, from two lenses) flagged this as the same read-vs-150ms-async-dump-write-timer race class that hit `testAccumulatedRemainderResetsBetweenGestures` on the graphics VM tier (see Risks below and `packer/README.md`'s "Graphics-tier read-freshness race" section); fixed by polling each tick for up to 300ms before concluding no change occurred. *Alternative rejected:* reuse the byte-echo (`cat -v`) technique instead of a real program — rejected because it only proves bytes reach *a* child process, not that a genuine, unmodified, mouse-aware program correctly interprets and visibly acts on them; the two are complementary, not redundant.

## Maintainer choices settled (from the research §3–§4; recorded here, not re-litigated)

| # | Choice | Peers diverge | Settled |
|---|---|---|---|
| Scope | Targeted vs full peer-alignment | — | **Targeted** (user) — guard delete + cap-fix on the precise program branches; BRANCH 3 accumulator rework deferred |
| Config | knob-free vs `scroll-multiplier` | all peers expose ≥1 | **Knob-free** (user) — hardcoded constants; config deferred |
| Mouse-report bound | 1× / full / magnitude-less | kitty·Alacritty 1× · Ghostty·iTerm2 full · WezTerm 1/event | **1× (moot under knob-free)** — accumulator already ~1 report/cell (D5) |
| Remainder reset | `.began` / reversal / timer | iTerm2 `.began` · WezTerm reversal+250 ms | **`.began`** (D3) |
| Pixels-per-line | cell height / 15 / raw-fast | most: cell height | **Cell height** — already `cellDimension.height` |

## Risks / Trade-offs

- **Momentum-flood on BRANCH 1 is peer-analogy, not measured** → a hard trackpad flick emits ~1 report/cell over the whole coast (~1000–2000 reports/1.5 s for a multi-screen flick); the peers accept this as smooth because each report is cheap and the child coalesces its own redraws. **Mitigation:** the physical-trackpad verify (below) measures the actual reports/sec and confirms htop stays interactive (no pty-write / redraw back-pressure); the whole-cell quantization (D2) is the same structural bound the peers rely on. If the verify shows a pathological burst, the fallback is D2's carry-with-cap safety valve and/or the D-deferred latch.
- **iTerm2's late-report latch is deferred** → the one momentum-specific correctness safeguard the most-mature peer kept ("fingers lifted, region changed, stop firing into a now-inappropriate target"). **Mitigation:** recorded as a known deferred correctness risk; xtty's coordinate is recomputed per event via `calculateMouseHit`, and 4/5 peers ship without it — add it only if a real misfire is observed.
- **The momentum routing *logic* is automatable; only the OS coast *delivery* is not.** The automation channel (`scroll(byDeltaX:)`) carries `momentumPhase == none`, so it can only prove the crisp negative — but the D8 injection hook drives a **constructed** momentum `NSEvent` through the real router, so momentum-not-dropped + lossless-carry + `.began`-reset **are** automated regression guards. **Un-automatable and left to the manual physical-trackpad verify:** whether macOS actually *delivers* populated momentum frames into xtty's precise path during a real coast, the coast reports/sec, and htop consume-side responsiveness (back-pressure) — consistent with the original forensics' deferred physical-mouse ❓. The two are complementary, not redundant (D8).
- **Removing the guard changes no synthetic-test behavior** → synthetic gestures never hit the (momentum-only) guard, and the 3 routing branches are untouched, so the existing 48 `XttyMouseWheelUITests` routing/monotonicity assertions + the full VM routing matrix stay green unchanged under the guard removal (the suite grows only by the added momentum-injection tests 3.2–3.4, and `packer/README.md`'s envelope is updated per task 4.2). Low regression risk; confirmed by the Tier-1/VM validator run.
- **D9's real-pager test's VM-tier behavior — RESOLVED, measured clean.** Task 4.6 measured `testWheelScrollsRealMouseTrackingPager` green on all 5 environments including both graphics-zsh samples; the `after == nil` discriminator never fired, so the read-freshness hardening (a 300ms settle poll per tick) held under real VM conditions, including the graphics tier where the precedent race (`testAccumulatedRemainderResetsBetweenGestures`) had previously surfaced. The task-4.6 sweep as a whole still came back OUT-OF-ENVELOPE, but from an unrelated cause: a graphics-zsh-only XCUITest runner-level `typeText()` stall (~930s, in an unrelated test) that starved a short fixed timeout on a *different* wheel test each of 2 samples — not this test, not the routing/read-freshness logic D9 concerns. Tracked as a new, separate, open anomaly in `packer/README.md`'s expected-difference matrix ("XCUITest runner-level event-delivery stall (graphics-zsh)"), not a recurrence of this risk.

## Migration Plan

1. Revise the `scrollWheel(with:)` + `xttyWheelRowCount(for:)` hunk and extend `XttyWheelRouting` in `patches/swiftterm/xtty-accessors.diff`; `scripts/bootstrap-swiftterm.sh` reapplies on next bootstrap.
2. Surface `momentum`/`precise` in the App-layer DEBUG wheel-routing dump field; add the DEBUG synthetic-wheel-event injection hook (D8) following the existing XttyApp trigger family; add the crisp-negative + the three injection-based momentum XCUITests.
3. Verify: `make test-core`; **update `packer/README.md`'s acceptance envelope for the grown test count** (the injection tests add methods — 48 → the new total) *before* the validator runs; the Tier-1 XCUITest suite + full VM matrix via the validator (regression on the routing branches unchanged, the new momentum-injection tests green); then the **manual physical-trackpad verify** (the shipped build — flick under htop, confirm coast reports keep arriving after finger-lift + no lost distance + no back-pressure, xtty-vs-iTerm2 A/B — closes the OS-delivery/rate part D8 cannot). D9's real-pager test (added mid-flight, after the manual verify's step 2 succeeded) needs its own follow-up VM full-matrix re-run (task 4.6), separate from and after the byte-level test's own re-run (task 4.5) — each addition gets the VM matrix re-measured before the change is considered fully verified.
4. File the upstream SwiftTerm PR — **deferred to a maintainer action**.
- **Rollback:** revert the patch-hunk revision + the two dump booleans; `bootstrap` restores the prior (routing-fix) `scrollWheel`.

## Open Questions

- **Physical-flick measurement (human-run, closes the residual ❓) — RESOLVED, task 4.4 complete.** All 5 sub-checks confirmed by the user with live DEBUG-dump/routing-log corroboration: real momentum frames flow into xtty's precise path during coast (`momentum:true`, `branch:"report"` sustained through the gesture), a hard flick covers proportionally more distance in less time with no cap-truncation, htop stays interactive and responsive through the coast, local scrollback and the alt-screen pager both coast naturally (including under a direction reversal, with no redraw corruption on that path), and xtty feels comparable to a real iTerm2 A/B on the same flick. See `tasks.md` 4.4 for the full outcome note.
- **Does any pathological single event exceed a sane per-event burst?** Not observed — the physical-flick verify (task 4.4) saw counts scale naturally with gesture intensity (no fixed ceiling), so D2's carry-with-cap safety valve was not needed.
