## 1. Source the local-host name set without a network lookup

- [x] 1.1 Add a view-free pure helper to `XttyCore` — `LocalHost.names(from rawHostName: String) -> Set<String>` — reproducing today's derivation (`{"", "localhost", name.lowercased(), the short form up to the first "."}`) from a **given** host-name string, with no syscall or network I/O of its own (design D2). *(Added `XttyCore/Sources/XttyCore/LocalHost.swift` — `enum LocalHost.names(from:)`, pure, mirrors the prior `{"", "localhost", host.lowercased(), shortForm}` derivation.)*
- [x] 1.2 Rewire `PaneController.localHostNames` (`App/PaneController.swift:92`) to read the host name once via `gethostname(2)` and pass it to `LocalHost.names(from:)`, **removing** the `ProcessInfo.processInfo.hostName` reverse-DNS call (`:94`, design D1). Leave the sole consumer (`:265` → `OSC7.decode(_:localHostNames:)`) unchanged. *(`localHostNames = LocalHost.names(from: systemHostName())`; new `private static func systemHostName()` calls `gethostname(2)` into an `NI_MAXHOST` buffer, returns `""` on failure. `ProcessInfo.processInfo.hostName` removed — `grep` confirms zero remaining uses.)*
- [x] 1.3 (verify, inline) `make test-core` — a new `LocalHost.names(from:)` unit test passes (covers the "Local-name derivation runs without the app" scenario), and the 20 existing `OSC7Tests` are unchanged (they inject their own set and cannot see the swap). *(237/0 — new `LocalHostTests` (5 methods incl. an OSC7-decode integration assertion) all pass; the 14 `OSC7Tests` methods unchanged and green.)*

## 2. Verify the reverse-DNS trigger is gone (by-effect)

- [x] 2.1 (verify — by-effect, VM graphics tier) On a **fresh** `xtty-test-zsh:26.5` clone (G3 — the LN arbiter remembers per-bundle-id, so a clean clone rules out a stale NE-store record) running the **fixed** xtty build in a **graphics** session (WindowServer live, so the modal *would* appear if the trigger survived), capture the forensics **P1** `log stream` and confirm **P2** reads **0 / 0 / 0** — no reverse-DNS PTR volley (`client pid … (xtty)`), no `policy 'pending'`, no "find devices on local networks?" modal — **and** that no `com.xtty.app` record is created in the NE store (P5 — the query never happens) **and** that the first prompt does **not** freeze the UI (design D3; recipe `research/03-analysis/local-network-privacy-forensics.md` §8g). This is the **red→green complement** to `add-zsh-test-image` task 4.1 (same graphics zsh rig, the *unfixed* build → 40 `pending` + modal). Keep the `log stream` + NE-store dump + a graphics screenshot as the review bundle. ⟶ xtty-test-validator (graphics VM, fresh zsh clone — LN-gate by-effect, expect 0/0/0; **not** a full-envelope run) *(GREEN — validator `Definition v4`, VERDICT IN-ENVELOPE. Fresh graphics clone `xtty-osc7-lncap-0708` of `xtty-test-zsh:26.5` (3 vCPU), fixed build HEAD `4ebe0cb`: **P2a reverse-DNS volley=0, P2b modal=0, P2c `pending`=0**, mDNSResponder xtty-absent (0 of 453), no on-screen modal, no first-prompt freeze. **P5 `com.xtty.app`=1 but benign** — a pre-existing `UUID cache hit` on a golden already used to build xtty (default-deny enumeration alongside com.tcltk.wish/com.apple.TV; P2c=0 corroborates), NOT the LN gate — decisive discriminators are P2a/b/c + mDNS-absence, all clean. GREEN complement of `add-zsh-test-image` 4.1. Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-08-fix-osc7-hostname-lncapture/`; captured in `local-network-privacy-forensics.md` §9 + the benign-P5 golden-hygiene caveat (§9b, G3 corollary).)*

## 3. Confirm no regression (acceptance)

- [x] 3.1 (verify — full sweep, product-code change) Run the acceptance sweep and confirm the documented envelope is unchanged: **local bare-metal `make test`** on the zsh dev host (where the swapped path actually executes — the file-link relative-resolve, git-review non-repo empty-state, and live-cwd tests must stay green) **plus** the VM rigs (≥2 headless + graphics; the bash rig's `40/1/1` is untouched by construction since bash emits no OSC 7). ⟶ xtty-test-validator (full sweep — local bare-metal + bash `xtty-test:26.5`, product-code change) *(IN-ENVELOPE — validator `Definition v4`. **Tier 0 core 237/0/0** (the +5 = new `LocalHostTests`); **Tier 1 local zsh host 41/0/1** `** TEST SUCCEEDED **` — every host-name/cwd-touching test green (`testRelativeFileLinkResolvesAgainstCwd`, `testNonRepositoryShowsEmptyState`, `testNonPermittedSchemeIsBlocked`) where the swapped `gethostname` path actually executes; **Tier 2 headless ×2 = 40/1/1, Tier 3 graphics = 40/1/1**, identical failing set. Sole VM red in all 3 = `testMultiLinePasteIsNotAutoExecuted` → documented benign **bash-3.2 bracketed-paste** bucket, unrelated to the swap. Bash envelope held **unchanged** `40/1/1`-of-42 across all three runs (bash emits no OSC 7 → swapped path inert there, as designed); no new reds attributable to the fix; find-bar green every VM run. Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-08-fix-osc7-hostname-reverse-dns/` (run{1,2}-headless + run3-graphics `.xcresult`, per-tier logs, `REVIEW.md`). Reconcile duty for 4.3: AGENTS.md snapshot "232 XttyCore unit" → **237**; `packer/README.md` XCUITest envelope `40/1/1` unaffected (no XCUITests added).)*

## 4. Reverse duty + on completion

- [x] 4.1 Update `research/03-analysis/local-network-privacy-forensics.md` (§6 residual + §8) and its `research/README.md` line: flip the product `gethostname` fix from "proposed / owner-dropped" to **landed in `fix-osc7-hostname-reverse-dns`**, and record the §8g by-effect verification result from task 2.1 (append a dated note; do not rewrite the prior addenda). *(Done: **§9** dated 2026-07-08 addendum appended (§8 untouched) — §9a records the 2.1 by-effect GREEN result (the executed half of §8g's product-fix re-verify line), §9b/§9d/§9e/§9f add the G3 corollary + fate T11 + guideline G12; **§6 residual flipped ❓→✅ RESOLVED** and **§1 fix-classes note flipped proposed/dropped→implemented**, both forward-pointing to §9; `research/README.md` index line refreshed. Also `packer/README.md` Runtime workflow gained the host-side-capture convention (§9c). §9 capture committed `55597c0`; the §1/§6 flip + these ticks are uncommitted.)*
- [x] 4.2 (verify — coherence, pre-archive) Review the change for coherence against AGENTS.md's rulebook + disk state. ⟶ xtty-openspec-critic (fix-osc7-hostname-reverse-dns) *(COHERENT — critic `Definition v1 (2026-07-07)`, no BLOCKER. Spec-delta syntax clean (1 `## MODIFIED`, 6 four-hashtag scenarios, SHALL throughout); proposal↔specs contract holds both ways (`terminal-session` ↔ Modified Capabilities); **MODIFIED pastes the entire established block** (4 original scenarios verbatim + 2 new, superset body); delegation markers correct; requirement body mechanism-neutral; design↔requirements traceable both ways; established specs 19/19 == disk; active changes 5/5 == Current-status table; red→green pair with `add-zsh-test-image` 4.1 intact (both cite each other). Two informational REVIEW items, both pre-accounted: (a) no `verification-harness` delta — deliberate per D3 (the novel property is the *absence* of a network call, invisible to the DEBUG dump; behavior-preserving, acceptance by-effect on 2.1); (b) AGENTS.md "232 XttyCore unit" → 237 snapshot lag = this task 4.3's job. Recommendation: proceed to 4.3 reconcile / archive.)*
- [x] 4.3 On completion, reconcile trackers per AGENTS.md "Keep progress current": add/refresh this change's Current-status row + snapshot in `AGENTS.md` (note it removes the shipping zsh LN trigger and **enables** `add-zsh-test-image` to drop its refuted rig-level LN machinery), append the dated narrative to `HISTORY.md`, add a one-liner to **Learned refutations** if warranted, and verify against disk (`openspec list`, `ls openspec/changes/archive/`, `ls openspec/specs/`). *(Done: AGENTS.md snapshot bumped 2026-07-08 (**232→237** core, post-ref → `fix-osc7`) + tail flipped "proposed"→"landed" with the 0/0/0 by-effect result; **fix-osc7 row removed** from the Open-changes table and its narrative appended to the Shipped-and-archived **Tooling** row; `add-zsh-test-image` row updated "active fix-osc7"→"landed & archived"; **Learned-refutations** LN line got a dated landed clause. HISTORY.md: full dated narrative appended (append-only, newest-first). `milestones.md` unchanged — a P-tooling product fix advances no build-plan phase (consistent with the sibling harden-* changes). Archive + post-archive spec-merge verification driven via the `/openspec-archive-change` skill; verified against disk after.)*
