## 0. Precondition (apply-ordering gate)

- [x] 0.1 Confirm `add-zsh-test-image` is archived and its divergence measured (bash-rig vacuous/red vs zsh-rig real-green) and that `harden-paste-wrap-assertion` has landed (wrap-tolerant zsh paste `:82`/`:84`) — this change's per-shell assertions + per-test assert/skip decisions are grounded in that measurement; if `harden-paste-wrap-assertion` has not landed, fold its wrap-tolerance into task 2. Recommended order: `harden-paste-wrap-assertion` → this change.

## 1. Harness: expose bracketed-paste mode (verification-harness delta)

- [x] 1.1 Add the focused pane's `bracketedPasteMode` to the DEBUG state dump (`App/UITestDump.swift` + the `writeStateDump` callers), read from `terminal.bracketedPasteMode` (SwiftTerm `public private(set)`), `#if DEBUG` + `-UITestGridDump`-gated, observe-only.
- [x] 1.2 (verify, inline) One-off `-UITestGridDump` launches confirm the field reads correctly after the first prompt — false under bash 3.2, true under zsh. *(cheap iterate — inline)* — verified on this zsh machine: `bracketedPasteMode = true`, key present in the dump.

## 2. Parameterize the multi-line-paste test per shell (test-only)

- [x] 2.1 Rename `testMultiLinePasteIsNotAutoExecuted` (`XttyUITests.swift:62`) to a capability-neutral name (e.g. `testMultiLinePasteMatchesShellBracketing`) and branch on the focused pane's **observed** `bracketedPasteMode` (from 1.1), read from the state dump **after** the computed-marker shell-readiness gate (`harden-churn-shell-readiness`) — never before the first prompt (design D5).
- [x] 2.2 zsh / bracketed-on arm: assert both pasted lines are present in the grid (keep the wrap-tolerant matcher from `harden-paste-wrap-assertion`) and that `"command not found"` is **absent** (staged, not executed).
- [x] 2.3 bash 3.2 / bracketed-off arm: assert the newline-terminated first line was **executed** (`"command not found"` present) while the unterminated tail line remains **staged** — xtty's faithful forwarding (design D2). Assert exactly one execution (no second `command not found` for the tail).
- [x] 2.4 (verify, inline) One-off/focused run confirms the branch selects correctly and each arm asserts. *(cheap iterate — inline)* — focused `testMultiLinePasteMatchesShellBracketing` passed on this zsh machine (attachment `paste-bracketed-mode-true`; the bracketed arm asserted both lines staged / no execution). The bash-off arm runs on the bash VM rig in the delegated matrix (6.1).

## 3. Parameterize the semantic-capture family (test-only)

- [x] 3.1 For `XttySemanticCaptureUITests`, `XttySessionSidebarUITests`, `XttyBlockSidebarUITests`, `XttyGitReviewUITests`, `XttySpatialBlocksUITests`, `XttyFileLinkOpenUITests`: when `waitForSemanticCaptureActive()` is true, assert the real behavior (blocks form; sidebar populates). When false, assert a **crisp capability-absent negative** where one is meaningful and cheap (e.g. no OSC 133 command boundaries / the semantic sidebar stays empty); only where no meaningful assertion exists, replace the silent `guard … else { return }` with `throw XCTSkip(…)`. Decide each method's arm from `add-zsh-test-image`'s measured divergence (which produced a `"…capture inactive…"` attachment) — design D4/Q2. **Outcome:** every vacuous `guard waitForCaptureActive(...) else { attach; return }` (all 12 across the 6 files, plus the file-link OSC-7-cwd guard) now calls the shared `assertSemanticCaptureInactive(...)` helper — a crisp negative (no semantic action + no command boundaries on a non-injecting shell) exists for **all** of them, so **zero last-resort `XCTSkip`s** were needed. `testFullScreenAppDoesNotBecomeACommandBlock`, `testNonPermittedSchemeIsBlocked`, and `testConfiguredTreeLayoutIsReported` were already shell-agnostic (no capture guard) and are untouched.
- [x] 3.2 The i18n-paste half of `testTruecolorEmojiAndWideChars` (delivers emoji/CJK via Cmd+V) parameterizes like the paste test (2.x); keep its shell-agnostic ASCII-color assertion asserting on every shell.

## 4. Validator: make the zsh rig a standing environment (test-validation delta)

- [x] 4.1 Update `.claude/agents/xtty-test-validator.md` so a full sweep runs the whole suite on the zsh golden (capability-present arm asserts; bash rig runs the same suite on the capability-absent arm), and update the enumerated environments; **bump the `Definition:` stamp** and update the `/xtty:validate` launcher's stamp check (agent-definition edits reach spawns with unpredictable lag — verify by report stamp, never assume delivery).

## 5. Reverse duty: reconcile the runtime docs (same change)

- [x] 5.1 Update `packer/README.md` Acceptance/matrix: the paste test's **bash execution arm** is now an **asserted** bash behavior (not a red, not a skip); the semantic family is asserted-or-last-resort-skip; record both rigs' full-suite envelopes (no per-plan split).
- [x] 5.2 Update `research/03-analysis/github-actions-ci-cd.md` §19b: `bash32-no-bracketed-paste` is now an **asserted bash execution arm** on the bash rig / hosted CI (not a red), covered on both arms.
- [x] 5.3 (verify, inline) `openspec validate split-shell-dependent-testplan` passes; the proposal↔specs capability contract holds (verification-harness + test-validation). *(cheap — inline)* — `openspec validate` returned "is valid" (exit 0); proposal Modified Capabilities = {verification-harness, test-validation} matches the two spec-delta dirs.

## 6. Validate (acceptance)

- [x] 6.1 (verify — full acceptance matrix, both rigs) Run the matrix on both goldens: bash rig (shell-dependent tests assert their bash arm, or last-resort-skip) and zsh rig (assert their zsh arm); confirm **no vacuous passes remain** (no `"…capture inactive…"` attachment coincides with a "passed"), no product red, and the paste test green on **both** arms. ⟶ xtty-test-validator (full matrix, both goldens — bash `xtty-test:26.5` + zsh `xtty-test-zsh:26.5`) — **VERDICT: IN-ENVELOPE** (xtty-test-validator; served stale v4 def but two-golden scope supplied explicitly, so coverage complete). Measured: Tier 0 `237/0/0`; Tier 1 local (zsh) `41/0/1`; Tier 2 headless bash ×2 `41/0/1`+`41/0/1`; Tier 2 headless zsh `41/0/1`; Tier 3 graphics zsh `41/0/1`. **Vacuous-pass audit PASSED: 0 `capture inactive` strings on both goldens** (bash ran 16 `semantic-capture-absent-arm-asserted` crisp negatives; zsh ran the active arm, 0 absent-arm attachments). Paste green on both arms (`bracketed-mode-false`→first line executed once/tail staged; `bracketed-mode-true`→both staged). Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-08-split-shell-dependent-testplan-fullmatrix/`.
- [x] 6.2 (verify — coherence, pre-archive) Review the change for coherence against AGENTS.md's rulebook + disk state, including the sibling `harden-paste-wrap-assertion` interaction (the paste-test rename supersedes its `:82`/`:84` edits) and the archive ordering. ⟶ xtty-openspec-critic (split-shell-dependent-testplan) — **VERDICT: COHERENT, proceed** (xtty-openspec-critic, Definition v1). Contract/spec-delta/harness-coupling/delegation-markers all pass; MODIFIED test-validation is a strict superset of the established block; archive-after-`harden-paste-wrap-assertion` satisfied (already archived, no live red→green pair); no cross-change collision with add-ci-pipeline. Sole finding was a non-blocking "may lack"→"might lack" wording nit — **fixed** (re-validated green).

## 7. On completion (post-verify)

- [x] 7.1 Reconcile trackers per AGENTS.md "Keep progress current": Current-status table row + snapshot (the shell-dependent tests are parameterized per shell; both rigs' full-suite envelopes), append the narrative to `HISTORY.md`, advance `research/04-design/02-milestones.md` if applicable, and flip `research/03-analysis/shell-dependent-test-partitioning.md` from "decided" to "built + parameterized" (note the pivot away from the test-plan partition). Confirm this change archives **after** `harden-paste-wrap-assertion`. — Done: HISTORY.md narrative appended; research doc Status + fates table + "decided strategy" §2 flipped to "built + parameterized" (partition ⊘ superseded → parameterize-and-assert ✅ shipped); research/README.md line updated; milestones has no reference (P-tooling, no advance). AGENTS.md row-move + snapshot + Learned-refutation done in the archive-ritual finalize below. `harden-paste-wrap-assertion` already archived (2026-07-08) → ordering satisfied.
- [x] 7.2 Archive + reconcile: run the full ritual — `openspec archive split-shell-dependent-testplan` (merge the `verification-harness` + `test-validation` deltas), finish-by-hand (correct the merged text to what shipped, `openspec validate --all --type spec`), confirm the trackers reconciled (7.1) + the archive ordering, and verify against disk (`openspec list`, `ls openspec/changes/archive/`, `ls openspec/specs/`). ⟶ archive-ritual — Done: see the archive-ritual run below.
