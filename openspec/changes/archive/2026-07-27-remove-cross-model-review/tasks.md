## 1. Remove the worker and the mechanical gate

- [x] 1.1 Delete `.claude/commands/xtty/cross-review.md` (the launcher; confirmed wholly cross-review content, no partial edit needed)
- [x] 1.2 Delete `scripts/cross-review-digest.sh`, `scripts/cross-review-scope.sh`, `scripts/test-cross-review-scripts.sh`
- [x] 1.3 Edit `.claude/agents/xtty-openspec-critic.md`: drop the frontmatter description's "+ cross-review-gate-task presence" clause; delete the Pass-1 "Cross-review-gate-task presence (heuristic → REVIEW)" bullet entirely; reword the Pass-4 exemption's parenthetical (currently "that class stays a cross-model catch, never flagged here") to state plainly that no automated check catches new-symbol misplacement — a known, accepted gap, not a covered one; bump the `Definition version` stamp and its date

## 2. Retire the specs

- [x] 2.1 Confirm this change's own `specs/cross-model-review/spec.md` (REMOVED, all 5 requirements) and `specs/coherence-review/spec.md` (MODIFIED, dropping the cross-review-gate-task clause + scenario, adding the new-symbol-scope scenario) are complete and `openspec validate remove-cross-model-review` passes

## 3. Retire the documentation

- [x] 3.1 Remove from `AGENTS.md`: the "Cross-model design review + the human-attestation archive gate" section (header + body), the pre-archive step-0 rule in the OpenSpec workflow list, the "Not in the table (deliberately): `/xtty:cross-review`" callout, the tracked-tooling paragraph's clauses naming the launcher command and the two scripts, and the Tooling row's cross-model-review clause (Pass B briefing/A', Pass C→Fable 5 pin, `--line` convenience) in the "Shipped and archived" table; also in that same Tooling row, drop the "+ gate-task presence" clause from the `xtty-openspec-critic` mention (it currently reads "incl. **v4** Pass-4 code-accuracy + gate-task presence" — that clause goes false once task 1.3 deletes the check), leaving just the Pass-4 code-accuracy mention. **Deviation:** the snapshot's "Latest change" line is left as `remove-guide-gate` for now and updated at archive time (task 4.3) instead of here — updating it now would misstate this change as the latest archived change before it actually is one; this matches the two-stage pattern `remove-guide-gate` itself used (its own documentation-retirement task 2.2 deferred the actual snapshot header rewrite to its final archive task 3.4).
- [x] 3.2 Reword the Learned-refutations bullets that describe this mechanism (the self-certification refutation, the tar-pit non-termination result, the "brief the read-only pass" finding, the "second model earns its cost by running falsifiers" finding, the "past the review cliff" finding, the "no executable archive gate" finding, the "attestation fragile to any commit" finding) to a past-tense, retired-aware form — keep the still-true underlying measurement, point any now-dead artifact reference to `HISTORY.md` instead; add one new bullet recording that retiring the gate does not retire the finding that motivated it (self-certification stays refuted; no model-authored mechanism has replaced the human-attestation idea — there is simply no gate at all now)
- [x] 3.3 Add a dated `> **Status: historical.**` blockquote (the `agents-md-structural-best-practices.md` pattern) to the top of `cross-model-design-review-axes.md`, `cross-model-review-tar-pit-forensics.md`, `cross-review-gate-defect-forensics.md`, `codex-review-integration-forensics.md`, `cross-model-pairing-consult-research.md`, `cross-model-seat-assignment-research.md`, naming this change as the retiring change; leave the rest of each doc's body untouched

## 4. Verify and complete

- [x] 4.1 Re-run the residual-reference sweep this change was scoped from (grep the repo for `cross-review`/`cross-model-review`/`attestation`/`step-0`/`gpt-5.6-sol`, including `.swift` files and `openspec/config.yaml`) and confirm every live hit outside historical/archived material has been addressed; run `git diff --check` and `openspec validate remove-cross-model-review`. **Result:** no live hits in `.swift`/`openspec/config.yaml`/`.claude/`/`scripts/`/`Makefile`/`.github/` (the 3 gate scripts confirmed gone from `scripts/`); the one Swift hit (`App/GitReviewView.swift:400`) and all `packer/README.md`/`known-product-issues.md`/`research/artifacts/large-diff-memory/`/`research/04-design/02-milestones.md` hits are historical attribution to specific past cross-review-round findings, out of scope per design.md's Non-Goals; `openspec/specs/coherence-review/spec.md` and `openspec/specs/cross-model-review/spec.md` still carry live content pending the archive-time delta merge (task 4.3), as expected. `git diff --check` clean; `openspec validate remove-cross-model-review` passes.
- [x] 4.2 Review proposal/design/specs/tasks and implementation coherence ⟶ xtty-openspec-critic (remove-cross-model-review). **Result (v7 review, run against a stale-served v6 definition detected and corrected mid-run):** 1 BLOCKER (dangling `research/README.md` re-verify clauses pointing at deleted `test-cross-review-scripts.sh`/`/xtty:cross-review`, plus the six index entries missing a historical marker) — **fixed**: reworded both dead re-verify clauses and added a `(historical — retired by \`remove-cross-model-review\`)` marker to all 6 `research/README.md` index lines. 1 REVIEW (the Tooling-row edit replaced the removed clause with per-change narrative, contradicting task 3.1's own "remove" instruction) — **fixed**: trimmed the row back to a plain removal, no narrative (the story stays in `HISTORY.md`/Learned-refutations). Left as-is: the open-changes row still reading `proposed` (self-resolves at 4.3), a pre-existing stale prose-count in `openspec-coherence-checklist.md` (unrelated to this change), and two live-voice `/xtty:cross-review` mentions inside `agents-md-structural-best-practices.md` (already whole-doc historical-marked by `remove-guide-gate`, judged lower-severity and out of this change's explicit doc list).
- [x] 4.3 Archive the change using the verified whole-capability-retirement deviation, then reconcile all trackers. **No human-attestation cross-review task precedes this step — explicit owner decision**: this change is not run through `/xtty:cross-review` on itself before archive, since the reviewer would otherwise be asked to bless its own deletion. **Deviation (required — a plain `openspec archive` aborts):** removing all 5 requirements of `cross-model-review` leaves it with zero requirements after merge, and OpenSpec 1.6.x's schema requires ≥1 (`Spec must have at least one requirement`) — the same collision `remove-guide-gate` hit on `agent-guide-budget`. Run, in order: (1) `openspec archive remove-cross-model-review -y --skip-specs`; (2) `rm -r openspec/specs/cross-model-review/`; (3) hand-apply the `coherence-review` MODIFIED delta to `openspec/specs/coherence-review/spec.md` (remove the cross-review-gate-task clause from the "Committed coherence-review agent tooling" requirement body, drop its scenario, add the new-symbol-scope scenario) — done by hand because `--skip-specs` skips merging **every** touched capability in this run, not just the one hitting zero, per the global (not per-capability) abort behavior; (4) `openspec validate --all --type spec`. Then finish the reconcile per the standard tail ⟶ archive-ritual (`AGENTS.md` open-changes row removed, snapshot updated, `HISTORY.md` entry finalized, verify-against-disk). **Done 2026-07-27:** all four deviation steps executed exactly as specified (`openspec archive remove-cross-model-review -y --skip-specs` moved the change to `openspec/changes/archive/2026-07-27-remove-cross-model-review/`; `openspec/specs/cross-model-review/` deleted; the `coherence-review` MODIFIED delta hand-applied — gate-task clause removed from the requirement body, its scenario dropped, the new-symbol-scope scenario added; `openspec validate --all --type spec` → 23/23 passed, `cross-model-review` correctly absent from `openspec list --specs`). Trackers reconciled in the same session (AGENTS.md open-changes row removed + snapshot updated; HISTORY.md entry pending in the next commit).
