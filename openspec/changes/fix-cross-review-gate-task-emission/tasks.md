## 1. Emission — the task-authoring configuration (propose-time)

- [ ] 1.1 Add a fifth `rules.tasks` entry to `openspec/config.yaml`: a change **in scope for cross-model review** MUST carry, in its tail, exactly **one blocking human-attestation cross-review task** — placed **after** the `⟶ xtty-openspec-critic` coherence-review task and **before** the `⟶ archive-ritual` task — with its **HUMAN-ONLY / model-MUST-STOP** wording (the model may neither tick it nor derive/compute the attested value). Mirror the four existing rules' shape and **defer to AGENTS.md** for the scope boundary rather than restating it.
- [ ] 1.2 State in that rule that it is an **authoring instruction, NOT a delegation marker** — it directs what task is *written*, and emitting it MUST NOT fire the paid external review on task arrival (design D2; AGENTS.md refutes an auto-firing `⟶ xtty-cross-review` marker).
- [ ] 1.3 State in that rule that the propose-time scope call is **semantic and fail-closed**: the mechanical classifier cannot classify an uncommitted change, so **when in doubt, emit the task** (design D1/D3 — over-emission is a strikeable task; under-emission is the bug being fixed).

## 2. Enforcement — the coherence-review agent (pre-archive)

- [ ] 2.1 Rewrite the gate-task check in `.claude/agents/xtty-openspec-critic.md` to be **classifier-driven**: run `scripts/cross-review-scope.sh <change>` and branch — **exit 10 (in scope)** + task absent or lacking the human-only wording ⇒ **BLOCKER**; **exit 0 (out of scope)** ⇒ not flagged; **exit 2 (cannot resolve — uncommitted change)** ⇒ fall back to the **semantic** judgment, report at most **REVIEW**, and **say the classifier could not resolve the change** (design D4 — the degraded arm must never be a silent pass).
- [ ] 2.2 Bump the agent's definition stamp to **`Definition: v5 (<date>)`** — the delivery-check probe lies if the stamp does not move on every edit (design R4; the stale-served-definition refutation).
- [ ] 2.3 Verify (by effect, per design D6) the **BLOCKER** arm: take a scratch copy of a change the classifier resolves **in scope** (`exit 10`) with its gate task removed, run the critic, and confirm a **BLOCKER** finding and a **non-coherent** verdict — not a REVIEW. Confirm the report's first line reads the **v5** stamp (a v4 stamp means a stale-served definition ⇒ the run is invalid, not a pass).
- [ ] 2.4 Verify (by effect) the **degraded** arm: run the critic against a change whose artifacts are **not committed** (classifier `exit 2`) and confirm it reports at most a **REVIEW** and **states the classifier could not resolve** — never a silent pass and never a BLOCKER.

## 3. Guide

- [ ] 3.1 Update `AGENTS.md`'s cross-review gate section + the standard-change-tail rule: the gate task is **emitted by the task-authoring configuration** at propose (fail-closed on a semantic scope call) and **enforced mechanically by the critic** at pre-archive. Keep the existing statement that there is **no auto-firing `⟶ xtty-cross-review` marker** — this change does not introduce one.
- [ ] 3.2 Add a **Learned-refutations** line: *a gate whose obligation lives only in AGENTS.md prose is inert — it reaches neither `/opsx:propose` (which reads `openspec/config.yaml`) nor a blocking review (the critic's semantic in-scope call can only REVIEW). Measured: the gate shipped 2026-07-12 and fired exactly once, by hand, on its own dogfood; both open in-scope changes carried zero attestation tasks. The obligation must sit in the authoring config, and its enforcement must borrow authority from the **mechanical** classifier.* (This is G13 recurring.)

## 4. Migration — the two open in-scope changes

- [ ] 4.1 Append the blocking human-attestation cross-review task (HUMAN-ONLY wording, correctly placed in the tail) to `openspec/changes/add-git-diff-wrap-toggle/tasks.md`. Safe per design D5: it has not attested, and the digest normalizes checkbox state and excludes the attestation line.
- [ ] 4.2 Append the same task to `openspec/changes/add-ci-pipeline/tasks.md`. Note in the change (do **not** attempt to fix here) that its reviewed range is **98.4% foreign** (304/309 paths), so attesting it in its current shape is of dubious value until the range-pollution change lands — design Open Questions.
- [ ] 4.3 Verify: `scripts/cross-review-scope.sh` still returns **exit 10** for both, and each `tasks.md` tail now carries **exactly one** attestation task in the correct position (after the critic task, before `⟶ archive-ritual`).

## 5. Verify the fix end to end

- [ ] 5.1 Verify **emission by effect** (design D6 — a grep for the rule string in `config.yaml` is a read-back check, NOT evidence, and is explicitly not this test): run `/opsx:propose` on a **scratch** in-scope change and confirm the generated `tasks.md` tail carries the blocking human-attestation task with its human-only wording, correctly positioned. Delete the scratch change afterwards.
- [ ] 5.2 Verify **no regression in the model-STOP boundary**: confirm the emitted task does not carry a delegation marker and that reaching it does not fire the paid external reviewer.
- [ ] 5.3 Run `openspec validate fix-cross-review-gate-task-emission --strict` (cheap, mechanical — stays inline).

## 6. Land the change

- [ ] 6.1 Pre-archive coherence review of this change against AGENTS.md's rulebook and live disk state. ⟶ xtty-openspec-critic (fix-cross-review-gate-task-emission)
- [ ] 6.2 **BLOCKING human-attestation cross-review — HUMAN-ONLY, the model MUST STOP.** This change is **mechanically in scope** (it touches `openspec/config.yaml`, `.claude/`, and `openspec/specs/` — all outside the docs/tracker allowlist). *This task was added **by hand**, because the rule that emits it is the very thing this change introduces — the bug reproducing on itself (design R1).* The **human** (deliberately; never auto-fired by reaching this task) runs `/xtty:cross-review fix-cross-review-gate-task-emission`, reads the complete advisory ledger, runs `scripts/cross-review-digest.sh` **themselves**, and ticks this task **recording the reviewed-state digest on the delimited attestation line**. The model SHALL NOT tick this task and SHALL NOT derive, compute, or fill in the attested value.
- [ ] 6.3 Archive + reconcile. ⚠️ **READ THIS FIRST — COMMIT NOTHING BETWEEN 6.2 AND THIS TASK.** The re-attestation deadlock (the separate, sequenced D2 change) makes **any** post-attestation drift unrecoverable by forward history: once 6.2's attestation is recorded, a single interleaving commit touching **any** reviewed path — including `AGENTS.md` / `HISTORY.md`, which are in this change's reviewed range — invalidates the digest, and **every** way to re-record the attestation then fails check (4) (`adds=2 dels=1` / `now=2`). This change must therefore **attest as the last pre-archive act and archive in the same sitting, with zero intervening commits, holding the attestation commit local until archive completes.** This is the one window where the checkbox-walking loop can brick the change by doing something otherwise routine.
  Then: **step-0 precondition first** — run the committed scope classifier (⇒ in scope), then the fail-closed four-check precondition (attestation present + well-formed; reviewed-state digest recomputes to the attested value; clean tree; exempt-by-act) **before** merging. Run the digest/scope tooling **as of the reviewed base** (this change does **not** modify `cross-review-digest.sh` or `cross-review-scope.sh`, so there is no self-validation hazard). Then merge the spec deltas (`openspec archive`), finish the merge by hand, tick trackers (Current-status row + snapshot + HISTORY narrative + milestone), add the Learned-refutations line (3.2), and verify-against-disk. ⟶ archive-ritual
