## 1. Implement the `--line` emitter

- [x] 1.1 Edit `scripts/cross-review-digest.sh`: capture the digest into a variable; parse an optional leading `--line` flag before the existing positional change-name argument; assemble `LINE="<!-- cross-review-attestation: base=$B head=$(git rev-parse HEAD) digest=$DIGEST reviewed=$(date +%F) -->"` from the already-computed `$B` and digest plus `git rev-parse HEAD` and `date +%F`. Default invocation (no flag): print the bare digest to stdout unchanged, then a short label plus the assembled line to **stderr**; with `--line <change>`: print ONLY the assembled line to stdout (for `| pbcopy`). Update the script's header comment (usage, output contract, and a one-line pointer to the new ADDED spec requirement + design.md explaining why writing/ticking/committing are permanently out of scope). Do NOT touch the shared base-resolution block (it must stay byte-identical with `scripts/cross-review-scope.sh`). Develop/exercise the edit in a scratch clone (e.g. `git clone . /tmp/xtty-scratch`) — the script refuses on a dirty tree, so editing the tracked script here dirties the tree before it can be run
- [x] 1.2 Prove the default stdout contract is byte-identical before vs. after the edit: run the pre-edit script (via `git show <parent-of-this-change's-proposal-commit>:scripts/cross-review-digest.sh`) and the edited script against the same already-existing active change (e.g. `add-git-diff-wrap-toggle` — the original example, `pin-cross-review-pass-c-fable`, was archived mid-flight by this same session and is no longer resolvable by name) and diff their stdout — must be byte-identical (stderr may differ)
- [x] 1.3 Prove the shared base-resolution block is untouched: diff that block between `scripts/cross-review-digest.sh` and `scripts/cross-review-scope.sh` before vs. after the edit — still byte-identical
- [x] 1.4 Validate the assembled `--line` output two ways, since this repo's own convention (attest, then archive, back-to-back) means no active change ever carries a live recorded attestation to diff against directly: **(a) shape** — compare the emitted line's delimiter syntax, field names, and ordering byte-for-byte modulo field values against an already-recorded line read straight from an archived change's `tasks.md` (e.g. `bound-the-agents-md-guide` or `pin-cross-review-pass-c-fable`); **(b) values** — on a still-active change (e.g. `add-git-diff-wrap-toggle`), confirm `head=` string-equals `git rev-parse HEAD`, `digest=` string-equals the same invocation's default-mode stdout digest, and `base=` matches the value `scripts/cross-review-scope.sh` computes for the same change (a mirror-integrity cross-check, not an independent source — the two scripts share the base-resolution block by design; the load-bearing external anchors are 1.2 and 1.3)
- [x] 1.5 Confirm/finalize the already-drafted spec delta at `specs/cross-model-review/spec.md` in this change directory — verify the ADDED requirement + scenarios match the shipped emitter behavior (this task is confirmation, not drafting it fresh)
- [x] 1.6 `openspec validate "emit-attestation-line"` — mechanical validation of the change artifacts
- [x] 1.7 Round-2 fixes from this change's own `/xtty:cross-review` (Pass B + Pass C, both run against 1.1-1.6): (a) gate the default-mode stderr emission on `[ -t 2 ]` (interactive terminal only) — Pass B (high) and Pass C (medium) independently found the unconditional always-on emission put the ready-to-paste line in a model's own tool transcript on every routine run, undercutting D4's own taint rule; (b) pin one `HEAD_SHA` (checked, `|| die`) immediately after the dirty-tree check and reuse it for the whole digest traversal AND the emitted `head=` field, plus a final re-check that HEAD/cleanliness didn't move mid-computation — Pass B (medium): a concurrent commit could otherwise bind a digest from one snapshot to a `head=` from another, and a failing `git rev-parse HEAD` could be silently masked by `set -e`'s last-substitution-wins rule inside the old combined `LINE=` assignment (confirmed empirically: a bare `x="pre-$(false)-mid-$(date +%F)"` under `set -e` does NOT abort); (c) reject unexpected trailing positional arguments instead of silently ignoring a misplaced `--line` — Pass C (low). Re-verified by-effect in a fresh scratch clone: default stdout still byte-identical to the pre-edit script; base-resolution block still byte-identical to `scripts/cross-review-scope.sh`; `--line` shape/values still correct; `scripts/test-cross-review-scripts.sh` still 12/12 green
- [x] 1.8 Add a `--line` scenario to the existing `scripts/test-cross-review-scripts.sh` fixture suite (Pass B, medium: the shipped `--line` capability had no durable regression coverage, only by-effect validation) — covers the emitted line's shape, its field values against independently-known ground truth, and that invoking `--line` produces no repository mutation
- [x] 1.9 Round-3 fixes from round 2 of this change's own `/xtty:cross-review` (Pass B + Pass C, run against 1.1-1.8): (a) **drop the TTY-gate entirely** rather than harden it — Pass B (high) demonstrated a PTY-wrapped invocation (an agent runner, `script(1)`, tmux) satisfies `[ -t 2 ]` without a human actually watching, so the gate narrowed round-1's exposure gap but did not close it; Pass C independently reproduced the same PTY-emission and rated it low/residual, but given both models demonstrated the same real mechanism, the fully-closing fix (never emit the line on the default path, full stop — `--line` remains the sole way to obtain it, unconditionally) was adopted instead of a third heuristic-hardening attempt, converging the round-1 vs round-2 disagreement toward the more conservative reading; (b) fix `digest_stream`'s reviewed-path enumeration to capture-and-check `git diff`'s exit status before the loop instead of piping it in via process substitution, whose producer's exit status bash does not propagate to the consumer — Pass B (high, split into two parts): a failing `git diff` could otherwise silently drive zero loop iterations and let an empty-input digest exit 0 as if there were no reviewed content; **the newline-in-filename half of this same finding was considered and explicitly NOT addressed** — an adversarially-crafted filename requires the same repo-write access as any more direct attack, which this gate's own documented threat model already treats as an accepted residual (R3: adversarial resistance rests on human ledger-reading, not on these mechanical checks); (c) fix `clean_tree()` to capture-and-check `git status`'s own exit status rather than only inspecting its stdout, so an operational `git status` failure (not just a dirty tree) is not silently read as "clean" — Pass B (medium); (d) rewrite task 2.2's reviewed-base command to be genuinely self-contained and copy-pasteable (it computes `$B` itself rather than leaving a raw `<B>` placeholder that reads as shell redirection syntax) — Pass B (medium) — and in writing it, independently caught a real zsh footgun by actually running it: `"$B:path"` (no braces) is silently mangled by zsh's colon-modifier expansion, while `"${B}:path"` is not; (e) wording fixes — the ADDED spec requirement's "can derive deterministically" softened to "derives mechanically, never authors" so it stops overclaiming determinism for the intentionally-non-deterministic `reviewed=` field (Fable, low); design.md's Non-Goals corrected from "one scenario" to reflect the three scenarios actually shipped (Fable, low); D4's claim that "the line only ever appears where a human is actually looking" corrected to describe what the mechanism (b)'s removal of the emission-on-default-path actually guarantees, not an overstated absolute (Fable, low). Re-verified by-effect in a fresh scratch clone: default stdout still byte-identical to the pre-edit script; base-resolution block still byte-identical to `scripts/cross-review-scope.sh`; `--line` shape/values still correct; `scripts/test-cross-review-scripts.sh` still 21/21 green; the rewritten task 2.2 command tested end-to-end and confirmed working from an actual zsh prompt

## 2. Pre-archive review

- [x] 2.1 Pre-archive coherence review ⟶ xtty-openspec-critic (emit-attestation-line) — verdict COHERENT; 3 findings (stale pin-cross-review-pass-c-fable example refs in 1.2/1.4/design.md, AGENTS.md tracker drift owed to archive-ritual, 1.6 unticked-but-validated) — the two actionable ones fixed in `ffa0df3`, the tracker drift left for the archive-ritual step as expected
- [x] 2.2 Human-attestation cross-review (this change touches `scripts/cross-review-digest.sh`, a path outside the docs/tracker allowlist, so it will be mechanically **in scope** per `scripts/cross-review-scope.sh`). Run `/xtty:cross-review emit-attestation-line`, read the complete ledger. Then compute the reviewed-state digest from the **actual pre-modification (reviewed-base) version** of the tool — plainly running `scripts/cross-review-digest.sh emit-attestation-line` at current HEAD invokes the CURRENT (post-modification) script, which is this change's own emitter under review self-supplying its own attestation fields; that is NOT the reviewed-base rule, it only reads like it. From the repo root, on a clean tree, run this self-contained command (verified working from a zsh prompt — the shell this repo's tooling actually runs under; note it deliberately uses `"${B}:scripts/..."` with braces, NOT `"$B:scripts/..."` — zsh's colon-modifier expansion silently mangles the bare unbraced form, dropping everything after the colon, which was caught only by actually running it) — it prints all four attestation fields, each individually labeled, still requiring YOU to assemble and paste the delimited line yourself:
  ```
  B="$(git rev-parse "$(git log --diff-filter=A --format=%H -- openspec/changes/emit-attestation-line/proposal.md | tail -1)^")" && TMP="$(mktemp)" && git show "${B}:scripts/cross-review-digest.sh" > "$TMP" && echo "base=$B" && echo "head=$(git rev-parse HEAD)" && echo "reviewed=$(date +%F)" && echo "digest=$(bash "$TMP" emit-attestation-line)"; rm -f "$TMP"
  ```
  Record the reviewed-state digest on a delimited attestation line. **Source the pasted line only from a command YOU ran yourself in your own terminal — a line visible in a model's transcript, or already sitting on a clipboard a model populated, is not attestable provenance.** **HUMAN-ONLY — the model MUST NOT tick this task or derive the attested value:**
  `<!-- cross-review-attestation: base=<B> head=<HEAD> digest=<sha256> reviewed=<date> -->`
<!-- cross-review-attestation: base=363048d8b669bb32cb54cab03fbc59721d0f6263 head=8bd1998f28a46f3820b9ef2958f650f329156272 digest=466bd2621d10ed4206bb470c6b45eacabb0c1c9e468e2f802fc537d487840025 reviewed=2026-07-19 -->

## 3. Archive

- [ ] 3.1 Archive + reconcile ⟶ archive-ritual
