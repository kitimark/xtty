# Tasks — bound-the-agents-md-guide

> **The prototype is already built and measured.** `research/artifacts/guide-gate/` carries the hook, the installer, **45 isolated fixtures (45/45 green)**, and an **18-mutant matrix** proving every fix load-bearing. This change **lands** it: tracked sources, a `make` entry point, CI, and the spec. Port it — do not redesign it. Every rule in it was settled by measurement, and the plausible-sounding alternatives are refuted in `design.md`.

## 1. Land the gate's sources

- [x] 1.1 Add the tracked hook source `.githooks/pre-push`, ported from `research/artifacts/guide-gate/pre-push`. Keep the ownership **sentinel** line (the installer's ours-vs-foreign discriminator) and the `XTTY_GUIDE_CEILING` / `XTTY_GUIDE_FLOOR` env overrides (the fixtures depend on them).
- [x] 1.2 Add `scripts/install-hooks.sh`, ported from `research/artifacts/guide-gate/install-hooks.sh`: install into `$(git rev-parse --git-common-dir)/hooks` (**never** `--git-path hooks` — it follows a *global* `core.hooksPath` and would arm the gate for every repository the user pushes); stamp the repo with `git config xtty.guide-gate true`; refuse to write into a hooks dir we do not own; never clobber a foreign `pre-push`; **always exit 0**.
- [x] 1.3 **Measure** `AGENTS.md`'s actual committed size at the time this task runs (`git cat-file -s HEAD:AGENTS.md`, resolving the `CLAUDE.md` symlink — do not hardcode a number from this document, which has already gone stale twice: 64,079 B at proposal time, 51,252 B by 2026-07-17, before this change was even applied). Edit line 32 of the just-ported hook — `CEILING="${XTTY_GUIDE_CEILING:-65666}"` — replacing the prototype's `65666` default with that measured size plus **~1.6 KB headroom**. This is the *only* edit 1.1's port needs; the prototype carries no separate ratchet marker or comment to add or withhold — do not invent one (this file's own header: "port it, do not redesign it"). **This value is PROVISIONAL** — tasks 2–4 need something to reference (the target exists and is directly callable, but is not yet wired into the build — see task 2's note), and tasks 5.1–5.3 below still grow `AGENTS.md` further, so this literal *will* be edited again, once, at task 5.4. Design D6 / spec `agent-guide-budget`'s "may only ever be lowered" rule is a **review-time discipline on future edits to this line**, not anything the file itself encodes — it doesn't apply to this provisional-to-finalized step, since the gate isn't armed with either value yet (task 5.5).

## 2. Add the build target (deliberately not yet armed)

- [x] 2.1 Add a **phony** `hooks` target to the `Makefile` that runs `scripts/install-hooks.sh` and cannot fail.
- [x] 2.2 Add the `hooks` target to `make`'s self-documenting target list.

**Not wired as a build prerequisite here** — see task 5.5. Tasks 3–4 below only need `make hooks` to exist as a directly-callable target; wiring it into `build`/`test`/etc. now, before the ceiling is finalized (5.4), would arm every clone that happens to run `make build`/`make test` during tasks 3–5 with the *provisional* ceiling from 1.3, and then require 5.4 to raise an already-armed ratchet — contradicting design D6 / spec `agent-guide-budget`'s "once armed, only lower" invariant.

## 3. Land the fixture suite (the gate's regression net)

- [x] 3.1 Add the fixture suite at `scripts/test-guide-gate.sh`, ported from `research/artifacts/guide-gate/suite.sh` — **45 arms, each in an isolated scratch repo (fresh repo + remote per arm)**. The arms were previously coupled through shared remote state and one real red cascaded into spurious ones. Verified: 45/45 green. **Later grew to 47 arms** (see 6.1) when the Codex review (5.1's/7.1's cross-model pass equivalent, run during this apply session) found two real gaps in the ported logic — A-15-1/A-15-2, fixed directly in `.githooks/pre-push`, each proven by a new fixture.
- [x] 3.2 Keep **setup failure FATAL**: an earlier version of this suite reported **21 "passes"** in a sandbox where `mktemp` was failing. A green suite that never ran is a lie.
- [x] 3.3 Add the mutation matrix at `scripts/test-guide-gate-mutants.sh`, ported from `research/artifacts/guide-gate/mutants.sh` — it reverts each fix and asserts the arm guarding it goes **red**. **Four of this suite's own arms were once green and proved nothing**; the matrix is what exposed them. A fixture that never fails is decoration. Verified: all 18 mutants caught, zero vacuous.
- [x] 3.4 Add a `make test-guide-gate` target running both.

## 4. Continuous integration

- [x] 4.1 Add a CI step that runs `make hooks` and asserts the installed hook is **byte-identical** to `.githooks/pre-push`.
- [x] 4.2 Add a CI step running the fixture suite + mutation matrix.
- [x] 4.3 In `research/03-analysis/github-actions-ci-cd.md`, describe the check honestly as an **installer regression test over CI's own clone** — it is **not** a detector of unarmed developer clones, and the earlier claim that it was is corrected in `design.md`.

## 5. Documentation

- [x] 5.1 In `AGENTS.md` → *Building*, document `make hooks` and the guide ceiling (bounded — a table row and a pointer, not a narrative; this change is about not doing that). **Also fold in the verified Building+CI trim** (2026-07-18 section-by-section delegation-mapping verification — 14 sections classified and adversarially re-checked, zero spec deltas required for this one: `build-workflow` spec's SHALL is satisfied verbatim by the replacement wording below). Condense five mechanism bullets to doc-pointer wording; keep the raw commands and required-gate facts (current lines 84, 90, 93–95, 98, 104, 106–107) verbatim:
      - Line 86's final clause → "`make install`'s options are self-documented in the `Makefile` (rationale: `research/03-analysis/local-install-workflow-research.md`)."
      - Line 91 → "**SwiftTerm checkout (one-time):** a **gitignored upstream clone** at `external/SwiftTerm`, reconstituted from a pinned ref with a tracked patch applied by `scripts/bootstrap-swiftterm.sh` (run once after cloning; the `make` build targets auto-run it). **Never edit the clone directly** — bootstrap restores it pristine every run; change the tracked patch instead. Mechanism + rationale: `research/03-analysis/swiftterm-fork-vs-patch-strategy.md`."
      - Line 92 → "**Local VM test image (optional):** `make image` builds the Tart image for CI-parity XCUITest runs (the per-launch race class reproduces at 3 vCPU in a VM, not on bare metal). Prereqs, runtime workflow, acceptance envelope, expected-difference matrix: **`packer/README.md`**."
      - Line 96 → "**Install a stable build:** `make install` (Release build → self-contained copy in `/Applications`, survives `make clean`); `make restart` relaunches. **$0 — no notarization, no Apple account** (a locally-compiled app is never quarantined). Options are `Makefile`-self-documented; rationale: `research/03-analysis/local-install-workflow-research.md`."
      - Line 100 → "**Optional stable local signing:** the ad-hoc identity changes every rebuild (TCC re-prompts) — run `scripts/create-signing-cert.sh` once, then build with `XTTY_SIGN_IDENTITY=xtty-dev`. Distribution signing is researched green but gated on the ~$99/yr program: `research/03-analysis/distribution-signing-research.md`."
      - Line 109 → "Both jobs reconstitute SwiftTerm via `scripts/bootstrap-swiftterm.sh` (cached, keyed on pin + patch); `pr-lint.yml` enforces Conventional Commit **PR titles**. Current CI truth: `test-core` is green and gate-worthy; `build-and-test` is non-blocking (2 known rig fragilities). Full investigation record + expected-difference matrix: `research/03-analysis/github-actions-ci-cd.md`."
      Net ~2.6–3.0 KB / ~1,050–1,220 tokens saved on top of the hooks/ceiling addition.
- [x] 5.2 In `AGENTS.md` → *How to work here*, state the **park-don't-lose** refusal protocol: on refusal, the finding goes to its named lazily-loaded surface and the guide keeps a bounded pointer. Never relocate refused content to another **eagerly-loaded** surface — the meter covers those too.
- [x] 5.3 Add a **Learned refutation** one-liner: the leanness rule was **overridden, not unheard** (it existed at three surfaces); the disease is **admission, not reach**; a guard scoped to one surface **displaces** growth rather than stopping it.
- [x] 5.3a Shrink `AGENTS.md`'s Snapshot "Latest change" narrative (current line 15, ~2,708 B) to a bounded one-liner (2026-07-18 verification: **CONFIRMED**, zero spec deltas — the cut *complies with* `research-capture` spec's own SHALL that the guide's status surface stay a bounded entry with full narrative in the not-loaded history log; the full narrative already survives, strictly richer, at `HISTORY.md` lines 32–65). Replace the line with:
      "- **Latest change:** `fix-cross-review-gate-task-emission` — **ABANDONED as won't-do** (archived 2026-07-13 via `--skip-specs`; **1/20 tasks, no spec deltas merged**). Its own 4-round cross-review refuted its ground: the scope classifier is **non-attributive and latches** (see Learned refutations; the follow-on conclusion "narrowing the range is the prerequisite" was itself **REFUTED 2026-07-13**). Findings preserved in `cross-review-gate-defect-forensics.md` (§9). ⚠️ **Emission + enforcement remain UNFIXED**, and neither open change carries the gate task. Prior change: `brief-cross-review-pass-b` (archived 2026-07-13) — Pass B is now **briefed** (A'), dogfooded through the full human-attestation gate. Full narratives: [HISTORY.md](HISTORY.md)."
      Optional non-blocking: append a one-line ❌ addendum to HISTORY.md's 2026-07-13 section noting the "narrowing the range" prescription was later refuted (it currently closes on that prescription unmarked).
- [x] 5.3b Delete `AGENTS.md`'s cached Established-specs list (current line 36, the 23-name list) and replace with a derive-on-demand pointer (2026-07-18 verification: **MODIFIED** — confirmed correct target, verification found a 4th collateral consumer beyond the original 3; zero spec deltas required). Replace line 36 with:
      "**Established specs:** derive on demand — `openspec list --specs` (or `ls openspec/specs/`); disk is the truth, no cached list here."
      Then make the 4 collateral edits verification found as the exhaustive consumer set: (1) `AGENTS.md` line 126 — reword the verify-against-disk clause `ls openspec/specs/ (established specs must match the list above)` → `ls openspec/specs/ (the guide carries no cached list — disk is the truth)`; (2) `.claude/agents/xtty-openspec-critic.md:22` — remove "Established-specs line" from the tracker enumeration; (3) `.claude/agents/xtty-openspec-critic.md:46` — remove the operational list-vs-disk comparison (else the critic fires a phantom drift finding every run); (4) `.claude/skills/xtty-capture-research/SKILL.md:40` — remove the same list reference. (`.claude/commands/xtty/capture-research.md:10` needs no edit — verified it names only the disk triple.)
- [x] 5.4 **Finalize the ceiling now that 5.1–5.3, 5.3a, and 5.3b have landed.** Committed as `4ff9afa`/`e4bb11b`; re-measured `git cat-file -s HEAD:AGENTS.md` = **50,170 B** (net reduction from the 52,135 B provisional measurement — the Building trim + Snapshot dedup + established-specs removal outweighed the additions). Line 32 finalized to `51770` (50,170 + 1,600 headroom). Re-verified: 47/47 fixtures still green (the suite always overrides `XTTY_GUIDE_CEILING` per-arm, so this default is untested by the suite itself — confirmed by inspection + syntax check). Task 1.3's edit to line 32's `CEILING="${XTTY_GUIDE_CEILING:-N}"` default was only a provisional value set early so tasks 2–4 had something to reference — but 5.1–5.3 grow `AGENTS.md` (the `make hooks`/ceiling docs, the park-don't-lose protocol, and a new Learned Refutation one-liner) while 5.1's folded-in trim, 5.3a, and 5.3b shrink it — net effect should be a **smaller** file than at 1.3's measurement. **Commit 5.1–5.3, 5.3a, and 5.3b first** (the measurement below reads `HEAD`, matching D4's "measure the committed blob, never the disk" rule for the gate itself — re-running `git cat-file -s HEAD:AGENTS.md` against an *uncommitted* set of edits would silently re-read the pre-edit blob and leave the provisional value unchanged). Then re-run task 1.3's measurement and edit line 32's literal again if it has drifted from the provisional value. Design D6 / spec `agent-guide-budget`'s "may only ever be lowered" review-time discipline governs edits to this line from here onward, once the gate ships armed with this finalized value (task 5.5) — not the provisional-to-finalized edit itself. This is what makes "green on arrival" (design D6, Migration Plan) true for the state that will actually ship, not an early snapshot that predates this change's own documentation.
- [x] 5.5 **Now — only now — arm the gate.** Wired `| hooks` as an order-only prerequisite of `setup`/`build`/`run`/`install`/`test`/`test-core`. Verified by dry-run (`make -n <target>` for all six) that `scripts/install-hooks.sh .githooks/pre-push` fires before each target's own recipe. Ran `make hooks` for real: this clone is now armed (`git config --get xtty.guide-gate` → `true`), installed hook byte-identical to the tracked source. Make `hooks` an **unconditional, order-only** prerequisite of `build`, `test`, `test-core`, `run`, `install`, and `setup` (`target: | hooks`). Order-only so it never marks the build out of date; phony so it re-copies every run and the installed copy can never go stale. **This is the task that arms already-bootstrapped clones — a hook wired only to `setup` leaves the maintainer's own machine, which produces essentially every push, permanently unarmed.** It runs *after* 5.4 deliberately: wiring it earlier (e.g. back in section 2) would arm clones with the provisional ceiling and then require 5.4 to raise an already-installed ratchet, contradicting the "once armed, only lower" invariant.

## 6. Verify

- [x] 6.1 Run the fixture suite and the mutation matrix: **63/63 green**, **32/32 mutants** caught, 0 vacuous, exit 0. History: 50 fixtures/21 mutants pre-cross-review → round 1 (2026-07-18) added arms 51/58 (A-C-3 unclosed/no-opening frontmatter), 53/54 (A-C-2/A-C-2b directory-symlink escapes), 55 (A-C-1 field-order escape), 56/57 (install-hooks.sh A-C-5/A-C-6) = **58/27**, extending the matrix driver with `INSTSRC`/`mut_inst()` (it previously could only mutate the hook) — round 1 also caught its OWN pre-existing "frontmatter check = grep ^paths:" mutant going **vacuous** (A-C-3's fix incidentally superseded arm 39 on the opening-delimiter dimension), fixed by arm 58. Round 2 (same day, the bounded loop's FINAL round) found and fixed 5 MORE real defects surviving round 1: arm 59 (`seen` dedup was a substring match, not exact membership — a pre-existing bug independent of round 1's brief), arm 60 (`is_dir_symlink` resolved only one hop — a 2-hop symlink chain escaped totally undetected, worse than the original bug), arm 61 (an empty queue hardcoded `root_missing`, discarding an already-set `unresolved_symlink` status), arm 62 (comma-delimited dangling paths mis-split a comma-containing filename, switched to a `$'\x1f'` unit-separator delimiter), arm 63 (the installer's OWN round-1 recovery instructions told the user to run a command that caused the NEXT routine reinstall to destroy their merge — verified end-to-end both broken and fixed) = **63/32 final**. Two round-2 findings were adjudicated as accepted residuals (documented in `design.md` Risks, not fixed): a stale-tracking-ref sibling class (confidence 0.45) and a literal-newline-in-dangling-path gap (confidence 0.85, R3 adversarial-construction-only). One finding (alternate-eager-root deletion) was considered and judged NOT a defect — the comparator's intended shrink-always-allowed behavior. Final clean solo runs throughout (never two instances against the same directory at once, per the round-15 concurrency lesson).
- [x] 6.2 **Verify the gate by effect on the real repository, in a throwaway clone** (never on `main`): arm it, attempt a push that grows `AGENTS.md` past the ceiling **finalized in task 5.4** (not task 1.3's superseded provisional value) ⇒ **REFUSED**; attempt an unrelated push ⇒ **ALLOWED**; attempt a push that deletes the guide ⇒ **REFUSED**. Done in a local bare-mirror throwaway clone (no network, never touched the real GitHub remote): growth to 53,172 B ⇒ `REFUSE ... 53172 B > ceiling 51770 (baseline be86cc7ba was 50170)`; an unrelated new-file commit ⇒ allowed, pushed clean; deleting `AGENTS.md`+`CLAUDE.md` ⇒ `REFUSE ... the guide ROOT (CLAUDE.md) was DELETED`. All three by real `git push` exit codes, not a syntax check.
- [x] 6.3 Confirm the meter reads the real repository correctly: `resolve_guide HEAD` → matches `git cat-file -s HEAD:AGENTS.md` (whatever that measures at the time — **50,170 B** as of this commit, re-derived not assumed) through `main`'s actual symlink. Confirmed: `CLAUDE.md` is a real `120000`-mode symlink to `AGENTS.md`, and all three 6.2 effect-tests correctly measured/gated against this exact committed state.
- [x] 6.4 Confirm an already-bootstrapped clone is armed by a routine `make build` alone (no `setup`, no re-bootstrap): `git config --get xtty.guide-gate` → `true`, and the installed hook is byte-identical to the source. Confirmed by dry-run (`make -n <target>` for all six routine targets shows `scripts/install-hooks.sh .githooks/pre-push` firing before each target's own recipe) plus a real `make hooks` invocation on this actual clone: armed (`true`), byte-identical.
- [x] 6.5 Run the standard test suite to confirm no regression ⟶ **xtty-test-validator (Tier-1 `make test` + `make test-core`)** — Tier 0 237/0/0, Tier 1 54/0/1 of 55 (sole skip: opt-in benchmark e2e). Matches the living acceptance envelope exactly; guide-gate hook armed throughout, zero attributable effect.
- [x] 6.6 `openspec validate --all --type spec` — 26/26 passed, 0 failed.

## 7. Completion

- [x] 7.1 Pre-archive coherence review ⟶ **xtty-openspec-critic (bound-the-agents-md-guide)** — **VERDICT: COHERENT**, no blockers (critic Definition v6, 2026-07-18). Confirmed correct: D6/Migration Plan accuracy, spec-deltas-vs-implementation accuracy, tasks.md internal consistency (28 tasks, matches `openspec list`), all delegation markers. Three REVIEW-level (non-blocking) findings, deferred to 7.3's reconcile per the critic's own recommendation: (1) `AGENTS.md`'s open-changes row is stale (17/28 shown, actually 25/28 at review time — moot once the row is removed at archive); (2) `proposal.md`/`design.md`/`ci.yml`'s CI step name still cite the propose-time "45 fixtures/18 mutants", now 50/21; (3) `design.md`'s fates tables don't record the round-15/15.1 defects (acceptable — a propose-time artifact, the real record is in `tasks.md` §6.1 and `research/artifacts/guide-gate/README.md`).
- [ ] 7.2 **HUMAN-ONLY — THE MODEL MUST STOP HERE.** This change is **in scope** for the cross-model gate (it touches `.githooks/`, `scripts/`, `Makefile`, `.github/` — all outside the docs allowlist). The **human** runs `/xtty:cross-review bound-the-agents-md-guide`, **reads the complete ledger**, runs `scripts/cross-review-digest.sh bound-the-agents-md-guide` themselves, and ticks this task **recording the reviewed-state digest on a delimited attestation line** in this file:
      `<!-- cross-review-attestation: base=<B> head=<HEAD> digest=<sha256> reviewed=<date> -->`
      **The model may not tick this task and may not derive, compute, or fill in the attested value.** A model-authored attestation certifies the opposite of what it claims.
- [ ] 7.3 Archive + reconcile the trackers ⟶ **archive-ritual** *(step 0 — the fail-closed precondition — runs FIRST, before `openspec archive`)*
