## 1. Parameterize the template by login shell

- [x] 1.1 Add a Packer `shell` variable (default `bash`) to `packer/xtty-test.pkr.hcl`; drive the `chsh` provisioner and the output image tag off it (`bash` → `xtty-test:26.5`, `zsh` → `xtty-test-zsh:26.5`), keeping the `shell=bash` build byte-identical to today's image.
- [x] 1.2 Wire a `make image` selector for the shell parameter (`make image-zsh` alias + `make image IMAGE_SHELL=zsh`; **not** `SHELL=`, a make built-in) so both variants build from one entry point; leave the default (`make image`) producing the bash image unchanged.
- [x] 1.3 (verify, inline) `packer validate` the template for both `shell=bash` and `shell=zsh`; confirm the default path is unchanged. *(bash/zsh/default all "configuration is valid"; `shell=fish` rejected with the custom message; tag interpolates `xtty-test` vs `xtty-test-zsh`.)*

## 2. zsh guest: shell + Local Network gate neutralization attempt (refuted — see §4)

- [x] 2.1 Add a zsh-only provisioner block gated on `shell=zsh`: `chsh -s /bin/zsh` for the auto-login account (and root, matching the bash block's pattern). *(Implemented by parameterizing the existing `chsh` block as `/bin/${var.shell}` — zsh guest → `/bin/zsh`, bash guest → `/bin/bash`; functionally the zsh-only arm without a duplicate `chsh`.)*
- [x] 2.2 ~~Install a per-boot `LaunchDaemon` … so `getnameinfo` answers the reverse-PTR from the files module before mDNS.~~ **REFUTED by effect (4.1) — the daemon does not neutralize the gate; no rig-level machinery ships.** *(Assets built: `packer/zsh/xtty-seed-etc-hosts.sh` + `packer/zsh/com.xtty.ln-neutralize.plist`, installed only on the zsh build. They seed loopback/ULA/link-local PTRs correctly, but macOS does **not** preempt the routable-IPv4 reverse PTR, which escapes to the local nameserver and trips the gate anyway (4.1). Retained only as a documented dead-end; removable at apply/cleanup. Design D2 records the full refutation + T7–T10.)*
- [x] 2.3 (verify, inline) On a booted zsh clone, confirm the daemon ran (the `/etc/hosts` entries exist for the live addresses) and the auto-login shell is `/bin/zsh`. *(Smoke boot: `UserShell=/bin/zsh`; daemon `runs=1 exit=0`; managed block seeded loopback + ULA `fdd5:…` + link-local `fe80::…` (Q2 answered, %zone stripped); log line `seeded 5 address line(s)`. Seeder hardened to absolute `/sbin/ifconfig`+`/usr/sbin/scutil` after the first smoke showed bare `ifconfig` fails under a stripped PATH — image rebuilt, re-verified.)*

## 3. Build the zsh image

- [x] 3.1 Build `xtty-test-zsh:26.5` via the parameterized template (human-gated prereqs unchanged: Packer + the pre-downloaded Xcode `.xip`); confirm it is Metal-toolchain-free (`xcodebuild -showComponent MetalToolchain` → `uninstalled`) and xtty builds in-guest. *(Built in 3m37s, artifact `xtty-test-zsh:26.5`; `xcodebuild -showComponent MetalToolchain` → `Status: uninstalled`. Toolchain provisioning is identical to the proven bash image (same template); full in-guest build/launch is exercised by the delegated 4.1/5.1 graphics runs.)*

## 4. The Local Network gate is not neutralizable at the rig level (refutation record + red baseline)

- [x] 4.1 (record — refutation, unfixed-build red baseline) Rig-level LN-gate neutralization was tested by effect and **refuted**: a graphics UI session on a zsh clone under `log stream` fired the modal + **40** `policy 'pending'` events, and the follow-up sweep closed every remaining rig-level lever (T7–T10, design D2). No rig-level machinery ships; the standalone divergence measurement runs **headless** (§5). This same graphics run stands as the **red baseline** for the red→green pair with `fix-osc7-hostname-reverse-dns` task 2.1 (same graphics zsh rig, the *fixed* build → 0/0/0 by-effect green). *(Original delegated verify-by-effect run below, kept as the evidence record. ⟶ xtty-test-validator (graphics VM, zsh golden — LN-gate verify-by-effect) — completed; result: refuted.)*
  - **FAIL (2026-07-07, run #1) — Risk-1 materialized. Fix-class (b) `/etc/hosts` seeding is insufficient.** Validator (Def v4) confirmed the modal appeared + **40** `mDNSResponder … policy 'pending' for com.xtty.app` events. **Not a missing address:** all 10 live addresses (incl. routable IPv4 `192.168.64.36`) were seeded; the live-present/block-absent set is EMPTY (DHCP-timing hypothesis disproven). **Refined root cause:** macOS answers the IPv6 (ULA/link-local) reverse PTR from `/etc/hosts` (files) but does **not** preempt the **routable-IPv4** reverse PTR — `getnameinfo(192.168.64.36)` returns numeric, so the query `36.64.168.192.in-addr.arpa` escapes to the local-network nameserver `192.168.64.1` (`scutil --dns`), and *that* local-network DNS send is what the trust gate evaluates → `pending` → nehelper prompts. Seeding more address lines cannot fix it; needs a different mechanism (loopback PTR authority / resolver redirect, or the product hostname fix). Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-07-add-zsh-test-image-ln-gate/` (`guest-screen-during.png`, `precise-gate-logshow.txt`, `mechanism-probe.txt`, `resolver-and-counts.txt`, `REVIEW.md`). Clone `xtty-run-zsh-ln1` kept (stopped). **Blocked pending the neutralization-mechanism decision (owner).**
  - **UPDATE (2026-07-07) — every rig-level neutralization is now refuted by effect; the LN gate cannot be neutralized at build time (decision: don't try).** On the freshly-built `xtty-test-zsh:26.5` (which bakes the TN3179 `defaults write com.apple.network.local-network Allowed{Ethernet,WiFi}LocalNetworkAddresses` RFC-1918 arrays — `10/8`+`172.16/12`+`192.168/16`, ⊇ the VM's `192.168.64.x`), a graphics `open` of xtty **still fired the modal** — confirming the `Allowed*` keys are a *destination-address* exemption, orthogonal to xtty's reverse-DNS *query* trigger (T7). Also refuted this session: an **NE-store pre-seed** — bake a *granted* `com.apple.networkextension.plist` into a fresh clone → cold boot → `nesessionmanager` **prunes the record** (count 0; only a live *explicit* Allow persists+silences; the record is `SigningIdentifier`-keyed with empty `DesignatedRequirement`, so not cdhash-bound — the killer is boot provenance-pruning) (T8/T9); and the **SSH/CLI-child auto-grant** — xtty launched as a direct `sshd-session` child still prompted (a GUI app bundle is its own responsible code) (T10). **Implication for §5:** there is no rig-level LN neutralization, so measure the divergence **headless** (the modal is graphics-only; headless resolves the gate to the default deny and the semantic tests take their graceful-degradation arms), and the durable *product* path is the `gethostname()` swap in `PaneController.localHostNames` (matches the shell's injected `${HOST}`; behavior-preserving + more correct). Full record + probes + fates T7–T10 + guideline G11: `research/03-analysis/local-network-privacy-forensics.md` §8.

## 5. Measure the divergence, headless (the change's acceptance)

- [ ] 5.1 (verify — headless acceptance matrix, both rigs) Run the existing, unmodified suite **headless** on the **bash** rig (×2) and the **zsh** rig (×2); capture per-rig counts + failing sets + the `.xcresult` bundles. The zsh rig is headless-only here — the unfixed-build modal is graphics-only and un-neutralizable at the rig level (D2), so the modal-free graphics real-arm run is `fix-osc7-hostname-reverse-dns` task 2.1, not this task. ⟶ xtty-test-validator (headless matrix, both goldens — bash `xtty-test:26.5` + zsh `xtty-test-zsh:26.5`)
- [ ] 5.2 (verify) Confirm the divergence *by effect* (design D3): bash rig = documented headless envelope (paste red, family carries `"…capture inactive…"` attachments); zsh rig = the semantic family **asserting** (no `"…capture inactive…"` attachments — the integration is live). A zsh pass that still shows capture-inactive attachments FAILS acceptance. Which arm each individual test lands under the headless-denied gate (paste green?; OSC 7 cwd-classification tests still local?) is recorded as a measured discovery, not asserted here.
- [ ] 5.3 (verify) Record whatever count the zsh rig lands (headless) as its measured envelope; note any new zsh-specific behavior (prompt width, integration load on a fresh guest). Graphics parity on the unfixed build is not measured here (D2); it rides `fix-osc7` task 2.1.

## 6. Reverse duty: reconcile the runtime docs (same change)

- [ ] 6.1 Update `packer/README.md`: document the zsh-variant build; record that the rig-level LN-gate neutralization was **refuted** (4.1 / D2 — the durable fix is the product change `fix-osc7-hostname-reverse-dns`) and that the zsh rig's standalone measurement runs **headless**; add the zsh rig's measured **headless** Acceptance envelope alongside the bash envelope; add/repoint the Expected-difference matrix "Shell arm" row to describe bash-vacuous vs zsh-real-coverage.
- [ ] 6.2 Update `research/03-analysis/github-actions-ci-cd.md` §19b to record how the `bash32-no-bracketed-paste` residual behaves on the zsh rig (as measured in §5 — the paste guarantee is exercised, not skipped, on a shell with bracketed paste), and that the zsh divergence is measured headless.
- [ ] 6.3 (verify, inline) `openspec validate add-zsh-test-image` passes; the proposal↔specs capability contract holds (build-workflow only).

## 7. On completion (post-verify)

- [ ] 7.1 Reconcile trackers per AGENTS.md "Keep progress current": AGENTS.md Current-status table row + snapshot (a zsh rig now exists; both envelopes), append the narrative to `HISTORY.md`, advance `research/04-design/02-milestones.md` if applicable, and update `research/03-analysis/shell-dependent-test-partitioning.md` from "decided/unbuilt" to "built + measured".
- [ ] 7.2 (verify — coherence, pre-archive) Review the change for coherence against AGENTS.md's rulebook + disk state. ⟶ xtty-openspec-critic (add-zsh-test-image)
