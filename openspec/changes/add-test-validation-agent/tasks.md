# Tasks — add-test-validation-agent

## 1. Durable homes for the numbers and the workflow (before the agent exists)

- [ ] 1.1 Capture the **expected-difference matrix** into `packer/README.md` next to the Acceptance section — recording the difference **classes with their causes** (local zsh vs VM bash shell arms; menu-race sensitivity by machine speed; bracketed-paste capability; confirm-close/interference flake sources) and **pointing to the adjacent Acceptance section for the authoritative counts** (one number home per fact — never a second copy of the envelope); cite the session evidence (fix-main-menu-clobber validation + §12/§13/§18)
- [ ] 1.2 Note in the same section that the envelope + matrix are the **runtime source for `xtty-test-validator`** (editing them re-tunes the agent), and state the **reverse duty**: any change that alters test counts or expected residuals updates this section in the same session
- [ ] 1.3 Correct the README's **Runtime workflow** to the proven interim recipe (its current step-4 in-guest build cannot run on the Metal-free golden until `retire-metal-renderer`): host `xcodebuild build-for-testing` → rsync `Products/` + the xctestrun (note it is `__TESTROOT__`-relative — no path rewriting; supersedes §8b's shared-`/tmp` variant) → guest `xcodebuild test-without-building`; fresh-clone SSH-key injection (`tart exec` appending `~/.ssh/xtty-vm.pub` to the guest's `authorized_keys`); mark the block *interim until `retire-metal-renderer` is applied to the repo under test* and pre-register the flip-back as belonging to that change / `add-xtty-test-image`

## 2. The agent + launcher

- [ ] 2.1 Write `.claude/agents/xtty-test-validator.md`: frontmatter (name; a `description` that triggers on "run/verify the test suite, locally or on the VM rigs"; the `tools` field decided explicitly — omit to inherit all, which the agent needs: Bash incl. background execution, Read/Grep, Write for REVIEW.md); body = the tier procedures (Tier 0 `make test-core`; Tier 1 local `make test` with the hands-off warning; Tier 2 headless VM; Tier 3 graphics VM), the VM mechanics **by deference to the README's Runtime workflow** (probe `TART_HOME` first — the golden may live on an alternate volume; take the commands from the README; the prompt carries only the discipline) with the **precedence rule** (never execute the README's `tart delete` step — clone removal goes into the cleanup manifest), the **long-tier execution mechanic** (every tier ≥1 runs backgrounded — `run_in_background`/nohup → log file — with sparse 30–60 s polling; a foreground call dies at the Bash tool's 120 s default / 600 s max), the D2 runtime reads (envelope + matrix from `packer/README.md`), the D3 report skeleton (verdict / verbatim per-tier / classification with UNEXPLAINED / consistency / evidence + REVIEW.md to `~/Downloads/xtty-vm-poc/artifacts/<run>/` / feedback / cleanup manifest), and the D5 defaults + D6/D8 guardrails
- [ ] 2.2 Write `.claude/commands/xtty/validate.md`: a thin launcher whose body **instructs the main loop to spawn the `xtty-test-validator` subagent** (agent invocation by name — NOT the Skill tool; a command is a prompt template and cannot spawn anything itself), forwarding `$ARGUMENTS` as the requested tiers/runs; mirrors `/xtty:capture-research`'s *role* (thin, no workflow content of its own), not its literal Skill-tool call
- [ ] 2.3 Extend `.gitignore`: append the exception block `!.claude/agents/` / `.claude/agents/*` / `!.claude/agents/xtty-*` after the existing `.claude` section (empirically verified in a scratch repo: the agent file becomes trackable, non-`xtty` agents + `settings.local.json` stay ignored; the file pattern deliberately has **no trailing slash** — the skills pattern's `/` is dir-only and would not match a `.md` file); verify with `git check-ignore -v` that the agent file is **not ignored** and machine-local files still are

## 3. AGENTS.md wiring

- [ ] 3.1 Add the test-validation tooling to AGENTS.md: the two spawn scenarios (user request via `/xtty:validate`; OpenSpec verify-task delegation — *verify tasks that execute the suite are delegated to `xtty-test-validator`, the apply loop consumes only its report and ticks tasks with verbatim results*), the deference chain **in both directions** (agent → AGENTS.md for rules + `packer/README.md` for numbers/workflow; envelope-shifting changes → update the README in the same session), and the tracked-tooling note extended to `.claude/agents/xtty-*`

## 4. Verify

- [ ] 4.1 Tracking check: `git status` shows the two new tooling files as trackable; `git check-ignore -v` confirms the exception behaves (agent file not ignored; machine-local files ignored)
- [ ] 4.2 Live smoke, Scenario A: invoke the agent for a **Tier 0 + Tier 1** quick confirm; verify the report follows the D3 skeleton (verbatim counts, classification, evidence path, feedback, cleanup manifest), that the **hands-off warning was surfaced before Tier 1 started**, and that the main session received only the report
- [ ] 4.3 Live smoke, Tier 2 **headless ×2** (~7 min/run — a single run cannot verify the multi-run mechanics): verify clone-per-run, **sequential** execution (the second run starts only after the first completes; each a separate full run with no retry flag), the runtime envelope read (report cites the README numbers), REVIEW.md written to the artifacts dir, and the cleanup manifest names the clone(s) with removal commands (and the agent did NOT delete them)
- [ ] 4.4 Scenario B dry-run **in a fresh session** (the implementing session is biased — the agent file and rule are already in context): issue an apply-style verify-task prompt (e.g. "verify task: run the suite on the headless rig") scoped to **Tier 0 only**, or cancel once the spawn is observed — the assertion is agent selection per the AGENTS.md rule / description match, not a full sweep

## 5. Docs / trackers

- [ ] 5.1 `openspec validate "add-test-validation-agent"` passes; update AGENTS **Current status** + `research/04-design/02-milestones.md` (tooling implemented); verify trackers against disk (`openspec list` / archive dir / specs dir)
- [ ] 5.2 At archive: hand-touch `openspec/specs/research-capture/spec.md`'s **Purpose** enumeration (it names the committed commands + skills; agents are now tracked too — a one-line `docs(openspec)` edit so specs record what is true)
- [ ] 5.3 On completion, note in the change (or commit message) the deferred design options: CI-inspect mode as a fifth matrix column of this agent; family members only when a recurring noisy workflow proves itself; per-task delegate markers only if delegation drift appears
