## 1. SwiftTerm patch revision (the product fix)

- [x] 1.1 In `patches/swiftterm/xtty-accessors.diff`, **delete the blanket momentum guard** `if event.momentumPhase != [] { return }` from `scrollWheel(with:)` (D1). Keep the `deltaY == 0` horizontal guard and the priority-ordered 3-way branch (BRANCH 1/2/3) unchanged.
- [x] 1.2 Add the **per-gesture remainder reset** (D3): zero `xttyWheelRowRemainder` when `event.phase.contains(.began)`, so a stale sub-cell fraction cannot leak across separate flicks.
- [x] 1.3 Rewrite `xttyWheelRowCount(for:)`'s **precise** (`hasPreciseScrollingDeltas`) path to carry the full sub-cell remainder and **emit every accumulated whole row** — drop `min(cap, whole)`; subtract only what is emitted (D2). Drop the `cap` on the non-precise path too (D4); a discrete notch's behavior stays effectively unchanged.
- [x] 1.4 Extend `public struct XttyWheelRouting` (`XttyAccessors.swift`) with `let momentum: Bool` + `let precise: Bool` (D6); set them at all three `xttyLastWheelRouting = …` assignments (report / cursorKey / scrollback).
- [x] 1.5 Re-run `scripts/bootstrap-swiftterm.sh` to confirm the revised hunk applies cleanly from the pristine pin.

## 2. App-layer observability + injection hook

- [x] 2.1 Surface `momentum` + `precise` in the App-layer DEBUG state dump's wheel-routing field (mirror the existing branch/button/count fields; `#if DEBUG` + `-UITestGridDump` gated, observe-only).
- [x] 2.2 Add the **DEBUG synthetic-wheel-event injection hook** (D8), following the existing `App/XttyApp.swift` XCUITest-trigger family (link-open / spatial-op / git-select / git-open / block-select): the test writes a wheel-event spec (momentum phase, precise flag, vertical delta, modifiers); the app constructs a faithful wheel `NSEvent` via `CGEvent(scrollWheelEvent2Source:…)` → `NSEvent(cgEvent:)` and drives it through the **real** `scrollWheel(with:)` on the focused pane's terminal view. `#if DEBUG` + `-UITestGridDump` gated; route through the real handler, never a reimplemented branch.

## 3. Tests

- [x] 3.1 In `AppUITests/XttyMouseWheelUITests.swift`, add the **crisp-negative** assertion that a real automation-channel gesture is recorded with `momentum == false` (proves the field is wired on the real-gesture path). Update the stale `capped at 5` comment/assumption in the existing suite (the cap is removed by 1.3). Leave the existing 48 routing/monotonicity assertions otherwise unchanged.
- [x] 3.2 **Momentum-not-dropped** (via 2.2's injection hook): inject a wheel event carrying an inertial-coast (momentum) phase over a focused pane and assert it routes on its branch with `momentum == true` — not silently dropped. Guards the D1 guard-deletion against regression.
- [x] 3.3 **Lossless-carry** (injection): inject precise input whose accumulated travel exceeds several whole cells and assert the routed emission count tracks the cells traversed (carrying the remainder), not clamped to a fixed cap. Guards the D2 cap-discard fix.
- [x] 3.4 **`.began` reset** (injection): inject a precise gesture that leaves a sub-cell remainder, then begin a new gesture, and assert no phantom extra cell is emitted from the leftover remainder. Guards D3.
- [x] 3.5 **Byte-level SGR mouse-report regression** (user-requested, added after the manual physical-trackpad verify's step 1 succeeded): `testInjectedMomentumFrameProducesRealSGRMouseReportBytes` arms `printf '\033[?1002h\033[?1006h'; cat -v` (the same technique the human used manually) and injects a momentum frame, asserting the real SGR wheel-report bytes (`^[[<64;…M`) reach the child process — not just the `lastWheelRouting` DEBUG-dump bookkeeping `testInjectedMomentumFrameIsRoutedNotDropped` already checks. Does not and cannot automate OS-level coast delivery (still task 4.4's job). Red→green verified against a reintroduced D1 regression.
- [x] 3.6 **Real mouse-tracking-program integration test** (user-requested, native-command alternative to htop for step 2 of the manual verify; design D9): `testWheelScrollsRealMouseTrackingPager` runs `seq 1 500 | less --mouse` — a real, unmodified, native (`/usr/bin/less` 668, no brew dependency) pager that requests its own button-event mouse tracking, unlike every other test in this suite which arms mouse mode via a `printf`-simulated escape sequence — and asserts its own visible content genuinely advances (a grid-content diff, polled across up to 15 real wheel ticks since a single XCUITest-synthesized precise tick doesn't reliably cross one whole cell) alongside the DEBUG dump's `branch == "report"`. Spike-verified `less --mouse` requests an included mouse mode (not the excluded X10-only) and is present with identical `less 668`/`--mouse` support on both the bash and zsh VM goldens (SSH-checked directly, not assumed). Red→green verified: disabling BRANCH 1's gate correctly fails the `branch == "report"` assertion (the grid content still advances via BRANCH 2's cursor-key fallback, confirming the routing check — not the grid-diff alone — is the regression-discriminating assertion). **Hardened post-adversarial-review** (two independent reviewers converged on the same finding): the readiness wait was ambiguous (`waitForContains("1", …)` could match the typed command's own literal "1" before `less` even rendered — fixed to `"1\n2\n3"`, a shape only `less`'s line-by-line rendering produces) and the per-tick grid read was a single unguarded synchronous `GridDumpReader.read()` racing the app's 150ms async dump-write timer — the same read-freshness race class documented in `packer/README.md`'s "Graphics-tier read-freshness race" section — fixed with a `waitForGridChange` helper that polls each tick for up to 300ms. Re-verified red→green and `12/0/0` locally after hardening.

## 4. Verify

- [x] 4.1 `make test-core` — `XttyCore` regression (inline, cheap).
- [x] 4.2 **Update `packer/README.md`'s acceptance envelope** for the grown test count (the 3.2–3.4 injection tests add methods: 48 → the new total) so the validator's runtime read matches — do this *before* 4.3 (reverse-duty; else the validator flags the extra passes as unclassified).
- [x] 4.3 Run the **Tier-1 XCUITest suite + full acceptance matrix** (both goldens) and confirm the routing branches are regression-clean and the new momentum-injection tests (3.2–3.4) + the crisp negative (3.1) are green, 0 vacuous passes. ⟶ xtty-test-validator (Tier-1 + full matrix, both goldens) (First run OUT-OF-ENVELOPE: `testAccumulatedRemainderResetsBetweenGestures` red on the bash-golden **graphics** tier only — a test-side `routing()` read-freshness race, not a product regression; fixed test-side + re-verified red→green against the isolated D3 regression; re-delegated and **IN-ENVELOPE** on the re-run: `51/0/1` of 52 on all 5 environments including the previously-failing tier. See `packer/README.md` → "Graphics-tier read-freshness race found and fixed".)
- [x] 4.4 **MANUAL physical-trackpad verify-by-effect** (HUMAN-RUN — closes only what D8's injection cannot: the OS delivery/rate/back-pressure). On the shipped build, flick the trackpad under htop and confirm: (a) wheel reports **continue after finger-lift** during the coast — `cat -v` under `\e[?1002h\e[?1006h` shows `^[[<64/65…M` arriving after the fingers lift; (b) a fast gesture **does not lose distance**; (c) **htop stays interactive** (no pty-write / redraw back-pressure) — record the coast reports/sec; (d) local scrollback and an alt-screen pager also coast; (e) an xtty-vs-iTerm2 A/B on the same flick. (Step (a)/(b)'s byte-level technique — `printf '\033[?1002h\033[?1006h'; cat -v` — was successfully manually confirmed by the user and, at their request, captured as an automated regression guard: task 3.5, `testInjectedMomentumFrameProducesRealSGRMouseReportBytes`. That automates only "given a momentum event, are the bytes correct" — (c)/(d)/(e) and the OS actually *delivering* a real coast remain outstanding, HUMAN-RUN only.) **All 5 sub-checks confirmed by the user, corroborated with live DEBUG-dump/routing-log capture (a peekaboo-driven harness — `-UITestGridDump` state/grid dumps polled + logged, no hardware-in-the-loop automation): (a) confirmed earlier in-session. (b) a hard flick in `seq 1 100000 | less --mouse` covered ~219 lines in ~1.6s vs. an earlier gesture's ~209 lines over ~3.6s — no cap-truncation pattern. (c) htop's redraw + keypress response stayed immediate through a sustained coast (real momentum frames observed, `branch:"report"` throughout). (d) both local scrollback (`branch:"scrollback"`) and the alt-screen pager (`branch:"cursorKey"`, `keyForm:"application"`) coasted naturally, including under a direction reversal, with zero redraw corruption (unlike the htop/SGR-report path — see below). (e) same coast feel on xtty vs. a real iTerm2 window running the same htop, confirmed via a live `screencapture -x -D 1` A/B mid-session. Step (e) also re-triggered the already-tracked, out-of-scope `scroll-reversal-redraw-corruption-forensics.md` finding on the xtty side only (iTerm2 stayed clean in the same screenshot) — a fresh real-hardware corroboration of an existing tracked issue, not a new blocker.**
- [x] 4.5 **Re-run the VM full acceptance matrix** (both goldens) to measure task 3.5's new byte-level SGR test alongside the rest of the suite (suite 48 → 53; task 4.3's `51/0/1` of 52 predates 3.5 and only covers local Tier-1 for it — `11/0/0`, per `packer/README.md`). Update `packer/README.md`'s envelope from "local Tier-1 confirmed, VM re-run pending" to a measured full-matrix result once complete. ⟶ xtty-test-validator (full matrix, both goldens, re-run for 3.5) (Measured `52/0/1` of 53 on Tier-1 local, headless-bash, graphics-bash, graphics-zsh — **all 11 `XttyMouseWheelUITests` methods green on all 5 environments, 0 vacuous passes**, closing this change's own VM-matrix gap. One **unrelated** red surfaced on headless-zsh: `XttyMultiplexingUITests.testNewTabOpensAndLastPaneCloseEscalates` — root-cause-traced (not re-run) to a pre-existing, still-open latent race class distinct from this change (the confirm-close `hasForegroundJob` refuted-signal class `harden-churn-shell-readiness` fixed for the churn test only, never applied to this test); confirmed **not** the retired menu-clobber class (the menu was installed and dispatching — Cmd+T succeeded on it moments before); confirmed **not caused by** this change's diff (purely additive `#if DEBUG` injection-hook code, nothing touching tab/window/close logic). Recorded durably in `packer/README.md`'s expected-difference matrix (Confirm-close row, item (c)) as a tracked, not-yet-fixed follow-up — does not block this change.)
- [x] 4.6 **Re-run the VM full acceptance matrix** (both goldens) to measure task 3.6's new real-pager integration test (suite 53 → 54; task 4.5's `52/0/1` of 53 predates 3.6 and only covers local Tier-1 for it — `12/0/0`, per `packer/README.md`). `less --mouse` is confirmed present + identical (`less 668`) on both VM goldens via direct SSH check, so no environment-support risk is expected, but the actual VM behavior (does `less --mouse`'s own redraw genuinely advance under the VM's constrained CPU / windowed compositor, matching the graphics-tier timing sensitivity already observed once this session) is unverified until measured. ⟶ xtty-test-validator (full matrix, both goldens, re-run for 3.6) (Measured: `testWheelScrollsRealMouseTrackingPager` itself is **green on all 5 environments including both graphics-zsh samples** — the design.md D9 discriminator (`branch == "report"` vs `after == nil`) never fired, confirming the post-adversarial-review hardening (task 3.6) held under real VM conditions. The sweep as a whole returned **OUT-OF-ENVELOPE**, not from wheel-routing logic: graphics-zsh hit an **unrelated** XCUITest runner-level stall — an unrelated test hung ~930s inside XCUITest's own `typeText()` synthesis, tripling the suite's runtime and starving a short fixed timeout on a *different* wheel test in each of 2 independent samples (`testAccumulatedRemainderResetsBetweenGestures` in one, `testInjectedMomentumFrameProducesRealSGRMouseReportBytes` in the other). Confirmed **not caused by this change**: graphics-bash ran the identical `#if DEBUG` injection code clean in the same sweep, and the stall site is inside the runner's own event-synthesis internals, a path this change's diff never touches. Per advisor consultation: padding the collateral timeouts to survive an unbounded ~930s stall was considered and rejected (no fixed wait survives it; it would only relocate the casualty and ~3× routine graphics-zsh runtime for no signal), and a third full-matrix re-run was rejected as unwarranted wash-out re-running (2 independent samples already corroborate the same root cause). Recorded durably in `packer/README.md`'s expected-difference matrix as a new tracked, open, not-yet-root-caused environmental anomaly — does not block this change; not yet proposed as a follow-up change.)

## 5. Upstream (deferred)

- [x] 5.1 Prepare the upstream SwiftTerm PR delta (the pure branch logic + this momentum/accumulation correction; the `momentum`/`precise` observability + injection hook stripped) — **DEFERRED to a maintainer action** (outward-facing; not filed unilaterally), extending the routing fix's retire-on-merge plan. (The delta + retire-on-merge extension is recorded in `design.md` D7/D8 + Migration Plan step 4, mirroring `fix-scroll-wheel-mouse-reporting`'s 5.1 precedent. **Actual PR filing deferred to a maintainer action** — no PR link yet.)

## 6. Coherence + archive (the standard change tail)

- [ ] 6.1 Pre-archive coherence review of this change. ⟶ xtty-openspec-critic (smooth-scroll-wheel-momentum)
- [ ] 6.2 Archive + reconcile: `openspec archive`; correct the merged requirement text to what actually shipped; reconcile the trackers (AGENTS Current-status row + snapshot, HISTORY.md narrative, `research/04-design/02-milestones.md`, and — **contingent on the 4.4 manual verify passing** — a Learned-refutations one-liner "don't drop momentum / cap-and-discard / time-throttle the wheel"); confirm `packer/README.md`'s envelope reflects the final test count (per 4.2); verify against disk (`openspec list` / `ls openspec/changes/archive/` / `ls openspec/specs/`). ⟶ archive-ritual
