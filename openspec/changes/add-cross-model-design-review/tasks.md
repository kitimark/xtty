## 1. Critic code-accuracy pass + gate-task check (coherence-review MODIFY)

- [ ] 1.1 Add a **Pass 4 — design-claim code-accuracy** to `.claude/agents/xtty-openspec-critic.md`'s single-change pass: extract claims about **existing** symbols from `design.md`/`tasks.md` (`` `Type.field` ``, `` `File.swift` ``, `"mirrors `X`"`, symbol names), grep the source, flag an **existing** claimed symbol that is **absent/contradicted** as BLOCKER and a fuzzy/unlocatable claim as REVIEW. Bound it to the *existing* codebase — a **new/to-be-created** symbol is exempt (design D6); state in the agent that Pass 4 therefore does **not** catch a misplaced *new* field (that class stays a cross-model catch) and that it checks **in-repo symbols only** — blind to external-dependency claims (design D6).
- [ ] 1.2 Add the **gate-task-presence check** (REVIEW-severity) to the same single-change pass: for an **in-scope** change (product code, a spec delta beyond docs/tooling, or the review/verification/CI tooling itself), verify the `tasks.md` tail carries the **blocking human-attestation cross-review task** with its **model-STOP/HUMAN-ONLY** wording; report a **REVIEW** (never BLOCKER) when absent or missing that wording, so the gate-task convention reaches change authoring (design D6).
- [ ] 1.3 Bump the agent stamp to **`Definition: v4 (<date>)`** and **repair the launcher's stale delivery-check stamp `v1 → v4`** in `.claude/commands/xtty/review.md` (it currently expects `v1` while the agent is `v3`, so it false-rejects every valid review).
- [ ] 1.4 Verify by effect (`/xtty:review`): a wrong claim about an **existing** symbol → BLOCKER naming the claim + contradicting source; a change whose existing-symbol claims match → not flagged; a *new* field on the wrong owner → **not** flagged (honest scope); and an in-scope change whose tail lacks the human-attestation task → REVIEW.

## 2. Cross-model review worker (cross-model-review)

- [ ] 2.1 Author `.claude/commands/xtty/cross-review.md` as the **main-loop protocol**: given an in-scope change, launch **Pass B** (`codex-companion.mjs adversarial-review`) in the **background via the harness's Bash `run_in_background`** — the companion's own `--background` is a **no-op for `adversarial-review`** — owned by the main loop, never a subagent (the strand refutation); run **Pass A** (the Opus critic) and **Pass C** (an inline Opus soundness `Agent` call mirroring B's lens, emitting the real `review-output` schema) as subagents, then **await Pass B's completion re-invoke before merging** (concurrent passes, barrier merge — design D3). State that `/xtty:cross-review` is **user-launched only** and that **one launch authorizes the whole bounded run** (up to N rounds of paid Pass B); spend is human-initiated **per run** (design D4).
- [ ] 2.2 Specify **portable location + the recorded-base deterministic range** for Pass B: resolve the companion by globbing `~/.claude/plugins/cache/openai-codex/codex/*/scripts/codex-companion.mjs`, picking the highest version **numerically** (`1.0.10 > 1.0.6`); an absent companion is a **reported skip** per 2.5, not a fail. Scope by **substrate, not focus text**: base **B** = the change's **recorded review base** (`openspec/changes/<name>/review-base`, written at change creation before implementation — design D5, **no ownership trailers/manifest**); **fail closed with a named error** — never a guessed, narrowed, or rewritten range — when the record is **absent, unresolvable, not an ancestor of HEAD, failing the range-completeness invariant (a base — original or re-recorded — later than the change's creation commit; design D5, Round 4), or modified after its introducing commit** (the base-immutability rule, design D5); the modified-record refusal is the **default only** — an explicit human-invoked re-anchor acknowledgment (`--ack-re-anchor`, design D5) proceeds while loudly naming the record's full modification history **and the range-completeness evidence (the derived creation commit, the verified base-precedes-creation bound, and the included pre-creation commits)** in the ledger header (the re-record recovery; the other arms — absent, unresolvable, non-ancestor, and range-completeness — are never liftable); document honestly that work committed *before the recorded base* (before the change existed) is invisible to the range; that a base later than the change's **creation commit** is **mechanically refused** by the range-completeness invariant (git-computable ⇒ deleted-mechanically class, **not** an R3 residual — design D5, Round 4); and that only a false base within the pre-creation window stays at the human boundary (deliberate falsification there being the R3 affirmative-act class — design D5); require a clean tree; checkpoint-commit each round; run `adversarial-review --base B`. **Name `B`, HEAD, and the reviewed file list in the ledger header** so the reviewed scope is transparent to the human (design D5). Do **not** attempt to prove per-commit ownership — the deleted trailer machinery could not even dogfood itself; a single recorded git anchor + transparency replaces the assertion chain because the worker has no authority.
- [ ] 2.3 Specify the **advisory union-with-heuristic-consensus ledger**: **Pass C emits Codex's real `review-output` JSON schema** (per-finding `severity/title/body/file/line_start/line_end/confidence/recommendation` — **no `line`**, `additionalProperties:false`); merge findings tagged by source; flag a finding raised by **both** B and C as a **heuristic** consensus signal (a semantic judgment, not a mechanical join). State that the ledger — and every consensus flag, coverage description, and recorded dismissal in it — is **advisory in its entirety**; it MAY be persisted as a **gate-inert** `cross-review-ledger.md`, and **nothing downstream reads it mechanically** (design D3).
- [ ] 2.4 Specify the **bounded, non-gating convergence loop**: each round adjudicate every finding as fix | dismiss-with-recorded-rationale, apply fixes, re-review; bound at **N = 2 rounds**, then escalate the residual. On **every run**, surface the **full ledger including all dismissals + rationales** to the human. State explicitly that **"converged" carries no gate force, a dismissal is a proposal (not an accepted state), and the worker produces no receipt or reviewed-state binding** — archive eligibility lives entirely in the human gate (design D4/D7).
- [ ] 2.5 Specify **detectable, non-gating degradation**: read `setup --json` fields (`node.available`/`codex.available`/`auth.loggedIn`; `status` has **no `ready`**) only to decide **run Pass B or skip-and-report** — resolvable companion + available + logged-in → run; otherwise **skip, complete on the remaining passes, and state in the header which passes ran, which did not, and why**. A present-but-broken integration MAY be surfaced as a header warning. **No availability state, coverage label, or verdict auto-satisfies or auto-blocks archive** — the prior iteration's 3-way hard-fail/auto-satisfy classifier is deleted (design D5).
- [ ] 2.6 Specify **model-family diversity of the soundness passes**: Pass B on the codex CLI's GPT-family model (**`gpt-5.6-sol`**, design D9), Passes A/C on **Opus**; pin the **model** explicitly at invocation (`--model gpt-5.6-sol`) — the companion exposes **no per-call effort flag**, so Pass B's expected **`xhigh`** effort is codex-config-governed, not an invocation pin — and **record the effective model of each soundness pass, and its effort where determinable (Pass C invocation-controlled; Pass B from config when readable, else marked undetermined), in the ledger header**. A divergent/unverifiable local config yields an **honestly-labeled header** (not a blocked gate); a skipped Pass B → the header says **single-model** (design D9).
- [ ] 2.7 Confirm the command is committed (un-ignored) alongside the other `.claude/commands/xtty/` tooling (`.gitignore` allowlist covers it).
- [ ] 2.8 Verify the worker by effect: run `/xtty:cross-review` on a real in-scope change (tri-pass fires; the ledger merges with a heuristic consensus flag; the header names the range + both soundness models/effort; the loop converges/escalates; the full ledger is surfaced). Then exercise the **Codex-absent** path — move the companion out of the glob path and/or strip `codex` from PATH — and confirm the run **completes on the remaining passes, the header says which passes ran and why Pass B did not, and nothing downstream reacts** (no gate is satisfied or blocked). Make the semver-resolution check **non-vacuous** (a throwaway second version dir, or a unit-test on a synthetic version list, proving `1.0.10 > 1.0.6`).

## 3. Human-gate plumbing (cross-model-review)

- [ ] 3.1 Author the **committed deterministic reviewed-state digest tool** under `scripts/` (design D8): given a change name, resolve base **B** from the change's **recorded review base** (`openspec/changes/<name>/review-base` — **refusing with a named error** when the record is absent, unresolvable, not an ancestor of HEAD, or failing the **range-completeness invariant**: B must be a strict ancestor of the change's creation commit, the earliest commit in HEAD's history touching `openspec/changes/<name>/` — design D5, Round 4), **refuse on a dirty tree/index**, and print a digest of `git diff B..HEAD` **excluding exactly** the bookkeeping surfaces — the advisory ledger and the `tasks.md` attestation/tick surface (the **delimited attestation line** + checkbox-state ticks); the `review-base` record itself is **not** a bookkeeping surface (a post-attestation base rewrite changes the digest) — so recording the attestation cannot self-invalidate the digest; and **refuse with a named error when the record was modified after its introducing commit** (the base-immutability rule, design D5/D8 — a *pre*-attestation narrowing rewrite never reaches the digest arithmetic, so it must be refused up front); that refusal is the **default only** — an explicit human-invoked re-anchor acknowledgment (`--ack-re-anchor`) lifts just the modified-record arm (never absent/unresolvable/non-ancestor/range-completeness) and prints every post-introduction commit touching the record **plus the range-completeness evidence (the derived creation commit, the verified base-precedes-creation bound, and the included pre-creation commits)** loudly alongside the digest, so the re-record recovery's fresh attestation is computable and the human's re-anchor verification covers completeness as well as ownership (design D5/D8). Deterministic; trust class of `git`/`openspec`.
- [ ] 3.2 Author the **committed mechanical scope classifier** under `scripts/` (design D5/D7): given a change name, compute the range's changed paths over the **same recorded review base** the review and digest use (`git diff --name-only B..HEAD`) and classify against a **fixed allowlist of documentation/tracker surfaces** — **any path outside the allowlist ⇒ in scope; an unrecognized or ambiguous path ⇒ in scope (fail-closed); an absent, unresolvable, or non-ancestor recorded base ⇒ in scope (fail-closed); a recorded base failing the range-completeness invariant (later than the change's creation commit) ⇒ in scope (fail-closed); a `review-base` record modified after its introducing commit ⇒ in scope (fail-closed) regardless of the allowlist patterns** (the record is allowlist-eligible only in its introducing commit — design D5/D8, closing the out-of-scope flip a pre-attestation base rewrite could otherwise buy) — never classify a change out of scope on a range that could not be resolved or whose anchor was rewritten. Deterministic, path-based, **no model judgment**; the script is the authority on the allowlist patterns.
- [ ] 3.3 Establish the **review-base recording convention** (design D5): at change creation — **before implementation work begins** — write the current HEAD's full sha to `openspec/changes/<name>/review-base` (one line) and commit it with the proposal; the record is **immutable from that introducing commit onward** — byte-identical through HEAD, never edited (a needed re-anchor, e.g. after a rebase that makes the sha unresolvable, is a deliberate human re-record that leaves the modification permanently visible in history and moves the change onto D8's human-anchored bootstrap boundary — and is itself bounded by the **range-completeness invariant**: any base, original or re-recorded, MUST be a strict ancestor of the change's creation commit, so a re-anchor can never silently exclude the change's committed lifetime — design D5, Round 4); document the one-liner + the immutability and range-completeness rules in the AGENTS.md gate convention (§3.5). **Record this change's own review base retroactively** (it predates the tooling): the parent of its `proposal.md`-introducing commit, chosen and verified by a human against `git log` — acceptable here under D8's bootstrap boundary, since this tooling-introducing change is anchored by human code review anyway; retroactive recording simply sets the introduction point (byte-identical-since-introduction holds trivially from there).
- [ ] 3.4 Add to the hand-authored **`⟶ archive-ritual`** procedure in `AGENTS.md` a **step 0** that first **invokes the committed scope classifier (§3.2) to decide applicability** over the recorded-base range — never a semantic call — and, for an in-scope change, **FAILS CLOSED** unless all five hold (design D7/D8): (1) the human attestation exists and is well-formed on its **delimited attestation line**; (2) the reviewed-state digest **recomputes** and equals the attested value; (3) the tree/index is clean; (4) **exempt-by-act** — the attestation line appears **exactly once** and stays **byte-identical from its introducing commit through archive** (any later add/delete/modify of it invalidates the attestation → fresh review + attestation); the introducing commit's `tasks.md` hunk is confined to the **delimited line + checkbox ticks**, and every later commit touches only the remaining bookkeeping surfaces (no reviewed artifact, no gate implementation); (5) **base-anchor immutability** — the recorded `review-base` is **byte-identical from its introducing commit through HEAD** (exactly one commit in history touches it); a post-introduction add/delete/rewrite — pre- or post-attestation alike — is an unmet precondition, recoverable only by a deliberate human re-record + fresh review and attestation over the re-anchored range (the check that reaches the pre-attestation narrowing window checks 2 and 4 cannot — design D5/D8) — a recovery that never mechanically re-passes check 5 (the re-record is itself a post-introduction touch): document the **re-anchor boundary** — checks 1–4 verified mechanically over the re-anchored range (digest via `--ack-re-anchor`), check 5's failure reported naming every record-touching commit, the model MUST STOP on it, and only the human, having verified each named commit is their own deliberate re-record **and** that the re-anchored base predates all the change's work — range completeness, whose creation-onward half is mechanically guaranteed by D5's range-completeness invariant (a base later than the change's creation commit is refused by every consumer under an arm `--ack-re-anchor` never lifts, the tooling surfacing the derived creation commit + verified bound at this boundary), leaving only pre-creation work to the human's judgment — proceeds past it (the same human-anchored bootstrap class as the tooling-introducing change — design D5/D8). The step runs the digest/scope tooling **from the reviewed-base version wherever a prior committed version exists — including when the change under archive *modifies* that tooling** (only a tooling-*introducing* change like this one has none — the stated anchor there is human code review of the scripts, design D8). The check reads **no model-authored artifact** (inputs: the human's line, the git-computed digest, git ground truth). Note the **optional-not-default** signing strong form (`verify-commit` when the attestation is signed).
- [ ] 3.5 Document the **gate convention** in `AGENTS.md`: the **review-base recording rule** (§3.3 — recorded at change creation before implementation; **immutable — byte-identical from its introducing commit through HEAD**; **complete — a strict ancestor of the change's creation commit (the range-completeness invariant)**; resolved identically by review, digest, and scope; fail-closed when absent, unresolvable, not an ancestor of HEAD, later than the change's creation, or modified after its introduction); the **mechanical path-based scope classifier** (fixed docs/tracker allowlist, computed over the recorded-base range; outside ⇒ in scope; ambiguous ⇒ in scope, fail-closed; purely-allowlisted docs/tracker/rename changes stay critic-only); the **human-attestation task template** — placed after `⟶ xtty-openspec-critic` and before `⟶ archive-ritual`, **HUMAN-ONLY / model-MUST-STOP**, ticked only by a direct human act recording the reviewed-state digest on the **delimited attestation line**, **no** auto-firing `⟶ xtty-cross-review` marker; and the archive step-0 checks incl. the reviewed-base tooling rule + its bootstrap boundary (§3.4) + the **re-anchor boundary** (a re-recorded base never mechanically re-passes check 5; the change archives only on the human's verification of the record's ownership history **and the range's completeness** — its creation-onward half mechanically enforced by the range-completeness invariant — the worker/digest `--ack-re-anchor` path making the fresh review + attestation computable while surfacing the completeness evidence). Also add the **worker protocol** (passes, advisory ledger + header contract, **complete-ledger-every-run surfacing**, bounded rounds, one-launch spend, model-family diversity, detectable non-gating degradation) and the two-axes framing (conformance vs soundness; Pass 4 fact-checks *stated existing-symbol claims*, a bounded in-repo check, not open-ended code soundness).
- [ ] 3.6 Update `AGENTS.md`'s committed-tooling bullet + the agents/launchers table to list `/xtty:cross-review`, the digest tool, and the scope classifier, noting Pass C is inline (emitting the common schema), not a standing agent (anti-roster refutation).
- [ ] 3.7 Verify by inspection: `AGENTS.md` states the worker protocol (incl. complete-ledger-every-run), the review-base recording rule (incl. its since-introduction immutability and the range-completeness invariant), the mechanical path-based scope classifier (over the recorded-base range, incl. the rewritten-base-forces-in-scope and completeness-failure-forces-in-scope arms) + pre-archive timing, the human-attestation gate convention (critic tail marker retained; the cross-review gate is its own HUMAN-ONLY blocking task with no auto-firing marker; the delimited-line digest tick), the archive step-0 fail-closed precondition (classifier-decided applicability; the five checks incl. exempt-by-act + base-anchor immutability + the reviewed-base rule + its bootstrap boundary + the re-anchor boundary (check 5 never mechanically re-passes; human proceeds on verified record ownership **and range completeness** via the acknowledgment path) + optional signing), the model-family diversity rule (GPT-family Pass B pinned by `--model`, effort config-governed; Opus A/C; skipped B → single-model header), and the detectable non-gating degradation behavior.
- [ ] 3.8 Verify the gate's **bypass resistance by effect** (fail-closed drills, complementing 5.3's live archive test): (a) **scope bypass** — a change whose range touches a product/tooling path **cannot** be classified out of scope, and an unrecognized/ambiguous path classifies **in scope**; (b) **attestation-commit bundling / rewrite** — an attestation commit that also modifies the digest/scope tooling, or edits a reviewed artifact, **fails the archive precondition closed**, and a *later* commit that adds, deletes, or modifies the delimited attestation line **invalidates the attestation** (fail closed); (c) **pre-proposal base bypass (the Round-1 hole, closed)** — a product/tooling commit landing after the recorded base but before the `proposal.md` commit, touching **no change-dir path**, lies **inside** the range: it is reviewed, digested, and drives the classifier **in scope** (it can no longer silently become the base and vanish from review, digest, and scope alike); (d) **unresolvable base** — an absent, unresolvable, or non-ancestor `review-base` makes every consumer **refuse with a named error** (never a guessed or narrower range), the classifier classify **in scope**, and the digest uncomputable, so the archive precondition cannot pass; (e) **post-attestation base rewrite** — a post-attestation commit that modifies `review-base` **invalidates the attestation** (digest mismatch + exempt-by-act + base-anchor immutability, fail closed); (f) **pre-attestation base rewrite (the Round-2 hole, closed)** — a commit **before** attestation that rewrites `review-base` from the true creation base to a later ancestor fails the archive precondition on **base-anchor immutability (check 5)** even though the digest recomputes cleanly and exempt-by-act holds over the narrowed range under a genuine human tick; every range consumer refuses the modified record with a named error; and the classifier forces **in scope** even when the narrowed range's changed paths are all allowlisted (the out-of-scope flip is closed); and (g) **legitimate re-anchor reaches archive (the Round-3 deadlock, closed)** — starting from an unresolvable or modified `review-base`, a deliberate human re-record leaves every default invocation still refusing (worker/digest) or forcing **in scope** (classifier) — no silent mechanical pass anywhere; the `--ack-re-anchor` path computes the fresh digest and runs the fresh review while loudly naming the record's modification history; and after a fresh human attestation over the re-anchored range, an archive attempt verifies checks 1–4 mechanically, reports check 5's failure naming the re-record commit, and reaches archive via the human's documented proceed-past on verified ownership **and completeness** — proving the recovery terminates in a reachable archive, never a permanent deadlock and never a mechanical pass of check 5; and (h) **too-late re-anchor refused (the Round-4 hole, closed)** — re-record a base that is a descendant of the change's creation commit (a base whose range already excludes implementation work): every range consumer refuses with the named range-completeness error **even under `--ack-re-anchor`** (the arm is never liftable), the classifier forces **in scope**, the digest stays uncomputable so no attestation can be produced, and an archive attempt never reaches a check-5 proceed-past over the narrowed range — while a *valid* re-anchor's acknowledgment output is confirmed to name the derived creation commit, the verified base-precedes-creation bound, and the included pre-creation commits (the completeness evidence the human's check-5 boundary reads).

## 4. Research capture

- [ ] 4.1 Capture `research/03-analysis/cross-model-design-review-*.md`: the complementary-axes finding with **two worked exemplars** — (a) critic COHERENT ↔ Codex caught the `XttyConfigSet` config bug on `add-git-diff-wrap-toggle`; (b) this session's **six-round refutation of the self-policing automated gate** and the live Fable-5 + `gpt-5.6-sol` fan-out that re-architected it (why authority-free worker + human gate; why signing is optional-not-default; the two failure classes). Index it in `research/README.md`.

## 5. Land the change (dogfooded on this change)

- [ ] 5.1 Pre-archive coherence review of this change. ⟶ xtty-openspec-critic (add-cross-model-design-review)
- [ ] 5.2 **BLOCKING human-attestation cross-review — HUMAN-ONLY, model MUST STOP.** The **human** (deliberately, never auto-fired by reaching this task) runs `/xtty:cross-review add-cross-model-design-review` to dogfood the worker built in groups 1–3 (this change is in scope **mechanically** — its range touches `scripts/` + `.claude/` tooling paths outside the docs/tracker allowlist), reads the complete ledger, runs the digest tool, and ticks this task **recording the reviewed-state digest on the delimited attestation line** (the committed template's stable marker). The model SHALL NOT tick this task or compute the digest value. **Run the dogfood in a fresh session after the v4 critic is committed** (or gate on an explicit `Definition: v4` delivery check) — an in-session dogfood may be served the stale v3 critic (design R9). This is the first real attestation and the gate's acceptance test (design R10).
- [ ] 5.3 Archive + reconcile — **step-0 precondition (design D7/D8): first run the committed scope classifier (⇒ in scope for this change), then the fail-closed check must pass** (attestation present + well-formed on its delimited line; reviewed-state digest recomputes to the attested value; clean tree; exempt-by-act — the attestation line singular and byte-identical since its introducing commit, that commit's `tasks.md` hunk confined to the delimited line + ticks, every later commit bookkeeping-only; base-anchor immutability — the recorded `review-base` byte-identical since its introducing commit; tooling run per D8's bootstrap boundary — this change introduces the scripts, so the anchor is their human code review) **immediately before** merging (no intervening commits between the step-0 pass and the merge); separately confirm the gate **fails closed** on a deliberately stale/rewritten attestation (the by-effect proof of R8/R10). Then merge the spec deltas (`openspec archive`), fill the new `cross-model-review` spec Purpose, tick trackers (Current-status row + snapshot + HISTORY narrative + established-specs line + milestone), add the self-policing-gate refutation to the Learned-refutations list, and verify-against-disk — keeping these post-gate commits **confined to the committed ritual's enumerated archive surfaces** (the spec merge, the Purpose fill, the tracker documents): they mutate the repo after the digest was verified, the accepted R11 TOCTOU residual (the ritual's own mechanical `openspec`-CLI merge + bookkeeping, the R3/R7 trust class), and R11's hardening knob — re-running the digest tool over the final merged candidate and confirming the post-gate delta is archive-surfaces-only — MAY be exercised here. ⟶ archive-ritual
