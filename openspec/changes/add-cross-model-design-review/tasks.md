## 1. Critic code-accuracy pass + gate-task check (coherence-review MODIFY)

- [x] 1.1 Add a **Pass 4 — design-claim code-accuracy** to `.claude/agents/xtty-openspec-critic.md`'s single-change pass: extract claims about **existing** symbols from `design.md`/`tasks.md` (`` `Type.field` ``, `` `File.swift` ``, `"mirrors `X`"`, symbol names), grep the source, flag an **existing** claimed symbol that is **absent/contradicted** as BLOCKER and a fuzzy/unlocatable claim as REVIEW. Bound it to the *existing* codebase — a **new/to-be-created** symbol is exempt (design D6); state in the agent that Pass 4 therefore does **not** catch a misplaced *new* field (that class stays a cross-model catch) and that it checks **in-repo symbols only** — blind to external-dependency claims (design D6).
- [x] 1.2 Add the **gate-task-presence check** (REVIEW-severity) to the same single-change pass: for an **in-scope** change (product code, a spec delta beyond docs/tooling, or the review/verification/CI tooling itself), verify the `tasks.md` tail carries the **blocking human-attestation cross-review task** with its **model-STOP/HUMAN-ONLY** wording; report a **REVIEW** (never BLOCKER) when absent or missing that wording, so the gate-task convention reaches change authoring (design D6).
- [x] 1.3 Bump the agent stamp to **`Definition: v4 (<date>)`** and **repair the launcher's stale delivery-check stamp `v1 → v4`** in `.claude/commands/xtty/review.md` (it currently expects `v1` while the agent is `v3`, so it false-rejects every valid review).
- [x] 1.4 Verify by effect (`/xtty:review`): a wrong claim about an **existing** symbol → BLOCKER naming the claim + contradicting source; a change whose existing-symbol claims match → not flagged; a *new* field on the wrong owner → **not** flagged (honest scope); and an in-scope change whose tail lacks the human-attestation task → REVIEW. — *DONE (fresh session, v4 critic delivery-checked). Two throwaway fixtures: `zz-verify-pass4-blocker` claimed **existing** `XttyProfile.gitReviewLayout`/`.confirmClose` (both actually on `XttyConfigSet`) → **BLOCKER** naming both + `XttyProfile.swift:57,60`, and its in-scope tail missing the human-attestation task → **REVIEW**; `zz-verify-pass4-clean` matched `XttyConfigSet.gitReviewLayout`/`.confirmClose` → not flagged, a **new** `diffWrap` on the wrong owner → **not flagged** (honest-scope boundary held), gate task present → not flagged. All four arms confirmed; fixtures deleted.*

## 2. Cross-model review worker (cross-model-review)

- [x] 2.1 Author `.claude/commands/xtty/cross-review.md` as the **main-loop protocol**: given an in-scope change, launch **Pass B** (`codex-companion.mjs adversarial-review`) in the **background via the harness's Bash `run_in_background`** — the companion's own `--background` is a **no-op for `adversarial-review`** — owned by the main loop, never a subagent (the strand refutation); run **Pass A** (the Opus critic) and **Pass C** (an inline Opus soundness `Agent` call mirroring B's lens, emitting the real `review-output` schema) as subagents, then **await Pass B's completion re-invoke before merging** (concurrent passes, barrier merge — design D3). State that `/xtty:cross-review` is **user-launched only** and that **one launch authorizes the whole bounded run** (up to N rounds of paid Pass B); spend is human-initiated **per run** (design D4).
- [x] 2.2 Specify **portable location + the deterministic best-effort range** for Pass B: resolve the companion by globbing `~/.claude/plugins/cache/openai-codex/codex/*/scripts/codex-companion.mjs`, picking the highest version **numerically** (`1.0.10 > 1.0.6`); an absent companion is a **reported skip** per 2.5, not a fail. Scope by **substrate, not focus text**: base **B** = the **parent of the commit that added the change's `proposal.md`** (a positional git rule, **no ownership trailers/manifest**); **assert fail-closed that the `proposal.md`-introducing commit is the *first* commit touching `openspec/changes/<name>/`** (an earlier change-dir commit ⇒ refuse with a named error, never silently narrow the range), and document honestly that pre-proposal commits *outside* the change dir are a known limitation the human catches via the header's named file list (design D5); require a clean tree; checkpoint-commit each round; run `adversarial-review --base B`. **Name `B`, HEAD, and the reviewed file list in the ledger header** so the reviewed scope is transparent to the human (design D5). Do **not** attempt to prove per-commit ownership — the deleted trailer machinery could not even dogfood itself; transparency replaces the assertion chain because the worker has no authority.
- [x] 2.3 Specify the **advisory union-with-heuristic-consensus ledger**: **Pass C emits Codex's real `review-output` JSON schema** (per-finding `severity/title/body/file/line_start/line_end/confidence/recommendation` — **no `line`**, `additionalProperties:false`); merge findings tagged by source; flag a finding raised by **both** B and C as a **heuristic** consensus signal (a semantic judgment, not a mechanical join). State that the ledger — and every consensus flag, coverage description, and recorded dismissal in it — is **advisory in its entirety**; it MAY be persisted as a **gate-inert** `cross-review-ledger.md`, and **nothing downstream reads it mechanically** (design D3).
- [x] 2.4 Specify the **bounded, non-gating convergence loop**: each round adjudicate every finding as fix | dismiss-with-recorded-rationale, apply fixes, re-review; bound at **N = 2 rounds**, then escalate the residual. On **every run**, surface the **full ledger including all dismissals + rationales** to the human. State explicitly that **"converged" carries no gate force, a dismissal is a proposal (not an accepted state), and the worker produces no receipt or reviewed-state binding** — archive eligibility lives entirely in the human gate (design D4/D7).
- [x] 2.5 Specify **detectable, non-gating degradation**: read `setup --json` fields (`node.available`/`codex.available`/`auth.loggedIn`; `status` has **no `ready`**) only to decide **run Pass B or skip-and-report** — resolvable companion + available + logged-in → run; otherwise **skip, complete on the remaining passes, and state in the header which passes ran, which did not, and why**. A present-but-broken integration MAY be surfaced as a header warning. **No availability state, coverage label, or verdict auto-satisfies or auto-blocks archive** — the prior iteration's 3-way hard-fail/auto-satisfy classifier is deleted (design D5).
- [x] 2.6 Specify **model-family diversity of the soundness passes**: Pass B on the codex CLI's GPT-family model (**`gpt-5.6-sol`**, design D9), Passes A/C on **Opus**; pin the **model** explicitly at invocation (`--model gpt-5.6-sol`) — the companion exposes **no per-call effort flag**, so Pass B's expected **`xhigh`** effort is codex-config-governed, not an invocation pin — and **record the effective model of each soundness pass, and its effort where determinable (Pass C invocation-controlled; Pass B from config when readable, else marked undetermined), in the ledger header**. A divergent/unverifiable local config yields an **honestly-labeled header** (not a blocked gate); a skipped Pass B → the header says **single-model** (design D9).
- [x] 2.7 Confirm the command is committed (un-ignored) alongside the other `.claude/commands/xtty/` tooling (`.gitignore` allowlist covers it).
- [x] 2.8 Verify the worker by effect: run `/xtty:cross-review` on a real in-scope change (tri-pass fires; the ledger merges with a heuristic consensus flag; the header names the range + both soundness models/effort; the loop converges/escalates; the full ledger is surfaced). Then exercise the **Codex-absent** path — move the companion out of the glob path and/or strip `codex` from PATH — and confirm the run **completes on the remaining passes, the header says which passes ran and why Pass B did not, and nothing downstream reacts** (no gate is satisfied or blocked). Make the semver-resolution check **non-vacuous** (a throwaway second version dir, or a unit-test on a synthetic version list, proving `1.0.10 > 1.0.6`). — *DONE. **SEMVER** (Scenario G: `sort -V` picks `1.0.10 > 1.0.9 > 1.0.6`; lexical would wrongly pick `1.0.9`). **Live tri-pass** (this session's 2-round dogfood: A=v4 critic ‖ B=`gpt-5.6-sol`/`xhigh` ‖ C=inline Opus; ledger merged with the range+model header; loop converged/escalated; complete ledger surfaced + persisted as the gate-inert `cross-review-ledger.md`). **Codex-absent** (skip DETECTED both ways — unresolved companion glob → empty `COMPANION`; `codex` off PATH → `setup --json` reports `codex.available:false`/`loggedIn:false`; documented single-model header; "nothing downstream reacts" proven structurally — `scope.sh`/`digest.sh`/archive step-0 carry zero availability/Pass-B dependency). Honest scope: the skip DETECTION + no-downstream-reaction are verified; a full live A+C-only run emitting the single-model header was not re-run (a trivial composition of the A/C passes already exercised this session).*

## 3. Human-gate plumbing (cross-model-review)

- [x] 3.1 Author the **committed deterministic reviewed-state digest tool** under `scripts/` (design D8): given a change name, resolve base **B** (parent of the `proposal.md`-introducing commit, **asserting fail-closed** that it is the first commit touching the change dir — design D5), **refuse on a dirty tree/index**, and print a digest **over the content (at HEAD) of the `B..HEAD` range's changed files** **excluding exactly** the bookkeeping surfaces — the advisory ledger and the `tasks.md` attestation/tick surface (the **delimited attestation line** + checkbox-state ticks) — so recording the attestation cannot self-invalidate the digest. Deterministic; trust class of `git`/`openspec`.
- [x] 3.2 Author the **committed mechanical scope classifier** under `scripts/` (design D5/D7): given a change name, compute the range's changed paths (`git diff --name-only B..HEAD`) and classify against a **fixed allowlist of documentation/tracker surfaces** — **any path outside the allowlist ⇒ in scope; an unrecognized or ambiguous path ⇒ in scope (fail-closed)**. Deterministic, path-based, **no model judgment**; the script is the authority on the allowlist patterns.
- [x] 3.3 Add to the hand-authored **`⟶ archive-ritual`** procedure in `AGENTS.md` a **step 0** that first **invokes the committed scope classifier (§3.2) to decide applicability** — never a semantic call — and, for an in-scope change, **FAILS CLOSED** unless all four hold (design D7/D8): (1) the human attestation exists and is well-formed on its **delimited attestation line**; (2) the reviewed-state digest **recomputes** and equals the attested value; (3) the tree/index is clean; (4) **exempt-by-act** — the attestation line appears **exactly once** and stays **byte-identical from its introducing commit through archive** (any later add/delete/modify of it invalidates the attestation → fresh review + attestation); the introducing commit's `tasks.md` hunk is confined to the **delimited line + checkbox ticks**, and every later commit touches only the remaining bookkeeping surfaces (no reviewed artifact, no gate implementation). The step runs the digest/scope tooling **from the reviewed-base version wherever a prior committed version exists — including when the change under archive *modifies* that tooling** (only a tooling-*introducing* change like this one has none — the stated anchor there is human code review of the scripts, design D8). The check reads **no model-authored artifact** (inputs: the human's line, the git-computed digest, git ground truth). Note the **optional-not-default** signing strong form (`verify-commit` when the attestation is signed).
- [x] 3.4 Document the **gate convention** in `AGENTS.md`: the **mechanical path-based scope classifier** (fixed docs/tracker allowlist; outside ⇒ in scope; ambiguous ⇒ in scope, fail-closed; purely-allowlisted docs/tracker/rename changes stay critic-only); the **human-attestation task template** — placed after `⟶ xtty-openspec-critic` and before `⟶ archive-ritual`, **HUMAN-ONLY / model-MUST-STOP**, ticked only by a direct human act recording the reviewed-state digest on the **delimited attestation line**, **no** auto-firing `⟶ xtty-cross-review` marker; and the archive step-0 checks incl. the reviewed-base tooling rule + its bootstrap boundary (§3.3). Also add the **worker protocol** (passes, advisory ledger + header contract, **complete-ledger-every-run surfacing**, bounded rounds, one-launch spend, model-family diversity, detectable non-gating degradation) and the two-axes framing (conformance vs soundness; Pass 4 fact-checks *stated existing-symbol claims*, a bounded in-repo check, not open-ended code soundness).
- [x] 3.5 Update `AGENTS.md`'s committed-tooling bullet + the agents/launchers table to list `/xtty:cross-review`, the digest tool, and the scope classifier, noting Pass C is inline (emitting the common schema), not a standing agent (anti-roster refutation).
- [x] 3.6 Verify by inspection: `AGENTS.md` states the worker protocol (incl. complete-ledger-every-run), the mechanical path-based scope classifier + pre-archive timing, the human-attestation gate convention (critic tail marker retained; the cross-review gate is its own HUMAN-ONLY blocking task with no auto-firing marker; the delimited-line digest tick), the archive step-0 fail-closed precondition (classifier-decided applicability; the four checks incl. exempt-by-act + the reviewed-base rule + its bootstrap boundary + optional signing), the model-family diversity rule (GPT-family Pass B pinned by `--model`, effort config-governed; Opus A/C; skipped B → single-model header), and the detectable non-gating degradation behavior.
- [x] 3.7 Verify the gate's **bypass resistance by effect** (fail-closed drills, complementing 5.3's live archive test): (a) **scope bypass** — a change whose range touches a product/tooling path **cannot** be classified out of scope, and an unrecognized/ambiguous path classifies **in scope**; (b) **attestation-commit bundling / rewrite** — an attestation commit that also modifies the digest/scope tooling, or edits a reviewed artifact, **fails the archive precondition closed**, and a *later* commit that adds, deletes, or modifies the delimited attestation line **invalidates the attestation** (fail closed); (c) **range omission** — a change directory with a commit predating its `proposal.md` commit makes the tooling **refuse** (fail closed), never silently narrow the range. — *DONE (deterministic drills): `test-cross-review-scripts.sh` 12/12 — (a) Scenario A/B (docs-only→out, scripts+.claude→in) + ambiguous⇒in; (b) Scenario E (reviewed-artifact edit changes the digest) + Scenario H (attestation rewrite `adds=2,dels=1` and delete `now=0,dels=1` both fail the exempt-by-act grep) + digest also covers a bundled tooling edit since `scripts/` is a reviewed path; (c) Scenario C (range-omission ⇒ scope+digest both refuse rc=2). The live archive by-effect proof is task 5.3.*

## 4. Research capture

- [x] 4.1 Capture `research/03-analysis/cross-model-design-review-*.md`: the complementary-axes finding with **two worked exemplars** — (a) critic COHERENT ↔ Codex caught the `XttyConfigSet` config bug on `add-git-diff-wrap-toggle`; (b) this session's **six-round refutation of the self-policing automated gate** and the live Fable-5 + `gpt-5.6-sol` fan-out that re-architected it (why authority-free worker + human gate; why signing is optional-not-default; the two failure classes). Index it in `research/README.md`.
- [x] 4.2 Capture the **mechanical-airtightness tar pit + the thin-claim correction (design D10 + Open Questions)**: the four-round convergence experiment that did **not** converge (each round's provenance hardening opened the next git-manipulation narrowing — pre-attestation rewrite → re-record deadlock → ownership-vs-completeness → directory-rename rename-blindness; 45% spec bloat; a round-4 regression of a round-1 guarantee), the finding that **mechanizing provenance integrity against a repo-controlling model is non-terminating** (the closable-bypass set for git history is effectively unbounded; every member reduces to the R3 residual), the resulting **thin-claim boundary** (mechanical checks = accident tripwires + human-legibility aids; adversarial resistance = the human reading the complete ledger + R3), and the second-order diminishing-returns finding (a dev tool is validated by building and using it, not by reviewing its spec forever) — following the capture-depth conventions (fates table of the four rounds; the evidence commits are the kept `converge Round 1..4` history). **Done at re-baseline:** `research/03-analysis/cross-model-review-tar-pit-forensics.md`, indexed in `research/README.md` (landed with the re-baselining commit).

## 5. Land the change (dogfooded on this change)

- [x] 5.1 Pre-archive coherence review of this change. ⟶ xtty-openspec-critic (add-cross-model-design-review) — *DONE (fresh session; v4 critic delivery-checked `Definition: v4 (2026-07-12)`, served==committed). Ran as Pass A of the 5.2 dogfood, both bounded rounds → **COHERENT** (round 2 confirmed the round-1 fixes clean); only REVIEW-severity notes (a mild test-precision flag; the pre-existing `AGENTS.md:196` 25-of-49→25-of-51 count drift, a §5.3 reconcile item). Detail in `cross-review-ledger.md`.*
- [ ] 5.2 **BLOCKING human-attestation cross-review — HUMAN-ONLY, model MUST STOP.** The **human** (deliberately, never auto-fired by reaching this task) runs `/xtty:cross-review add-cross-model-design-review` to dogfood the worker built in groups 1–3 (this change is in scope **mechanically** — its range touches `scripts/` + `.claude/` tooling paths outside the docs/tracker allowlist), reads the complete ledger, runs the digest tool, and ticks this task **recording the reviewed-state digest on the delimited attestation line** (the committed template's stable marker). The model SHALL NOT tick this task or compute the digest value. **Run the dogfood in a fresh session after the v4 critic is committed** (or gate on an explicit `Definition: v4` delivery check) — an in-session dogfood may be served the stale v3 critic (design R9). This is the first real attestation and the gate's acceptance test (design R10).
- [ ] 5.3 Archive + reconcile — **step-0 precondition (design D7/D8): first run the committed scope classifier (⇒ in scope for this change), then the fail-closed check must pass** (attestation present + well-formed on its delimited line; reviewed-state digest recomputes to the attested value; clean tree; exempt-by-act — the attestation line singular and byte-identical since its introducing commit, that commit's `tasks.md` hunk confined to the delimited line + ticks, every later commit bookkeeping-only; tooling run per D8's bootstrap boundary — this change introduces the scripts, so the anchor is their human code review) **before** merging; separately confirm the gate **fails closed** on a deliberately stale/rewritten attestation (the by-effect proof of R8/R10). Then merge the spec deltas (`openspec archive`), fill the new `cross-model-review` spec Purpose, tick trackers (Current-status row + snapshot + HISTORY narrative + established-specs line + milestone), add the self-policing-gate refutation to the Learned-refutations list, and verify-against-disk. ⟶ archive-ritual
