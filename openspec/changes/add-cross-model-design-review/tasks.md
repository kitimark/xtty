## 1. Critic code-accuracy pass + gate-task check (coherence-review MODIFY)

- [ ] 1.1 Add a **Pass 4 — design-claim code-accuracy** to `.claude/agents/xtty-openspec-critic.md`'s single-change pass: extract claims about **existing** symbols from `design.md`/`tasks.md` (`` `Type.field` ``, `` `File.swift` ``, `"mirrors `X`"`, symbol names), grep the source, flag an **existing** claimed symbol that is **absent/contradicted** as BLOCKER and a fuzzy/unlocatable claim as REVIEW. Bound it to the *existing* codebase — a **new/to-be-created** symbol is exempt (design D6); state in the agent that Pass 4 therefore does **not** catch a misplaced *new* field (that class stays a cross-model catch) and that it checks **in-repo symbols only** — blind to external-dependency claims (design D6).
- [ ] 1.2 Add the **gate-task-presence check** (REVIEW-severity) to the same single-change pass: for an **in-scope** change (product code, a spec delta beyond docs/tooling, or the review/verification/CI tooling itself), verify the `tasks.md` tail carries the **blocking human-attestation cross-review task** with its **model-STOP/HUMAN-ONLY** wording; report a **REVIEW** (never BLOCKER) when absent or missing that wording, so the gate-task convention reaches change authoring (design D6).
- [ ] 1.3 Bump the agent stamp to **`Definition: v4 (<date>)`** and **repair the launcher's stale delivery-check stamp `v1 → v4`** in `.claude/commands/xtty/review.md` (it currently expects `v1` while the agent is `v3`, so it false-rejects every valid review).
- [ ] 1.4 Verify by effect (`/xtty:review`): a wrong claim about an **existing** symbol → BLOCKER naming the claim + contradicting source; a change whose existing-symbol claims match → not flagged; a *new* field on the wrong owner → **not** flagged (honest scope); and an in-scope change whose tail lacks the human-attestation task → REVIEW.

## 2. Cross-model review worker (cross-model-review)

- [ ] 2.1 Author `.claude/commands/xtty/cross-review.md` as the **main-loop protocol**: given an in-scope change, launch **Pass B** (`codex-companion.mjs adversarial-review`) in the **background via the harness's Bash `run_in_background`** — the companion's own `--background` is a **no-op for `adversarial-review`** — owned by the main loop, never a subagent (the strand refutation); run **Pass A** (the Opus critic) and **Pass C** (an inline Opus soundness `Agent` call mirroring B's lens, emitting the real `review-output` schema) as subagents, then **await Pass B's completion re-invoke before merging** (concurrent passes, barrier merge — design D3). State that `/xtty:cross-review` is **user-launched only** and that **one launch authorizes the whole bounded run** (up to N rounds of paid Pass B); spend is human-initiated **per run** (design D4).
- [ ] 2.2 Specify **portable location + the deterministic best-effort range** for Pass B: resolve the companion by globbing `~/.claude/plugins/cache/openai-codex/codex/*/scripts/codex-companion.mjs`, picking the highest version **numerically** (`1.0.10 > 1.0.6`); an absent companion is a **reported skip** per 2.5, not a fail. Scope by **substrate, not focus text**: base **B** = the **parent of the commit that added the change's `proposal.md`** (a positional git rule, **no ownership trailers/manifest**); require a clean tree; checkpoint-commit each round; run `adversarial-review --base B`. **Name `B`, HEAD, and the reviewed file list in the ledger header** so the reviewed scope is transparent to the human (design D5). Do **not** attempt to prove per-commit ownership — the deleted trailer machinery could not even dogfood itself; transparency replaces the assertion chain because the worker has no authority.
- [ ] 2.3 Specify the **advisory union-with-heuristic-consensus ledger**: **Pass C emits Codex's real `review-output` JSON schema** (per-finding `severity/title/body/file/line_start/line_end/confidence/recommendation` — **no `line`**, `additionalProperties:false`); merge findings tagged by source; flag a finding raised by **both** B and C as a **heuristic** consensus signal (a semantic judgment, not a mechanical join). State that the ledger — and every consensus flag, coverage description, and recorded dismissal in it — is **advisory in its entirety**; it MAY be persisted as a **gate-inert** `cross-review-ledger.md`, and **nothing downstream reads it mechanically** (design D3).
- [ ] 2.4 Specify the **bounded, non-gating convergence loop**: each round adjudicate every finding as fix | dismiss-with-recorded-rationale, apply fixes, re-review; bound at **N = 2 rounds**, then escalate the residual. On **every run**, surface the **full ledger including all dismissals + rationales** to the human. State explicitly that **"converged" carries no gate force, a dismissal is a proposal (not an accepted state), and the worker produces no receipt or reviewed-state binding** — archive eligibility lives entirely in the human gate (design D4/D7).
- [ ] 2.5 Specify **detectable, non-gating degradation**: read `setup --json` fields (`node.available`/`codex.available`/`auth.loggedIn`; `status` has **no `ready`**) only to decide **run Pass B or skip-and-report** — resolvable companion + available + logged-in → run; otherwise **skip, complete on the remaining passes, and state in the header which passes ran, which did not, and why**. A present-but-broken integration MAY be surfaced as a header warning. **No availability state, coverage label, or verdict auto-satisfies or auto-blocks archive** — the prior iteration's 3-way hard-fail/auto-satisfy classifier is deleted (design D5).
- [ ] 2.6 Specify **model-family diversity of the soundness passes**: Pass B on the codex CLI's GPT-family model (**`gpt-5.6-sol`**, design D9) at **`xhigh`**, Passes A/C on **Opus**; pass the model **explicitly** (`--model gpt-5.6-sol`; the companion exposes no per-call effort flag) and **record the effective model + effort of each soundness pass in the ledger header**. A divergent/unverifiable local config yields an **honestly-labeled header** (not a blocked gate); a skipped Pass B → the header says **single-model** (design D9).
- [ ] 2.7 Confirm the command is committed (un-ignored) alongside the other `.claude/commands/xtty/` tooling (`.gitignore` allowlist covers it).
- [ ] 2.8 Verify the worker by effect: run `/xtty:cross-review` on a real in-scope change (tri-pass fires; the ledger merges with a heuristic consensus flag; the header names the range + both soundness models/effort; the loop converges/escalates; the full ledger is surfaced). Then exercise the **Codex-absent** path — move the companion out of the glob path and/or strip `codex` from PATH — and confirm the run **completes on the remaining passes, the header says which passes ran and why Pass B did not, and nothing downstream reacts** (no gate is satisfied or blocked). Make the semver-resolution check **non-vacuous** (a throwaway second version dir, or a unit-test on a synthetic version list, proving `1.0.10 > 1.0.6`).

## 3. Human-gate plumbing (cross-model-review)

- [ ] 3.1 Author the **committed deterministic reviewed-state digest tool** under `scripts/` (design D8): given a change name, resolve base **B** (parent of the `proposal.md`-introducing commit), **refuse on a dirty tree/index**, and print a digest of `git diff B..HEAD` **excluding exactly** the advisory ledger and the `tasks.md` attestation/tick surface (so recording the attestation cannot self-invalidate the digest). Deterministic; trust class of `git`/`openspec`.
- [ ] 3.2 Add to the hand-authored **`⟶ archive-ritual`** procedure in `AGENTS.md` a **step-0 in-scope precondition** that **FAILS CLOSED** unless all four hold (design D7/D8): (1) the human attestation exists and is well-formed; (2) the reviewed-state digest **recomputes** and equals the attested value; (3) the tree/index is clean; (4) **exempt-by-act staleness** — the attestation line's introducing commit is the sole post-review commit and no later commit altered a reviewed artifact or rewrote the attestation record. The check reads **no model-authored artifact** (inputs: the human's line, the git-computed digest, git ground truth). Note the **optional-not-default** signing strong form (`verify-commit` when the attestation is signed).
- [ ] 3.3 Document the **gate convention** in `AGENTS.md`: the mechanical **scope criterion** (product code / a non-doc spec delta / review-verification-CI tooling carries the gate; purely mechanical docs/tracker/rename changes stay critic-only); the **human-attestation task template** — placed after `⟶ xtty-openspec-critic` and before `⟶ archive-ritual`, **HUMAN-ONLY / model-MUST-STOP**, ticked only by a direct human act recording the reviewed-state digest, **no** auto-firing `⟶ xtty-cross-review` marker; and the archive step-0 checks (§3.2). Also add the **worker protocol** (passes, advisory ledger + header contract, bounded rounds, one-launch spend, model-family diversity, detectable non-gating degradation) and the two-axes framing (conformance vs soundness; Pass 4 fact-checks *stated existing-symbol claims*, a bounded in-repo check, not open-ended code soundness).
- [ ] 3.4 Update `AGENTS.md`'s committed-tooling bullet + the agents/launchers table to list `/xtty:cross-review` and the digest tool, noting Pass C is inline (emitting the common schema), not a standing agent (anti-roster refutation).
- [ ] 3.5 Verify by inspection: `AGENTS.md` states the worker protocol, the mechanical scope criterion + pre-archive timing, the human-attestation gate convention (critic tail marker retained; the cross-review gate is its own HUMAN-ONLY blocking task with no auto-firing marker; the digest tick), the archive step-0 fail-closed precondition (the four checks incl. exempt-by-act + optional signing), the model-family diversity rule (GPT-family Pass B vs Opus A/C; skipped B → single-model header), and the detectable non-gating degradation behavior.

## 4. Research capture

- [ ] 4.1 Capture `research/03-analysis/cross-model-design-review-*.md`: the complementary-axes finding with **two worked exemplars** — (a) critic COHERENT ↔ Codex caught the `XttyConfigSet` config bug on `add-git-diff-wrap-toggle`; (b) this session's **six-round refutation of the self-policing automated gate** and the live Fable-5 + `gpt-5.6-sol` fan-out that re-architected it (why authority-free worker + human gate; why signing is optional-not-default; the two failure classes). Index it in `research/README.md`.

## 5. Land the change (dogfooded on this change)

- [ ] 5.1 Pre-archive coherence review of this change. ⟶ xtty-openspec-critic (add-cross-model-design-review)
- [ ] 5.2 **BLOCKING human-attestation cross-review — HUMAN-ONLY, model MUST STOP.** The **human** (deliberately, never auto-fired by reaching this task) runs `/xtty:cross-review add-cross-model-design-review` to dogfood the worker built in groups 1–3 (this change is in scope via the **review/verification-tooling** clause), reads the full ledger, runs the digest tool, and ticks this task **recording the reviewed-state digest on the tick line**. The model SHALL NOT tick this task or compute the digest value. **Run the dogfood in a fresh session after the v4 critic is committed** (or gate on an explicit `Definition: v4` delivery check) — an in-session dogfood may be served the stale v3 critic (design R9). This is the first real attestation and the gate's acceptance test (design R10).
- [ ] 5.3 Archive + reconcile — **step-0 precondition (design D7/D8): the archive ritual's fail-closed check must pass** for this in-scope change (attestation present + well-formed; reviewed-state digest recomputes to the attested value; clean tree; exempt-by-act staleness — the attestation act is the sole post-review change) **before** merging; separately confirm the gate **fails closed** on a deliberately stale/rewritten attestation (the by-effect proof of R8/R10). Then merge the spec deltas (`openspec archive`), fill the new `cross-model-review` spec Purpose, tick trackers (Current-status row + snapshot + HISTORY narrative + established-specs line + milestone), add the self-policing-gate refutation to the Learned-refutations list, and verify-against-disk. ⟶ archive-ritual
