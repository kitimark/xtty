# Tasks: add-xtty-test-image

Dependency gate: **`retire-metal-renderer` must be applied (zero-Metal xtty build) before task group 3+.** Tasks marked **[HUMAN-GATED]** need the owner (Apple ID / tool install / disk); everything else is agent-runnable.

Explicitly out of scope (not blurred into any task): ghcr `tart push` (deferred until a second consumer, design D10); any `ci.yml` change (the Metal-guard cleanup belongs to `retire-metal-renderer`); baking xtty source into the image (design D6); fixing the menu-clobber failures (independent, design D9).

## 1. Prerequisites (one-time, human-gated)

- [x] 1.1 **[HUMAN-GATED]** Install Packer (done 2026-07-04: packer 1.15.4 via the hashicorp tap; `packer init` then fetched the tart plugin v1.20.0 and `packer validate` passed — the template is machine-validated): `brew tap hashicorp/tap && brew install hashicorp/tap/packer` (design D1 + Prerequisites 1 apply-time correction — plain `brew install packer` left homebrew-core; Packer is not currently installed). No manual `packer init` here — the `github.com/cirruslabs/tart` plugin can only be pulled once the template's `required_plugins` block exists (task 2.1), and `make image` runs `packer init` + `packer build` itself (design D8)
- [x] 1.2 **[HUMAN-GATED]** Download the official Xcode 26.5 installer archive with an Apple ID (done 2026-07-04: `~/XcodesCache/Xcode_26.5.xip`, **~2.3 GB — the slim Xcode-26.x Apple-Silicon distribution, sha1-verified `a6d5be15…` against xcodereleases.com**; archived to `/Volumes/savepoint/images/`; note: `xcodes` does no integrity check — a truncated first attempt shipped as "downloaded", caught only by the checksum, now a mandatory README step) — `xcodes download 26.5 --directory ~/XcodesCache` (the flag matters: bare `xcodes download` defaults to `~/Downloads`; or manual developer.apple.com) → `~/XcodesCache/Xcode_26.5.xip`; also archive a copy on `/Volumes/savepoint/images/` (design: Prerequisites 2 — the only Apple-authenticated step)
- [x] 1.3 **[HUMAN-GATED]** Confirm ~45+ GB free disk for the build (base pull + `.xip` + unxipped Xcode) on top of the ~40 GB resulting image — **checked on the volume holding the Tart storage (`TART_HOME`)**. Done 2026-07-04: internal `/` has **210 Gi free** (post-cleanup; the 47–86 GiB figure was stale) → default `~/.tart` chosen; external `savepoint` (770 Gi) is the fallback (recorded in `packer/README.md` prereq 3)

## 2. Image-build template + docs (build)

- [x] 2.1 Create `packer/xtty-test.pkr.hcl`: `vm_base_name` = pinned `ghcr.io/cirruslabs/macos-tahoe-base` **reference** (resolved at apply time to the manifest **digest** `sha256:a8e1c830…` — only a `:latest` tag exists upstream, so the digest is the pin; design D2 as amended + Open Questions resolved), tart plugin block, VM build settings per design D5
- [x] 2.2 Add the provisioner sequence (design D5): `file` copy of `~/XcodesCache/Xcode_26.5.xip` host→guest → `brew install xcodes` → `sudo xcodes install 26.5 --experimental-unxip --path … --select --empty-trash` → move to `/Applications` + `xcode-select -s` + license → `xcodebuild -runFirstLaunch` (**no** `-downloadPlatform`/`-downloadAllPlatforms`, **no** `-downloadComponent MetalToolchain`) → `brew install xcodegen` → cleanup (delete the in-guest `.xip`, empty trash, purge caches)
- [x] 2.3 Confirm-by-diff against cirruslabs `xcode.pkr.hcl` that every removal in design D5 is actually absent (simulators/platforms, Android SDK, Flutter, fastlane/cocoapods/rbenv/tuist, libimobiledevice, casks, Metal component, multi-Xcode loop)
- [x] 2.4 Write `packer/README.md`: prerequisites (the three human-gated steps, incl. **where the Tart storage lives** — the `TART_HOME` volume that must hold the base pull, the build VM, and the golden image; likely the external `savepoint` volume — design: Prerequisites 3), build command, the golden-clone runtime workflow (clone → run at 3 vCPU → deploy source via rsync or git clone → `xcodegen generate` + `xcodebuild build-for-testing`/`test` → collect `.xcresult` → delete the clone; never build in the golden — design D7), maintenance (when to rebuild, pin-bump procedure, tart/lume OCI incompatibility note — design: Maintenance, D10), and the current pins + last-verified parity result
- [x] 2.5 Add the `make image` target: checks packer + the `.xip` at the expected path and fails with install/download instructions when missing (advise, don't privileged-install), then runs `packer init` + `packer build` (design D8); ensure it appears in the no-target `make` help listing
- [x] 2.6 Update AGENTS.md → Building with a short "local VM test image" subsection pointing at `packer/README.md` (pins, `make image`, golden-clone workflow, the `retire-metal-renderer` dependency)

## 3. Build the image (verify: the build itself)

- [ ] 3.1 Verify `retire-metal-renderer` is applied: a local xtty build succeeds with no `.metal` compile step (the hard dependency for a Metal-free guest — the gate the tasks header pre-registers for group 3+)
- [x] 3.2 Run `make image` end-to-end (done 2026-07-05: `Build 'tart-cli.tart' finished after 41 minutes 17 seconds` — first attempt was externally interrupted at 51%, relaunch rebuilt from scratch since packer's cleanup pruned the partial pull cache; **zero Apple authentication** — Xcode entered the guest only via the file-provisioned `.xip`)
- [x] 3.3 Report the resulting image footprint (done 2026-07-05: **35 GB** per `tart list` — under the ~40 GB estimate thanks to the slim Xcode-26.x distribution; vs 87 GB prebuilt; guest-internal usage ~19 Gi of 55; recorded in `packer/README.md` last-verified)
- [x] 3.4 Verify the golden image was produced from pinned inputs only and no xtty source in the guest (done 2026-07-05: inputs = the D2 base digest + the sha1-verified `.xip` by construction; in-guest `ls` shows no xtty source, empty Downloads — the `.xip` cleanup ran; only the base-inherited `actions-runner`)

## 4. Verify the guest (spec scenarios)

- [x] 4.1 Clone the golden image, boot the clone, verify base-layer inheritance (done 2026-07-05 on `xtty-verify` @ 3 vCPU: console owned by `admin` — auto-login live; `sudo -n` OK; brew 5.1.15 + xcodegen 2.45.4; **guest macOS 26.5/25F71** — the digest-pinned base is newer than the old rigs' 26.4/25E246; Xcode 26.5/17F42 selected at `/Applications/Xcode_26.5.app`)
- [x] 4.2 In-guest Metal-absence check (done 2026-07-05: `xcodebuild -showComponent MetalToolchain` → `Status: uninstalled`; and `xcrun -f metal` indeed *succeeds* on the stub — the §8b false positive reproduced on this image exactly as the design risk predicted; positive proof remains 4.3's build)
- [ ] 4.3 In-guest: deploy the xtty source at test time (rsync or git clone), run `scripts/bootstrap-swiftterm.sh`, then verify `xcodegen generate` + `xcodebuild build-for-testing` **succeed with no Metal toolchain** (spec scenario: no Metal toolchain yet builds xtty)
- [x] 4.4 In-guest: run the **full test suite including the XCUITests** on the 3-vCPU clone and collect the `.xcresult` (done 2026-07-05 via host-built products + `test-without-building` — the §8 workaround, since native in-guest build pends `retire-metal-renderer`/4.3: **33/8/1 headless**, `.xcresult` at `~/Downloads/xtty-vm-poc/artifacts/minimal-image-run/`)

## 5. Acceptance: parity with the big-image rig (pre-registered)

- [x] 5.1 Compare the 4.4 results against the proven big-image rig / CI envelope (done 2026-07-05: **headless 33/8/1**, all 8 failures menu-clobber-class — zero non-menu failures; graphics 32/9/1 with the +1 = `testNewWindowOpensSecondWindow` dialog-inflated, passes headless. The failing **set** = parity ✓; count is 1 above the §8/§9 envelope max of 7, attributable to per-launch race variance — a 2nd/3rd run would tighten the distribution). **Pre-registered expectation (design D9): if `fix-main-menu-clobber` has not landed, the run shows the race results — primarily the failing-test SET (split / directional-focus / new-tab / find-bar / paste / truecolor / churn, unchanged by the sibling); counts net of `retire-metal-renderer`'s deletion of the Metal e2e (42 → 41 tests): 33/7/1-ish of 41, possibly 35/5/1 (the big-image rigs measured 34/7/1 ↔ 36/5/1 of 42; the two Cmd+D split tests are the known per-launch-flaky pair, §9d). Acceptance = the failing-test set falls within that envelope (parity), NOT an all-green suite.** If the first run is ambiguous, run a second and compare the union
- [ ] 5.2 Delete the test clone (golden stays pristine); record the parity result + run counts in `packer/README.md` (last-verified section)
- [ ] 5.3 Run `openspec validate add-xtty-test-image`; tick trackers per "Keep progress current" (AGENTS Current status + the research doc already carries §10/§11/§11g — no new research doc needed unless the build surfaces a durable finding). **Archive ordering (pre-registered): archive this change only after — or together with — `retire-metal-renderer`**, whose `build-workflow` delta drops the Metal toolchain from the Prerequisite-check requirement; archiving this change first would merge a `build-workflow` spec that both requires the Metal toolchain and asserts the image needs none
