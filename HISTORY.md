# HISTORY — xtty development log

Moved **verbatim** out of AGENTS.md by the `slim-agents-context` change (2026-07-06) so the
always-loaded guide carries rules + orientation while the narrative record lives here, one `Read`
away. **Append-only:** when a change completes, its full narrative is appended under the matching
section with a dated lead-in (the AGENTS.md *Keep progress current* rule); AGENTS.md itself gets a
bounded table-row update. Not loaded at session start.

## Per-change milestone narratives (formerly AGENTS.md → Current status)

- ✅ Landscape + internals research, requirements, and a design (stack + milestones) — in `research/`.
- ✅ Spec-driven workflow scaffolded — `openspec/`.
- ✅ Build system live: `project.yml` (XcodeGen) → `xtty.xcodeproj` (gitignored), `XttyCore` SPM package. See **Building** below.
- ✅ **P0 `add-app-skeleton`** — implemented **and archived**.
- ✅ **P1 `integrate-swiftterm`** — implemented **and archived**: live login shell in a SwiftTerm view hosted in an AppKit `NSWindow` (SwiftUI hosting renders the view black on macOS 26), engine routed through `XttyCore`, window opens on the built-in display.
- ✅ **`add-verification-harness`** — implemented **and archived** (13/14 tasks; optional Peekaboo-MCP registration deferred): an XCUITest e2e target (terminal content asserted via a DEBUG engine grid-dump, since the custom-drawn view exposes no text to accessibility) plus Peekaboo for manual driving.
- ✅ **P2 `add-daily-driver-baseline`** — implemented **and archived** (23/23 tasks): a view-free `XttyCore` config loader (`~/.config/xtty/config` → font/theme/scrollback/option-as-meta, unit-tested) applied to the live view; bounded scrollback (default 10 000 / ceiling 100 000); Cmd+F find via SwiftTerm's native bar; live Cmd +/−/0 font sizing. Harness (8/8 UI tests) asserts config-applied + bounded-scrollback (new DEBUG state dump), find, and truecolor/emoji/wide (grid dump fixed for CJK + non-BMP emoji). Ligatures are a no-op in SwiftTerm's grid path; SwiftTerm's Metal renderer evaluated and **deferred to the P7 gate** (`research/03-analysis/swiftterm-metal-renderer-spike.md`).
- ✅ **P3a `add-tabs-and-splits`** — implemented **and archived**: native macOS tabs + custom `NSSplitView` splits/panes + multiple windows; a view-free pane/session model in `XttyCore` (`Pane`/`PaneNode`/`SessionRegistry`); unified close/exit escalation (pane → tab/window → quit, confirm-close on a running foreground job); **configurable keybindings** (`iterm`/`ghostty` presets + per-action `keybind-<action>` overrides, view-free `KeybindParser`); clickable URL links (SwiftTerm-inherited; non-`http(s)` guard deferred — see the archived change's design D7). 52 `XttyCore` unit tests + 12 XCUITests green.
- ✅ **P3b** — ✅ **`add-quick-terminal`** — implemented **and archived**: a global-hotkey "quake" drop-down — a borderless non-activating `NSPanel` toggled by a Carbon `RegisterEventHotKey` hotkey (no TCC prompt), hosting one persistent scratch shell; a view-free `HotKeyParser`/`HotKeySpec`/`HotKeyResolver` in `XttyCore` (positional `kVK_*` codes, reusing `ModifierSet` + a shared `ChordTokenizing` grammar with `KeybindParser`); accessory lifecycle via a **private `SessionRegistry`** (excluded from the main inventory + quit accounting); `quick-terminal` + `quick-terminal-hotkey` config keys kept out of `terminal-configuration`. 69 `XttyCore` unit + 13 XCUITests green. ✅ **`add-profiles`** — implemented **and archived**: named `[profile "name"]` sections in the existing config file (case-preserving `parseSections`, backward-compatible — flat file == base), each a `LaunchOverride` + inherited `XttyConfig` assembled into an `XttyConfigSet`; a `command` runs through the user's login+interactive shell (`$shell -l -i -c '<command>'` — `execve`/no-PATH reality) with `cwd` + additive `env` (PATH off-limits); `PaneController` takes a resolved `XttyProfile` and retains it so a split inherits it; `Pane` carries `profileName`; `default-profile` + a dynamic "New Tab with Profile ▸" menu; quake uses base appearance + a plain login shell; `confirm-close` wired (D10). 91 `XttyCore` unit + 14 XCUITests green. Decisions: `research/03-analysis/p3b-shell-ux-decisions.md`.
- ✅ **P4a `add-semantic-capture`** — implemented **and archived**: the keystone's data half, fork-free on SwiftTerm's public API. **OSC 7** live cwd captured via the wired `hostCurrentDirectoryUpdate` delegate + view-free `OSC7` decoder (`file://`/`kitty-shell-cwd://`, remote-host flag); **splits open in the focused pane's live cwd**. **OSC 133** registered on the engine; view-free `OSC133` parser (A/B/C/D/P, bare-positional exit code, `cmdline`/`cmdline_url`, `k=s`) → view-free `BlockTracker` state machine + per-session `Block` list (command/exit/cwd/timestamps/state — **no fragile row coords**). **Auto-injects zsh** via `ZDOTDIR` redirection (bundled `.zshenv` restores the user's config; additive `add-zsh-hook`; skipped for `command` one-shots; manual fallback in `config.example`). **Alt-screen gating** via an `open bufferActivated` override + public `isCurrentBufferAlternate` (full-screen apps → `opaque`; OSC 133 best-effort, tmux/ssh → plain output). 126 `XttyCore` unit + 17 XCUITests green (block/cwd e2e drive a real injected zsh). **P4b split in two:** **P4b-1 `add-file-link-open`** + **P4b-2 `add-spatial-blocks`** both shipped (next bullets); gutter fail-marks dropped (folded into the P5 sidebar). Decisions: `research/03-analysis/p4-semantic-capture-decisions.md` + `research/03-analysis/p5-sidebar-and-p4b-sequencing.md`.
- ✅ **P5 `add-session-sidebar`** — implemented **and archived**, fork-free on P4a's block model. View-free additions in `XttyCore`: `BlockTracker.runningBlock` (the in-flight command, no coordinates), a `SessionActivity` enum + pure `derive` (fullScreen→running→failed→succeeded→idle) surfaced on `TerminalSession.activity`/`runningCommand`, and an `@Observable SessionRegistry` with a `revision` bumped on register/unregister/focus + `noteActivityChange()` (called from the main-actor OSC handlers). App: a SwiftUI `Tab ▸ Pane` sidebar (`SessionSidebar.swift`) hosted in a collapsible left panel (Auto-Layout `terminalContainer` refactor in `TerminalWindowController`), click → `focusPane` (reuses `setActivePane`, never scroll-to-row), live duration via `TimelineView`, View ▸ Toggle Sidebar (⌃⌘S). State dump gains `sessionActivity` + `runningCommand`; new sidebar e2e. 134 `XttyCore` unit + 18 XCUITests green.
- ✅ **P4b-1 `add-file-link-open`** — implemented **and archived**, the fork-free agent-CLI half of P4b on SwiftTerm `v1.13.0`. **Cmd-click a `file:line[:col]`** (or bare/relative/rooted path) → opens in the user's editor at that line, resolved against the focused session's live OSC-7 cwd; plus the deferred **D7 scheme guard** (only `http`/`https`/`mailto` auto-open; other schemes blocked). View-free `XttyCore` `LinkOpen.swift` — classify (URL vs `path:line:col`) → scheme guard → cwd-relative resolution → opener-invocation build (template tokenization + `$VISUAL`/`$EDITOR` editor-inference table), composed in `LinkRouter`; new `link-opener` config key. Interception is a **vetted `terminalDelegate` proxy** (`LinkRoutingTerminalDelegate`) that forwards the other 9 delegate methods to the view and routes only `requestOpenLink` — because `requestOpenLink` is *not* forwarded to `processDelegate` and its protocol-extension default can't be subclass-overridden (design D1). `FileOpener` execs the editor via `Process` with login-shell PATH resolution; the path is always a literal argv element, never a shell string (D4). Harness: DEBUG dump gains `lastLinkOpen`; an `XTTY_TEST_LINK_PATH` launch-env file trigger drives routing in-process (the sandboxed runner can't write `/tmp`). 148 `XttyCore` unit + 20 XCUITests green (validated robust under deliberate mouse interference — the mis-click surfaced as a 50s `Synthesize event` timing anomaly, not a failure).
- ✅ **P4b-2 `add-spatial-blocks`** — implemented, **lit up**, **and archived**. **Jump-to-prompt** (Cmd+Shift+↑/↓ → `scrollTo` an earlier prompt) + **copy-command-output** (Cmd+Shift+C → engine-only `getText`→clipboard, trailing prompt excluded, + a non-modal `SpatialToast`). Built on a **2-accessor** SwiftTerm addition (`getScrollInvariantCursorLocation` + `scrollbackBase`) shipped via a **pinned gitignored upstream clone + `git apply`'d patch — no fork repo** (`external/SwiftTerm` @ `v1.13.0` pinned via `patches/swiftterm/UPSTREAM_CONFIG.sh` + `git apply`'d `patches/swiftterm/xtty-accessors.diff` via `scripts/bootstrap-swiftterm.sh` + a local-path SPM dep; the Playwright-style patch-in-repo, see `research/03-analysis/swiftterm-fork-vs-patch-strategy.md`). View-free `XttyCore`: optional best-effort `BlockAnchor` (prompt/output-start/output-end rows captured at OSC-133 A/C/D, epoch-stamped) + pure `BlockNavigation` (reverse-map, stateless prev/next, copy range); **invalidation** = `bumpEpoch` on `sizeChanged` (resize/reflow) + `liveTop` high-water-drop on `scrolled`/marks (clear/reset) + validate-at-use, all degrading gracefully to no-op. Built behind a **deferred injectable seam** (Phase 1 nil → no-op, fake-tested; Phase-2 light-up = a 2-line seam swap). **Tier-2 visual-select deferred** (forks the view, P7-gated); upstream PR prepared (the accessor file), not yet filed. 162 `XttyCore` unit + 23 XCUITests green (3 spatial e2e on real injected zsh). Decisions: `research/03-analysis/p4b-2-spatial-blocks-decisions.md`.
- ✅ **P6a `add-git-review`** — implemented **and archived**: a read-only **git-review panel** — the focused pane's changed files grouped **Changes / Untracked / Conflicts** → click-to-diff (a plain **unified** SwiftUI diff), ⌘-click → editor (reuses P4b-1 `LinkRouter`/`FileOpener`), in a new collapsible **trailing-edge** panel (the left edge stays the P5 session sidebar), **default-collapsed** (View ▸ Toggle Git Review, ⌃⌘G). Git via **shell-out** (no libgit2): view-free `XttyCore` `GitStatusParser`/`NumstatParser`/`DiffParser` + an `@Observable GitReviewStore`; App-layer `GitRunner` (login-shell PATH, `GIT_OPTIONAL_LOCKS=0`, **porcelain v1 `-z`**, `--no-ext-diff --no-color`, `git diff HEAD` / `--no-index` exit-1-ok) + a `GitReviewController` (refresh on OSC-133 `commandEnd` + a ~5 s poll backstop + focus + manual, **debounced + serialized + visible/local-gated**). New `diff-context` config key (editor reuses `link-opener`). Read-only (staging deferred, model forward-compatible). `gitReview` DEBUG dump + e2e. **185 `XttyCore` unit + 25 XCUITests green.** Decisions: `research/03-analysis/p6-file-diff-decisions.md`.
- ✅ **P6b `add-git-review-tree`** — implemented **and archived**, the fork-free Scope-A increment: a **flat↔tree toggle** over the git-review changed-files list — a **pure presentation transform** (no new git/filesystem call, no new dep), on-mission for large agent-generated refactors where the flat list scrolls. View-free `XttyCore` `GitFileTree.build([GitChangedFile]) → [GitTreeNode]` (dirs-first/alphabetical, input preserved) + a `GitReviewLayout` on `@Observable GitReviewStore` (`setLayout`, revision-bumping); a global `git-review-layout = flat|tree` config key (default `flat`, base-only). App: `GitReviewView` renders a recursive `DisclosureGroup` tree (a *collapsed*-set → default-expanded, per-repo reset) with a shared `GitFileButton` leaf (select/⌘-open identical to flat) + a header toggle (a11y `gitReview.layoutToggle`); `gitReview` dump gains a `layout` field. The **full *project* file-tree browser (Scope B)** stays **rejected** as off-mission IDE-creep — see the [P6b addendum](research/03-analysis/p6-file-diff-decisions.md#p6b-addendum--file-tree-scope-decided-scope-a-scope-b-rejected). Adversarial-reviewed (0 confirmed defects). **213 `XttyCore` unit + 26 XCUITests green.**
- ✅ **P7a `add-latency-memory-harness`** — implemented **and archived**, the fork-free measurement harness behind the P7 gate. Base-only `renderer = coregraphics|metal` config key wired to SwiftTerm `setUseMetal` after window-attach (+ `-UITestRenderer` for rebuild-free A/B); view-free `XttyCore` `RendererBackend` + Codable `BenchScenario`/`BenchResult`; a `task_info` `phys_footprint` **memory sampler** over independently-reset scenarios (idle / N-panes / saturated-scrollback / alt-screen); an in-process **key-to-photon latency probe** (ScreenCaptureKit one-shot `SCScreenshotManager` + `CGEvent`, caret hidden — `CGWindowListCreateImage` is unavailable on the SDK) that is **documented coarse** (each capture ~20 ms > the signal → can't resolve key-to-photon, can't distinguish renderers); a `#if DEBUG -Benchmark` mode + `make bench` writing a `BenchResult` JSON report (latency degrades to an explicit "unavailable" marker without the Screen-Recording grant). State dump gains `renderer` + `memoryFootprintBytes`; the benchmark e2e is opt-in via `XTTY_RUN_BENCH_E2E` so routine `make test` is prompt-free. **Result: memory is lean & trustworthy** (idle ~68 MB footprint, 0 % idle CPU — M1 met); the **trustworthy latency probe + the CoreGraphics-vs-Metal verdict are P7b**. New `performance-harness` spec. Adversarially reviewed (proposal + implementation; confirmed findings folded in incl. a cumulative-scenario memory bug). **224 `XttyCore` unit + 31 XCUITests green** (3× bench results in `research/03-analysis/p7-measurement-methodology.md`). Companion **`add-local-signing-identity`** — implemented **and archived**: an opt-in stable self-signed `xtty-dev` code-signing identity (`scripts/create-signing-cert.sh` + `XTTY_SIGN_IDENTITY` Makefile override) so TCC grants (Screen Recording) persist across rebuilds; the committed default stays ad-hoc/portable (`build-workflow` delta; full Developer ID/notarization still deferred).
- ✅ **P7b `add-trustworthy-latency-probe`** — implemented **and archived**, the fork-free instrument that closed the P7 renderer gate. Replaced P7a's screenshot-polling latency probe with a continuous **`SCStream`** reading the first changed frame's **`displayTime`** (`App/LatencyProbe.swift`: an `SCStreamOutput` `FrameSink` + blink-guarded change detection; the next `.complete` frame after t0 *is* the keystroke), fixing P7a's clock-unit bug by normalizing t0 (`mach_absolute_time`) and t1 (`displayTime`, mach ticks) through one `CMClockMakeHostTimeFromSystemUnits`. A startup **timebase-calibration gate** marks results untrustworthy (no fabricated numbers) rather than trusting a mismatched clock; a renderer-independent **`ProbeOverlay`** CA-strip baseline records the common capture/compositor floor. View-free `XttyCore` `BenchResult` gained `timebaseCalibration`/`frameQuantizationMs`/`noOpBaseline` (Codable). **Benched 3× per renderer: calibration passed (offset <4 ms → absolute numbers trustworthy on macOS 26.2); verdict = keep CoreGraphics, skip Phase 8** — CG faster median (~31 vs ~33 ms), much tighter tail (p99 ~40 vs ~50–120 ms), leaner memory (~55 vs ~62 MB idle) than SwiftTerm's experimental Metal path; the shared ~16.7 ms `queuePendingDisplay` throttle dominates the median (the real latency lever) and the probe exposed Metal's worse tail. Resolution is frame-quantized (~one refresh interval), valid for the renderer delta; comparators (Terminal.app-first) were scoped a stretch (iTerm2 Secure-Event-Input blocks injection); no hardware rig needed. `performance-harness` + `verification-harness` spec deltas merged. 226 `XttyCore` unit + 31 XCUITests green. Decisions: `research/03-analysis/p7-measurement-methodology.md` (P7b methodology + apply-result addenda).
- 📋 **Established specs** (`openspec/specs/`): `app-shell`, `build-workflow`, `terminal-configuration`, `terminal-keybindings`, `terminal-links`, `terminal-multiplexing`, `quick-terminal`, `terminal-session`, `terminal-semantics`, `shell-integration`, `session-sidebar`, `git-review`, `terminal-spatial-blocks`, `lifecycle-census`, `verification-harness`, `performance-harness`, `research-capture`, `test-validation`.

## Change log, later era (formerly embedded in AGENTS.md → OpenSpec workflow)

### 2026-07-11 — revamp-agents-md (P-tooling / docs + agent-tooling) — implemented 14/14, coherence COHERENT, archived

Grounded in two `/xtty:research` fan-outs (`agents-md-structural-best-practices.md` + its same-day addendum) that measured *why* AGENTS.md kept regrowing after the `slim-agents-context` diet fully eroded within ~4–5 days: the "Shipped and archived" table's Tooling row was **change-keyed** (edited on every archived tooling change) rather than **category-keyed** like its sibling phase rows (edited only when a phase closes), growing to 16,645 bytes — 93% of the table, 24% of the whole file — across ~24 change narratives already duplicated verbatim in HISTORY.md. The Snapshot paragraph (5,768 bytes) carried the identical anti-pattern at smaller scale, and "How to work here" carried four near-identical delegation-boundary essays (`xtty-test-validator`, `xtty-ci-investigator`, `xtty-openspec-critic`, `/xtty:research`) that were structurally one table pretending to be four essays.

**What shipped:** (1) the Tooling row replaced with a bounded, category-keyed one-liner — 7 categories (CI, dev-workflow agents, install/signing, test-image/VM, launchers, product fixes, guide structure; the last two emerged from the actual content, beyond the 5 illustrative examples in the research/tasks) — measured **23,097 → 1,374 bytes** for the row alone; (2) the run-on Snapshot paragraph replaced with a 3-bullet block (milestone position; the authoritative test envelope + a `packer/README.md` pointer; a "latest change" pointer) — confirmed no information lost (CI-specific facts already live in the Building → CI section and HISTORY.md); (3) the four delegation essays replaced with one 5-column table (agent/launcher · spawn triggers · inline↔delegate boundary · marker · mechanism/rationale) — all three load-bearing markers (`⟶ xtty-test-validator`, `⟶ xtty-ci-investigator`, `⟶ xtty-openspec-critic`) confirmed surviving verbatim; (4) one new REVIEW-severity Pass-1 heuristic added to `.claude/agents/xtty-openspec-critic.md` (flags a tracker reconcile that appends a per-change narrative to a category-keyed row instead of leaving its category summary as-is), `Definition version` bumped v2 → v3. Whole-file size: **80,107 → 58,384 bytes (−27%)**. Explicitly deferred (design.md non-goals): a mechanical CI byte/line-count gate (the critic heuristic is the proportionate primary mechanism; sufficiency is a measured, not assumed, open question — track the Tooling row/Snapshot byte size after the next several archives) and nesting AGENTS.md into per-directory files (xtty is a single app + one SPM package, not a monorepo).

**The `harden-test-precision-vs-claim` collision (design.md D4), resolved during apply, not deferred to archive:** that change archived first (2026-07-11) and had already added a test-precision heuristic sentence + scenario to the same `coherence-review` "Committed coherence-review agent tooling" requirement. This change's own `coherence-review` spec delta, drafted against the pre-harden baseline, would have **overwritten** that addition (dropped the sentence, deleted the scenario) rather than extending it — caught during task 5.3's re-check of the established specs against the delta's assumptions, before archive rather than as a post-archive hand-merge. Fixed by hand-correcting the delta to a proper superset (restore the test-precision sentence + scenario verbatim, then append the new category-keyed sentence + scenario) — verified twice by `xtty-openspec-critic` (once with a caveated stale-v2-definition-stamp spawn, once re-spawned fresh for a clean v3-stamped confirmation) as a clean superset, not a truncated block, and the archived `openspec archive` merge confirmed byte-for-byte via diff: both established scenarios/sentences preserved, both new ones appended, nothing lost.

**Verification:** `openspec validate` clean throughout (both before and after the mid-session delta correction). `xtty-openspec-critic` pre-archive review (re-run fresh for the definition-stamp confirmation): **COHERENT**, recommendation proceed — 0 BLOCKER findings, one non-blocking REVIEW note (the category-keyed bound's inline discoverability — well-anchored in the `research-capture` spec + the critic heuristic + the self-evidently-categorized Tooling row, but not restated in the "Keep progress current" prose a reconciling agent reads day-to-day; left as an optional future addition, not required). No cross-change collision with the other open change (`add-ci-pipeline` touches only `build-workflow`). Per the critic's forward note and the very rule this change ships, **this reconcile does not append a new per-change clause to the Tooling row's existing "guide structure" category** — `revamp-agents-md` folds into that already-represented category (alongside `slim-agents-context`/`symlink-claude-md-to-agents-md`/`harden-test-precision-vs-claim`) without an edit, since the category itself didn't change.

**Reconciled on archive:** AGENTS.md's open-changes table lost this change's row; the Snapshot's "Latest change" bullet now points here with the headline byte figures; this HISTORY.md entry. No `verification-harness` delta and no product code — docs + agent-tooling only, parallel to `slim-agents-context`/`symlink-claude-md-to-agents-md`/`harden-test-precision-vs-claim`. Verified against disk: `openspec list` (1 change remains: `add-ci-pipeline`), archive dir `2026-07-10-revamp-agents-md` present, `openspec validate --all --type spec` → 23/23 valid.

### 2026-07-11 — harden-test-precision-vs-claim (P-tooling / docs + agent-tooling) — implemented 7/7, coherence COHERENT, archived

Captured the "match test precision to the claim" lesson from a near-miss surfaced while drafting `fix-scroll-reversal-redraw-corruption`'s regression test (see the 2026-07-10 entry below): a real full-screen-program end-to-end test was drafted first, mirroring an existing test, and would have shipped green while proving nothing — `vim` and `less` (the programs bundled in the test VM) never invoke the buggy `cmdScrollDown` path regardless of how they're driven (`vim` uses already-guarded `IL`/`DL`, `less` a bare `RI` with no scroll region), while `htop` (which does hit it, via ncurses' own hardware-scroll optimizer) is brew-only and absent from the VM image. Caught only because the test's author happened to investigate deeply — nothing in the project's coherence-review checklist would have caught a change that shipped the wrong-depth test with high confidence.

**What shipped (docs + agent-tooling only, no product code):** one new bullet in `AGENTS.md`'s "Keeping a change coherent" walk-list — **"Test precision vs. the claim (design-time, Decisions)"** — requiring `design.md`'s Decisions (or Risks/Trade-offs) to state (1) the exact claim a new/modified test proves, (2) the layer/mechanism that claim lives in, and (3) why the chosen driver (real program or direct/synthetic input) actually reaches that layer. Worded **symmetrically** per design D3 after adversarial critique found a first draft's disclaimer insufficient: a real end-to-end program is the *right* tool for a claim that lives upstream of program-specific behavior (e.g. `testWheelScrollsRealMouseTrackingPager` proving wheel routing reaches the program at all), and the *wrong* tool for a claim about one narrow internal code path a given program may or may not exercise (e.g. `fix-scroll-reversal-redraw-corruption`'s `printf`-driven synthetic test) — a skimming reader must not retain only the vivid cautionary half and over-apply it into "always prefer synthetic tests." One new REVIEW-severity Pass-1 heuristic added to `.claude/agents/xtty-openspec-critic.md` operationalizing the rule (flags a test-adding change whose `design.md` doesn't state the claim/layer/driver-reach; never a BLOCKER — a semantic judgment call, matching the existing `mechanism-neutrality`/`design↔requirements traceability` heuristics' own severity), plus the file's `Definition version` stamp bumped v1 (2026-07-07) → v2 (2026-07-11) per its own maintenance note. MODIFIES `coherence-review`'s "Committed coherence-review agent tooling" requirement (+1 scenario: "A test-adding change is checked for stated test precision").

**Scope decisions (design.md D1/D2):** no manual-verify task added to `fix-scroll-reversal-redraw-corruption` — an adversarial critique of the research synthesis found the causal chain already fully closed by primary evidence gathered earlier in the same session (real htop's byte emission captured and replayed through a headless `Terminal`; the unfixed corruption already reproduced live on real htop during `smooth-scroll-wheel-momentum`'s manual verify; the fixed code's handling of those exact bytes proven by the fix's own test), so a post-fix manual re-run would add no new causal link. No `config.yaml` `rules.tasks` marker and no new `design.md` template section — the rule is a one-time design-time judgment made before any task executes (not a dispatch signal a downstream task consumer needs), and two archived changes (`split-shell-dependent-testplan` D2, `fix-scroll-wheel-mouse-reporting` Risks) already independently demonstrate the pattern living inside existing Decisions/Risks sections.

**Verification:** `openspec validate` clean throughout. `xtty-openspec-critic` pre-archive review: **COHERENT** — all Pass-1 checks passed (spec-delta syntax, the MODIFIED block pasting the entire established requirement + all 3 original scenarios verbatim before appending the new sentence/scenario, the proposal↔specs contract, delegation markers, tasks coverage, design↔requirements traceability); confirmed on disk that the AGENTS.md bullet and the critic-agent heuristic both landed verbatim and the `git status` diff was exactly the 3 declared files. The review also flagged one **pre-existing, unrelated** disk-drift in the same AGENTS.md section — the "verification-harness" bullet's harness-delta fraction, stale since an earlier change, re-derived from disk (post-archive: 25 of 49 archived changes carry a `verification-harness` delta, not the stale "22 of the 43") — fixed opportunistically in this change's own reconcile pass since it was already touching that file.

**Cross-change:** this change and the still-open `revamp-agents-md` both MODIFY the same `coherence-review` requirement ("Committed coherence-review agent tooling") and both edit `.claude/agents/xtty-openspec-critic.md` (Pass-1 heuristic append + Definition-stamp bump) and `AGENTS.md` — a collision `revamp-agents-md`'s own design.md D4 anticipated. Since this change archived **first** (2026-07-10 propose → 2026-07-11 archive), its own `openspec archive` was a clean mechanical merge with nothing to hand-merge; the hand-merge duty **transfers forward** onto `revamp-agents-md`'s later archive, which must rebase its `coherence-review`/critic-agent/AGENTS.md edits onto this change's now-landed state rather than the pre-harden baseline.

**Reconciled on archive:** AGENTS.md's open-changes table lost this change's row; the `revamp-agents-md` row's note updated to describe the forward hand-merge duty concretely (naming the three shared edit surfaces: the `coherence-review` requirement, the critic-agent file, AGENTS.md itself) rather than the propose-time "whichever archives second" hedge; the Shipped-and-archived Tooling row gained this entry plus a note on the folded-in stale-prose-count fix; this HISTORY.md entry. No `verification-harness` delta and no product code — a docs/agent-tooling-only change, parallel to `add-openspec-critic-agent`'s own framing. Verified against disk: `openspec list` (2 changes remain: `add-ci-pipeline`, `revamp-agents-md`), archive dir `2026-07-10-harden-test-precision-vs-claim` present, `openspec validate --all --type spec` → 24/24 valid.

### 2026-07-10 — scroll-reversal redraw corruption forensics (research capture; found during `smooth-scroll-wheel-momentum`'s manual verify, not yet proposed as a change)

**Found during task 4.4** (the human physical-trackpad verify for `smooth-scroll-wheel-momentum`): while flicking the trackpad over `htop` to check back-pressure, the user spotted genuine visual corruption — fragments of one process row's command text bleeding into unrelated rows (e.g. `launchd` picking up a trailing `echd_syst` fragment from `corespeechd_syst`, several rows away). htop's own data kept updating live in the corrupted rows (CPU%/MEM%/TIME+ ticked normally) — a **redraw/stale-cell bug, not a hang**.

**Isolated via a live interactive debugging session** (DEBUG grid-dump as ground truth + `peekaboo` for input/screen capture + the existing DEBUG wheel-injection hook from this same change's task 2.2 — no `/xtty:research` fan-out, no real hardware needed for the repro). The trigger: scrolling via xtty's **SGR mouse-report path**, then **reversing direction** after having scrolled some distance. A 5-way isolation matrix nailed the one load-bearing variable — **not** htop-specific (iTerm2, identical recipe, stays clean), **not** any-multi-row-jump (keyboard Page Down/Page Up, matched magnitude and exact reversal, stays clean), **not** requiring real hardware event rate (a slow ~6–7 Hz DEBUG-injected sequence reproduces it every time once the direction reverses). Three single-direction repro attempts (keyboard, one CGEvent burst, sustained one-direction injection) all came back clean before the down-then-reverse-up sequence caught it immediately — yielding a reusable guideline (**G(scroll-reversal)**: test direction reversal explicitly with matched net position, not just single-direction volume).

**Scope call:** not a `smooth-scroll-wheel-momentum` regression — the corruption is character-level VT/redraw corruption in htop's own drawn output, nothing D1–D3 (momentum-guard removal, remainder reset, lossless quantization) plausibly touches. But it was **unreachable before `fix-scroll-wheel-mouse-reporting`** shipped (wheel→SGR-report was a dead no-op before that), so it's a latent SwiftTerm/xtty rendering defect newly exposed by the wheel→mouse-report feature family, not pre-existing-and-unrelated. SwiftTerm-source-level root cause not traced this session (❓).

Captured to `research/03-analysis/scroll-reversal-redraw-corruption-forensics.md` (fates table retires 6 theories incl. "large PID numbers are the bug" and "needs real hardware rate"); indexed in `research/README.md`; `packer/README.md`'s expected-difference matrix gets a new tracked-but-unfixed row alongside the confirm-close race. **Not yet an OpenSpec change** — real, reproducible, xtty-specific, and severity-worthy (breaks a common daily-driver workflow), but does not block `smooth-scroll-wheel-momentum`'s own archival.

### 2026-07-10 — scroll-reversal redraw corruption: root cause found (research capture addendum, same-day follow-up)

A same-day follow-up investigation (still `/opsx:explore`, no product code touched) closed every `❓` the original capture left open, via a live interactive session against the running Debug build plus two `/xtty:research` Workflow fan-outs.

**Live re-characterization (via `peekaboo` driving the real app, delivery confirmed each time through the DEBUG `lastWheelRouting` telemetry, never trusting a CLI's own success message):** momentum is not required — a plain discrete, non-precise tick reproduces the corruption identically to a real trackpad flick. Depth is not required — the *shallowest possible* reversal (one wheel tick down, one tick up — 5 SGR reports each direction after xtty's own quantization) corrupts from a clean baseline. It is fully deterministic — the identical minimal sequence produces byte-for-byte identical garbled text every run. A window resize that changes the actual character grid (crossing a real cell-size boundary, so htop gets a genuine `SIGWINCH`) heals it completely and instantly; a resize that doesn't change rows/cols does nothing — this became the decisive **model-vs-render discriminator** (a sub-cell resize forces the CoreGraphics view to fully redraw from the *same* wrong model without healing, proving the corruption lives in the `Terminal` model, not the renderer). A related glitch was found on terminal-width-shrink in the CPU-meter-bar area (same "survives repeated live redraws" signature), and one frame showed a full row (`configd`) duplicated at two physical positions.

**The mechanism hunt itself had a genuine wrong turn, caught and corrected.** A first `/xtty:research` fan-out (ncurses + SwiftTerm scroll-region readers → synthesis → adversarial critic → a bare-`pty.fork()` verify-by-effect) measured **zero** scroll-region-shift sequences in 80 captured htop redraw ticks — only `VPA`+`DCH`+`EL` — and concluded SwiftTerm's `cmdScrollUp`/`cmdScrollDown` were irrelevant. That measurement was honest but of the wrong regime: a later check found htop's process list had **never actually scrolled** in that bare-pty harness (content stayed byte-identical across 60+ ticks despite htop visibly reacting) — a mouse-coordinate/focus-calibration gap in driving bare htop outside the real app. This is now recorded as **guideline G(verify-the-regime)**: a verify-by-effect probe returning a clean/negative result must confirm it actually reached the state under test, not just that it sent input and got some reaction back.

**The real breakthrough:** captured genuine PTY bytes from the *live, already-reproducing* xtty app (relaunching htop wrapped in `script -q` inside the running session — a transparent tap, no code changes) during the proven minimal-reversal recipe, then replayed those exact bytes through a headless SwiftTerm `Terminal` (the `NoopTerminalDelegate` pattern already used in `XttyCore/Tests/XttyCoreTests/TerminalSessionTests.swift`). **Byte-for-byte identical corruption reproduced, with zero xtty-app involvement** — proving the defect is 100% in upstream SwiftTerm's parser. Grepping the real capture showed htop genuinely *does* use `DECSTBM`+`SU`/`SD` (scroll-region hardware-scroll) for real list-scrolling — the earlier "no scroll-region ops" finding is retracted — and bisection pinned the corruption's first appearance to immediately after the first SD op following a run of SU ops (the real direction-reversal point).

**Root cause, confirmed from source and numerically verified:** `cmdScrollDown` (SwiftTerm `Terminal.swift:4646`, handles `CSI Ps T`/SD — invoked scrolling back toward the top of htop's list) is missing the `if marginMode {…} else {…}` guard its sibling `cmdScrollUp` (`Terminal.swift:4672`) has — it always computes `columnCount = buffer.marginRight - buffer.marginLeft + 1` and does a narrow margin-bounded copy, with no full-width fallback when column-margin-mode is off (the normal case; htop never enables DECLRMM). Traced fully: neither `Buffer.init` (`Buffer.swift:273`) nor `activateAltBuffer` (`Terminal.swift:789`, which creates htop's alt-screen buffer) ever sets `marginLeft`/`marginRight`, so they sit at their raw defaults (`0`/`0`) — giving `columnCount = 1`. Every scroll-down shifts *only column 0*, leaving every other column frozen indefinitely — unifying both symptom classes (trailing garbage columns never touched; row duplication because the old physical row's untouched columns keep showing stale content while htop explicitly redraws the new position) under one cause, not two.

Captured as a dated addendum (§7, not a rewrite) to `research/03-analysis/scroll-reversal-redraw-corruption-forensics.md` — new fates-table rows T8–T12 (including the retracted-then-reinstated T9), reproducible probes (the mouse-position gotcha for `peekaboo`, the resize-heal recipe, the real-capture-and-replay recipe), a source-level re-verify, and guideline `G(verify-the-regime)`. `research/README.md`, this file's `AGENTS.md` table-row one-liner, and `packer/README.md`'s expected-difference matrix row all updated to reflect the found root cause. **Still not yet an OpenSpec change** — the diagnosis is settled; a fix proposal (mirroring `cmdScrollUp`'s guard onto `cmdScrollDown`) has not been drafted.

### 2026-07-10 — fix-scroll-reversal-redraw-corruption (product fix) — implemented 9/9, IN-ENVELOPE `54/0/1` of 55 across all 5 environments, archived

Proposed and implemented the fix for the root cause found in the same-day forensics addendum above. **The patch:** a new hunk in `patches/swiftterm/xtty-accessors.diff` mirrors `cmdScrollUp`'s existing `if marginMode {…} else {…}` shape onto `cmdScrollDown` (`Terminal.swift:4646`), direction-reversed — the `marginMode`-true narrow per-cell copy is left untouched; the new `else` branch does a full-width whole-line `splice`, deleting the line scrolling off `scrollBottom` and inserting a fresh blank at `scrollTop` per iteration of `p`. This is the first hunk in that patch file that isn't purely additive (every prior hunk only added new code — accessors, the Metal-shader-resource strip); `patches/swiftterm/UPSTREAM_CONFIG.sh`'s header comment now flags this explicitly for the next reader. `scripts/bootstrap-swiftterm.sh` re-run and confirmed the revised patch applies cleanly from the pristine `v1.13.0` pin.

**Hand-authoring the diff hunk surfaced a real gotcha, caught by testing rather than assumption:** the first attempt (typed by hand into the `.diff` file, copying the intended new code) silently dropped the mandatory leading-space marker on two blank *context* lines, and separately lost the original pristine source's incidental trailing whitespace on a third blank line inside the unchanged `marginMode`-true branch — both invisible in a normal read but fatal to `git apply` (`patch does not apply` / `error: while searching for:`). Fixed by never hand-transcribing diff text again: captured the real `git diff` output for the isolated `Terminal.swift` edit to a temp file (byte-exact, whitespace and all) and spliced it into the tracked patch file with `head`/`tail`/`cat`, rather than retyping it through an editor tool — `scripts/bootstrap-swiftterm.sh` then applied clean on the first try.

**Test:** `testScrollRegionReversalDoesNotCorruptOtherColumns` (`AppUITests/XttyMouseWheelUITests.swift`, added in the proposal session, confirmed red pre-fix) drives the exact `SU`/`SD`-inside-a-scroll-region escape sequence directly via `printf` — enter the alternate screen, set an 8-row scroll region, write three rows of distinct 10-character content, issue `SU` once then `SD` once, assert row 1 is fully restored in every column. A real full-screen-program test (mirroring the suite's existing `testWheelScrollsRealMouseTrackingPager`) was investigated and rejected during proposal: `vim` (uses already-guarded `IL`/`DL`) and `less --mouse` (uses a bare `RI`, no scroll region) never invoke `cmdScrollDown` regardless of how driven, and `htop` (which does, via ncurses' own hardware-scroll optimizer) is brew-only and absent from the minimal test VM image — so "find a bundled real program that happens to hit this" is unreliable in principle, not just this instance.

**Verification:** `make test-core` `237/0/0`; the single test confirmed green locally (7.8s) against the patched build; `xtty-test-validator` then ran the full Tier-1 + VM acceptance matrix (both bash and zsh goldens, headless + graphics) — **IN-ENVELOPE, `54/0/1` of 55 across all 5 environments**, byte-identical failing sets, the new test green everywhere (6.7–8.9s), the previously-tracked graphics-zsh runner-stall anomaly did not recur. `xtty-openspec-critic` pre-archive review: **COHERENT**, no blockers (two non-blocking REVIEW notes: a mechanism-neutrality style nit on the new `verification-harness` requirement body, and the disk-drift reminders this archive addressed — see below). Cross-change pass confirmed zero requirement collisions with the concurrently-open `smooth-scroll-wheel-momentum` (disjoint requirements in both `terminal-session` and `verification-harness`, per design.md D4).

**Reconciled on archive:** `AGENTS.md` snapshot (2026-07-08→2026-07-10, test count 48→55 with attribution noted — 1 from this change, 6 already landed by the unarchived `smooth-scroll-wheel-momentum`), the `smooth-scroll-wheel-momentum` open-changes row (its "not yet fixed" scroll-redraw note flipped to "fixed"), the missing `harden-test-precision-vs-claim` open-changes row added (a separate pre-existing drift, addressed opportunistically), the Shipped-and-archived Tooling row, and `packer/README.md`'s "Scroll-reversal redraw corruption" matrix row (still-open→FIXED) plus a new dated Acceptance blockquote recording the fresh full-matrix measurement. MODIFIES `terminal-session` (extends "Interactive terminal input and output" with a reversal scenario) + `verification-harness` (new "Scroll-region shift redraw-correctness coverage" requirement). Upstream SwiftTerm PR filing deferred to a maintainer action, per established precedent (`fix-scroll-wheel-mouse-reporting`, `smooth-scroll-wheel-momentum`).

**Same-day follow-up: the test's own assertion was under-proving its claim, caught by re-inspection after archive.** The merged `verification-harness` scenario promises "the affected **rows**" (plural) "fully restored... in every column," but the shipped test asserted only row 1. Widening the assertion to also check rows 0 and 2 surfaced a second, unrelated gotcha: with the assertion widened, row 2 read `"CCCCCCCCCC%"` — not the product defect, but a test-environment artifact. `SU`/`SD` never move the cursor, so after writing `"CCCCCCCCCC"` at row 2 the cursor stays parked at column 10 of that row for the rest of the shell command's life; since `printf`'s output ends without a trailing newline and the alternate screen is never explicitly exited, the shell's own next-prompt draw (zsh's reverse-video `%` no-newline marker) lands exactly there, inside the still-active alt screen. Fixed by appending `\033[10;1H` to the injected sequence, parking the cursor below the scroll region before the shell command exits. Re-verified: the single test green (8.7s) with all three rows now asserted, and the full `XttyMouseWheelUITests` class (13 tests) green with no neighboring regressions. Test-only change (`AppUITests/XttyMouseWheelUITests.swift`); already conforms to the merged spec scenario, so no new OpenSpec change or spec edit needed. A fitting live instance of the very `harden-test-precision-vs-claim` rule proposed earlier the same day.

### 2026-07-10 — smooth-scroll-wheel-momentum (product fix) — implemented 20/20 (build+test) + 2/2 (coherence+archive), IN-ENVELOPE across all measured environments, archived

Proposed and implemented the trackpad/Magic-Mouse feel refinement of the shipped `fix-scroll-wheel-mouse-reporting` routing patch. Root-cause + 5-peer consensus (`research/03-analysis/scroll-momentum-smoothness-research.md`, fan-out `wf_2cf7d159-6ef`) found two anti-flood guards the routing fix had added — both absent from pristine upstream SwiftTerm and implemented by **zero of five** surveyed mature peers (iTerm2, Ghostty, kitty, WezTerm, Alacritty): a blanket `if event.momentumPhase != [] { return }` guard above the 3-way dispatch that discarded **all** inertial-coast events on **every** branch (scrolling stopped dead on finger-lift, no native macOS coast), and a cap-and-discard accumulator (`xttyWheelRowCount`: `remainder -= whole; return min(5, whole)`) that subtracted the full accumulated delta while emitting at most 5 rows, silently dropping the overflow on a fast flick.

**The fix (Targeted, knob-free cut — user-chosen over the fuller per-branch accumulator rework + a deferred `scroll-multiplier` config knob):** D1 deleted the momentum guard entirely rather than narrowing it per-branch (peer-unanimous: 0/5 drop momentum on any branch). D2 rewrote the precise (`hasPreciseScrollingDeltas`) accumulator path to carry the full sub-cell remainder losslessly and emit every accumulated whole row, bounding flood structurally (≤~1 report/key per whole cell traversed) instead of by a fixed cap — the mechanism every peer uses. D3 reset the remainder on the user-driven `.began` phase so a stale sub-cell fraction can't leak across separate flicks as a phantom first-line jump. D6 extended `public struct XttyWheelRouting` (`XttyAccessors.swift`) with `momentum`/`precise` booleans, set at all three routing-decision sites, surfaced through the App-layer DEBUG state dump. All four changes landed as a revision to the existing `scrollWheel`/`xttyWheelRowCount` hunk in `patches/swiftterm/xtty-accessors.diff` (D7); `scripts/bootstrap-swiftterm.sh` re-applied cleanly from the pristine `v1.13.0` pin.

**Automating the momentum routing *logic* (D8):** since a real inertial-coast event cannot be produced through the XCUITest automation channel (synthesized gestures carry no momentum) and posting a raw CGEvent is dropped in the runner (`CGPreflightPostEventAccess=false`, the established refutation — about *posting*, not *constructing*), the change added a `#if DEBUG` + `-UITestGridDump`-gated synthetic-wheel-event injection hook following the established `App/XttyApp.swift` XCUITest-trigger family: the test supplies a wheel-event spec, and the app constructs a faithful wheel `NSEvent` via `CGEvent(scrollWheelEvent2Source:…)` → `NSEvent(cgEvent:)` and drives it through the **real** `scrollWheel(with:)` — never a reimplemented branch. Feasibility spike-verified: `NSEvent(cgEvent:)` faithfully bridges `momentumPhase`, `hasPreciseScrollingDeltas`, and `scrollingDeltaY`, exactly the fields the router reads.

**Tests — 6 new `XttyMouseWheelUITests` (suite 48 → 54):** a crisp negative (`momentum == false` on a real automation-channel gesture, proving the field is wired on the real-gesture path); 3 injection-based regression guards for D1/D2/D3 (momentum-not-dropped, lossless-carry, `.began`-reset); and two tests **added mid-flight at the user's request** after the manual verify below succeeded and asked for its own techniques to be captured as automated regression guards — `testInjectedMomentumFrameProducesRealSGRMouseReportBytes` (arms `printf '\033[?1002h\033[?1006h'; cat -v`, the exact manual technique, and asserts the real SGR wheel-report bytes reach a child process, not just DEBUG-dump bookkeeping) and `testWheelScrollsRealMouseTrackingPager` (D9: drives `seq 1 500 | less --mouse` — a real, unmodified, native pager that negotiates its **own** mouse tracking, unlike every other test in the suite which arms mouse mode via a `printf` simulation — and asserts its own visible content genuinely advances, polled up to 15 ticks). Both were **hardened post-adversarial-review**: the real-pager test's readiness wait was ambiguous (fixed to match only `less`'s actual line-by-line rendering shape) and its per-tick grid read raced the app's 150ms async dump-write timer (fixed with a `waitForGridChange` polling helper) — the same read-freshness race class `packer/README.md` already documented for the graphics tier.

**Verification, in stages:** `make test-core` `237/0/0` regression-clean. The Tier-1 + full VM matrix (task 4.3, delegated to `xtty-test-validator`) first came back OUT-OF-ENVELOPE — `testAccumulatedRemainderResetsBetweenGestures` red on the bash-golden **graphics** tier only, root-caused to the same test-side read-freshness race class (not a product regression), fixed test-side and re-delegated **IN-ENVELOPE `51/0/1` of 52** on all 5 environments. The **MANUAL physical-trackpad verify-by-effect** (task 4.4, HUMAN-RUN — closes only what D8's injection cannot: real OS coast delivery/rate/back-pressure) had all 5 sub-checks confirmed by the user with live DEBUG-dump/routing-log corroboration: coast reports continue after finger-lift, a hard flick loses no distance (no cap-truncation pattern), htop stays interactive through a sustained coast, both local scrollback and an alt-screen pager coast naturally including under direction reversal, and xtty feels comparable to a real iTerm2 A/B on the same flick — and this last A/B step **re-triggered the already-tracked scroll-reversal redraw corruption** on the xtty side only (a fresh real-hardware corroboration of the already-forensicked, separately-fixed issue above, not a new blocker). Task 4.5 re-ran the VM matrix for the byte-level SGR test: **`52/0/1` of 53**, with one **unrelated** red on headless-zsh (`XttyMultiplexingUITests.testNewTabOpensAndLastPaneCloseEscalates`) root-cause-traced to a pre-existing, still-open confirm-close race class distinct from this change (confirmed not the retired menu-clobber class, confirmed not caused by this change's purely-additive `#if DEBUG` diff), recorded in `packer/README.md`'s expected-difference matrix as a tracked follow-up, not blocking. Task 4.6 re-ran the matrix for the real-pager test: the test itself was **green on all 5 environments including both graphics-zsh samples** (the D9 discriminator never fired, confirming the hardening held under real VM conditions), but the sweep as a whole hit an **unrelated** ~930s graphics-zsh XCUITest-runner `typeText()`-synthesis stall — reproduced on 2 independent samples, starving a different collateral wheel-test's short timeout each time, confirmed not caused by this change (graphics-bash ran the identical injection code clean in the same sweep) and not the routing/read-freshness logic D9 concerns. Per advisor consultation, padding the collateral timeouts to survive an unbounded stall was rejected (no fixed wait survives it, and it would ~3× routine graphics-zsh runtime for no signal) and a third wash-out re-run was rejected as unwarranted (2 independent samples already corroborate the same root cause) — recorded as a new, still-open, not-yet-root-caused environmental anomaly in `packer/README.md`'s matrix, not blocking archive.

**Coherence (pre-archive `⟶ xtty-openspec-critic`, Definition v1): COHERENT**, archive-ready. Every check passed clean: spec-delta syntax, both MODIFIED requirements pasting the full established block (not truncated fragments), the proposal↔specs contract both ways, all delegation markers present and correctly scoped, verification-harness coupling, mechanism-neutrality, design↔requirements traceability, tasks coverage, and all three reverse-duty `packer/README.md` envelope updates (4.2/4.5/4.6) confirmed present on disk. One informational cross-change note: `harden-test-precision-vs-claim` and `revamp-agents-md` (both still open) share a `coherence-review` requirement collision constraining **their own** hand-merge order — orthogonal to this change, which touches `terminal-session` + `verification-harness` only and archives independently. One unrelated REVIEW-level repo-tracker note (the `revamp-agents-md` open-changes row was missing from AGENTS.md) folded into this change's own reconcile pass below.

**Reconciled on archive:** AGENTS.md snapshot (the stale "6 more tests… concurrently-open, unarchived" attribution replaced with a clean 48+6+1=55 breakdown and a "Most recently" narrative sentence for this change), the `smooth-scroll-wheel-momentum` row moved from Open-changes to the Shipped-and-archived Tooling cell, the missing `revamp-agents-md` open-changes row added (the critic's cross-change note, addressed opportunistically), a new Learned-refutations one-liner ("don't drop momentum / cap-and-discard / time-throttle the wheel"), and `research/03-analysis/scroll-momentum-smoothness-research.md` added to the Tooling row's Detail pointers. `packer/README.md`'s acceptance envelope already reflected the final 55-test count (updated live during tasks 4.2/4.5/4.6, ahead of this reconcile). MODIFIES `terminal-session` (the Mouse-wheel routing requirement's emission policy) + `verification-harness` (extends wheel-routing observability + end-to-end coverage; adds the momentum-injection-coverage requirement). Upstream SwiftTerm PR filing deferred to a maintainer action, per established precedent (`fix-scroll-wheel-mouse-reporting`, `fix-scroll-reversal-redraw-corruption`). Verified against disk: `openspec list` (3 changes remain: `add-ci-pipeline`, `harden-test-precision-vs-claim`, `revamp-agents-md`), archive dir `2026-07-10-smooth-scroll-wheel-momentum` present, `openspec validate --all --type spec` → 25/25 valid.

### 2026-07-10 — symlink-claude-md-to-agents-md (P-tooling / docs) — implemented 9/9, coherence COHERENT, archived

Made the `CLAUDE.md`/`AGENTS.md` relationship maximally boring and DRY, per an explicit user request during `/opsx:explore`. `CLAUDE.md` was a regular file: a Claude Code `@AGENTS.md` import plus a "Notes for Claude Code" appendix (treat AGENTS.md as source of truth / build via the Makefile / Conventional Commits + OpenSpec). Auditing the appendix against AGENTS.md's own Building/Conventions/How-to-work-here sections found zero non-redundant content — every bullet just restated what AGENTS.md already said in more depth. Replaced `CLAUDE.md` with a symlink (`CLAUDE.md -> AGENTS.md`), which makes the two files byte-identical by construction rather than by import-plus-review-discipline, and dropped the now-redundant appendix. New capability `agent-guide-parity` (1 requirement, 2 scenarios): the project maintains exactly one canonical, tool-agnostic guide, and tool-specific entry points carry no independently-editable content of their own.

**Verification:** `readlink CLAUDE.md` → `AGENTS.md`, `diff CLAUDE.md AGENTS.md` empty, `git ls-files -s CLAUDE.md` shows symlink mode `120000` (not `100644`). The context-load spot-check needed a bespoke instrument, not a literal `/context` run (not scriptable from a tool call): a same-environment before/after comparison using the repo's own reproducible startup-cost probe (`agents-md-context-budget.md` §3.1, `claude -p ... --output-format json`, summing `input_tokens + cache_creation_input_tokens + cache_read_input_tokens`), run in an isolated `git worktree` at the pre-change commit vs. the post-change working tree — **65,190 → 65,462 tokens (+0.4%, noise-level)**, not a doubling. A raw comparison against the 2026-07-06 baseline (35,589 tokens) would have been invalid on its own: many more skills/agents/MCP servers are installed today than in early July, which would have confounded an absolute-number check — the before/after design controls for that by holding the environment fixed. `xtty-openspec-critic` pre-archive review: **COHERENT**, two non-blocking REVIEW notes (the requirement names the concrete "symlink" mechanism in its body — defensible for a structural-parity capability, already softened as "or an equivalent construct"; and a reminder that this change never got a propose-time open-changes-table row, so the archive step adds the shipped entry directly rather than flipping one).

**Reconciled on archive:** `AGENTS.md`'s two self-references to the mechanism corrected (the intro line and the repo-tree comment, both "imports" → "symlinks to"/"symlink to"), the Established-specs list gained `agent-guide-parity`, the Shipped-and-archived Tooling row gained this entry, and this HISTORY.md entry. No `verification-harness` delta — a docs/tooling change with no product-observable behavior, framed in the new spec's Purpose as parallel to `build-workflow`'s and `research-capture`'s "meta/tooling capability" framing.

### 2026-07-09 — fix-scroll-wheel-mouse-reporting (P-tooling / product fix) — implemented 15/15, manual-verified by effect + full matrix `47/0/1` of 48, archived

**The bug:** full-screen apps that enable mouse tracking (htop, vim, tmux, fzf, less-with-mouse) **did not scroll with the mouse wheel** in xtty. Root cause (from the `mouse-wheel-scroll-forensics.md` research): xtty's embedded SwiftTerm `MacTerminalView.scrollWheel(with:)` unconditionally moved its own local scrollback and **never read `terminal.mouseMode`**, emitting **zero pty bytes** on the wheel; on the alternate screen that local scroll is a dead no-op (`maxScrollback = 0`). iTerm2 on identical input scrolls htop a full page.

**What shipped (product fix, via the SwiftTerm patch):** `scrollWheel(with:)` rewritten as the priority-ordered **3-way branch** every mature terminal converges on — (1) **mouse reporting active** (`allowMouseReporting && terminal.mouseMode.sendButtonPress()`, i.e. vt200/buttonEventTracking/anyEvent; excludes off/x10) → a **wheel mouse-report** via `encodeButton(button: up ? 4 : 5, …)` (SGR 64/65) → `sendEvent`, press-only, at the `calculateMouseHit` pointer cell (0-based col, `yDisp`-clamped row); (2) **alternate screen, mouse off** → cursor **Up/Down keys**, DECCKM-aware (`terminal.applicationCursor` selecting SS3 `ESC O A/B` vs CSI `ESC [ A/B`); (3) **primary, mouse off** → the existing local scrollback (unchanged). Plus the **Shift-bypass** (Shift-modified wheel → local scrollback even under mouse reporting), the `deltaY == 0` horizontal guard, momentum collapse (`momentumPhase != []` dropped), and a bounded per-event row count (`xttyWheelRowCount`, NOT `calcScrollingVelocity`-as-count which floods 20–32/notch). Delivered as a hunk in `patches/swiftterm/xtty-accessors.diff` (regenerated from the checkout), upstream-ready. **Observability:** a `lastWheelRouting` DEBUG state-dump field (branch + direction + count + button/keyForm), backed by a new `public struct XttyWheelRouting` in `XttyAccessors.swift` (xtty-local, demarcated for stripping when the fix lands upstream). MODIFIES `terminal-session` (+1 requirement) + `verification-harness` (+2: observable + e2e coverage).

**Manual verify-by-effect (peekaboo, the real Debug app) — the headline proof:** under `printf '\033[?1002h\033[?1006h'; cat -v`, scroll-down now emits `^[[<65;28;10M` and scroll-up `^[[<64;28;10M` at the pointer cell (**zero bytes before the fix**); **htop scrolls a full page** (top row `22490 -zsh` → `2095 VS Code`); **`less`** (alt-screen pager, no mouse) **advances via arrow keys**. All three branches confirmed against the running app before the harness was trusted.

**Tests — 6 new `XttyMouseWheelUITests` (suite 42 → 48), all green:** report (alt+mouse), cursor-key ×2 (DECCKM normal + application arms), local-scrollback, Shift-bypass, and a synthetic-event **fidelity precheck** (3 up-ticks strictly decrease `scrollbackDepth`, each recorded as the `scrollback` branch — proving the generator is faithful before any routing assertion). Shell-independent: terminal state armed with `printf` escape sequences the engine parses regardless of login shell.

**Two hard-won harness lessons (both now Learned refutations):** (a) **the XCUITest runner can't post raw CGEvents** (`CGPreflightPostEventAccess=false`, Codex-rescue-confirmed) — a `CGEvent` scroll to `.cghidEventTap` is silently dropped, and `CGWarpMouseCursorPosition` doesn't update the WindowServer's window-under-cursor (a pixel-correct raw scroll still misrouted). The fix: drive the gesture through XCUITest's **automation channel** — `scroll(byDeltaX:deltaY:)` (element-targeted) + `perform(withKeyModifiers: .shift)` for the Shift case (the `perform` wrapper was Codex's contribution; a held-Shift CGEvent keyDown is *not* picked up by the synthesized scroll). (b) **zsh's ZLE turns DECCKM/`smkx` ON at every prompt** — so a bare zsh prompt is application-cursor while bare bash is normal; the DECCKM tests hold a known state deterministically with a foreground `sleep` so ZLE can't re-toggle it between setup and gesture.

**Validation (task 4.3, `⟶ xtty-test-validator`, Definition v5): IN-ENVELOPE.** Tier-0 `237/0/0`, Tier-1 local `47/0/1`, headless ×2 (bash + zsh) both `47/0/1`, graphics ×2 both `47/0/1` — all six environments identical, 0 reds, 0 vacuous passes, graphics matched headless (no LN modal). Evidence `~/Downloads/xtty-vm-poc/artifacts/2026-07-09-fix-scroll-wheel-fullmatrix/`. `packer/README.md` updated (suite 42→48, `47/0/1` measured, a new `Mouse-wheel routing (shell-independent)` matrix row).

**Upstream (task 5.1):** the `scrollWheel` rewrite is a genuine SwiftTerm bug fix and is upstream-ready; the upstream-bound-vs-xtty-local demarcation + retire-on-merge plan are recorded in `design.md` → *Upstream note*. The **actual PR to `migueldeicaza/SwiftTerm` is deferred to a maintainer action** (outward-facing; not filed unilaterally) — no PR link yet.

**Coherence (pre-archive `⟶ xtty-openspec-critic`, Definition v1): COHERENT**, no blockers; the 3 REVIEW items (Current-status row absent + snapshot count 42→48 + the deferred-PR tick) were all owed to and folded into this reconcile. **Reconcile:** research index line flipped "candidate change (not yet proposed)" → shipped; AGENTS snapshot count + envelope + Shipped-table entry + 2 refutations; this narrative; verified against disk (`openspec list` = only `add-ci-pipeline` active · archive dir `2026-07-08-fix-scroll-wheel-mouse-reporting` · 22 specs validate).

### 2026-07-08 — add-research-launcher (P-tooling / dev-tooling) — implemented 8/8, dogfood held by effect, archived (new `research-orchestration` spec)

**What shipped:** the committed **`/xtty:research`** launcher (`.claude/commands/xtty/research.md`) — the 4th `xtty-*` launcher, closing the `dev-workflow-agent-orchestration.md` §3 gap (*"explore/research… has no launcher+worker"*) and realizing §11's T3-reinstated-as-Workflow decision. Unlike the three sibling launchers, its **worker is a per-invocation multi-agent Workflow, not a standing agent** (D2) — the reader→synthesis→critic fan-out is "structured multi-stage at scale," which §3 maps to the Workflow mechanism, *not* the roster T1 refuted — so it carries **no `.claude/agents/xtty-*` definition and no `Definition:`-stamp / delivery-check / babysitter** machinery (a real advantage: the agent-definition-lag gotcha simply does not apply). The launcher is a **thin recipe**: the role→model tiering table (D3 — orchestrator Opus·xhigh · scout Haiku·low · readers Sonnet·medium · synthesis Opus·high · critic Opus·high→xhigh · verify Sonnet/main · capture `xtty-capture-research`), the staged pattern (explore → optional scout → readers fan-out → synthesis → critic → verify-by-effect → capture hand-off), and a pointer to AGENTS.md for the delegation boundary (not a restatement) — deferring to AGENTS.md for the rules (D1/D4/D6).

**AGENTS.md wiring:** one new *How to work here* delegation rule (the inline-vs-delegate boundary: source-heavy multi-source research above the ~12k-token subagent-inheritance floor → a `/xtty:research`-launched Workflow; light existing-`research/`-doc reads and single-file lookups stay inline in main/fork — mirroring the validate/investigate/review rules, AGENTS.md the single source of truth) + the *Conventions* "Don't track local tooling" tooling-exception line.

**New capability `research-orchestration`** (4 requirements — the *doing* counterpart of `research-capture`'s *write-up* phase, composing research-orchestration → research-capture): committed source-research launcher tooling (thin launcher, Workflow worker, git-tracked); documented role→model tiering; the delegation boundary; and the fan-out's adversarial-critique + verify-by-effect + capture hand-off. **No `verification-harness` delta** (dev-tooling only, parallels `add-openspec-critic-agent` / `add-ci-investigator-agent`). **No product/app/`XttyCore`/test code.**

**Dogfood — by-effect proof held (run `wf_7dac0fa9-ea9`).** Drove `/xtty:research` end-to-end on a real question ("how do native terminals implement bracketed paste / OSC 2004, and how does it map to xtty?"): a per-invocation Workflow with the tiered models — **4 agents, 0 errors, 0 empty results, ~207k subagent tokens, 394 s**. Readers (Sonnet·medium, ∥) returned file:line-grounded records for Ghostty + Alacritty (shallow-cloned to `/tmp`); synthesis (Opus·high) produced a compact comparison + xtty mapping; and the **adversarial critic (Opus·high) earned its keep** — it read SwiftTerm's own source (which neither reader touched) and **refuted** the synthesis's central mechanism claim (that xtty's bash-3.2 first-line-execute residual comes from a `\n`→`\r` CR-rewrite like the two references). **Verify-by-effect (main loop, D5)** confirmed the refutation firsthand: `MacTerminalView.swift:1202-1208` wraps only when `bracketedPasteMode` is on and otherwise `send(txt:)`s raw bytes (`AppleTerminalView.swift:1982` — pure UTF-8, no CR-rewrite), so the residual is a **shell tty-line-discipline effect**, not an emulator rewrite; `EscapeSequences.bracketedPasteEnd` = `ESC[201~` byte-identical to both references; the measured `AppUITests/XttyUITests.swift:126-139` bash arm confirms the predicted *effect* (first `\n`-terminated line executes, tail staged). **Capture (D6) fired null-delta** — the owning doc `research/03-analysis/shell-dependent-test-partitioning.md:43-47` already carried the correct raw-bytes mechanism, and the critic's "injection gap" note was an unverified future-hardening idea, correctly **not** minted as durable research. Net: the adversarial-critic + verify-by-effect stages stopped a wrong mechanism claim from ever reaching `research/` — the recipe working as designed.

**Coherence (pre-archive `⟶ xtty-openspec-critic`): ISSUES-FOUND → fixed.** The critic (Definition v1) caught a real **BLOCKER** — the launcher was on disk but **git-untracked** (`??`), contradicting its own headline "tracked by git" requirement; a routine `git commit -am` would have silently omitted the change's central deliverable. Fixed by `git add` before ticking the archive task. Two REVIEW items (the stale `0/8` status row → cleared by this reconcile; a mechanism-neutrality note → acceptable-by-design per D2/D4, mirrors the sibling tooling specs) were non-blocking. Archive + reconcile ran inline from the `⟶ archive-ritual` marker.

**Reconcile:** `dev-workflow-agent-orchestration.md` §11 flipped "decided, NOT yet built" → built; `research/04-design/02-milestones.md` updated; established-specs line gains `research-orchestration`; the open-changes row removed. Verified against disk (`openspec list` = only `add-ci-pipeline` active · archive dir `2026-07-08-add-research-launcher` · `research-orchestration` in `openspec/specs/` · all 22 specs validate).

### 2026-07-08 — source-research launcher design + T3 revisit (research capture; informs a future `add-research-launcher`)

**Question (`/opsx:explore` → capture):** the previous exploration ran a lot of research from source code (the 12-agent `add-install-workflow` comparator sweep — clone 8 OSS terminals, read build machinery, synthesize, critic, verify by effect). The user asked to **formalize that pattern as reusable agents/workflow** (explore → research → critic → verify) with a **recommended model per role**.

**Grounding:** the ask lands squarely on the settled [`dev-workflow-agent-orchestration.md`](research/03-analysis/dev-workflow-agent-orchestration.md), which on 2026-07-07 (a) **demoted a source-researcher agent as too-rare-to-build (T3)** and (b) named the exact gap — *"explore/research… has no `xtty-*` launcher+worker."* Two probes over `research/03-analysis/*.md` were run to test whether T3's "build only if clone-heavy research recurs" condition is now met: **13 docs cite a multi-agent fan-out; 9 cloned external source** (distribution, CI, install, confirm-close, VM-repro, p4, p4b, p6, this doc's own fan-out). So clone-heavy source research has recurred across ~7–9 changes — **§8's reactive test flips from "don't build" to "build it."**

**Design decided (captured as `dev-workflow-agent-orchestration.md` §11, a dated addendum + a T3 forward-pointer — evolving decision, not a rewrite):**
- **The mechanism is a *Workflow launcher*, not a roster** — §3's rule (*structured multi-stage at scale → Workflow*) means the worker is one parameterized fan-out (readers → synthesis → critic → verify), **not** standing reader/synth/critic agents (which would be the roster-of-4 that T1 refuted). This completes the §3 launcher gap in the form §3 prescribes, without contradicting the reactive/no-roster rule.
- **Model tiers = §3.D** (the ask), consistent with the committed agents: main-orchestrator **Opus/xhigh** (never delegated) · optional scout **Haiku/low** · readers (fan-out) **Sonnet/medium** · synthesis **Opus/high** · critic **Opus/high→xhigh** · verify-by-effect **Sonnet/medium or main** · capture main+`xtty-capture-research`. (Fable intentionally unused — not an analytical tier.)
- **Packaging = Medium** (of Minimal/Medium/Full): a committed **`/xtty:research`** command carrying the tier table + the staged pattern + the verify/capture tail; the main loop authors the *heterogeneous* fan-out inline each time. Chosen over **Full** (a rigid committed workflow script — research shapes have been too varied: per-terminal/per-facet/per-repo/forensics) and **Minimal** (doc-the-tiers-only — banks nothing invokable). Medium needs **no new `.gitignore` exception** (`!.claude/commands/xtty/` covers it) and **no new standing agent** → no `Definition:`-lag concern (worker is a Workflow).
- **Verify-by-effect** defaults to the **main loop** (the orchestrator sees the probes firsthand, as the install Release/quarantine/plist spikes were run inline); a Sonnet verify stage only when probes are many/isolated.
- **New reusable guideline (§9.7):** formalize a recurring fan-out as `command=launcher / worker=Workflow` with §3.D tiers baked into `agent()` calls, width main-authored — reactive, not a roster.

**Status:** decided; **not yet built.** The build is a small OpenSpec change (working name `add-research-launcher`: the `/xtty:research` command + an AGENTS.md *How-to-work-here* delegation rule mirroring the validate/investigate/review inline-vs-delegate boundary + this addendum). No product/spec/code touched; AGENTS.md's existing "don't pre-build a speculative roster" refutation is unchanged and consistent (this is the reactive build that rule endorses).

### 2026-07-08 — local-install-workflow comparator research (research capture; informs a future `add-install-workflow`)

**Question (`/opsx:explore` → capture):** the user wants to **daily-drive a stable, versioned build** of xtty from `/Applications` at `$0`, via a `make install` — not the deferred `$99` download channel. Today `make run` builds **Debug** and launches it **in place** from the volatile `build/` dir (ad-hoc identity churns each rebuild, wiped by `make clean`, absent from Spotlight/Dock). Preceded by an explore thread that first weighed **brew tap distribution** and settled it is the wrong lens for a *local* install — a Homebrew cask is a download channel layered on notarization (`distribution-signing-research.md` §9), whereas a locally-compiled app is never quarantined, so `make install` needs none of that ceremony.

**Method:** a **12-agent research workflow** (run `wf_e6a35c9f-517`, ~974k subagent tokens, 0 errors) — 10 parallel per-terminal comparator readers over `research/01-terminals/*` + shallow clones of the 8 OSS terminals to `/tmp/xtty-term-research/<name>` (Warp + Apple Terminal closed → research doc + public facts), a synthesis pass, and an adversarial completeness critic (verdict **usable-with-caveats**) — then **3 local verification spikes** on the dev machine (Xcode 26.6, Apple Silicon) to close the critic's load-bearing unknowns.

**Findings (full record → [`research/03-analysis/local-install-workflow-research.md`](research/03-analysis/local-install-workflow-research.md)):**
- **Field:** only **iTerm2** (`make install`) and **rio** (`make install-macos`) ship a first-class copy-to-`/Applications` target; Ghostty/kitty/Alacritty/wezterm/Hyper/Wave all stop at an artifact and make the user drag it. So a real `make install` beats the field.
- **Recommended shape = iTerm2's**, with **Ghostty's** git-derived versioning: build `-configuration Release … $(SIGN_FLAGS) MARKETING_VERSION=$(VERSION) CURRENT_PROJECT_VERSION=$(BUILD)` → `mv` old to `xtty.app.bak` → **`ditto`** a self-contained copy into `INSTALL_DIR ?= /Applications`; add `make restart` (kill + relaunch). No assembly/re-sign (xcodebuild emits a signed `.app`), **no `make dmg`/notarization/auto-updater** (download-channel-only, `$99`, deferred). `make run` stays Debug.
- **Version strategy (corrected):** cut a **`v0.0.1`** tag; keep `CFBundleShortVersionString`/`CFBundleVersion` **numeric** (`git describe --abbrev=0` minus `v` / `git rev-list --count HEAD`, floor = `project.yml` `0.0.1`); put the full git-describe stamp in a **separate** field. The critic **refuted** the first `git describe --tags --always --dirty || echo 0.0.1` draft — `--always` emits a bare hash when there are 0 tags, so the `||` floor never fires and the plist field goes non-numeric (Apple requires ≤3 dotted integers).
- **3 spikes, all green** (§6 of the doc, re-runnable): the default `Release` config genuinely optimizes (`SWIFT_OPTIMIZATION_LEVEL=-O`, `SWIFT_COMPILATION_MODE=wholemodule`, `ENABLE_NS_ASSERTIONS=NO`; Debug `-Onone`) → **no `configs:` pin needed**; SwiftTerm is **statically linked** (no `Frameworks/`, no dylib in `otool -L`) → nothing to re-sign, Ghostty's `disable-library-validation` **N/A**; a CLI `MARKETING_VERSION=9.9.9 CURRENT_PROJECT_VERSION=4242` override **reaches the synthesized `Info.plist`** under `GENERATE_INFOPLIST_FILE=YES` (PlistBuddy read back `9.9.9`/`4242`, `Signature=adhoc`, `com.xtty.app`) → **no PlistBuddy post-step**. Bonus: both Debug + Release builds carry **no `com.apple.quarantine`** → `$0` clean launch confirmed.
- **Reusable guideline G14** (a `$0` local install needs no signing ceremony beyond what the toolchain emits; reserve DMG/notarization/Homebrew for a *downloaded* artifact) captured. Decisions locked in the explore session: **same bundle id** `com.xtty.app` for the stable build, and **verify-the-seam-first** (done).

**Status:** research captured; **`add-install-workflow` not yet proposed** (like `add-distribution-signing`, it stays out of the Current-status open-changes table until proposed). No product/spec/code change in this capture. Refutation added to AGENTS.md (local install ⇒ no quarantine ⇒ no DMG/notarization/brew).

### 2026-07-08 — add-install-workflow (P-tooling / build-tooling) — implemented 14/14, verified by build-output measurement, archived

**`add-install-workflow`** — realizes the G14 research above as a real command. Adds a `make install` that builds the optimized **Release** config and lands a stable, version-stamped, self-contained `xtty.app` in `/Applications` at **$0** — the daily-driver install path that `make run` (Debug, in-place, wiped by `make clean`) never provided. **No product/app/`XttyCore`/test code** — the change is a `Makefile` + `AGENTS.md` edit plus the first annotated git tag; per **D7** it carries **no `verification-harness` delta** (build-tooling only, no app runtime behavior — parallels `add-ci-pipeline` / `add-local-signing-identity`), so acceptance is **build-output verification**, not an XCUITest observation, and the verify tasks are cheap/inline (no `⟶ xtty-test-validator` marker).

**What shipped (Makefile):** `install` builds `-configuration Release` with `$(SIGN_FLAGS)` + `MARKETING_VERSION="$(VERSION)" CURRENT_PROJECT_VERSION="$(BUILD)"` CLI overrides (order-only prereqs on the SwiftTerm-sentinel/xcodeproj file-targets so it auto-reconstitutes/generates), backs up any existing `$(INSTALL_DIR)/xtty.app` → `xtty.app.bak`, then `ditto`-copies the built bundle into `INSTALL_DIR ?= /Applications`. `VERSION` = `git describe --tags --abbrev=0` with the `v` stripped, falling back through `project.yml`'s `MARKETING_VERSION` to `0.0.1`; `BUILD` = `git rev-list --count HEAD` (fallback `1`); both degrade cleanly in a tagless/no-git tree (`$(or …)`, GNU make 3.81). `restart` = `pkill -x xtty` + `open $(INSTALL_DIR)/xtty.app` (D-restart — a formal decision block added post-review). `make build`/`run`/`test`/`bench` are untouched (still Debug). AGENTS.md → Building gained the two table rows + a bounded stable-install note. First annotated tag **`v0.0.1`** cut on `main` HEAD (push is a separate owner action).

**Acceptance — measured on the dev machine (Xcode 26.6, Apple Silicon), all D7 build-output checks green:**
- **4.1** default ad-hoc `make install` → `** BUILD SUCCEEDED **`; the Release config genuinely optimizes (`SWIFT_OPTIMIZATION_LEVEL=-O`, `SWIFT_COMPILATION_MODE=wholemodule`, `ENABLE_NS_ASSERTIONS=NO`); `/Applications/xtty.app` is a **real copy** (not a symlink); `codesign -dvv` = `Signature=adhoc`, `Identifier=com.xtty.app`.
- **4.2** version stamp reached the **synthesized** `Info.plist`: `CFBundleShortVersionString=0.0.1` (numeric, from tag `v0.0.1`), `CFBundleVersion=256` (commit count) — no PlistBuddy post-step.
- **4.3** `$0` launch: **empty** `com.apple.quarantine` (locally-compiled ⇒ never quarantined); the installed copy launched via `make restart` with **no Gatekeeper prompt** (pid-confirmed running from `/Applications/xtty.app/Contents/MacOS/xtty`). (`spctl` reports `rejected` for ad-hoc, which does **not** gate launch — only quarantine does.)
- **4.4** survival + override: `INSTALL_DIR=~/Applications make install` installed to `~/Applications` (real copy); a second default install produced `/Applications/xtty.app.bak`; after `make clean` removed `build/`, `/Applications/xtty.app` **still launched** (it is a copy).
- **4.5** `make test-core` **237/0** — no regression from the Makefile change.

**Coherence (task 5.1, `xtty-openspec-critic` `Definition v1`): COHERENT, proceed** — spec-delta syntax clean (single `## ADDED`, 3 requirements each ≥1 four-hash scenario, all SHALL), the proposal↔specs contract holds both ways (sole `build-workflow` dir ↔ Modified Capabilities), no `⟶ xtty-test-validator` marker required or over-applied, the standard change tail correctly ordered, and the no-harness-delta omission is **sound** (D7, precedents confirmed on disk). Two non-blocking REVIEW items were folded in this session: the cited **`D-restart`** label resolved to no formal decision block → added one to `design.md`; and the repo-wide "**21 of the 41**" harness-delta prose count was stale (pre-existing drift, not introduced here) → re-derived from disk and bumped to **22 of the 43** (43 archived, 22 with a harness delta; this change adds one with none). No cross-change collision — `add-ci-pipeline` also touches `build-workflow` but with disjoint requirement names (they merge cleanly). MODIFIES `build-workflow` (+3 requirements: stable local install / version-stamped from VCS / reinstall-relaunch helper). Archived as `2026-07-08-add-install-workflow`.

### 2026-07-08 — split-shell-dependent-testplan (P-tooling / test-only) — implemented 17/17, both goldens `41/0/1`, 0 vacuous passes, archived

**`split-shell-dependent-testplan`** — the second half of the shell-dependent-test-partitioning plan (`research/03-analysis/shell-dependent-test-partitioning.md`), which **pivoted** from the original *skip-and-partition* strategy to **parameterize-and-assert**. `add-zsh-test-image` had measured that ~half the XCUITest suite is shell-dependent and dishonest on the bash rig: the semantic-capture family early-returned before asserting (vacuous passes that read as coverage) and the multi-line-paste test hard-failed on bash 3.2's missing bracketed paste (`:87`). A *skip* is a coverage hole (it verifies nothing on that shell), and the shell behaviors are already measured — so this change asserts the **correct behavior for each shell** instead. **No product/app behavior change** — SwiftTerm's paste bracketing + xtty's zsh-only OSC injection were already correct; only the test suite conflated "the shell can't do this" with pass/fail.

**What shipped (test-only + one DEBUG harness field):**
- **New DEBUG state-dump field `bracketedPasteMode`** (`App/TerminalWindowController.swift` `writeStateDump()`, read from `engine.bracketedPasteMode` — SwiftTerm `public private(set)`), `#if DEBUG` + `-UITestGridDump`-gated, observe-only. The true capability predicate a shell-dependent paste test branches on, rather than sniffing the shell binary/version (design D3/D5). Backed by a `verification-harness` ADDED requirement + scenario.
- **Paste test parameterized + renamed** `testMultiLinePasteIsNotAutoExecuted` → **`testMultiLinePasteMatchesShellBracketing`** (`AppUITests/XttyUITests.swift`). It branches on the *observed* `bracketedPasteMode` — sampled from the state dump **after** a computed-marker readiness gate (`waitForShellReady`, reusing the `harden-churn-shell-readiness` roundtrip) — and asserts per shell: **zsh/bracketed-on** → both lines staged, no `command not found` (the wrap-tolerant matcher from `harden-paste-wrap-assertion` preserved); **bash-3.2/bracketed-off** → the newline-terminated first line **executed** (`command not found` present **exactly once**), the unterminated tail **staged** — xtty's faithful forwarding (design D2, the layer-2 forwarding-fidelity framing). So the former `:87` bash red is now an **asserted green execution arm**.
- **Semantic-capture family de-vacuumed** — the 12 silent `guard waitForCaptureActive() else { attach; return }` early-returns across six suites (`XttySemanticCaptureUITests`, `XttySessionSidebarUITests`, `XttyBlockSidebarUITests`, `XttySpatialBlocksUITests`, `XttyGitReviewUITests`, `XttyFileLinkOpenUITests`) now call a shared `assertSemanticCaptureInactive()` **crisp negative** (asserts `lastSemanticAction` + `blocks` both empty — proving OSC 133 did not leak into a non-injecting shell). A crisp negative existed for **all** of them, so **zero last-resort `XCTSkip`s** were needed (design D4/Q2). The i18n-paste half of `testTruecolorEmojiAndWideChars` got wrap-tolerance only (single line + explicit Return → no bracketing execution divergence, so no branch). The already-shell-agnostic tests (`testFullScreenAppDoesNotBecomeACommandBlock`, `testNonPermittedSchemeIsBlocked`, `testConfiguredTreeLayoutIsReported`) were untouched.
- **The two-`.xctestplan` partition was DROPPED, never introduced** (design D1) — with per-shell assertions there is nothing to skip-around, so the original `Intersection.xctestplan`/`ShellInteractive.xctestplan` split + its `project.yml` scheme wiring (and XcodeGen-fidelity risk, the original Open-Question Q1) **dissolved**. The whole suite runs on every rig.
- **Validator now runs both goldens** — `.claude/agents/xtty-test-validator.md` bumped to **`Definition: v5 (2026-07-08)`**, learned the zsh golden `xtty-test-zsh:26.5` as a standing environment (both goldens in the full sweep — bash = capability-absent arm, zsh = capability-present arm), and a **VACUOUS-PASS** classification row added to its report skeleton; `/xtty:validate` launcher stamp check bumped to v5. Modified `test-validation` (the environment enumeration + a scenario).

**Acceptance — IN-ENVELOPE, both goldens `41/0/1` of 42, measured (`xtty-test-validator`, task 6.1).** The validator was served the **stale v4** definition (the known agent-definition-delivery-lag — a same-session spawn ~15 min after the v5 edit; confirmed by effect against the modified-uncommitted file) but the two-golden scope was supplied explicitly, so coverage is complete. Tier 0 core **237/0/0**; Tier 1 local zsh **41/0/1**; Tier 2 headless bash ×2 **41/0/1**+**41/0/1**; Tier 2 headless zsh **41/0/1**; Tier 3 graphics zsh **41/0/1**. **Vacuous-pass audit PASSED — 0 `capture inactive` strings on both goldens:** bash ran **16** `semantic-capture-absent-arm-asserted` crisp negatives (`bracketedPasteMode=false`, paste executes the first line once), zsh ran the capability-present arm (**0** absent-arm attachments, `bracketedPasteMode=true`, paste stages both lines). The bash↔zsh divergence is proven **by effect**; headless↔graphics on zsh identical (`fix-osc7-hostname-reverse-dns` holds on graphics); two headless bash runs identical (per-launch race settled green). A focused local zsh run of the renamed paste test also passed inline (bracketed arm). Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-08-split-shell-dependent-testplan-fullmatrix/`.

**Coherence (task 6.2, `xtty-openspec-critic` `Definition v1`): COHERENT, proceed** — the proposal↔specs contract (`verification-harness` + `test-validation`), spec-delta format, the harness coupling (new `bracketedPasteMode` observable → delta + task), and all delegation markers pass; the MODIFIED `test-validation` requirement is a strict superset of the established block; the declared archive-after-`harden-paste-wrap-assertion` ordering is satisfied (already archived, no live red→green pair — this change folds harden's `:82`/`:84` wrap-tolerance into the renamed test and asserts, not skips, the bash `:87` arm); no collision with `add-ci-pipeline` (disjoint capabilities). Sole finding was a non-blocking "may lack"→"might lack" wording nit — fixed, re-validated. **Reverse-duty this session:** `packer/README.md` Acceptance/matrix (bash paste red → asserted green arm; both goldens `41/0/1` measured; the matrix rows retitled — "Bracketed-paste — bash execution arm (ASSERTED, was a red)", the Shell-arm row rewritten to per-shell assertion), `research/03-analysis/github-actions-ci-cd.md` §19b (`bash32-no-bracketed-paste` RETIRED → asserted arm; the deferred guard-shell fix marked SHIPPED-and-better), and `shell-dependent-test-partitioning.md` flipped to "built + parameterized." Modified capabilities **`verification-harness`** + **`test-validation`**. Archived as `2026-07-08-split-shell-dependent-testplan`.

### 2026-07-08 — harden-paste-wrap-assertion (P-tooling) — implemented 14/14, red→green pair #3, IN-ENVELOPE, archived

**`harden-paste-wrap-assertion`** (P-tooling, **test-only — no product code**) — the third member of the type-at-prompt soft-wrap class, after `harden-focus-typing-assertion` (`:53`) and `harden-findbar-wrap-assertion` (`:198`). `testMultiLinePasteIsNotAutoExecuted`'s two **positive** grid-content assertions (`:82` first pasted line, `:84` second) were switched from the strict `GridDumpReader.waitForContains` to the wrap-tolerant `ignoringLineWraps: true` matcher already shipped at `:53`/`:198` — so a pasted line soft-wrapped behind a long prompt is still asserted present (paste *insertion* is what's checked; a line that genuinely never landed still fails). The `:87` "command not found" not-executed check stays **strict** (design D3 — it is not the failure path and cannot false-pass: on zsh the paste stages and never executes, and a shell error begins at column 0 where it does not wrap). **No new regression guard** — this change *reuses* the deterministic `testSoftWrapGuardIsWrapTolerant` that `harden-findbar-wrap-assertion` added, which already proves the matcher property in `make test` independent of prompt width; adding a paste-specific guard would be redundant (design D2 — the structural difference from the findbar change, which had to introduce the guard).

**Root cause — a misfiled residual, corrected.** `add-zsh-test-image` had filed the zsh-rig `:82` red as a "grid-capture arm" whose cause was a "staged/highlighted region scrape miss." The real cause (graphics-zsh forensic, 2026-07-08): the `add-vm-prompt-width-parity` 59-char hostname makes the zsh prompt exactly **70 columns**; the pasted first line `alpha<tag>` is always **9 chars**, so `70 + 9 = 79 > 78`-column terminal and the 9th glyph **soft-wraps** to the next physical row, which `App/UITestDump.swift` joins with `\n` — so strict `contains("alpha…")` can never span it. It is the **exact same wrap class** as the find-bar fix, not a capture bug, and it is **fully deterministic** (the token is always 9 chars → always wraps), not the per-launch race class. The paste *did* stage both lines correctly; the "does not stage both lines" reading was a strict-matcher artifact.

**Acceptance — red→green proven on both zsh VM arms (D-by-effect), delegated to `xtty-test-validator` (`Definition v4`, IN-ENVELOPE).** Local Tier-1 bare metal stayed **`41/0/1`** (the short local prompt does not wrap, so the wrap-tolerant matcher did not regress the strict-pass case; the guard passed 9.6s). On the wide-prompt zsh golden `xtty-test-zsh:26.5`, `testMultiLinePasteIsNotAutoExecuted` **flipped `:82` red→green on both arms** (headless 4.77s, graphics 5.11s), moving the **zsh-rig envelope `40/1/1` → `41/0/1`**, with **0** `…capture inactive…` attachments (the semantic family still asserts for real on zsh, all six live tests green 3.9–8.8s) and **0** `mDNSResponder policy 'pending'` events / no Local-Network modal on either arm (`fix-osc7-hostname-reverse-dns` holds, incl. on graphics). The **bash** `:87` execution arm is untouched — it is a genuine shell-capability difference (bash 3.2 has no bracketed paste) owned by `split-shell-dependent-testplan`, which has pivoted to **assert** that arm (not skip it). Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-08-harden-paste-wrap-assertion/`.

**Coherence (task 5.2, `xtty-openspec-critic` `Definition v1`): COHERENT, no BLOCKER** — the MODIFIED `verification-harness` "Soft-wrap-robust content assertion" block is a clean superset (all 4 prior scenarios verbatim + 1 new "soft-wrapped pasted line" scenario, prose extended to multi-line-paste coverage), the proposal↔specs contract holds (only `verification-harness`), delegation markers correct, cross-change ordering + orthogonality with `split-shell-dependent-testplan` intact. Two non-blocking REVIEW items were folded in this session: the critic caught harden's own D4/proposal narrative still describing split as **skipping** the bash arm via `XCTSkipUnless` (stale vs split's parameterize-and-assert pivot) — corrected skip→assert in all four spots; and it predicted the AGENTS.md "20 of the 39" harness-delta count would go stale — bumped to **21 of 41** (disk-verified) as this change carries a harness delta. **Reverse-duty this session:** `packer/README.md` (zsh envelope `40/1/1`→`41/0/1`, the matrix row retitled "zsh soft-wrap arm (FIXED)"), `github-actions-ci-cd.md` §19b (cross-check note rewritten; a stale bash-row `:84`→`:87` fixed), `shell-dependent-test-partitioning.md` (zsh `:82` reframed as a matcher fix, not a skip candidate), and the interaction pointer added to `split-shell-dependent-testplan`'s design D2. Modified capability `verification-harness`. Archived as `2026-07-08-harden-paste-wrap-assertion`.

### 2026-07-08 — add-zsh-test-image (P-tooling / test infra) — implemented 17/17, IN-ENVELOPE both rigs, divergence proven by effect, archived

**`add-zsh-test-image`** — the measurement-first first half of the shell-dependent-test-partitioning plan (`research/03-analysis/shell-dependent-test-partitioning.md`), now built and measured. The Packer test-image template is **parameterized by login shell** (`shell = bash | zsh`, D1): one source, two goldens from the same pinned inputs — `xtty-test:26.5` (bash, byte-identical to before) and the new **`xtty-test-zsh:26.5`** (built 3m37s, Metal-toolchain-free), selected by `make image-zsh` / `make image IMAGE_SHELL=zsh`. The zsh guest exercises xtty's zsh-only OSC 7/133 integration for real, so the 19-method semantic-capture family takes its **asserting** arm instead of the bash rig's early-returning **vacuous** arm. **No product/app/test code** — this change only adds an environment that can exercise the already-correct product.

**The rig-level Local Network neutralization was refuted, and the change re-scoped around it (D2).** The original design carried a per-boot `/etc/hosts`-seeding `LaunchDaemon` to answer the reverse-PTR from the files module and silence the zsh-only LN modal; it was **built and refuted by effect** (task 4.1: 40 `policy 'pending'` events — macOS answers the IPv6 PTR from files but escapes the **routable-IPv4** PTR to the local nameserver), and the follow-up sweep closed every remaining rig-level lever (TN3179 `Allowed*` arrays, NE-store pre-seed, SSH-child auto-grant → T7–T10, `local-network-privacy-forensics.md` §8). So no rig-level gate machinery ships; the durable fix is the **product** change `fix-osc7-hostname-reverse-dns` (the `gethostname(2)` swap, landed+archived 2026-07-08), and the standalone divergence is measured **headless** (the modal is graphics-only). Task 4.1's unfixed-build graphics run is the **red baseline** of the red→green pair whose green half is `fix-osc7` task 2.1.

**Acceptance is the measured divergence, proven by effect (D3), not an all-green — and it held on both arms.** Delegated to `xtty-test-validator` (Def v4). A user-requested **graphics** pre-check on both goldens ran first (both `40/1/1`; zsh asserted-for-real with 0 capture-inactive; **fix-osc7 confirmed modal-free on graphics** — 0 `policy 'pending'`, no modal, app typing at t=3.2s vs the earlier unfixed run's modal + freeze). Then the acceptance **headless ×2 matrix on both goldens** (task 5.1, **IN-ENVELOPE**, per-launch stable): bash `xtty-test:26.5` ×2 = **40/1/1** (semantic family vacuous — 16–17 `…capture inactive…` attachments; paste red at `:87`, executed); zsh `xtty-test-zsh:26.5` ×2 = **40/1/1** (semantic family **asserting for real** — **0** capture-inactive; the live OSC 133/7 tests all green and fast, no 38–62s freeze; paste red at `:82` via a **grid-capture** miss, not execution — zsh's bracketed paste correctly stages the line). **Measured discovery:** the fix removes the reverse-DNS call entirely, so there are **0** `policy 'pending'` even headless and the OSC 7 cwd-classification tests land on the **live/passing** arm (not the anticipated degrade arm). **Measured zsh headless envelope = `40/1/1` of 42.** Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-08-add-zsh-test-image-headless-matrix/` + `.../2026-07-08-graphics-both-goldens/`.

**Reverse duty this session:** `packer/README.md` gained a "Login-shell variant" build subsection, the LN-gate refutation → product-fix record, the **zsh rig headless `40/1/1` envelope** alongside bash, and an Expected-difference-matrix update — the Shell-arm row now reads bash-vacuous↔zsh-real, and the paste residual split into a **bash-execution `:87`** arm and a new **zsh grid-capture `:82`** arm so the zsh residual is codified (a future validator run against the zsh rig won't flag it UNEXPLAINED). `github-actions-ci-cd.md` §19b gained the shell-arm cross-check note (the paste guarantee is *exercised, not skipped* on zsh; divergence measured headless). Non-goal deferred to `split-shell-dependent-testplan`: converting the vacuous returns + the paste hard-assert into honest `XCTSkip`/`XCTSkipUnless` (the zsh grid-capture red is exactly what that follow-up skips). Modified capability **`build-workflow`** (the test-image build supports a zsh login-shell variant).

### 2026-07-08 — harden-archive-completion-trigger (P-tooling / workflow) — implemented 10/10, by-effect proof held on its own archive, archived

**`harden-archive-completion-trigger`** — a workflow fix with **no product code**. The `fix-osc7-hostname-reverse-dns` apply (2026-07-08) measured the archive + reconcile completion ritual failing to reach the `/opsx:apply` loop: task 4.3 read *"On completion, reconcile trackers…"* with **no archive verb and no procedure pointer**, so the loop improvised `openspec archive` and **stalled** — it never reached the full ritual / the expected `/openspec-archive-change` skill until the user intervened mid-apply. Root cause is an **affordance failure at the point of action**, not a missing rule: the generic apply skill walks `tasks.md` checkboxes, models only *implement* tasks, and never re-reads AGENTS.md; `config.yaml` `rules` don't ride `openspec instructions apply` either — so a rule that lives only in AGENTS.md is reconstructed from memory or skipped. And the obvious anchor was unusable: the `/openspec-archive-change` skill is **openspec-generated and gitignored** (`git ls-files` empty; `.gitignore` un-ignores only `xtty-*`), so no committed artifact could cite it.

**The fix reuses the project's proven point-of-tick marker** (built 3× for delegation — validator / ci-investigator / openspec-critic): give every change the same **standard change tail** — a pre-archive coherence task `⟶ xtty-openspec-critic`, then an **archive + reconcile** task carrying `⟶ archive-ritual`. Two deliberate departures from the delegation markers, both documented so the coherence critic doesn't misfire: **(D1)** `⟶ archive-ritual` anchors to **committed** guidance (AGENTS.md → OpenSpec "After archiving, finish the merge by hand" + Keep-progress-current + the `openspec archive` CLI), with the gitignored skill an **optional local accelerator**, never the committed anchor; **(D2)** it is a **procedure pointer, not an agent delegation** — the ritual runs inline and produces the very tracker edits the session must own, so there is nothing to delegate and `xtty-openspec-critic` does **not** expect an agent for it. Seeded at propose via a 4th `config.yaml` `rules.tasks` entry (defers to AGENTS.md, no restatement) + added to the "keeping a change coherent → tasks.md" checklist so the critic flags its absence; the four then-open changes (`add-zsh-test-image`, `split-shell-dependent-testplan`, `add-ci-pipeline`, `add-openspec-critic-agent` — the last unblocking its frozen "pending archive") were **retrofitted** with the marked task so the fix is live now, not only for future proposals. Modified capability **`research-capture`** (one requirement: the completion ritual is driven by a point-of-tick archive task whose marker anchors to committed guidance; +2 scenarios). Process forensics captured to the depth bar in `research/03-analysis/archive-trigger-affordance-forensics.md` (mechanism, gitignore evidence, scaffold-gap table, fates, guideline **G13**).

**By-effect proof (task 5.2 — the primary acceptance, held-open until a real archive) — HELD.** This change's own final task (5.3) carried `⟶ archive-ritual`, so archiving it *was* the test. Measured this session: on a **fresh/compacted context** (opened `/clear` → `/opsx:apply harden-archive-completion-trigger`) with **no user prompt to archive**, the apply loop reached 5.3, read the marker, resolved it to the committed AGENTS.md ritual, and ran the full sequence **from the marker** — `openspec validate` → `openspec archive` (merged the `research-capture` delta, `23 passed / 0 failed`, `+0 ~1 -0`) → finish-by-hand → tracker reconcile → verify-against-disk — **not** the fix-osc7 improvise-and-stall failure mode. Transparency caveat: being self-referential, the loop had read `tasks.md`/`design.md` in the normal apply "read context" step; the strictly-uncontaminated confirmation is the **next** open change's archive (`add-openspec-critic-agent`, non-self-referential), which 5.2's own text names as the standing alternative. Thread B (a committed `/xtty:archive` launcher) was **not** triggered — the inline ritual was not skipped. Coherence (task 5.1, `xtty-openspec-critic` `Definition v1`): **COHERENT**, 0 blocker — MODIFIED block a clean superset (4 paras + 7 scenarios), retrofit markers verified on all four open changes, `⟶ archive-ritual` correctly agent-less, harness delta correctly absent (docs/workflow). Archived as `2026-07-07-harden-archive-completion-trigger` (openspec dates the dir from the change's creation).

### 2026-07-08 — fix-osc7-hostname-reverse-dns (P-tooling / product fix) — implemented 8/8, by-effect GREEN, regression-clean, archived

**`fix-osc7-hostname-reverse-dns`** — the product fix locked by the 2026-07-07 LN refutation, now landed. `PaneController.localHostNames` (`App/PaneController.swift`) was rewired to source the local host-name set from **`gethostname(2)`** — the exact syscall behind the shell's injected OSC 7 `${HOST}` authority — via a new **pure, view-free** `XttyCore` helper `LocalHost.names(from:)` (mirrors the prior `{"", "localhost", name.lowercased(), short-form}` derivation), **dropping** the reverse-DNS `ProcessInfo.processInfo.hostName` call. This removes the trigger for the zsh first-launch macOS **Local-Network privacy modal** (+ its 38–62 s main-actor freeze) and is **strictly more correct** on corporate networks where reverse-DNS yields a divergent FQDN (the old path could misclassify a local cwd as remote); behavior-preserving in the common case — the sole consumer `OSC7.decode(_:localHostNames:)` is unchanged. **No `verification-harness` field** — the novel property is the *absence* of a network call, invisible to the DEBUG state dump (design D3); acceptance is therefore **by-effect**, delegated to `xtty-test-validator`.

**Task 2.1 (by-effect, graphics zsh rig — fresh `xtty-test-zsh:26.5` clone, fixed build):** measured **0/0/0** — no reverse-DNS PTR volley (`client pid … (xtty)`), no `policy 'pending'`, no on-screen modal, no first-prompt freeze; mDNSResponder xtty-absent (0 of 453). The lone deviation — a benign `com.xtty.app` NE-store **`UUID cache hit`** baked into the build-used golden (P5 predicted 0, measured 1) — was *investigated, not dismissed*: a default-deny enumeration artifact (alongside `com.tcltk.wish`/`com.apple.TV`), corroborated by `pending`=0, captured as the **G3 corollary** (P5 is not a clean discriminator on a build-used golden — forensics §9b). This is the **green half** of the red→green pair with `add-zsh-test-image` task 4.1 (the *unfixed* build on the same rig → 40 `pending` + modal). **Task 3.1 (regression, full sweep, `xtty-test-validator` `Definition v4`, IN-ENVELOPE):** Tier 0 core **237/0/0** (the +5 = the new `LocalHostTests`), Tier 1 local zsh host **41/0/1** `** TEST SUCCEEDED **` — every host-name/cwd-touching test green (the swapped path executes at runtime only on the zsh dev host); Tier 2 headless ×2 = **40/1/1** + Tier 3 graphics = **40/1/1**, sole VM red the documented benign bash-3.2 bracketed-paste residual; the bash VM envelope held **unchanged** because bash emits no OSC 7 (swapped path inert there — as designed). **Coherence (task 4.2, `xtty-openspec-critic` `Definition v1`):** COHERENT, no blocker — MODIFIED `terminal-session` pastes the entire established block + 2 new scenarios; the harness absence is the documented D3 call; red→green pair intact. Modified capability **`terminal-session`** (local-vs-remote cwd classification sources the host name without a network lookup).

**Reverse-duty this session:** forensics **§9** dated addendum (the GREEN result + G3 corollary + fate **T11** [pre-seed rejected-by-design] + guideline **G12** [don't pre-seed to rescue low-rung evidence]); §1/§6 flipped proposed→landed; `research/README.md` index + `packer/README.md` Runtime-workflow host-side-capture convention — for a graphics-arm screenshot, capture the Tart VM *window* host-side (`screencapture -l<windowid>`), **not** guest `screencapture`-over-SSH, which trips the macOS ScreenCaptureKit private-window-picker consent attributed to `com.apple.sshd-session`; **do not** pre-seed a guest grant. Landed `4ebe0cb` (code) + `55597c0` (§9 capture) + `021b7a2` (2.1/4.1 ticks + §1/§6 flip); archived 2026-07-08. Evidence bundle: `~/Downloads/xtty-vm-poc/artifacts/2026-07-08-fix-osc7-hostname-reverse-dns/` (+ the 2.1 lncapture bundle).

### 2026-07-07 — Local Network build-time-grant refutation + product-fix lock (research capture; informs `add-zsh-test-image`)

A graphics-VM session on the freshly-built goldens `xtty-test-zsh:26.5` (zsh) and `xtty-test:26.5` (bash) closed the open question behind `add-zsh-test-image` task 4.1 ("can the Local Network gate be neutralized at the rig / build time so the zsh graphics rig runs modal-free?"). Answer, **proven by effect from every angle: no — it cannot be granted or neutralized at build time by any means.** Captured to `research/03-analysis/local-network-privacy-forensics.md` **§8** (a dated addendum, hitting the depth bar: TN3179 read, three-tier provenance map, retired-theory fates T7–T10, re-verify-by-effect, guideline G11).

**TN3179 read in full (macOS considerations).** The `AllowedEthernet/WiFiLocalNetworkAddresses` keys are a **destination-address exemption** ("treats every address on that network as if it were not a local network address … Every program can access that address") — they gate *connections to addresses*, orthogonal to xtty's reverse-DNS *query* trigger, so they can't cover it. Apple states device managers **cannot** configure LN via MDM. The technote also documents auto-grant exceptions: `launchd` **daemons**, **root** programs, and CLI tools run over Terminal/**SSH and their children** — but *not* `launchd` agents, and attribution tracks down "**the responsible code**" (= the app).

**Refuted every way (by effect):** (1) **TCC record** — impossible: the guest's `TCC.db` has no `kTCCServiceLocalNetwork` (LN isn't a TCC service; xtty appears nowhere in TCC even when granted — the grant lives in the NetworkExtension store). (2) **`defaults write` Allowed\* ranges baked into the image** — the zsh golden bakes `10/8`+`172.16/12`+`192.168/16` (⊇ the VM's `192.168.64.x`), yet a graphics `open` of xtty **still fired the modal** (T7). (3) **NE-store pre-seed** — the decisive experiment: extract xtty's *granted* record, bake it into a fresh clone before first boot, cold-boot → `nesessionmanager` **prunes it** (count 1→0), even with xtty installed + LaunchServices-registered, while Apple system-app records (`com.apple.TV`, `com.tcltk.wish`) transplant fine; the record is `SigningIdentifier`-keyed with an **empty `DesignatedRequirement` + `AllowEmptyDesignatedRequirement=true`** (so *not* cdhash-bound — the cdhash hypothesis T9 was retracted; the killer is boot **provenance-pruning**, T8). This yielded the **three-tier provenance map**: hand-baked → pruned on boot; **implicit** (timed-out prompt) → survives boot but **re-prompts**; **explicit** (a real *Allow* click) → survives boot **and** silences (all three confirmed by cold-boot + relaunch cycles). (4) **SSH/CLI-child auto-grant** — `exec`'d xtty directly over SSH so its parent was `sshd-session: admin@notty` (ps-confirmed); the modal **still appeared** — a GUI app bundle is its own responsible code, so the exception doesn't cover it (T10). Corollary: this is **not** why headless rigs are quiet (headless = no interactive WindowServer to raise the modal; the op resolves to the default deny and the semantic tests take their graceful-degradation arms; XCUITest launches via `testmanagerd`, attributed to the app).

**The product fix, now locked (behavior-preserving + more correct).** The injected OSC 7 emitter (`App/Resources/shell-integration/zsh/xtty-integration:30`) reports the cwd authority as **`${HOST}`** = zsh's `$HOST` = **`gethostname(3)`** (no reverse-DNS). The *only* consumer of xtty's own hostname is `PaneController.localHostNames` (`App/PaneController.swift:92`), built today from `ProcessInfo.hostName` (reverse-DNS) and fed to `OSC7.decode` solely for local-vs-remote cwd classification. Building that set from **`gethostname()`** instead matches the *exact* syscall behind the shell's `$HOST` → a guaranteed match rather than a lucky short-form overlap (strictly more correct on networks where reverse-DNS yields a divergent FQDN), is behavior-preserving in the common case, and removes the reverse-DNS → kills the trigger under zsh. **Refinement to the prior §6 note:** use `gethostname()`, **not** `SCDynamicStoreCopyLocalHostName()` (the Bonjour `LocalHostName`, which can differ from `$HOST`). One call-site swap + one `localHostNames`-contains-`gethostname` unit test; still owner-deferred (the test image already gets bash for CI-parity, so no urgency), but the fix is de-risked.

**Implication for `add-zsh-test-image`:** there is no rig-level LN neutralization to build, so the divergence measurement (task 5) proceeds **headless** (the modal is graphics-only). Trackers reconciled this session: the AGENTS.md row (`(LN-gate neutralized at rig level)` → `rig LN-neutralization refuted`) + snapshot, a new Learned-refutation one-liner, the forensics §8 addendum + its `research/README.md` index line, and the task 4.1 record extended with T7/T8/T10. Session clones (`zsh-manual`, `zsh-preseed`, `zsh-sshtest`, `bash-manual`) left stopped/disposable; goldens untouched. **Note:** the working-tree `packer/xtty-test.pkr.hcl` still carries the now-triply-refuted zsh `defaults write` block (inert; bash skips it) — flagged for removal as a follow-up, not part of this capture.

### 2026-07-07 — harden-findbar-wrap-assertion (P-tooling) — implemented 14/14, red→green pair #2, landed `b89a2d6`+`4270dfd`+`1452439`, archived

**`harden-findbar-wrap-assertion`** (P-tooling, **test-only — no product code**) — pair #2, the *green* half that fixes the `findbar-marker-wrap` flake reproduced by `add-vm-prompt-width-parity`. `testFindBarOpensLocatesAndDismisses`'s focus-restore assertion (`:198`) was switched from the strict `waitForContains` to the wrap-tolerant `ignoringLineWraps: true` matcher already shipped at `:53` by `harden-focus-typing-assertion` — so a marker soft-wrapped behind a long prompt is still asserted present (focus restoration is what's checked; a genuinely absent marker still fails). A new **deterministic self-validating soft-wrap guard** (`testSoftWrapGuardIsWrapTolerant`) types a ~165-char contiguous marker and asserts the wrap-tolerant match succeeds while a **strict** whole-token match **fails** — so it can never pass without a genuine wrap, reproducing the wrap class in `make test` on bare metal independent of hostname/prompt width (the earlier column-pinning spike is dissolved by this guaranteed-wrap-marker design — no new launch hook). Proven **red→green across all three environments**: the wide-prompt **VM** flipped `38/2/1`→**`40/1/1` of 42** (find-bar red→green, D2-confirmed — the `AFTERFIND####` marker genuinely soft-wraps behind the 58-char `\h`, and the guard's 165-char marker spans 3 physical rows, so neither passes vacuously); **local** bare-metal (zsh, short prompt) **`41/0/1`** (find-bar green trivially, guard forces its own wrap); and **hosted CI** run `28841237111` — `test-core` green and find-bar **absent from the `build-and-test` failures** = green under the runner's real long prompt, leaving only the bash-3.2 paste residual. A **host-setup gotcha surfaced and was fixed**: this Mac's Automation Mode had lapsed to "requires user authentication", timing XCUITest out at session init until `sudo automationmodetool enable-automationmode-without-authentication` (Touch ID) restored it — captured as a memory. Reverse-duty: `github-actions-ci-cd.md` §19b `findbar-marker-wrap` residual→**fixed** (a recurrence is now a regression; one residual — bash-3.2 paste — remains); `packer/README.md` Acceptance/matrix + forensics §6 updated. Modified capability `verification-harness` (the *Soft-wrap-robust content assertion* requirement extended to the find-bar focus-restore coverage + the deterministic guard). The Learned-refutations reproducibility refutation still holds — the fix doesn't make the runner hostname derivable.

### 2026-07-07 — add-vm-prompt-width-parity (P-tooling) — implemented 8/8, red→green pair #1, landed `d1d5d21`+`bb9e6bc`, archived

**`add-vm-prompt-width-parity`** (P-tooling) — a harness-fidelity investment (image + docs only, **no product code**) closing the last CI-parity gap in the local Tart test image: **prompt width**. The guest hostname was short (`Manageds-Virtual-Machine`, 24 chars) while the hosted runner's is a ~61-char datacenter name, so stock `PS1='\h:\W \u\$ '` wraps a typed marker on the runner but not in-guest — which is why the `findbar-marker-wrap` flake was invisible to the VM and only reddened CI. A Packer provisioner now sets a **59-char single-label hostname** on all three `scutil` keys (`HostName`/`LocalHostName`/`ComputerName`), mirroring `actions/runner-images`' own boot-time hostname provisioning; the image was rebuilt (`make image`, owner-gated) with the working golden protected first by a reversible `tart rename`. **Verified by effect** (D2) on all three VM clones: bash `\h` renders the long name and a typed marker soft-wraps across ≥2 physical rows (grid dump `…admin$ AFTERFIND`/`786`) — which **closed the forensics T6 gethostname spike** (in the NAT'd guest, setting all three keys makes `\h` read the long name; the effective key didn't need isolating). Applied **alone** (pair #1, before the fix) it made `testFindBarOpensLocatesAndDismisses` **red in-guest** — reproducing the CI flake locally, which *is* the acceptance signal that the parity works — for an interim VM envelope of **`38/2/1` of 41** (headless ×2 + graphics, byte-identical, validator-delegated). **One prediction refuted by measurement:** the paste residual's failing line did **not** shift `:87`→`:84` as forensics §6 option-C guessed — it **stayed `:87`** (bash 3.2 executes the pasted newline, so line B echoes contiguously and fits before col 80), corrected in every tracker rather than transcribed — the "measured, not predicted" discipline (design D3) in action. The parity itself is **durable**, guarding *future* type-at-prompt assertions after its sibling `harden-findbar-wrap-assertion` (pair #2) greens find-bar to the final `40/1/1` of 42. The Learned-refutations reproducibility bullet is unaffected — the fix doesn't make the runner's exact hostname derivable; only its *length* (which we control) drives the wrap. Modified capability `build-workflow` (+1 requirement: the image reproduces the hosted-runner prompt width). Mechanism + fates: `research/03-analysis/ci-runner-prompt-width-forensics.md`.

### 2026-07-07 — add-openspec-critic-agent (P-tooling) — implemented 11/11, dogfood-proven, archived 2026-07-08 (new `coherence-review` spec)

**`add-openspec-critic-agent`** (P-tooling) — the third `xtty-*` family member and the first with a **cross-artifact (change-set) pass**: a committed **`xtty-openspec-critic`** Claude Code agent (`.claude/agents/xtty-openspec-critic.md`, `model: opus`) + its thin **`/xtty:review`** launcher that reviews an OpenSpec change (or the whole active change-set) for coherence in an isolated context and returns a fixed-skeleton findings verdict — **observe-and-report only, never repairs**. Motivation + the reactive-friction justification: the dev-workflow forensics (`research/03-analysis/dev-workflow-agent-orchestration.md`, produced this session by a 3-agent parallel fan-out over git history + the agent/context corpus + the tooling anatomy) ranked coherence-checking the one loop friction that recurs **every change**, is **entirely unautomated**, and is **provably under-done** — evidenced by AGENTS.md's own "7 of the 10 archived changes carried a harness delta" being stale against the disk truth (**19/34**). That is the reactive signal the project's own rule ("build agents reactively, not a speculative roster") requires; the same forensics **refuted** a proposed roster-of-4 down to this one build (+ a fork-per-change authoring habit) and refuted a persistent-state ledger (cross-change state is computed on demand). Three passes: **single-change coherence** (the AGENTS.md "Keeping a change coherent" + spec-delta-format rules operationalized — `## ADDED/MODIFIED` headers, 4-hashtag scenarios, MODIFIED-pastes-the-entire-established-block, the proposal↔specs contract both directions, delegation-marker presence on suite/VM/CI tasks, verification-harness coupling, mechanism-neutrality); **disk-drift** (established specs vs `ls openspec/specs/`, active changes vs the open-changes table, prose counts re-derived against disk, promised reverse-duty edits present); and **change-set-aware coherence** (shared-requirement collisions, declared archive/apply ordering incl. red→green pairs, inter-change dependencies — derived at run time, never cached). Findings vocabulary `COHERENT / ISSUES-FOUND / BLOCKED-PREREQUISITE` with each finding tagged **BLOCKER** (hard rule) or **REVIEW** (heuristic) so heuristics inform without false-failing. **Deference chain:** AGENTS.md is the single source of truth for the *rules* (read fresh each run); the live repository is the ground truth for *drift*; the agent encodes only the operationalization and **flags a rule it can't mechanically check for human review** rather than dropping it. **Delegation boundary (validate vs review):** `openspec validate` stays inline; a full coherence review is delegated, carried by `⟶ xtty-openspec-critic (<change|all>)` (seeded into `openspec/config.yaml`'s `rules.tasks`, pointing at AGENTS.md). Deliberately **lighter than the validator** (short read-only `openspec`/`git`/`grep` — no VM/turn-alive/babysitter; only the `Definition:` stamp + delivery check). **Delivery-lag observed live (§4.3):** the just-created agent was **not spawnable in the same session** (`Agent type 'xtty-openspec-critic' not found`) — the documented definition-propagation lag in its strongest form; the delivery check surfaced it, a skills reload made it spawnable, and the re-run passed. **By-effect dogfood held:** on the 4-change active set the agent returned a fresh `Definition: v1 (2026-07-07)` stamp and **VERDICT: COHERENT** (0 BLOCKERs), autonomously surfacing the **red→green pair** (`add-vm-prompt-width-parity` before `harden-findbar-wrap-assertion`) and the **`7/10`→`19/34` harness drift it predicted** (+ a bonus open-changes-table drift), while correctly confirming the MODIFIED-superset paste and clearing legitimate RFC-2119 `MAY` without false-flagging. Both drift findings were then **actioned in the same reconcile** (this entry's session): the `7/10`→`19/34` fix and the open-changes table brought level with `openspec list`. New capability `coherence-review` (6 requirements); no product code touched.

### 2026-07-06 — add-ci-investigator-agent (P-tooling) — implemented 14/14, archived

**`add-ci-investigator-agent`** (P-tooling) — a committed **`xtty-ci-investigator`** Claude Code agent (`.claude/agents/xtty-ci-investigator.md`, `model: sonnet`) + its thin **`/xtty:investigate-ci`** launcher that diagnoses a failed GitHub Actions run in an isolated context and returns a fixed-skeleton verdict, mirroring `xtty-test-validator` from the opposite direction — it **reads** an already-produced CI run (job status, the triggering commit's changed-file set, failing-job logs, and for UI-test reds the `.xcresult` bundle + exported attachments/grid dumps read against the test source) rather than **running** the suite. Motivation: hand-triaging a red run floods the session with tens of thousands of tokens of logs/xcresult/attachment noise to reach a two-line conclusion; the reference investigation of run `28801860582` this session pulled ~30–50k tokens in to conclude "two documented known-benign residuals." Scope is the whole CI-failure space (build break, required-gate red, UI-test red, pr-lint), triaged by job type; the agent is **observe-and-classify only** (never edits code or re-runs/cancels CI). **Deference chain (parallel to the validator's):** AGENTS.md for rules; a **new CI expected-difference matrix** — `research/03-analysis/github-actions-ci-cd.md` §19 — for the known-benign buckets (`findbar-marker-wrap`, `bash32-no-bracketed-paste`) + the required-gate/non-blocking job map, read fresh each run with a reverse duty to update §19 in the same session as any residual-set change. **Delegation boundary:** *watching* CI (`gh run watch`/`gh run view`) stays inline; *investigating a red run* is delegated, carried to the apply loop by a per-task marker `⟶ xtty-ci-investigator (<run>)` (seeded into `openspec/config.yaml`'s `rules.tasks`, pointing at AGENTS.md as the single source). Evidence, when kept, lands in a dedicated gitignored `ci-investigations/<run>/` (never `build/`, which stays Xcode-only). Deliberately **lighter than the validator** — short read-only `gh`/`xcrun` calls, so no VM/turn-alive-invariant/babysitter apparatus; only the shared `Definition: <version>` stamp + delivery check survive. **By-effect proof held (§5.3):** the apply loop's marked verify task autonomously delegated to the agent, which returned `Definition: v1 (2026-07-06)` (fresh — delivery check passed), **IN-ENVELOPE** on run `28801860582` with both reds mapped to their §19b buckets, `build-and-test` marked non-blocking, and the **docs-only short-circuit firing on its own** (the triggering commit touched no product/test code, so it classified without the full `.xcresult` forensics pass). New capability `ci-investigation`; no product code touched.

### 2026-07-06 — test-image-bash-shell (P-tooling) — implemented 9/9, archived

**`test-image-bash-shell`** (P-tooling) — set the local VM test image's guest auto-login shell to **`/bin/bash`** (via a `chsh -s /bin/bash` provisioner, the same configuration GitHub's runner-images apply through `configure-shell.sh`), so the in-guest suite runs in the *exact* shell environment CI does. This closed a fidelity gap and, as a consequence, **eliminated the macOS Local Network privacy modal** that a graphics UI run otherwise hit. Root cause (measured 2026-07-05, lldb backtrace + per-config log captures — it superseded the earlier "XCUITest IPC over the routable vmnet address" theory and the macOS-version framing): xtty reads its own host name via `ProcessInfo.hostName` → `NSHost` → a **reverse-DNS lookup of every local interface address** (~20 dnssd queries/launch), and on a stock guest those locally-scoped queries hit the Local Network privacy gate; the call fires **only when the shell emits OSC 7** — i.e. under zsh (xtty injects shell integration into zsh only), never bash. So bash → no OSC 7 → zero reverse DNS → no dialog, on any macOS version. Two refuted non-fixes are on record (a static `HostName` via `scutil --set HostName`; the vmnet-address theory) in `research/03-analysis/local-network-privacy-forensics.md`. Reproducing what the image *tests* (the per-launch main-menu race) is shell-independent, so the switch costs no coverage. Merged an ADDED `build-workflow` requirement, "Test image matches the hosted-runner login shell" (+2 scenarios: the guest login shell is bash; a graphics UI run surfaces no Local Network prompt), alongside `add-xtty-test-image`'s VM-image requirement — a clean second ADD, no collision.

### 2026-07-06 — add-xtty-test-image (P-tooling) — implemented 20/20, native in-guest build proven, landed `13d0576`, archived

**`add-xtty-test-image`** (P-tooling) — a reproducible **minimal local macOS VM test image** for CI-parity XCUITest runs (the per-launch race class reproduces at 3 vCPU in a VM, not on bare metal). The image (`xtty-test:26.5`, **35 GB** vs the 87 GB prebuilt `macos-tahoe-xcode`) is built by Packer + Tart from **pinned, version-controlled inputs** — the `macos-tahoe-base` manifest digest + Xcode 26.5 — plus a **one-time human-provided Xcode `.xip`**, with **zero Apple credentials at build time** (Xcode enters the guest only via the file-provisioned archive). It bakes in **no simulators, no Metal toolchain, and no xtty source** (source arrives per-run). Groups 1–2 (prereqs, `packer/xtty-test.pkr.hcl`, `packer/README.md`, `make image`) and the build itself (group 3: 41 min, 35 GB, zero Apple auth) + guest verification (group 4: auto-login, `MetalToolchain uninstalled`, base-layer inheritance) landed 2026-07-04/05; the whole change was **gated on `retire-metal-renderer`** (a Metal-toolchain-free guest can only build xtty once the Metal build dependency is gone) and unblocked when that archived 2026-07-06.

The final apply (this session, 16/20 → 20/20) closed the verification gap that the gate had held open. **Task 4.3 — the native in-guest build — is the headline:** every prior VM run (including the newest 39/1/1 `harden-churn` validator sweeps) used the **§8 host-build workaround** (build on the Mac host, rsync products, `test-without-building` in-guest), because Metal had been a build dependency and fetching the toolchain in-guest is the deterministic Apple-catalog-rotation trap. 4.3 proved the workaround is no longer *necessary*: on a fresh 3-vCPU clone, the source was rsync'd in (no products), **SwiftTerm was cloned+patched in-guest** by `bootstrap-swiftterm.sh` (v1.13.0 + `xtty-accessors.diff`), `xcodegen generate` produced the project, and a **native `xcodebuild build-for-testing` succeeded** (`** TEST BUILD SUCCEEDED **`, ~43 s, 0 errors, 158 Swift compile steps; `SwiftTerm.o`/`.swiftmodule` + `xtty.app` arm64 + `xttyUITests-Runner.app` + the `.xctestrun` all built fresh) with the Metal toolchain `uninstalled` and **zero** `.metal`/`Metal.xctoolchain` steps in the guest log — the positive proof the §8b `xcrun -f metal` false positive cannot give. So the minimal image is a complete self-sufficient **build+test** environment. Per the explore steer, this stayed a **one-time proof**: `packer/README.md`'s routine sweep recipe deliberately keeps host-build (identical binary across all test tiers → the validator's cross-tier consistency; dodges the Metal-catalog trap), with the proven native single-run recipe documented alongside and the "interim recipe" framing resolved. 5.2 deleted the 4.3 clone (golden pristine; the ~17 prior-run clones left for a separate cleanup decision); 5.3 validated + reconciled trackers. **Archive-ordering held**: `retire-metal-renderer` archived first (2026-07-06), so its `build-workflow` Metal-toolchain drop was merged before this change's ADDED "Reproducible minimal local VM test image" requirement (3 scenarios) landed onto `build-workflow` — no contradiction. Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-06-add-xtty-test-image-inguest-build/`. Full mechanism/forensics: `research/03-analysis/github-actions-ci-cd.md` §10/§11, `local-macos-vm-ci-reproduction.md`, and `packer/README.md`.

### 2026-07-06 — harden-validator-delegation-trigger (P-tooling) — implemented 18/18, §6 proof held (autonomous-spawn), landed `0ed9fb9`+`b199c03`, archived

**`harden-validator-delegation-trigger`** (P-tooling) — closed the affordance gap that let a change's verify task run the test suite *inline* instead of delegating to the `xtty-test-validator` agent (measured: the `retire-metal-renderer` apply ran the Tier-1 suite + full matrix inline, 0 autonomous delegations). Root cause: the delegation rule lived only in AGENTS.md, read at session start — but `/opsx:apply` is a generic skill that does **not** re-read AGENTS.md against its own loud prompt, so the rule never reached the apply step where a task is ticked. Docs-only fix, three seams: (1) **AGENTS.md** gained the explicit **iterate-vs-validate boundary** (inline = `make test-core`, one-off `-UITestGridDump` launches, greps, `make doctor`, `openspec validate`, `gh run watch`; delegate = Tier-1 `make test`, any VM tier, or a full acceptance matrix — `make bench` inline on its own but rides with a full-matrix task's delegation), the **per-task delegation marker** `⟶ xtty-test-validator (<tier/scope>)` required on delegate-set verify tasks, and the **pre-tick self-check**; the "keeping a change coherent" checklist now also requires the marker on suite-executing verify tasks. (2) **`openspec/config.yaml`** `rules.tasks` seeded with a short pointer (deferring to AGENTS.md for the boundary — single source of truth), verified by effect that a populated `rules.tasks` surfaces in `openspec instructions tasks --json`. (3) The **`test-validation` spec** MODIFIED "Two documented spawn scenarios" to encode the boundary + marker + self-check and add the "authored with a delegation marker" / "cheap check stays inline" scenarios. Open changes retrofitted: `harden-churn-shell-readiness` task 3.2 (full-suite acceptance) marked `⟶ xtty-test-validator (full sweep)`; `add-xtty-test-image` and `add-ci-pipeline` surveyed — no standard validator test-suite tier to mark (a VM *build* proof and GitHub-CI `gh run watch` respectively).

Held open for **proof by effect** (§6, the repo's "left open pending live validation" pattern) — a docs-only change is maximally primed to delegate, so the proof executes on a *different, real* apply. Sequencing held: this change applied first (`0ed9fb9`, markers live) → context compacted (`/compact`) → `harden-churn-shell-readiness` applied (`c4362e4`). **OUTCOME: autonomous-spawn — proof holds.** On the post-compact harden-churn apply, the marked task 3.2 triggered an `xtty-test-validator` spawn with **no user request that turn**; the task was ticked from the report's verbatim IN-ENVELOPE counts (v4 stamp matched — no stale-served definition) rather than run inline. **Boundary discrimination confirmed** as the strongest un-primed signal: the cheap iterate tasks 1.2 (`testBasicTypedEcho`) and 3.1 (churn ×5 via `-only-testing`) were run **inline**, and only the full-sweep 3.2 delegated — the D1/D4 split behaved as designed, not blanket-delegation. Priming disclosed as moderate-to-high (the compaction summary carried the proof intent), so this proves the **machinery** (a `tasks.md` marker reaches the generic apply loop and routes delegation), with a genuinely-blind re-proof noted as the residual confidence gap — not a blocker. Negative-result protocol (§6.4) N/A: no inlined-anyway, so the deferred harder apply-side gate (task 5.2) stays deferred. Merged the `test-validation` delta ("Two documented spawn scenarios" +2 scenarios, boundary/marker/self-check paragraph) into `openspec/specs/`; Purpose refreshed to name the boundary + marker.

### 2026-07-06 — harden-churn-shell-readiness (P-tooling) — implemented 12/12, full-sweep-validated, landed `c4362e4`, archived

**`harden-churn-shell-readiness`** (P-tooling) — made the lifecycle-census churn e2e test (`testLifecycleChurnReturnsCensusToBaseline`) deterministic on slow-shell-startup machines (local baseline **F/F/P → 5/5 green**) without touching the product close path. The flake was a race: the churn loop sent ⌘W into a *freshly-created* pane/tab whose zsh was still sourcing startup files, so the default close-confirmation NSAlert sometimes appeared and swallowed the close, stranding a lifecycle object above baseline. Fix has three parts (design D1–D5): (1) a **computed-marker readiness gate** — before each ⌘W, both loops type `echo $((base+i))` (base 41000 pane / 42000 tab, unique per iteration) and wait via `GridDumpReader.waitForContains` for the *output* token (e.g. `41001`), which never appears in the un-evaluated echoed input line `echo $((41000+1))`, so a grid match proves the shell **executed** a command — i.e. rc-file sourcing finished. This replaces the refuted `hasForegroundJob`/`paneCount`-only waits (fg==shellPid from ~3 ms; child bursts invisible to the 150 ms dump sampler). (2) **Fail-fast asserted steps** — `continueAfterFailure = false` plus a `waitShellReady` helper that, on a 15 s timeout, attaches the grid dump + screenshot, `XCTFail`s naming the loop/iteration, and **returns without sending ⌘W** — a stuck precondition fails at its own iteration instead of cascading a later, misattributed timeout, and never drives a close into a still-starting shell (D3). (3) A **DEBUG-only dump-liveness fix** — the `-UITestGridDump` timer moved from `Timer.scheduledTimer` (registers `.default` mode only) to an explicit `Timer` + `RunLoop.main.add(timer, forMode: .common)`, so state/grid dumps keep ticking during the confirm-close alert's `NSModalPanelRunLoopMode` (D5) — observe-only, no repair. Test-only + one DEBUG-gated App timer line; **no product close-path change**, test counts unchanged (232 core + 41 XCUITests, envelope 39/1/1).

Validated by the `xtty-test-validator` agent (Definition v4, fresh — stamp matched), **autonomously delegated** per task 3.2's `⟶ xtty-test-validator (full sweep)` marker (this apply doubled as the `harden-validator-delegation-trigger` §6 by-effect proof — *autonomous-spawn, proof holds*; cheap iterate tasks 1.2/3.1 stayed inline, so the D1/D4 boundary discriminated rather than blanket-delegating). **VERDICT: IN-ENVELOPE** — Tier 0 core **232/0/0**; Tier 1 local **40/0/1** (skip = bench-e2e-gated `testBenchmarkRunWritesReport`); Tier 2 headless ×2 **39/1/1 + 39/1/1**; Tier 3 graphics **39/1/1** — every red the sole benign residual `testMultiLinePasteIsNotAutoExecuted` (bash-3.2 no bracketed paste), zero unexplained reds, no menu-class red. The churn test was **green in every tier** (Tier 1 14.9 s + 3 VM runs 14.56–14.95 s) — the readiness gate holds on the CI-parity rigs where the per-launch race lives. Evidence: `~/Downloads/xtty-vm-poc/artifacts/2026-07-06-harden-churn-shell-readiness/`. **CI readout (corrected):** the churn test's *other* root cause — the SwiftUI menu clobber — already landed (`fix-main-menu-clobber`, archived 2026-07-05), so with this change both root causes are fixed and churn is now expected **green** on CI; the only remaining `build-and-test` reds are the 2 pre-existing non-churn rig fragilities (find-bar marker-wrap on the runner's ~72-char prompt; bash-3.2 multi-line paste). Trackers reconciled in-session: the three-way `verification-harness` delta (re-pasted the current established "Deterministic content assertion channel" block preserving retire-metal's single-CoreGraphics wording + fix-main-menu-clobber's menu/window sentence & scenarios, re-applied only the modal-liveness sentence/scenario) + the "Lifecycle-census end-to-end coverage" shell-readiness sentences & 2 scenarios; `packer/README.md` Acceptance + matrix row (b) (reverse-duty: confirm-close class now *fixed*, not a standing hazard); AGENTS snapshot/tables; milestones §16; and a "landed clean, no divergence from §5" addendum in `research/03-analysis/confirm-close-shell-readiness.md`.

### 2026-07-06 — retire-metal-renderer (P-tooling) — implemented 24/24, CI-proven, graphics-VM-validated, landed `1ce4121`, archived

**`retire-metal-renderer`** (P-tooling) — full retirement of the Metal renderer option and the Metal-toolchain build dependency, enforcing the closed P7b gate (keep CoreGraphics). The SwiftTerm patch (`patches/swiftterm/xtty-accessors.diff`) is no longer add-only: it now carries **three hunks** — the accessors add, the `Package.swift` shader-resource strip (the shader moves into `exclude:` so SPM stays warning-free), and a hunk **discovered at first build**: SPM synthesizes `Bundle.module` only for targets declaring at least one resource, so `MetalTerminalRenderer.candidateBundles()`'s `#if SWIFT_PACKAGE` arm stopped compiling and was removed (the metallib lookup returns nil, which callers handle — recorded in design D1). `bootstrap-swiftterm.sh` gained a `reset --hard` pristine-restore (a dirty tracked `Package.swift` would otherwise survive re-runs) and the add-only prose was corrected across the script/pin/AGENTS. Removed end-to-end: `RendererBackend`, the `renderer` config key (**BREAKING, fail-soft** — a legacy `renderer = metal` is ignored under the forward-compat rule; verified: the loader has no unknown-key warning path), `-UITestRenderer`, `applyRenderer()`/`setUseMetal` wiring, `activeRenderer`, the bench Metal arm + the dead `-UITestRenderer` flag in `Makefile`/`audit-leaks.sh`, the `make doctor` Metal check, both `ci.yml` "Ensure Metal toolchain" guards (the deterministic Apple-catalog-rotation trap — gone from CI entirely), the `config.example` block, and the Metal e2e `testConfiguredMetalRendererIsReported`; the `renderer` field **stays** as the constant `"coregraphics"` in `BenchResult` (JSON wire format preserved) and the state dump. Cross-change reconciles done in-session: `add-ci-pipeline`'s pending `build-workflow` delta generalized its missing-build-component example to XcodeGen (+ its design D4 annotated superseded); `packer/README.md` acceptance updated (reverse duty); the owner's future-Metal intent captured as dated **re-evaluation triggers** in `research/04-design/01-stack-sketch.md` (reopen only via the archived P7b methodology). **Counts: 232 `XttyCore` unit (−4 renderer tests, +1 legacy-key test) + 41 XCUITests.** **Verification:** clean builds with **zero Metal compile steps / zero metallibs** (log-grepped, twice); local `make test` **39/1/1 of 41** — the sole red `testLifecycleChurnReturnsCensusToBaseline` failed 2× locally and was **arbitrated by a delegated `xtty-test-validator` graphics-VM run** (Sonnet, Definition v4, per user request): **39/1/1 of 41 IN-ENVELOPE**, churn **passed on the rig (9.336 s)** → the local red is the documented confirm-close race (fix = `harden-churn-shell-readiness`), not a regression; the rig's sole red is the bash-3.2 bracketed-paste residual; host `build-for-testing` had no Metal step (evidence `~/Downloads/xtty-vm-poc/artifacts/2026-07-06-graphics-retire-metal-renderer/`; clone `xtty-run-gfx-rm-0706a` left stopped). `make bench` writes the single CoreGraphics report (calibration passed, latency stats + reference baseline present). **CI-proven** (run `28779012465` on `main` @ `1ce4121`): `test-core` — the required gate — **green in 1m25s with no guard step**; `build-and-test` built and ran guard-free failing **only** the 2 pre-existing documented runner residuals (find-bar marker-wrap, bash-3.2 paste — the identical pre-change failing set), churn passed on CI. **Archived 2026-07-06** (`openspec/changes/archive/2026-07-06-retire-metal-renderer/`; deltas merged: +0 / ~7 / −1 across `terminal-configuration`, `performance-harness` — the "Renderer A/B selection" requirement REMOVED — `verification-harness`, `build-workflow`; the pre-registered three-way "Deterministic content assertion channel" collision protocol was executed before the merge — the delta re-pasted the then-current established block, so `fix-main-menu-clobber`'s `mainMenuTitles`/`windowCount` text survived alongside this change's single-backend rewording, and `harden-churn-shell-readiness` (archiving later) owes the same re-paste; post-archive prose fixes applied per task 6.3: the `performance-harness` Purpose no longer describes a live A/B toggle, and `terminal-spatial-blocks`' non-binding "add-only patch" wording became "tracked patch"; `openspec validate --all --type spec` 22/22).

### 2026-07-06 — slim-agents-context (P-tooling) — implemented, probe-verified, landed `d2a11fd`

**`slim-agents-context`** (P-tooling) — the AGENTS.md **rules/history split**, verified by a pre-registered fat-vs-slim probe A/B before landing. Motivation (measured): AGENTS.md had grown 12.3→80.6 KB in 8 days (~2.5k tokens/day) and cost 32.8k tokens at every session start — 56% of the fixed pre-message budget, tokenizing at 2.46 bytes/token — with ~19k tokens of it a third copy of history already recorded in `openspec/specs/`, `openspec/changes/archive/`, and `research/`. The split: the three history blobs (the ✅ milestone narratives, the OpenSpec-era changelog tail, the CI digest) moved **verbatim** into this file (losslessness-checked, 43/43 long lines byte-identical); *Current status* became snapshot ¶ + open-changes/shipped tables + 11 **learned-refutation one-liners**; *Building* kept mechanisms with short bullets; the *Keep progress current* rule was amended to the bounded-entry format (table row here in AGENTS.md, narrative appended to HISTORY.md — this entry is the first written under it); capture-research skill v1.2 + command updated; the stale CLAUDE.md greenfield line corrected. **Verification (all pre-registered in the change's `probes/README.md`, results in `probes/results.md`):** startup context 57,137 → 35,589 tokens (−21,548; gate ≥20k); behavioral A/B 12 probes × 3 reps with the fat baseline graded and frozen first — fat scored **34/36 with R1 (OpenSpec routing under "implement now" pressure) at 1/3**, slim scored **36/36** (zero regressions, R1 improved to 3/3 — the lost-in-the-middle prediction landing); recall medians within the +3 num_turns bound (C4 exactly at bound), slim answers often richer via pointer-following. **Ablation** (D7): V2 (−tables) 16/16 with the probe-set gap recorded (nothing prices open-change orientation; tables kept at ~1,320 tokens); V3 (−inoculations) 6/8 — **the retries trap regressed 0/2** ("Good instinct — … nothing to add"), proving the ~463-token inoculation tier load-bearing; all 11 lines kept. **Subagent-inheritance probe** (D8): a fresh subagent quoted the AGENTS.md marker line verbatim with zero tool uses — Task-tool subagents inherit CLAUDE.md/AGENTS.md, so the diet also saves ~25k × N per multi-agent workflow (a trivial spawn cost 49.3k at fat). Evidence: committed in `openspec/changes/slim-agents-context/probes/evidence/` (grading sheets, context-run JSONs, max-turns tool trails, the subagent-probe record; only the ~3.7 MB raw per-probe transcripts stayed machine-local, regenerable via the committed scripts); capture: `research/03-analysis/agents-md-context-budget.md`; landing commit `d2a11fd` (atomic: AGENTS.md + HISTORY.md + skill/command + rule amendment). **Archived 2026-07-06** (`openspec/changes/archive/2026-07-06-slim-agents-context/`; the `research-capture` MODIFIED requirement — lean status surface + history log + refutations-survive-slimming — merged into `openspec/specs/`; audit evidence committed in the archived change's `probes/evidence/`).

**`fix-main-menu-clobber`** (P-product) — **implemented, VM-validated, CI-confirmed, and archived 2026-07-05** (17/17; the `app-shell` "Durable custom main menu" ADDED requirement + the `verification-harness` menu-titles/window-count dump fields are merged into `openspec/specs/`): the SwiftUI menu-clobber product fix — dropped the SwiftUI App lifecycle for **`NSApplicationMain`** (deleted `@main struct XttyApp: App`; new `App/main.swift` with strong `let delegate` retention + `applicationSupportsSecureRestorableState → true`; `AppDelegate` otherwise unchanged; SwiftUI stays for `NSHostingView` views), plus a title-based `mainMenuTitles` canary (`requireXttyMainMenu`, observe-never-repair) and a `windowCount` red→green witness (the vacuous Cmd+N test now asserts a real second window). One `app-shell` ADDED requirement (durable custom main menu) + one `verification-harness` MODIFIED (dump gains menu titles + terminal-window count; tabs count, quake excluded). **VALIDATED: 3 VM runs (headless ×2 + graphics) all `40/1/1` on the ~100%-clobber bash `xtty-test:26.5` rig — the 7 menu-dispatch tests flipped green, canary present every launch, delegate-class = `xtty.AppDelegate` (mechanism confirmed by lldb)**; the single residual `testMultiLinePasteIsNotAutoExecuted` is **not the fix and not a product bug** — macOS **bash 3.2.57** lacks `enable-bracketed-paste`, so the rig's paste executes (zsh + bash 5.1+ users unaffected); a harness follow-up guards that test on a no-bracketed-paste shell. Graphics == headless now (the `windowCount` assert removed the old graphics focus-steal red). 235 `XttyCore` unit tests green; local `make test` 40/2/1 with the 2 reds being the separate `harden-churn-shell-readiness` confirm-close race (isolated re-run passed). Symmetric three-way *Deterministic content assertion channel* merge protection applied (patched `retire-metal-renderer` + `harden-churn-shell-readiness` task lists). Broader harness truthing + the `.function` strip deferred to a successor change. Adversarially reviewed at proposal (3-lens panel; 11 findings, 1 major, folded in). **CI-confirmed** (run `28747512367`, first push carrying the fix): `test-core` green; `build-and-test` shows the menu clobber gone — 5 pre-fix menu-dispatch failures flipped on `macos-26`; the 2 residuals (find-bar §12 marker-wrap on the runner's long prompt; multi-line-paste on bash-3.2) are un-masked pre-existing test/rig fragilities, not the fix, deferred to the harness-truthing successor. Design + apply result: `research/03-analysis/swiftui-mainmenu-clobber-forensics.md` §7 + `github-actions-ci-cd.md` §18; evidence `~/Downloads/xtty-vm-poc/artifacts/fix-main-menu-clobber-verify/`. **`add-test-validation-agent`** (P-tooling) — proposed + **implemented, live-validated, and archived 2026-07-06** (18/18 tasks incl. the post-apply v4 hardening section; the new **`test-validation`** spec — 5 requirements / 14 scenarios incl. the v4 stamp/continuous-execution/strand-recovery additions — merged into `openspec/specs/`, plus the `research-capture` Purpose touch for the now-tracked agents): the committed multi-environment **test-validation agent** — `.claude/agents/xtty-test-validator.md` (tiers: core `make test-core` / local `make test` with the hands-off warning / headless VM / graphics VM; fixed verdict-report contract — verbatim counts, per-red classification vs the repo-owned envelope + matrix with UNEXPLAINED forcing non-green, cross-environment consistency, evidence + REVIEW.md to the artifacts convention, cleanup manifest; guardrails: no retries, clone-per-run + unique names, serialized VM runs, observe-never-repair, stop-permitted-delete-never, incremental evidence writing, backgrounded long tiers) + the thin **`/xtty:validate`** launcher + the `.gitignore` exception extended to `.claude/agents/xtty-*` + the AGENTS.md **delegation rule** (see How to work here — verify tasks executing the suite are delegated; deference chain both directions) + `packer/README.md` gains the **expected-difference matrix** (the agent's runtime source, with the reverse duty) and its **Runtime workflow corrected to the proven interim host-build recipe** (host `build-for-testing` → rsync Products + `__TESTROOT__`-relative xctestrun → guest `test-without-building`; `--no-graphics` headless arm; `tart exec -i` key injection; `StrictHostKeyChecking=accept-new`; flip-back to in-guest builds pre-registered to `retire-metal-renderer`/`add-xtty-test-image`). New **`test-validation`** capability. **Live-smoked end-to-end:** Tier 0+1 (core 235/0/0; the local red = the documented churn confirm-close race, matrix-bucketed), Tier 2 ×2 (both exactly the post-fix envelope, identical sole bash-3.2 residual; clones kept + manifested; a post-run adversarial review re-labeled the runs windowed-boot — the rewritten README had dropped `--no-graphics`, since restored), and a fresh-context Scenario-B dry-run (the delegation auto-selected the agent and relayed a skeleton report). Adversarially reviewed at apply (24-agent workflow; all confirmed findings folded in). Deferred by decision: CI-inspect mode (a fifth matrix column of this agent, not a sibling), additional `xtty-*` family members (only when a recurring noisy workflow proves itself), per-task delegate markers (only if delegation drift appears). **Post-apply hardening (v4, 2026-07-06):** three stranded Sonnet sweeps → a 7-agent forensic workflow (+ red-team) found an **affordance failure, not model disobedience** — `run_in_background`'s "re-invokes you when it exits" promise is **false for subagents** (a subagent that stops calling tools is closed forever; the anti-sleep hook *recommends* the trap) — amplified by **definition-propagation lag** (a spawn 63 s after an edit served the stale copy, probe-proven: the anti-park rules were never delivered; ~40 min later fresh); v4 = `run_in_background` **banned** + the control-proven unfused foreground launch/wait recipe + a per-report `Definition:` version stamp (delivery probe) + incremental `ledger.log` + wedge escape + stop-permitted/delete-never; the `/xtty:validate` launcher carries the delivery check + the caller-side babysitter protocol (resume-not-respawn, resumes recorded). **Validated by effect: three consecutive clean Sonnet sweeps** (14-min quick-confirm; 65-min all-tiers with a transient local automation-mode blocker honestly reported as 0-tests-executed; 30-min all-tiers fully at envelope — zero strands, zero `run_in_background`, all reports `Definition: v4`; the VM tiers matched the 40/1/1 envelope in all six runs). Change artifacts reconciled to v4 (spec delta + D10–D12 + tasks §6); forensics captured to `research/03-analysis/claude-code-subagent-execution-forensics.md` (generalizable subagent guideline). **Current open changes:** **`retire-metal-renderer`** (P-tooling) — **proposed 2026-07-04, not yet applied**: full retirement of the Metal renderer option + the Metal-toolchain build dependency (the P7b gate is closed → keep CoreGraphics): SwiftTerm's bundled shader patched out via the existing patch mechanism (the build then needs **zero Metal toolchain**), the `renderer` config key removed (a legacy `renderer = metal` falls under the unrecognized-key rule → CoreGraphics), the renderer plumbing + `-UITestRenderer` override + `make bench` Metal arm + both `ci.yml` Metal guards + the `make doctor` Metal check dropped, `testConfiguredMetalRendererIsReported` deleted; the bench report + state dump **keep** a constant `renderer` field for schema stability. Four spec deltas (`terminal-configuration`, `performance-harness` — A/B requirement REMOVED, calibration + baseline safeguards kept — `verification-harness` ×2 MODIFIED, `build-workflow` doctor); pre-registered open-change interactions with `add-ci-pipeline` (owns `ci.yml`) + `harden-churn-shell-readiness` (shares a `verification-harness` requirement). Resurrection = revert one patch hunk + re-run the archived P7b methodology. Adversarially reviewed at proposal (3-lens panel; 15 findings, 3 major, folded in). Decisions: `research/03-analysis/local-macos-vm-ci-reproduction.md` §10b/§10c/§11g. **`add-xtty-test-image`** (P-tooling) — **proposed 2026-07-04, not yet applied**: the reproducible **~40 GB minimal test-VM image** (Packer, cirruslabs `macos-image-templates` style, on a pinned `macos-tahoe-base` — inheriting the XCUITest auto-login/TCC/automation-mode infra) + Xcode 26.5 installed from a pre-downloaded official `.xip` (one-time human-gated Apple-ID download; **zero Apple credentials at build time**), **no simulators** (the whole 87→40 GB win), **no Metal toolchain**, **no xtty source baked in** (generic guest + XcodeGen; the repo arrives at test time in a disposable clone of the golden image); one `build-workflow` ADDED requirement; ghcr push deferred until a second consumer. **Hard dependency: `retire-metal-renderer` applied first** (and its `build-workflow` delta archived before/with this one); independent of `fix-main-menu-clobber` (pre-registered: pre-menu-fix in-guest runs reproduce the race — acceptance = parity with the proven big-image rig, not all-green). Human-gated prereqs: `brew install packer`, the Xcode `.xip`. Adversarially reviewed at proposal (11 findings, 2 major, folded in). Research: §10/§11/§11g. **`test-image-bash-shell`** (P-tooling) — proposed + **implemented 2026-07-05** (9/9 tasks; pending archive): the `xtty-test` guest login shell set to **bash** (`chsh -s /bin/bash`, mirroring GitHub runner-images `configure-shell.sh`) — the measured fix for the §12 Local Network modal **and** an exact-parity improvement: the rebuilt image’s full-suite run = **34/7/1 with the identical CI failing set** (the zsh image’s 8th failure, block-menu, now soft-passes via the same degradation arm CI takes) and **0** reverse-DNS / 0 prompts in the whole-run log capture. Root-cause correction (research **§12f**, supersedes §12b/§12c): the modal was xtty’s own `ProcessInfo.hostName` → NSHost **reverse-DNS of all local addresses**, reachable only under **zsh** (OSC 7); NOT the macOS version (reproduced on bare-metal 26.2), NOT the XCTest IPC, NOT fixable by `scutil --set HostName` (measured ineffective); a product-side fix was proposed and **dropped by owner decision** (test-image scope only). One `build-workflow` ADDED requirement (sibling of `add-xtty-test-image`’s; shared template, pre-registered interaction, archive in any order after both apply). Evidence: `~/Downloads/xtty-vm-poc/artifacts/bash-image-verify/`. Deep forensics + probe catalog + the G1–G10 investigation guideline: `research/03-analysis/local-network-privacy-forensics.md`. **`deepen-research-capture`** (P-tooling) — implemented **and archived** (2026-07-05; the depth-bar paragraph + "Measured findings are captured reproducibly" scenario merged into `research-capture`): the research-capture conventions gain a **capture-depth bar** (scaled to the finding — any capture with measured claims/retired theories carries the mechanism, reproducible probes incl. dead instruments, a retired-theory fates table, re-verify-by-effect, a guideline when it generalizes, artifact pointers; exemplar = the LN forensics doc). One MODIFIED `research-capture` requirement; AGENTS "Capture depth" rule + skill v1.1 "Depth bar" sub-checklist + command clause (deference chain preserved: rules live only in AGENTS). **`harden-churn-shell-readiness`** (P-tooling) — **proposed 2026-07-03, not yet applied** (0/11 tasks): the churn test's local confirm-close flake fix per `research/03-analysis/confirm-close-shell-readiness.md` §5/§7 — a computed-marker shell-readiness gate in both churn loops (test-only) + asserted fail-fast steps + the DEBUG dump timer moved to `RunLoop` `.common` modes (dumps stay live during modals); two MODIFIED `verification-harness` requirements; pre-registered: churn stays red on CI until the separate menu fix lands. **`add-ci-pipeline`** (P-tooling) — proposed + **implemented** (`.github/workflows/ci.yml` + `pr-lint.yml`, the AGENTS CI section), **left open** after the first CI run (`28425122861`, `macos-26`): both researched unknowns resolved favorably — **`test-core` is a green required gate** (1m6s), **Metal is preinstalled** on the release-default Xcode 26.5 (guard a no-op), and the **XCUITest-on-hosted-runner risk is refuted (34/41 pass)**. Remaining: **7 deterministic CI-env-sensitive tests** (focus/activation cluster + clipboard + truecolor/locale — the non-blocking job correctly surfaced them), make the repo public, a PR to exercise `pr-lint`, optional branch protection, then archive. See `research/03-analysis/github-actions-ci-cd.md` §9. **The 7-test fix is researched + planned (§10) as a follow-up `harden-xcuitests-for-ci`** (a `verification-harness` delta, not yet proposed): an 8-repo per-project study **corrected the "headless" framing** (GitHub runners have a real auto-login Aqua session — the true issue is a native app can't *reliably* own the *shared* host session: key-window/menu-bar/clipboard; unreliable, not impossible — DuckDuckGo's `apple-browsers` *does* run menu/clipboard XCUITests green on hosted `macos-26`, while Ghostty (a native terminal) deliberately excludes its XCUITests from CI). Plan = **hybrid**: keep xtty's env-trigger/grid-dump side channel (the only path for its custom-drawn, no-a11y content) + DDG's primitives (a11y-IDs on NSMenuItems + click-first + `typeKey`) for the few must-drive-GUI tests + rewrite the focus-on-activate test + keep the GUI job non-blocking + `paths-ignore` for docs. **Artifact-level re-investigation refined this plan (research §11, 2026-07-01 — read the actual `.xcresult` screenshots/grid-dumps, first time):** the failing set is **30/7/1** (not 34/41); **fix the runner shell first** (it's `/bin/bash` → its zsh-deprecation banner corrupts grids, disables zsh shell-integration, and makes `testFocusTypingOnActivateWithoutClicking` a **false negative** → **keep that test, don't rewrite it**), then the residual is **pure Bucket B** (Cmd-key/menu key-equivalents not firing — incl. truecolor/emoji + paste, both **Cmd+V**, not font/locale); xtty **does** own its menu bar (§11e). See `research/03-analysis/github-actions-ci-cd.md` §11. **`silence-bash-deprecation`** (P-tooling, §11f step 1) — proposed + **implemented and archived** (2026-06-30; the new "Suppress the macOS bash deprecation banner" scenario merged into `terminal-session`): when xtty launches **bash**, `ShellResolver` now seeds `BASH_SILENCE_DEPRECATION_WARNING=1` (gated to `base == "bash"`, set before the `override.env` merge so a profile `env` still wins) — silencing the macOS "default interactive shell is now zsh" banner that the curated child env (only `TERM`/`COLORTERM`/`LANG`/`HOME`/`USER`/`LOGNAME`) otherwise let `/etc/bashrc` print every session. A real papercut for **all** bash users, not just CI; also de-noises the CI grids. Key finding: a `ci.yml`/`launchEnvironment` setting is **stripped by the seed wall** and silently no-ops, so the fix had to be the product seam. **Measured in CI post-merge (research §12, run `28467944762`): the fix works — banner gone from 0/221 attachments — but flipped *zero* tests green (still 30/7/1). It REFUTES §11f's "fix the shell → focus-typing flips green": `testFocusTypingOnActivateWithoutClicking` still fails because the runner's ~72-char hostname prompt wraps the typed marker across rows (banner-free run still wraps — so the cause was never the banner), and truecolor/paste are Cmd+V Bucket-B. So `silence-bash-deprecation`'s real value is de-noising every bash user's grids + retiring the CI Bucket-A corruption, not greening a CI test; the residual is a clean 6 Bucket-B + 1 marker-wrap for `harden-xcuitests-for-ci` (focus-typing's fix is now a wrap-tolerant assertion).** **`harden-focus-typing-assertion`** (P-tooling, the #1 wrap) — implemented **and archived**: a scoped opt-in wrap-tolerant `GridDumpReader` matcher (`gridContains(_,_,ignoringLineWraps:)` + `waitForContains(…, ignoringLineWraps:)`) used only by `testFocusTypingOnActivateWithoutClicking`; test-only (no product code, no SwiftTerm patch), one `verification-harness` ADDED requirement (soft-wrap-robust content assertion, merged). The matcher checks the raw haystack first then the newline-stripped haystack, so it never fabricates an absent token. New `GridDumpReaderTests` (4 synthetic-string cases, no app launch — incl. a strict-misses case proving the matcher reproduces the CI failure mode). Local verify: focus test green + all 4 matcher tests green (`** TEST SUCCEEDED **`); focus-typing passed in run `28472076179`, the first `build-and-test` carrying the fix (counts 34/7/1 of 42) — **but §15 softened the confirmation: the cross-run record is P/FFF/P (run 1 passed *without* the matcher), so the pass is consistent-with, not proof; the §12 wrap diagnosis itself stays artifact-proven for run 2.** **235 `XttyCore` unit + 42 XCUITests.** **Local `make test` also root-caused the Bucket-B churn test #4 (research §13):** `testLifecycleChurnReturnsCensusToBaseline` splits then closes before the new shell settles, so `hasForegroundJob` is true (a startup child owns the PTY foreground — the user's heavy `~/.zshrc`) and a **confirm-close `NSAlert` blocks the close** → census `final 2`; fix (deferred to `harden-xcuitests-for-ci`) = the churn test opts out via `confirm-close = false` (or waits for shell-ready) — **though §14b pinned the churn test's *CI* mode as different and pure Bucket-B** (Cmd+D dropped → Cmd+W closes the only pane → escalation quits the app → "not running"), so this fix greens only the local flake — **and §15 re-attributed the CI mode again: Cmd+D no-oped because the menu item didn't exist (SwiftUI menu clobber), and the quit was the *default* Window▸Close bypassing this very confirm-close gate; post-menu-fix the §13 race becomes newly reachable on CI in split/new-tab/churn. §16 then concretized wait-for-shell-ready as the computed-marker roundtrip (dump-state waits empirically refuted; local churn record F/F/P confirmed race-not-hard-failure).** Plus a **multi-monitor dev artifact**: local `.xcresult` recordings capture the **main/external** display while xtty forces its window onto the **built-in** screen (`positionOnBuiltInDisplay`), so they can't see the popup — CI is single-display so unaffected; optional companion fix = skip the built-in placement under test. A `terminal-session` spec delta (the "Shell resolution and launch configuration" requirement); **no `verification-harness` delta** — pure view-free `ShellResolver`, covered by `ShellResolverTests`. **235 `XttyCore` unit tests green.** **§15 root-cause forensics (2026-07-03, full `.xcresult` of `28472076179` + 12-agent workflow, adversarially verified) REFUTED the whole "Bucket B" mechanism and reshaped the follow-up:** the 7 CI failures share one product bug — **SwiftUI's default menu clobbers xtty's one-shot `NSApp.mainMenu` assignment** (per-launch race; the Cmd chords had no menu items to match; Cmd+W hit the *default* Window▸Close; Cmd+N/quake passes were vacuous tests) — so the §10e/§11f hybrid delivery-hardening plan is **superseded**: next is **`fix-main-menu-clobber`** (**`NSApplicationMain`** — drop the SwiftUI App lifecycle for an AppKit `App/main.swift`; design settled at source level 2026-07-05: SwiftUI's own `AppDelegate`→`updateMainMenu` reconciler is the mutator, `.commandsRemoved()` + rebuild-on-notification refuted live, Ghostty precedent, one-construct migration surface, validation = full suite ×2 in the VM rig — `research/03-analysis/swiftui-mainmenu-clobber-forensics.md`; `app-shell` delta) **+ the `verification-harness` truthing delta** (menu canary + a11y-IDs in Cmd-driving suites, Cmd+N `windowCount` assert, quake XCTFail-on-missing-Debug-menu, shared `requireStateDump()`, no in-test retries, `.function` strip as its own commit, pre-registered second-order matrix for the fix run — quake newly exercised, confirm-close newly reachable, Cmd+Opt+arrow on 26.4). **Proposed 2026-07-05** — see the `fix-main-menu-clobber` entry at the head of this list; research §15/§17 + the forensics doc. — **`add-block-sidebar`** (P4b-3) — implemented **and archived**: a clickable per-pane command-block list in the session sidebar (`Tab ▸ Pane ▸ Block`, collapsed-by-default disclosure; empty panes stay plain) — select a block → focus its pane + scroll to it; per-block menu = copy output / copy command / reveal cwd (no re-run); **bounded** `BlockTracker` history (lean-memory). Built **fork-free** on P4b-2's anchors/jump/copy + the P5 sidebar: new descriptor-based `PaneController` ops (`BlockTarget = running | index`), a **live** `isBlockActionable` trim-check (dims trimmed/epoch-stale rows — stronger than `validPromptRows`), snapshot dimming refreshed on epoch invalidation (`noteActivityChange()` after `bumpEpoch`/on `noteLiveTop` reset), reveal guarded record-only on the test path. New dump fields (`actionable` per block + `lastBlockMenuAction`) + an `XTTY_TEST_BLOCK_SELECT` trigger + 6 e2e (select/scroll, copy-designated, running-block, epoch-stale + trimmed arms, menu actions). **Adversarially reviewed pre-apply** (16-agent workflow, 9 findings folded in incl. the two majors above). **232 `XttyCore` unit + 38 XCUITests green** (1 skipped = opt-in bench). Decisions: `research/03-analysis/p4b-2-spatial-blocks-decisions.md` → P4b-3 addendum. **`add-lifecycle-census`** (P7c) — implemented **and archived**: the leak/retain pass shipped as a DEBUG **live-instance census** (`nonisolated(unsafe) static liveCount` init`++`/deinit`--` on `TerminalWindowController`/`PaneController`/`XttyTerminalView`/`GitReviewController`/`QuickTerminalController` + `TerminalSession` via its owner) surfaced as `liveInstanceCounts` in the state dump; a **churn XCUITest** that drives split/tab create-close cycles and asserts the census returns to baseline (the gated net — **proven by injecting a deliberate retain → it failed `baseline 1, final 8`, then reverted**); in-process **weak-sentinel** dealloc tests for `TerminalSession`/`SessionRegistry`/`Pane`; a diagnostic-not-gate **`make audit-leaks`** (`scripts/audit-leaks.sh`); and the two sweep nits fixed (`dumpTimer.invalidate()`, `BenchmarkRunner [weak controller]`). `leaks` reports **0 leaks at idle**; SwiftTerm's unbounded glyph caches stay a documented third-party watch-item (fork-free). New capability `lifecycle-census`; `verification-harness` gains the census field + churn coverage. Mechanism note: D1/D2 moved to `nonisolated(unsafe)` (Swift `deinit` is nonisolated → can't mutate a `@MainActor` static), the `GlobalHotKey` vouch. **229 `XttyCore` unit + 32 XCUITests green** (1 skipped = opt-in bench e2e). Methodology + apply result: `research/03-analysis/p7c-leak-retain-audit.md`. **`add-trustworthy-latency-probe`** (P7b) — implemented **and archived**: replaced P7a's coarse screenshot-polling latency probe with a continuous `SCStream` reading per-frame `displayTime` (on-glass, fork-free, ~90 % P7a reuse), fixed the clock-unit bug (`CMClockMakeHostTimeFromSystemUnits` both ends), added a startup epoch-calibration gate + a renderer-independent `ProbeOverlay` baseline, report latency frame-quantized. **Ran `make bench` 3× per renderer: calibration passed (offset <4 ms), and the verdict is decisive — keep CoreGraphics, skip Phase 8** (CG p50 ~31 ms vs Metal ~33 ms with a much tighter tail — p99 ~40 vs ~50–120 ms — and ~55 vs ~62 MB idle; the shared ~16.7 ms `queuePendingDisplay` throttle dominates the median, the real latency lever). 226 `XttyCore` unit + 31 XCUITests green. Spec deltas merged into `performance-harness` + `verification-harness`. See `research/03-analysis/p7-measurement-methodology.md` → P7b apply-result addendum. **`add-git-review-tree`** (P6b, Scope A: a flat↔tree toggle over the git-review changed-files list) is implemented **and archived** (213 `XttyCore` unit + 26 XCUITests green). **`add-git-review-polish`** (P6a+: token-level intra-line diff emphasis + a pause-during-own-git poll guard) is implemented **and archived**. **`add-capture-research-tooling`** (the committed `/xtty:capture-research` skill + command + the `.gitignore`/AGENTS capture-and-reconcile workflow → the new `research-capture` spec) is implemented **and archived**. **`add-git-review` (P6a)** is implemented **and archived**; the P6a+ decision is captured in `research/03-analysis/p6-file-diff-decisions.md` → "P6a+ addendum". P0/P1/P2/**P3a** + **P3b (`add-quick-terminal` + `add-profiles`)** + the verification harness + **P4a (`add-semantic-capture`)** + **P5 (`add-session-sidebar`)** + **P4b-1 (`add-file-link-open`)** + **P4b-2 (`add-spatial-blocks`)** + **P6a (`add-git-review`)** + **P6a+ (`add-git-review-polish`)** + **P6b (`add-git-review-tree`)** + **P7a (`add-latency-memory-harness`)** + **`add-local-signing-identity`** are implemented and archived. P4b-2 was the **first non-fork-free change** — it adds a 2-accessor SwiftTerm addition via a gitignored upstream clone + `git apply` patch (no fork repo; see `research/03-analysis/swiftterm-fork-vs-patch-strategy.md`). P6a is fork-free again (git via the system binary, no new deps). **P7b — the renderer decision: DECIDED (2026-06-29) → keep CoreGraphics, skip Phase 8.** The trustworthy probe (`add-trustworthy-latency-probe`, implemented & benched 3×) settled it: a continuous **`SCStream` reading per-frame `displayTime`** (on-glass, fork-free, ~90 % P7a reuse) with a **clock-unit fix** (`CMClockMakeHostTimeFromSystemUnits` both ends) and a **startup epoch-calibration gate** (passed, offset <4 ms — absolute numbers trustworthy on macOS 26.2). CG measured **faster median + much tighter tail + leaner memory** than SwiftTerm's experimental Metal path; the shared ~16.67 ms `queuePendingDisplay` throttle dominates the median (the real future latency lever), and the probe additionally exposed Metal's worse tail. Comparators were scoped Terminal.app-first stretch (iTerm2 Secure-Event-Input blocks injection); no hardware rig needed (the omitted tail cancels in the delta). See the [P7b apply-result addendum](research/03-analysis/p7-measurement-methodology.md#addendum-2026-06-29--p7b-apply-result-the-trustworthy-probe-works-verdict--keep-coregraphics-skip-phase-8). **P7c (leak/retain pass) — IMPLEMENTED & ARCHIVED 2026-06-29 (`add-lifecycle-census`; see Current open changes above; new `lifecycle-census` spec).** Headline: **no leak to hunt** — both explore-flagged retain cycles (`WindowController⇄GitReviewController`, `PaneController⇄LinkRoutingTerminalDelegate`) were **refuted** (every closure `[weak self]`, delegates `weak`, `GitReviewTarget` a transient value type); an App-layer sweep found zero cycles, only 2 cosmetic nits (`dumpTimer` not invalidated; `BenchmarkRunner` `Task` strong-captures `controller`). So P7c reframes from "find leaks" to **prove + guard + profile-SwiftTerm**: a DEBUG **live-instance census** (`liveCount` init`++`/deinit`--` on 6 lifecycle types → a `liveInstanceCounts` state-dump field → a churn XCUITest asserting return-to-baseline = the gated regression net), weak-sentinel teardown tests for the in-process `XttyCore` model types, and a one-time CLI `leaks`/`xctrace`/`vmmap` pass (no entitlements beyond the debug `get-task-allow`; **not** CI-gated — false-positive-prone) targeting the one risk the census can't name: SwiftTerm's **unbounded glyph/font caches** + incomplete renderer `deinit`. Footprint-after-churn stays **report-only** (too noisy to gate). Recommended change **`add-lifecycle-census`** (fork-free, harness-first); **drop** the synthesis's proposed `max-memory-mb` config key (a knob with no runtime behavior, contradicts the noisy-footprint finding). Gates nothing downstream. Full methodology: `research/03-analysis/p7c-leak-retain-audit.md`. The deferred **distribution** (Hardened Runtime + Developer ID + notarization; `add-local-signing-identity` shipped only the local-dev signing slice) remains — now **researched** (`/opsx:explore distribution`, 7-agent workflow → `research/03-analysis/distribution-signing-research.md`, 2026-06-30): **technically green, gated only on a ~$99/yr Apple Developer Program decision** (the HR-exception entitlement set is empty — HR constrains only xtty's own process image, so exec'ing unsigned shells/`$EDITOR`/CLIs and ZDOTDIR injection need no entitlement; Ghostty, the closest analog, ships exactly this; 5/6 surfaces SAFE, the one change is build-config not code). Recommended change **`add-distribution-signing`** (`build-workflow` + `verification-harness` deltas, human-gated prereqs); **captured, not yet proposed** — blocked on the procurement call only the owner can make. **Homebrew (`brew.sh`) researched as a §9 addendum** (2026-06-30): viable via a Homebrew **Cask** but a channel layered *on top of* notarization, **not a bypass** (cask quarantines by default → Gatekeeper still applies; Homebrew is disabling un-notarized casks Sept-2026), realistically an **own tap first** (`kitimark/homebrew-xtty`) then the official tap — a **separate** thin follow-on change `add-homebrew-distribution`, strictly downstream of the signed artifact. **$0-distribution researched as a §10 addendum** (2026-06-30): **$0 already works, split by who compiles the binary** — a **locally-compiled** app dodges Gatekeeper entirely (verified locally: no `com.apple.quarantine` on the repo's own Release build), so **source-build (`make`) is the canonical $0 + clean path and xtty is already there**; a downloaded ad-hoc prebuilt is $0 but costs a one-time "Open Anyway"/`xattr` per user; the clean download stays the $99 `add-distribution-signing` (optional, not required to ship). Maps to a README install section + a small `build-workflow` delta. **CI/CD on GitHub Actions researched** (2026-06-30 → `research/03-analysis/github-actions-ci-cd.md`; the repo `git@github.com:kitimark/xtty.git` exists but the local clone has **no remote + no `.github/`** yet): **$0, secret-free CI/CD is viable on GitHub-hosted `macos-26` runners today** (ad-hoc default needs no Apple secrets; free+unlimited on public repos). Recommended change **`add-ci-pipeline`** (a `build-workflow` spec delta — no `verification-harness` delta, CI just invokes existing tests): two jobs on push/PR — a **required** headless `test-core` (`swift test XttyCore`) + a **best-effort** `build-and-test` (XcodeGen → `xcodebuild test`, retry-tolerant); cache `external/SwiftTerm` on the pin+patch hash. Two unknowns to design defensively around: ❓ Metal toolchain on the runner (2-2 facet split → keep an idempotent `xcrun -f metal || sudo xcodebuild -downloadComponent MetalToolchain` guard) and ❓ XCUITest drive-path reliability (gate on the headless job, GUI job non-blocking). The $0 ad-hoc tag-release can ship with it; the $99 notarized release stays a separate `add-release-notarization`. Captured, not yet proposed. P7a (`add-latency-memory-harness`) shipped the fork-free harness and established that **memory is lean & trustworthy** (idle ~68 MB footprint, 0 % idle CPU; M1 looks met) while the latency half is coarse/experimental — see `research/03-analysis/p7-measurement-methodology.md` (2026-06-29 apply addendum: 3× bench results + the signing experiment). Also still open: the **Tier-2 visual on-screen selection** (forks the view; P7-gated). The optional **clickable per-block sidebar (P4b-3)** — reframed 2026-06-29 (explore `p4b-3` + external research: a WebSearch sweep + an iTerm2 `/tmp` source clone overturned D3's "no keyboard-first terminal ships a clickable command list" premise — iTerm2's Toolbelt does, single-click→scroll-to-mark) — is now implemented **and archived** as **`add-block-sidebar`** (see Current open changes above; the design is the *leaner lazy* twin of iTerm2's eager `VT100ScreenMark` recompute). Captured in the [P4b-3 addendum](research/03-analysis/p4b-2-spatial-blocks-decisions.md#addendum-2026-06-29--p4b-3-reframe-the-clickable-block-sidebar-the-d3-no-one-does-this-premise-is-overturned). (P6b's Scope-A changed-files tree shipped in `add-git-review-tree`; the full project file-tree browser, **Scope B**, stays **rejected** as off-mission IDE-creep — see the [P6b addendum](research/03-analysis/p6-file-diff-decisions.md#p6b-addendum--file-tree-scope-decided-scope-a-scope-b-rejected).) P6a+ shipped intra-line emphasis + the pause-during-own-git guard; syntax highlighting / FSEvents / unbalanced-run emphasis remain deferred. Sequencing rationale: `research/03-analysis/p5-sidebar-and-p4b-sequencing.md`. Outstanding manual-only checks: the quake's real global keypress / multi-monitor / focus-return; P4a/P5 shell-integration with *uncooperative* host zsh configs (the e2e degrades gracefully there); the sidebar's real cross-tab focus + collapse on a live multi-tab window. The architectural seam (all logic talks to the `Terminal` engine via `XttyCore`, never the view) holds — see `research/04-design/01-stack-sketch.md`.

## CI-run history digest (formerly AGENTS.md → Building → Continuous integration)

Both jobs guard the Metal toolchain (`xcrun -f metal || sudo xcodebuild -downloadComponent MetalToolchain`) and cache the reconstituted `external/SwiftTerm` (keyed on the pin + patch). `pr-lint.yml` enforces Conventional Commit **PR titles** (`amannn/action-semantic-pull-request`, on pull requests only). The workflows call `scripts/bootstrap-swiftterm.sh` directly (not `make`, whose `doctor` only advises). Release/notarization are **not** in CI (deferred). **Prerequisites the maintainer must do once:** wire the git remote, push, and make the repo public; optionally mark `test-core` a required status check. Formalized as the **`add-ci-pipeline`** OpenSpec change (a `build-workflow` spec delta). Methodology: `research/03-analysis/github-actions-ci-cd.md`. **First run (`28425122861`):** `test-core` green (1m6s, the gate), Metal preinstalled on the release-default Xcode 26.5, and XCUITests pass **30/7/1 (pass/fail/skip)** on `macos-26` (the hosted-runner risk refuted; §9's "34/41" was a looser count); **7 deterministic CI-env failures** remain non-blocking follow-up. **Artifact-level re-investigation (research §11, 2026-07-01 — first read of the actual `.xcresult` screenshots/grid-dumps) re-diagnosed them:** 6/7 are **Cmd-key/menu key-equivalents not firing** on the shared VM session (the genuine issue, now screenshot-confirmed), the runner shell is **`/bin/bash`** (its zsh-deprecation banner corrupts the grid, disables shell-integration, and makes focus-on-activate a *false negative*), and truecolor/emoji is **Cmd+V**, not a font/locale gap — see the research doc's §9 + §11. **§12 (2026-07-01) then measured the shipped `silence-bash-deprecation` fix in CI (post-merge run `28467944762`): the banner is gone from 0/221 attachments (Bucket A retired) but it flipped *zero* tests green — focus-on-activate **still fails because the runner's ~72-char hostname prompt wraps the typed marker across rows** (not the banner), so its fix is a wrap-tolerant assertion; the residual is a clean 6 Bucket-B + 1 wrap.** **§14 (2026-07-02) then measured the shipped `harden-focus-typing-assertion` fix in CI (post-merge run `28472076179`): the green-flip is CONFIRMED** — focus-typing passed on the first `build-and-test` carrying the wrap-tolerant matcher (counts now 34/7/1 of 42; the wrap bucket retired). The churn test's **CI** mode is pinned as pure Bucket-B (Cmd+D dropped → Cmd+W closes the only pane → escalation **quits the app** → "not running"), so the §13 `confirm-close = false` fix greens only the *local* flake; and **Bucket B is per-keystroke flaky, not absolute** (Cmd+W landed while Cmd+D didn't; split flaky-passed in `28467944762` then failed 3/3) — the `harden-xcuitests-for-ci` residual is a clean **7 Bucket-B, 0 wrap**. **§15 (2026-07-03) then found the ROOT CAUSE via full artifact forensics (12-agent workflow + adversarial verification) and REFUTED "Bucket B" entirely: SwiftUI's scene-synthesized default menu clobbers xtty's custom main menu** (one-shot `NSApp.mainMenu` assignment in `applicationDidFinishLaunching` under the SwiftUI `@main` Settings-scene lifecycle — a per-launch race the 26.4 CI VM loses, local 26.2 wins). Every failing chord is menu-dispatch-only, so the items **didn't exist to match**; delivery/dispatch were fine (Cmd+W fired the *default* Window▸Close → clean quit — churn/new-tab explained; zero crashes); Cmd+N's and the quake suite's passes were **vacuous tests**; "per-keystroke flaky" was wrong (the race is per-launch/binary); §14's green-flip claim is **softened** (focus-typing is P/FFF/P across runs — run 1 passed *without* the matcher). **A user-facing product bug (users can lose the Edit/Terminal menus) that CI correctly reported since run 1.** Plan reshaped: `fix-main-menu-clobber` (durable same-instance menu re-assert in unconditional product code → eventually `NSApplicationMain`; an `app-shell` delta) + a `verification-harness` truthing delta (menu canary + a11y-IDs in Cmd-driving suites, Cmd+N `windowCount`, quake XCTFail, shared `requireStateDump()`, no in-test retries, pre-registered second-order matrix). See research §15. **§16 (2026-07-03) settled the churn-flake fix design** (full capture: `research/03-analysis/confirm-close-shell-readiness.md`): a **computed-marker execution roundtrip** in both churn loops (test-only; `hasForegroundJob`-based waits **empirically refuted** — fg==shellPid from ~3 ms pre-`.zshrc`, 15–57 ms child bursts invisible to the 150 ms dump sampler, CI bash zero-flicker) + the DEBUG dump-timer **`.common`-modes** companion (default-mode registration froze the dumps during the confirm-close modal — the stale-dump artifact explained); prior art surveyed (no terminal fixes the fresh-shell race product-side; xtty's `tcgetpgrp` check field-defensible). Future product candidates captured, each its own change, none gating the fix: the **at-prompt confirm-close gate** (Ghostty/Kitty model; `BlockTracker.Phase.atPrompt` already exists), **bash shell integration** (`bash-preexec`/`--init-file` — protocol-complete, retires the marker, lights blocks/sidebar for bash users incl. CI), and the novel **zero-input latch** (`hasForegroundJob && userHasInteracted` — the only fix for the real-user mid-startup ⌘W papercut). **A local macOS-VM PoC to reproduce the §15 menu-clobber race was EXECUTED (2026-07-03/04) — JACKPOT** (`research/03-analysis/local-macos-vm-ci-reproduction.md` §8): a **lume** VM (macOS **26.4/25E246**, 3 vCPU, from the verified IPSW; minor-newer-guest-on-26.2-host works) ran the full `xttyUITests` suite in-guest and **reproduced CI run `28472076179` EXACTLY — 56 exec / 1 skip / 30 fail = 34/7/1, the same 7 failing tests**, all of which pass 100% on bare metal (~100% clobber at 3-vCPU idle). Build ran host-side (`build-for-testing` → rsync to a shared `/tmp` → `test-without-building`; the guest Metal toolchain is unfetchable — `xcrun -f metal` PATH-resolving is a **false positive**). **Mechanism found (CI couldn't): the clobber is IN-PLACE ITEM MUTATION of the same `NSMenu` object, not a pointer swap** (`mainMenuPtr==builtMenuPtr` measured True; the built menu's own items become SwiftUI defaults incl. a "Help" menu xtty never creates) → this **reshaped `fix-main-menu-clobber`**: **§15e Stage-A (re-assert same instance) is REFUTED** (a no-op), **`NSApplicationMain` (drop the SwiftUI lifecycle) is now the required fix**, and the diagnostic must compare menu **titles**, not object identity. Evidence bundles at `~/Downloads/xtty-vm-poc/artifacts/`; observe-only instrumentation on the uncommitted spike branch `spike/menu-clobber-diagnostics`. **A SECOND rig on Tart (§9, 2026-07-04) cross-confirmed it AND proved the race live:** Tart's prebuilt `macos-tahoe-xcode` image (macOS 26.4/25E246, Xcode+Metal+runner preinstalled) ran the ci.yml job **natively** in-guest (no Setup Assistant, no Metal fight, no host-build workaround — the clean rig lume couldn't be); **two native runs of the identical VM+binary diverged — run 1 `36/5/1` (the 2 Cmd+D split tests flaky-passed on a race-winning retry) vs run 2 `34/7/1` (they failed, exact CI+lume match)** → the clobber is a **non-deterministic per-launch race, proven live** (not just inferred). This reinforces the fix decision: because a retry-tolerant suite MASKS the race (run 1 passed 2 genuinely-broken tests), `fix-main-menu-clobber` must **eliminate** the race (`NSApplicationMain`), not add test retries. Rig tradeoff: Tart (native, zero workarounds) is cleaner at the cost of a 64 GiB download + external disk; lume is viable on internal disk with the Setup-Assistant + Metal + host-build workarounds. **Source-level forensics (2026-07-05, 6-agent workflow + adversarial verifier) then NAMED the machinery**: SwiftUI's private `AppDelegate` (the *real* `NSApp.delegate`; the adaptor delegate is only forwarded-to) → `makeMainMenu` → `AppKitMainMenuItem.updateMainMenu` — an identifier-keyed reconciler whose diff never matches plain `NSMenuItem`s, so it strips every custom item **in place** (`setMainMenu:` fires only on first install); reproduced **deterministically on bare metal** via a synthetic trigger; `.commandsRemoved()` and rebuild-on-notification **refuted live** (probe apps), **`NSApplicationMain` confirmed by construction** (both call sites die with `SwiftUI.AppDelegate`; Ghostty's 2023 migration is 3-year shipping precedent; xtty's surface = one construct — delete the `@main` struct, add `App/main.swift`). See `research/03-analysis/swiftui-mainmenu-clobber-forensics.md` (+ the CI doc's §17 pointer).

## Build-infrastructure history (formerly AGENTS.md → Building, full original bullets)

- **Metal Toolchain (one-time):** SwiftTerm bundles a `.metal` shader, so even though we don't use its renderer yet, the build compiles it. On Xcode 26+ the Metal compiler is a separate component — install it once with `sudo xcodebuild -downloadComponent MetalToolchain` (or Xcode → Settings → Components) or the build fails with `cannot execute tool 'metal'`. **Cleanup PROPOSED as `retire-metal-renderer` (2026-07-04 — `local-macos-vm-ci-reproduction.md` §10b/§11g):** this dependency exists *only* because SwiftTerm declares the shader as `.process("Apple/Metal/Shaders.metal")` (Package.swift:83); patching that out via the existing `patches/swiftterm/xtty-accessors.diff` makes xtty build with **zero Metal toolchain** — dropping the fragile `xcrun -f metal || sudo xcodebuild -downloadComponent MetalToolchain` guard from CI (the Metal download is a deterministic Apple-catalog-rotation trap, not flakiness) and aligning with the P7b keep-CoreGraphics decision. Cost: the Metal e2e arm (`testConfiguredMetalRendererIsReported`) is deleted — its behavior no longer exists. A lean **~38–40 GB golden-clone VM image** (macOS + Xcode-no-simulators, Metal patched out) is the researched minimal reusable test rig — see §10; the reproducible Packer-template version (§11) is now a **real target** — see the next bullet + `packer/README.md` (the `add-xtty-test-image` change; hard dependency: `retire-metal-renderer` first).

- **Local VM test image (optional):** `make image` builds the minimal **~40 GB** Tart image `xtty-test:26.5` from pinned inputs (`packer/xtty-test.pkr.hcl` — a cirruslabs `macos-tahoe-base` **digest** pin + Xcode 26.5 installed from a pre-downloaded official `.xip` + XcodeGen; **no simulators, no Metal toolchain, no xtty source baked in** — the repo arrives at test time in a disposable clone). Purpose: run the full suite incl. XCUITests in a local CI-parity VM — the per-launch race class reproduces at 3 vCPU in a VM, not on bare metal. Prereqs are **human-gated** and only advised by the target (`brew install packer`; a one-time Apple-ID `xcodes download 26.5` → `~/XcodesCache/`; `TART_HOME` on a volume with ~85 GB free). **Depends on `retire-metal-renderer`** (the zero-Metal build). Golden-clone runtime workflow, Metal-absence verification, pre-registered acceptance envelope, and maintenance: **`packer/README.md`**.

- **Optional stable local signing (dev convenience):** ad-hoc signing changes the app's code identity every rebuild, so any TCC grant (notably **Screen Recording**, which `make bench`'s latency probe needs) re-prompts on each build. To make a grant persist, create a self-signed code-signing cert once with **`scripts/create-signing-cert.sh`** (→ `xtty-dev`) and build with **`XTTY_SIGN_IDENTITY=xtty-dev make build|test|bench`**; unset, builds stay ad-hoc/portable (CI + other contributors unaffected). This is the lightweight slice of the deferred P7-distribution signing work — *not* Developer ID/notarization. Formalized as the **`add-local-signing-identity`** OpenSpec change (a `build-workflow` spec delta). Details + the experiment write-up: `research/03-analysis/p7-measurement-methodology.md` (2026-06-29 addendum). The **full distribution** (Developer ID + Hardened Runtime + notarization) is researched in `research/03-analysis/distribution-signing-research.md` (2026-06-30) — **technically green, gated only on a ~$99/yr Apple Developer Program decision**: the HR-exception entitlement set is empty (HR doesn't break exec'ing unsigned user binaries — child processes get independent AMFI evaluation), nothing xtty built breaks (one build-config change, not code), recommended change `add-distribution-signing`; captured, not yet proposed.
